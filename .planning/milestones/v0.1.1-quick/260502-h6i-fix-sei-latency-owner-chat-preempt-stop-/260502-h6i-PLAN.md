---
quick_id: 260502-h6i
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm/anthropicClient.js
  - src/llm/orchestrator.js
  - src/llm/sessionState.js
  - src/llm/inflight.js
  - src/behaviors/chat.js
  - scripts/verify-phase3-memory.js
  - scripts/verify-phase3.js
autonomous: true
requirements: []
base_sha: dcbc632
---

<objective>
Ship five named bug fixes from real-playsession-log analysis as five atomic commits. No discovery, no refactors, no new abstractions or config knobs. Each task = one commit using `fix(260502-h6i):` or `refactor(260502-h6i):` prefix.

ADR #3 invariant: `src/fsm.js` MUST be byte-unchanged at the end of all five tasks (verified by `git diff dcbc632 HEAD -- src/fsm.js` returning empty).

Final state: `node scripts/verify-phase3.js` passes 48/48 existing cases plus 3 new harness cases (no-op-loop-skips-diary, owner-chat-preempts-not-drops, stop-verb-hard-cancel) added by Tasks 2/4/5. Manual: `cache_read_input_tokens > 0` on the second turn after warmup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/phases/03-memory-persistence/03-SPEC.md
@.planning/phases/03-memory-persistence/03-CONTEXT.md
@.planning/phases/03-memory-persistence/03-03-SUMMARY.md
@src/llm/anthropicClient.js
@src/llm/orchestrator.js
@src/llm/sessionState.js
@src/llm/compaction.js
@src/llm/persona.js
@src/llm/inflight.js
@src/behaviors/chat.js
@scripts/verify-phase3.js
@scripts/verify-phase3-memory.js

<interfaces>
<!-- Key contracts the executor needs. Use directly, no codebase exploration. -->

`src/llm/anthropicClient.js` exports `createAnthropicClient(config)` returning `{ call, buildCachedSystem, model }`. `buildCachedSystem(systemInstructions, personaText, capParagraph, primer, learningLine, tools)` returns a 5-block `Array<{type:'text', text:string, cache_control?:{type:'ephemeral'}}>` with `cache_control: {type:'ephemeral'}` on the LAST (tool) block (D-18 / D-30..D-32). The 5-block order is locked: [system, persona+learning, capability, primer, tools].

`src/llm/orchestrator.js` constructs `cachedSystemBlocks` and `cachedCombinedSystemBlocks` once at startup via `rebuildPersonalitySystem()`. `assertNoMemoryInSystemBlocks(...)` defends Pitfall 4 at construction. The dispatch handler (line ~352 `handleDispatch(event, data, signal)`) currently has at line 366-376 the single-flight branch that drops non-owner-chat dispatches; the owner-chat-mid-loop path is `pendingInterrupt = { chatText }; loop.abortController.abort()` followed by the catch arm in `runIterations` calling `repairAfterAbort(loop)`. The dropped path is at line 367-370 (`logger.warn(...dropping)`).

`src/llm/sessionState.js` exports `createSessionState({...})`. Hot path is `onLoopTerminal({ messagesByteSize, loopMessages })` at line 219 — increments `loopCount`, accumulates `loopBatchMessages`, then if `loopCount >= loopCap || cumulativeLoopBytes >= bytesCap` calls `compactor.summarizeLoopBatch({ loopMessagesBatch: loopBatchMessages.flat(), when })`. NO mutation-aware gate currently exists. The session-end flush in `onPlayerLeft` (line 138) ALSO calls summarizeLoopBatch unconditionally.

`src/behaviors/chat.js` exports `startChat(bot, config)`. Owner-chat detection: `const ownerSpoke = username === config.owner_username`. Currently every owner message is debounced and emitted as `sei:chat_received`. Pre-debounce hook is the place for the stop-verb hard cancel (must happen BEFORE the orchestrator sees the chat).

`src/llm/inflight.js` line 17: `const PERSONALITY_ACTIONS = new Set(['setGoals', 'say', 'look'])` — this set decides whether an action blocks `follow.js`. After look removal, drop `'look'` from this set too.

`scripts/verify-phase3.js` lines 30-74 list `MEMORY_CASES` — new cases must be appended there AND implemented in `scripts/verify-phase3-memory.js` (which dispatches on `--case=<name>`).

Mutating action set (D-43-derived for Task 2 gate):
  MUTATING = {'dig','placeBlock','attackEntity','dropItem','depositItem','withdrawItem','consumeItem','activateItem','equip','sleep','openContainer'}
  MUTATING_CONDITIONAL = {'goTo'}  // only mutating if pre/post pos differs
  NON_MUTATING (skip diary) = {'look','say','setGoals','lookAt','handOffToMovement'}
</interfaces>

<scope_limits>
- Minimal diffs. NO refactors. NO new abstractions.
- Do NOT touch `src/fsm.js` (ADR #3 invariant).
- Do NOT add config keys.
- Remove dead code ONLY when surfaced by these fixes (e.g. `look` handler/branch becomes orphaned after Task 3 → delete it in Task 3).
- One atomic commit per task. Conventional prefix: `fix(260502-h6i):` or `refactor(260502-h6i):`.
- Pre-task base SHA: `dcbc632`. Verify `src/fsm.js` byte-unchanged after each commit.
</scope_limits>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Anthropic prompt cache miss (cache_read=0 / cache_creation=0)</name>
  <files>src/llm/anthropicClient.js, scripts/verify-phase3-memory.js (new harness case only)</files>
  <action>
    Investigate first, then minimal-diff fix. The symptom: every Haiku response logs `cache_read_input_tokens: 0 AND cache_creation_input_tokens: 0`. Per CONTEXT D-18 the marker IS placed on the LAST (tool) system block in `buildCachedSystem` (anthropicClient.js line 71). Per orchestrator construction `rebuildPersonalitySystem()` runs once, so the captured `learningLine` should be byte-stable across calls.

    Investigation steps (do these IN ORDER, stop at the first hit):

    1. Snapshot stability check — add a unit harness case `cache-system-blocks-byte-stable` to `scripts/verify-phase3-memory.js`:
       - Construct an orchestrator twice (or call `rebuildPersonalitySystem` indirectly via a fresh orchestrator), capture `_internal.getCachedSystemBlocks()` as JSON, and assert byte-equality across two constructions with the same `config.persona`.
       - Then assert the LAST block has `cache_control.type === 'ephemeral'` and NO other block does.
       - Also assert NO block contains the substrings `# Owner` or `# Diary` (Pitfall 4 — already checked at construction, but assert defensively).
       This case MUST pass on `dcbc632` head. If it fails → root cause is non-determinism in the system prefix; fix the source of mutation.

    2. If the snapshot test passes (prefix is stable), the cache miss is a marker-placement / minimum-token issue. Check `pass`:
       - Anthropic prompt caching has a minimum-tokens floor (~1024 tokens for Haiku). Sum the byte-length of `cachedSystemBlocks` text. If <~4 KB total it's likely below the floor — but the four blocks (system instr + persona + capability + primer + tool list) should comfortably exceed it. Verify by `node -e "import('./src/llm/anthropicClient.js')..."` style check or just `console.log` the total bytes.
       - If size is fine, the most likely remaining cause is that Anthropic now requires the `cache_control` marker on the FINAL element actually sent over the wire — i.e. when `tools.length > 0`, the cached prefix terminates at the tools array (tools live AFTER system blocks in the request). The fix: also place `cache_control: {type:'ephemeral'}` on the LAST tool definition's `input_schema` parent (i.e. mark the last `tools[]` entry). This makes the cache boundary land at the end of the tools section, matching Anthropic's caching scope.

    3. Apply the fix that the investigation surfaces. Most likely fix shape:
       In `anthropicClient.js`, in the `call({...})` function, after building the request object but before `sdk.messages.create`, mutate the LAST tool entry to carry `cache_control: {type:'ephemeral'}`:
       ```js
       const _tools = tools?.length
         ? tools.map((t, i) => i === tools.length - 1
             ? { ...t, cache_control: { type: 'ephemeral' } }
             : t)
         : undefined
       ```
       Pass `_tools` to `sdk.messages.create({tools: _tools, ...})`. Keep the existing system-block marker untouched — having BOTH markers is valid (Anthropic uses the last one as the cache boundary). This guarantees cache covers system + tools.

       Do NOT add cache_control to `messages` — those vary per call.

       If the investigation shows the actual cause is something else (e.g. `tools` array shuffles order, or `learningLine` reads a clock), apply the minimal fix at the source instead. Document the actual cause in the commit message body.

    4. Add a SECOND harness case `cache-marker-on-last-tool` that builds the same payload the orchestrator would send (via `_internal.callPersonalityTwoCall` with a stub anthropic SDK that captures the request) and asserts the request's `tools[tools.length-1].cache_control?.type === 'ephemeral'`. Use the existing stub patterns in `verify-phase3-memory.js`.

    5. Append both new case names to `MEMORY_CASES` in `scripts/verify-phase3.js`.

    Do not touch any caller of `anthropic.call()` — the fix is local to `anthropicClient.js`. Do not introduce config knobs.

    Commit: `fix(260502-h6i): land cache_control on last tool to enable prompt cache`
  </action>
  <verify>
    <automated>cd /Users/ouen/slop/sei && node scripts/verify-phase3.js</automated>
  </verify>
  <done>
    - `scripts/verify-phase3.js` reports 50/50 passed (48 existing + 2 new cache cases).
    - `git diff dcbc632 HEAD -- src/fsm.js` outputs nothing.
    - Diff to `src/llm/anthropicClient.js` is <20 lines.
    - Manual smoke (DEFERRED to end-of-plan, not blocking this task): boot Sei, observe `cache_read_input_tokens > 0` on the 2nd Haiku call after warmup.
    - Committed with conventional prefix.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate diary writes on at-least-one-mutating-action since last entry</name>
  <files>src/llm/sessionState.js, scripts/verify-phase3-memory.js, scripts/verify-phase3.js</files>
  <action>
    Stop hallucinated diary entries by gating `summarizeLoopBatch` on observable world-state mutation. Currently `onLoopTerminal` accumulates every Loop into `loopBatchMessages` and fires the summary on the cadence cap regardless of whether any mutating action occurred — Haiku then confabulates a diary entry that pattern-matches the goal text in the seed.

    Implementation in `src/llm/sessionState.js` (DO NOT add a config key — derive in-module):

    1. Define a module-level constant near the top:
       ```js
       const MUTATING_ACTIONS = new Set([
         'dig','placeBlock','attackEntity','dropItem','depositItem','withdrawItem',
         'consumeItem','activateItem','equip','sleep','openContainer',
       ])
       // 'goTo' is mutating only when bot position changed pre/post — handled separately.
       ```
       (Once `look` is removed in Task 3 it is naturally absent from this set; that's fine.)

    2. Add a helper `loopHasMutation(loopMessages)` (closure-private inside `createSessionState`) that walks `loopMessages` (the canonical `Loop._internal.messages` shape — same array passed in via `onLoopTerminal({ loopMessages })`):
       - Iterate assistant turns; for each `tool_use` block, if `MUTATING_ACTIONS.has(blk.name)` → return true.
       - For `name === 'goTo'`: pair with the FOLLOWING user turn's tool_result for that `tool_use_id`. The result string already encodes success ("goTo:ok" / "goTo:fail"); treat `goTo:ok` as mutating, `goTo:fail` as non-mutating. Pre/post position snapshot comparison is not required — the registry already distinguishes ok/fail and `ok` implies position changed.
       - All other tool names → not mutating.
       - If no mutating tool_use is found across all assistant turns in the batch, return false.

    3. In `onLoopTerminal({ messagesByteSize, loopMessages })` AFTER the existing counter increments (line ~221) BUT BEFORE the cadence-cap check (line ~230), evaluate `const mutatedThisLoop = loopHasMutation(loopMessages)`. Track a closure-level boolean `batchHasMutation` (init false) — set to true when ANY loop in the current batch was mutating. Reset to false when the batch is flushed (after a successful summary OR on session end after flush).

    4. Modify the cadence-cap branch: keep the existing trigger (`loopCount >= loopCap || cumulativeLoopBytes >= bytesCap`) BUT add `&& batchHasMutation`. When the cap fires WITHOUT mutation:
       - Do NOT call `compactor.summarizeLoopBatch`.
       - DO reset counters AND clear `loopBatchMessages` (to avoid the batch growing unbounded — non-mutating loops still count as "history considered" and the byte cap should not trip indefinitely).
       - Do NOT clear `batchHasMutation` (it was already false).
       - Log: `logger.info?.('[sei/session] loop-batch cadence hit but no mutation observed — skipping diary write')`
       - This implements SPEC A7's spirit: no idle write.

    5. Apply the SAME gate to the session-end flush branch in `onPlayerLeft` (line ~138): only call `summarizeLoopBatch` when `batchHasMutation === true`. Otherwise log and reset counters/batch.

    6. Add harness case `no-op-loop-skips-diary` to `scripts/verify-phase3-memory.js`:
       - Build a fake `loopMessages` that contains an assistant turn with `tool_use` blocks for ONLY `say` + `setGoals` (no mutating actions), terminate the loop, drive `onLoopTerminal` past the cadence cap (set `loop_batch_loop_count_cap: 1` in stub config).
       - Assert: stub `compactor.summarizeLoopBatch` was NOT called, counters DID reset, `loopBatchMessages` was cleared.
       - Then drive a SECOND loop with a `dig` tool_use; cap fires → assert summarizeLoopBatch IS called this time.

    7. Append `no-op-loop-skips-diary` to `MEMORY_CASES` in `scripts/verify-phase3.js`.

    DO NOT touch the existing D-51 / D-53 / a7-no-idle-write cases — they continue to pass because mutating loops still trigger as before.

    Commit: `fix(260502-h6i): gate diary writes on mutating-action presence`
  </action>
  <verify>
    <automated>cd /Users/ouen/slop/sei && node scripts/verify-phase3.js</automated>
  </verify>
  <done>
    - All Phase 3 verify cases pass (51/51 = 48 original + 2 from Task 1 + 1 new).
    - Diff to `sessionState.js` is <40 lines (helper + gate + flush gate).
    - `git diff dcbc632 HEAD -- src/fsm.js` empty.
    - Committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove the `look` tool from the personality LLM toolset</name>
  <files>src/llm/orchestrator.js, src/llm/inflight.js, scripts/verify-phase2_1.js (test update only)</files>
  <action>
    The world snapshot already rides on every user turn (orchestrator.js `snapshotText()` is appended on every `appendUserTurn` and on every `appendToolResults`). `look` adds a wasted iteration per Loop with no information gain. Remove it cleanly.

    In `src/llm/orchestrator.js`:

    1. Delete the `look:` entry from `ACTION_DESCRIPTIONS` (line ~60).
    2. Remove `'look'` from `PERSONALITY_NAMES` Set (line ~63).
    3. Delete the `{ name: 'look', description: ACTION_DESCRIPTIONS.look, input_schema: ... }` entry from `personalityTools` (lines ~165-168).
    4. Delete the `} else if (u.name === 'look') { lastActionResult = 'looked'; results[i] = ... }` branch in `runIterations` tool dispatch (lines ~541-543). After deletion, that toolname will fall through to the `else` (combined-path movement action) branch where the registry would reject unknown name — but since `look` is no longer offered to the LLM, it cannot be emitted. Add a brief comment explaining the removal: `// 'look' tool removed (260502-h6i): snapshot is already on every user turn — no second iteration needed.`
    5. Remove the word `look` from `COMBINED_SYSTEM` line 44 (the line that mentions "refresh your snapshot (`look`)") — rewrite that sentence minimally to drop the look mention. E.g. change `In a single response you may: speak in chat (`say`), set goals (`setGoals`), refresh your snapshot (`look`), and/or invoke movement actions ...` → `In a single response you may: speak in chat (`say`), set goals (`setGoals`), and/or invoke movement actions ...`. This is a string change inside the cached system prefix — it's fine because the cache key is per-instantiation and only changes when the bot restarts.

    In `src/llm/inflight.js`:

    6. Remove `'look'` from `PERSONALITY_ACTIONS` Set at line 17. Resulting set: `new Set(['setGoals', 'say'])`.

    In `scripts/verify-phase2_1.js`:

    7. Line 60: the assertion `assert(/name:\s*['"]look['"]/.test(orchSrc), ...)` will now fail. INVERT it:
       `assert(!/name:\s*['"]look['"]/.test(orchSrc), "orchestrator.js no longer declares personality tool name: 'look' (removed 260502-h6i)")`
    8. Line 143: the `tools` literal includes a `{ name: 'look', ... }` entry. Remove that entry from the array literal. Re-check the file: if any other assertion in `verify-phase2_1.js` references `look` (grep the file), remove or invert as appropriate. Do NOT add new assertions — minimal diff.

    Search the codebase one more time after edits: `grep -rn "look" src/ scripts/ | grep -v "\.git"` — anything referencing the removed tool (e.g. comments/docstrings that say "personality-only: setGoals/say/look") should have `look` stripped. Variable names like `lookAt` are unrelated and untouched.

    DO NOT touch `tests/`, `mineflayer-pathfinder` references, or `bot.lookAt` calls — `lookAt` is a mineflayer body method, NOT the tool we're removing.

    Run `node scripts/verify-phase2_1.js` AND `node scripts/verify-phase3.js` to confirm both pass.

    Commit: `refactor(260502-h6i): remove redundant look tool — snapshot is already per-turn`
  </action>
  <verify>
    <automated>cd /Users/ouen/slop/sei && node scripts/verify-phase2_1.js && node scripts/verify-phase3.js</automated>
  </verify>
  <done>
    - Both verify scripts pass.
    - `grep -rn "name:.*'look'" src/` returns nothing.
    - `grep -n "u.name === 'look'" src/llm/orchestrator.js` returns nothing.
    - `git diff dcbc632 HEAD -- src/fsm.js` empty.
    - Committed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Owner chat preempts active loop instead of dropping</name>
  <files>src/llm/orchestrator.js, scripts/verify-phase3-memory.js, scripts/verify-phase3.js</files>
  <action>
    Currently `handleDispatch` at lines 366-376 has correct branching IN PRINCIPLE:
    - If `currentLoop !== null && !isOwnerChat` → drop with warn.
    - If `currentLoop !== null && isOwnerChat` → set `pendingInterrupt`, abort the loop's controller (D-40 path).

    The bug from the playsession log is that the owner-chat events come in as `event === 'sei:chat_received'`, but the `isOwnerChat` predicate at line 353 only tests `event === 'chat' || event === 'sei:chat' || event === 'owner_chat'`. The actual FSM emits `sei:chat_received` (per `src/behaviors/chat.js` line 30). So the dispatch hits the DROP branch (line 367 `dropping`).

    Fix: extend `isOwnerChat` to also recognize `sei:chat_received` AND require `data.ownerSpoke === true` for the preempt branch. Non-owner `sei:chat_received` events should still drop (current behavior for nearby/addressed chat that isn't from the owner).

    In `src/llm/orchestrator.js` `handleDispatch(event, data, signal)`:

    1. Replace line 353:
       ```js
       const isOwnerChat = event === 'chat' || event === 'sei:chat' || event === 'owner_chat'
       ```
       with:
       ```js
       const isChatEvent = event === 'chat' || event === 'sei:chat' || event === 'owner_chat' || event === 'sei:chat_received'
       const isOwnerChat = isChatEvent && (data?.ownerSpoke === true || event === 'owner_chat')
       ```
       (The `event === 'owner_chat'` clause preserves backward-compat with any test or future caller that uses the legacy event name without an `ownerSpoke` flag.)

    2. The rest of the single-flight branch stays as-is — the owner-chat preempt path (lines 371-376) already does the right thing: stash `pendingInterrupt`, abort the loop, return. The catch arm in `runIterations` will then call `repairAfterAbort(loop)` which appends the `PLAYER INTERRUPT: <chat>` user turn (D-40) and a fresh iteration begins.

    3. Confirm the drop log still hits for non-owner chat: a `sei:chat_received` from a nearby player who is NOT the owner has `ownerSpoke: false`, so `isOwnerChat` is false → the drop branch fires → `logger.warn(...dropping)`. Good — that's the desired behavior.

    Add harness case `owner-chat-preempts-not-drops` to `scripts/verify-phase3-memory.js`:

    4. Build a stub orchestrator with a fake `bot`, fake `registry`, fake `anthropic` whose `call` returns a long-running Promise (settable via test seam). Start a Loop via `handleDispatch('sei:chat_received', { ownerSpoke: true, text: 'first message' })`. Confirm `currentLoop !== null`. While the call promise is unresolved, fire a SECOND `handleDispatch('sei:chat_received', { ownerSpoke: true, text: 'STOP doing that' })`. Assert:
       - `currentLoop.abortController.signal.aborted === true`
       - The drop log was NOT emitted (capture logger.warn calls; assert no `dropping` substring).
       - Resolve the original call promise as an AbortError so the catch arm runs; on the next iteration, assert that `loop._internal.messages` includes a user turn whose text content contains `PLAYER INTERRUPT: STOP doing that`.

    5. Add a sibling case `non-owner-chat-still-drops`: same setup, but the second dispatch has `ownerSpoke: false`. Assert the drop warn DID fire and the loop's abort signal is `false`.

    6. Append both new case names to `MEMORY_CASES` in `scripts/verify-phase3.js`.

    DO NOT modify `src/behaviors/chat.js` in this task — the chat behavior already emits the right event with `ownerSpoke` correctly set; the bug was purely on the consumer side. Task 5 will modify chat.js for the stop-verb shortcut.

    Commit: `fix(260502-h6i): preempt active loop on owner chat (sei:chat_received)`
  </action>
  <verify>
    <automated>cd /Users/ouen/slop/sei && node scripts/verify-phase3.js</automated>
  </verify>
  <done>
    - All Phase 3 verify cases pass (53/53 = 51 + 2 new).
    - Diff to `orchestrator.js` is <15 lines.
    - `git diff dcbc632 HEAD -- src/fsm.js` empty.
    - Committed.
  </done>
</task>

<task type="auto">
  <name>Task 5: Stop-verb pre-LLM hard cancel in chat behavior</name>
  <files>src/behaviors/chat.js, scripts/verify-phase3-memory.js, scripts/verify-phase3.js</files>
  <action>
    When the owner says one of `[stop, halt, cancel, nevermind, never mind]` (whole-word, case-insensitive), short-circuit BEFORE the orchestrator. We don't want to pay a Haiku round-trip just to learn "user said stop"; we also don't want Haiku to interpret it as conversation.

    Hard-cancel semantics (all four steps):
      (a) abort the orchestrator's current Loop (if any) via the existing abort path
      (b) clear `owner_goals` via the goal store
      (c) emit a single `bot.chat("stopping.")` line (cheap, no LLM)
      (d) DO NOT dispatch the chat to Haiku

    Implementation in `src/behaviors/chat.js`:

    1. The current signature is `startChat(bot, config)`. We need access to the orchestrator (for abort + goals). Change the export to `startChat(bot, config, orchestrator)` where `orchestrator` is optional (preserve backward-compat). Update the single call site (`grep -rn "startChat" src/ scripts/`) — find it (likely `src/bot.js` or similar) and pass the orchestrator. **Do NOT add a config key — pass the existing orchestrator instance directly.**

    2. Inside the `bot.on('chat', ...)` handler, AFTER the existing `ownerSpoke` computation (line 14) but BEFORE the proximity check / debounce dispatch (line 25), insert the stop-verb fast path:
       ```js
       if (ownerSpoke) {
         const trimmed = String(message).trim().toLowerCase()
         const STOP_VERBS = ['stop', 'halt', 'cancel', 'nevermind', 'never mind']
         // Whole-word match: exact equality OR the trimmed message is exactly the verb.
         // We deliberately do NOT match 'don't stop' — a leading 'don\'t ' fails equality.
         if (STOP_VERBS.includes(trimmed)) {
           // (a) abort current loop
           try { orchestrator?.currentLoop?.abortController?.abort() } catch {}
           // (b) clear owner goals (use the goal store's existing clear/remove API)
           try {
             const owner = orchestrator?.goals?.snapshot?.()?.owner_goals ?? []
             for (const g of owner) {
               try { orchestrator.goals.remove?.('owner', g) } catch {}
             }
           } catch {}
           // (c) confirm with a single chat line
           try { bot.chat('stopping.') } catch {}
           // (d) do NOT dispatch to Haiku
           return
         }
       }
       ```

       If `orchestrator.goals` does not expose `remove('owner', goalText)`, inspect `src/llm/goals.js` to find the right method (likely `setGoals` with `{list:'owner', op:'remove', goal: ...}` is the public API used by the registry tool — but the goal store itself almost certainly has a direct `remove`). Use the simplest existing method; if only `setGoals`-style exists, prefer a single bulk-clear method if one exists, else loop. Do NOT add new methods.

    3. The `STOP_VERBS` list is intentionally tight. The whole-word edge case to guard is "don't stop" — handled because the trimmed message would be `"don't stop"`, which is NOT `===` any element of the list. If the user sends `"stop "` (trailing space) it's normalized by `trim()`. If they send `"STOP"` it's normalized by `toLowerCase()`. If they send `"stop please"` it's NOT a stop verb (intentional — they're issuing a new instruction).

    4. Add harness case `stop-verb-hard-cancel` to `scripts/verify-phase3-memory.js`:
       - Build a fake `bot` with a Set-based `players`, an `entity.position`, an `on(event, listener)` that captures the listener, and a `chat(line)` that appends to a captured array.
       - Build a fake orchestrator with a fake `currentLoop = { abortController: new AbortController() }` and a goals stub: `goals: { snapshot: () => ({ owner_goals: ['build a house', 'mine diamonds'] }), remove: (list, goal) => { removed.push([list, goal]) } }`.
       - Wire `startChat(bot, { owner_username: 'shawn' }, orchestrator)`. Capture the chat-listener.
       - Fire the listener with `('shawn', 'stop')` → assert: abortController.signal.aborted === true; `removed` contains both goals; `bot.chat` called with `'stopping.'`; the dispatch event `sei:chat_received` was NOT emitted (capture `bot.emit` calls).
       - Fire the listener with `('shawn', 'STOP')` → same assertion path (case-insensitive).
       - Fire the listener with `('shawn', "don't stop")` → assert: abort NOT triggered, `bot.chat` NOT called with stopping, dispatch event WAS emitted (normal path).
       - Fire the listener with `('alice', 'stop')` (alice is not owner) → assert: abort NOT triggered, normal proximity/addressed logic runs.
       - Fire the listener with `('shawn', 'stop please')` → assert: abort NOT triggered, normal dispatch.

    5. Append `stop-verb-hard-cancel` to `MEMORY_CASES` in `scripts/verify-phase3.js`.

    Commit: `fix(260502-h6i): stop-verb pre-LLM hard cancel in chat behavior`
  </action>
  <verify>
    <automated>cd /Users/ouen/slop/sei && node scripts/verify-phase3.js</automated>
  </verify>
  <done>
    - All Phase 3 verify cases pass (54/54).
    - Diff to `chat.js` is <30 lines.
    - Single call site of `startChat` updated to pass the orchestrator.
    - `git diff dcbc632 HEAD -- src/fsm.js` empty.
    - Committed.
  </done>
</task>

</tasks>

<verification>
After all five tasks committed, run:

```
node scripts/verify-phase3.js                          # MUST be 54/54
git diff dcbc632 HEAD -- src/fsm.js                    # MUST be empty
git log dcbc632..HEAD --oneline | wc -l                # MUST be 5
git log dcbc632..HEAD --pretty=format:'%s' | grep -c '260502-h6i'  # MUST be 5
grep -rn "name:.*'look'" src/                          # MUST be empty
```

Manual smoke (DEFERRED, not gating commits):
- Boot Sei against a Minecraft server with the owner present.
- Send any chat to Sei. Observe Anthropic response: `cache_creation_input_tokens > 0` on first response (cache primed).
- Send a second chat. Observe `cache_read_input_tokens > 0` (cache hit). Latency should drop from ~1.5–3s to ~300–500ms.
- While Sei is mid-action (e.g. `dig` chain), say `stop`. Sei should immediately say `stopping.` and abort.
- While Sei is mid-action, say a normal new instruction. Sei should abort and start a fresh Loop responding to the new instruction (PLAYER INTERRUPT path).
- Trigger a no-op Loop (just say "hi" so Sei responds with `say` only). Confirm `[sei/session] loop-batch cadence hit but no mutation observed` log when the cadence cap is reached, and NO new diary entry is appended.
</verification>

<success_criteria>
- Five atomic commits on top of `dcbc632`, conventional `fix(260502-h6i):` / `refactor(260502-h6i):` prefix on each.
- `node scripts/verify-phase3.js` reports 54/54 passing (48 baseline + 6 new harness cases).
- `node scripts/verify-phase2_1.js` still passes after Task 3.
- `git diff dcbc632 HEAD -- src/fsm.js` outputs nothing (ADR #3 invariant).
- Manual cache-hit smoke confirms `cache_read_input_tokens > 0` after warmup.
- Each task's diff is minimal (<40 lines outside the harness file); no new files except harness case additions inline in `verify-phase3-memory.js`; no new config keys; no new abstractions.
</success_criteria>

<output>
After completion, append a short post-mortem note to `.planning/quick/260502-h6i-fix-sei-latency-owner-chat-preempt-stop-/SUMMARY.md` with: actual root cause confirmed in Task 1, total LOC changed per task, and any cache-hit-rate measurement from the manual smoke.
</output>
