---
phase: 07-pillar-up-scaffolding-behavior-place-equip-actions-and-pilla
plan: 07
type: execute
wave: 6
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05, 07-06]
files_modified:
  - scripts/verify-phase7.mjs
autonomous: false
requirements: [R-01, R-02, R-03, R-04, R-05, R-06]

must_haves:
  truths:
    - "Automated harness asserts schema gates, helper outputs, description plumbing, and snapshot enrichment end-to-end"
    - "Live-bot checkpoint exercises the three SPEC acceptance scenarios"
    - "Live-bot checkpoint surfaces timing-constant tuning data for D-04 (apex/landing ms)"
  artifacts:
    - path: "scripts/verify-phase7.mjs"
      provides: "End-to-end verification harness with offline + integration assertions"
      contains: "verify"
  key_links: []
---

<objective>
Close Phase 7 with a verification harness (mirrors `scripts/verify-phase5.mjs` / `verify-phase6.mjs` precedent) and a live-bot checkpoint that exercises the three SPEC acceptance scenarios in-world.

Purpose: Final gate before phase completion. Harness asserts every requirement R-01..R-06 via offline stubs; checkpoint catches anything the stubs cannot (real placeBlock face semantics, apex/landing timing, pathfinder unreachable-hint emission).
Output: `scripts/verify-phase7.mjs` + checkpoint approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pillar-up-scaffolding-behavior-place-equip-actions-and-pilla/07-SPEC.md (acceptance scenarios)
@scripts/verify-phase6.mjs (template for harness shape)

SPEC R-01 / R-02 / R-03 / R-06 acceptance scenarios (live):
1. Owner: "build a 4x4 wall in front of you" → visible 4×4 wall in-world.
2. Owner: "dig a 1x2x5 tunnel forward" → 10-cell tunnel the bot can walk into.
3. Owner: "go up to that tree" with an elevated target → bot receives the `unreachable — try build to Y=N` hint and chains a `build` pillar.
4. Mid-action owner chat preempt stops the cuboid loop and leaves the bot safe.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/verify-phase7.mjs end-to-end harness</name>
  <files>scripts/verify-phase7.mjs</files>
  <read_first>
    - scripts/verify-phase6.mjs (full — copy harness shape)
    - .planning/phases/07-.../07-SPEC.md
    - src/bot/adapter/minecraft/registry.js
    - src/bot/adapter/minecraft/behaviors/build.js
    - src/bot/adapter/minecraft/behaviors/dig.js
    - src/bot/adapter/minecraft/behaviors/pathfind.js
    - src/bot/brain/inflight.js
    - src/bot/brain/orchestrator.js
    - src/bot/adapter/minecraft/observers/snapshot.js
  </read_first>
  <action>
    Create `scripts/verify-phase7.mjs` modeled on `verify-phase6.mjs`. Use `node:assert` and `node:test` (preferred) or a top-level async IIFE with explicit assertions matching verify-phase5/6 idiom.

    Required assertion blocks (one per requirement):

    A. **R-01 schema (build)**: `createDefaultRegistry()` exposes `build`; schema accepts 16-cell build, rejects 324-cell build, rejects missing `block`, accepts hollow:true, accepts single-cell (1-cell case).

    B. **R-02 schema (cuboid dig)**: `dig` schema accepts legacy `{block}`, legacy `{x,y,z}`, cuboid `{x,y,z,to}`; rejects cuboid >256; rejects `{block, to}` (missing from coords).

    C. **R-04 inventory check**: `buildAction({block:'dirt',from,to},{inventory:{items:()=&gt;[]},...})` returns `no dirt in inventory` without invoking placeBlock.

    D. **R-05 cell counts**: enumerateBuildCells({0,64,0},{3,64,3},false).length === 16; ...({0,64,0},{3,66,3},true) returns wall-only cells; ...{0,64,0},{0,64,0},false).length === 1.

    E. **iteration order**: enumerateBuildCells first cell minY; enumerateDigCells first cell maxY; ITERATION_ORDER + CUBOID_ITERATION_ORDER constants exported.

    F. **R-06 unreachable hint**: stub `goTo` against a failing pathfinder, target.y=70, bot.y=64 → result contains `try build to Y=70`. With target.y=65, bot.y=64 → no `try build` substring.

    G. **D-05 occupied skip**: buildAction with stub bot whose blockAt returns non-air for 2 of 4 cells → result `built 2 placed, 2 skipped, of 4 cells`; placeBlock spy invoked exactly 2 times.

    H. **D-06 air skip**: digCuboid with stub bot whose blockAt returns air for 3 of 5 cells → result includes `3 skipped air`.

    I. **D-04 scaffolding contract**: scaffoldUp stub-bot test — when target Y > stand_y + 1, `bot.setControlState('jump', true)` is called and the loop exits when `bot.entity.position.y` reaches targetY-1.

    J. **D-08 grammar block in cached prefix**: `composeSeedBlocks(...)` returns array with `seed_cuboid_grammar` block BEFORE `seed_diary`; text mentions pillar/wall/platform/tunnel/hollow/256.

    K. **D-03 descriptions plumbed**: adapter.getActionDescription returns non-empty strings for 'build', 'placeBlock', 'equip'; dig description contains 'CUBOID MODE'.

    L. **D-10 snapshot enrichment**: stub inflight with `{name:'build', progress:{placed:47, total:256, currentY:66}}` → composeSnapshot output contains `47/256` and `y=66`.

    M. **abort discipline**: buildAction with `signal.aborted=true` mid-loop → returns `aborted after K placed of N cells`; digCuboid same.

    Each assertion logs `PASS [letter]` or throws with explicit message. Final line: `Phase 7 harness: X/13 PASS`.

    Exit code 0 on full pass, non-zero on any failure.
  </action>
  <verify>
    <automated>cd /Users/ouen/slop/sei &amp;&amp; node scripts/verify-phase7.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-phase7.mjs` exists.
    - Running `node scripts/verify-phase7.mjs` exits 0 and prints `Phase 7 harness: 13/13 PASS` (or stronger — at minimum 13 PASS lines covering A-M).
    - `grep -c "^assert\\|node:assert" scripts/verify-phase7.mjs` ≥ 1.
    - `grep -c "PASS" scripts/verify-phase7.mjs` ≥ 13.
  </acceptance_criteria>
  <done>End-to-end harness green for all 13 assertion blocks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-bot checkpoint — run SPEC acceptance scenarios</name>
  <files>(no file output — developer drives in-world)</files>
  <read_first>
    - .planning/phases/07-.../07-SPEC.md (Acceptance Criteria section)
  </read_first>
  <what-built>
    - `build` and cuboid `dig` actions registered with Zod schemas + 256-cell cap (R-01, R-02, R-05).
    - Internal jump+place scaffolding inside `build` (R-03).
    - `placeBlock` / `equip` / `build` LLM-facing descriptions in both ACTION_DESCRIPTIONS maps (D-03).
    - `seed_cuboid_grammar` system-prompt block taught at cached layer (D-08).
    - `unreachable — try build to Y=N` hint from goTo + dig on vertical failures (R-06, D-09).
    - Snapshot-enrichment progress ticks for cuboid actions (D-10, Option A).
  </what-built>
  <how-to-verify>
    Boot the bot (`npm run dev` / `sei start`) and connect a Minecraft client as owner.

    **Scenario 1 — 4×4 wall (R-01 acceptance):**
    1. Make sure the bot has a stack of dirt or oak_planks in inventory.
    2. In chat: `build a 4x4 wall in front of you using oak_planks`.
    3. Expected: bot picks two corners forming a 4-wide × 4-tall vertical wall, places 16 oak_planks blocks, ends with chat summary `built 16 placed, 0 skipped, of 16 cells` (or comparable).
    4. PASS if wall is visible and contiguous.

    **Scenario 2 — 1×2×5 tunnel (R-02 acceptance):**
    1. Stand near a hillside or solid ground.
    2. In chat: `dig a 1x2x5 tunnel forward`.
    3. Expected: bot enumerates two corners spanning a 1-wide × 2-tall × 5-long region; cells iterated top-down; bot walks into the tunnel afterward.
    4. PASS if tunnel is walkable end-to-end and bot did not get stuck mid-loop.

    **Scenario 3 — elevated target hint chain (R-06 acceptance):**
    1. Place yourself near a tree with leaves at y ≥ bot.y + 5.
    2. In chat: `go up to the top of that tree`.
    3. Expected: bot calls `goTo` to an elevated point; pathfinder returns `cant_reach — unreachable — try build to Y=N`; bot then issues a `build` pillar and reaches the elevation.
    4. PASS if (a) the unreachable hint appears in debug log, (b) bot subsequently calls build, (c) bot ends up at or near the target Y.

    **Scenario 4 — mid-action preempt safety:**
    1. While a `build` is in flight (Scenario 1 mid-loop), send a chat: `stop, come here`.
    2. Expected: cuboid loop aborts mid-iteration, returns `aborted after K placed of N cells`, bot disengages and responds.
    3. PASS if bot is in a safe state (not stuck mid-jump, not falling) and complies with new instruction.

    **Timing tuning record:** During Scenario 1, watch the bot's pillaring rhythm. If pillars look hitchy (failed placements / repeated jumps), record observed apex/landing milliseconds — Plan 07-02 used APEX_MAX_MS=600, LANDING_MAX_MS=800 as upper bounds; if real-world rhythm wants tighter timing, file a follow-up tuning task.

    **Progress-tick verification (D-10):** Look at the most recent snapshot's `in_flight:` line in debug logs during a long build. Expected format: `in_flight: build oak_planks (12s) — 16/64, y=66`.
  </how-to-verify>
  <acceptance_criteria>
    - All 4 scenarios PASS by developer judgment.
    - Progress-tick line observed in logs at least once.
    - No bot crash, disconnect, or stuck state during any scenario.
    - Developer signs off in chat: "approved" (or describes specific failures for gap closure).
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark Phase 7 complete, or describe issues for gap closure.</resume-signal>
</task>

</tasks>

<verification>
- Task 1 harness exits 0.
- Task 2 checkpoint receives developer "approved".
</verification>

<success_criteria>
- All 6 SPEC requirements verified — offline by harness, live by checkpoint.
- Mid-action abort safety confirmed in-world.
- Timing tuning data captured for D-04 (any follow-up flagged).
</success_criteria>

<output>
After completion, create `.planning/phases/07-.../07-07-SUMMARY.md` documenting:
- Final harness pass count and any flaky assertions.
- Live-bot scenario outcomes (1 paragraph per scenario).
- Timing tuning observations.
- Any deferred items surfaced during live testing (file as backlog if needed).
</output>
