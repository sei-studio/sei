---
phase: 09
plan: 08
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - scripts/verify-phase9.mjs
  - package.json
  - README.md
  - RELEASE-NOTES.md
  - .planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "scripts/verify-phase9.mjs runs all four Phase 9 verification harnesses in series (skinServer + mojang + phase9-installs + phase9-csl-config) and exits non-zero if any fails"
    - "package.json has a single `verify:phase9` script wiring the master harness"
    - "README.md documents the Phase 9 setup-wizard flow at a user level (one paragraph + bullet steps)"
    - "RELEASE-NOTES.md has a v0.2 entry describing the new skin + wizard feature and the Windows mod-install caveats"
    - "09-VERIFICATION.md catalogs every TRUTH from Plans 01-07's must_haves and marks each as PASS (with grep-confirmable evidence) or DEFER-TO-LIVE (with the specific live test required)"
  artifacts:
    - path: scripts/verify-phase9.mjs
      provides: "Master Phase 9 verification harness — runs all 4 sub-harnesses + emits PASS/FAIL summary"
    - path: README.md
      provides: "User-facing one-paragraph Phase 9 explainer"
    - path: RELEASE-NOTES.md
      provides: "v0.2 entry — skin pipeline + wizard + Windows caveats"
    - path: .planning/phases/09-.../09-VERIFICATION.md
      provides: "Goal-backward audit cross-referencing must_haves from Plans 01-07 against shipped evidence"
  key_links:
    - from: scripts/verify-phase9.mjs
      to: scripts/verify-skinServer.mjs
      via: "child_process spawn"
      pattern: "verify-skinServer"
    - from: scripts/verify-phase9.mjs
      to: scripts/verify-mojangSkinLookup.mjs
      via: "child_process spawn"
      pattern: "verify-mojangSkinLookup"
    - from: scripts/verify-phase9.mjs
      to: scripts/verify-phase9-installs.mjs
      via: "child_process spawn (Plan 05 verification script)"
      pattern: "verify-phase9-installs"
    - from: scripts/verify-phase9.mjs
      to: scripts/verify-phase9-csl-config.mjs
      via: "child_process spawn (Plan 05 verification script)"
      pattern: "verify-phase9-csl-config"
---

<objective>
Close out Phase 9 with verification, docs, and an honest goal-backward audit of every must_have truth from Plans 01-07. The master `verify:phase9` script lets a developer prove the entire phase still works after every commit (CI gate). The README + RELEASE-NOTES set user expectations honestly: skins work via a one-time wizard install, peers need the same mod to see custom skins on plain vanilla LAN (RESEARCH §5).

Purpose: ROADMAP Phase 9 success criteria pin the user workflow "download Sei → setup wizard auto-installs Fabric Loader + CustomSkinLoader → upload or search a skin in Sei's character page → preview in 3D → on next MC launch select the new Fabric Loader profile → bot joins their world wearing the chosen skin under any chosen username". This plan asserts each step has a documented + verified path.

Output: 1 master verification harness, package.json wiring, README + RELEASE-NOTES updates, the 09-VERIFICATION.md goal-backward audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/CONTEXT.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/RESEARCH.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-UI-SPEC.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-01-PLAN.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-02-PLAN.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-03-PLAN.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-04-PLAN.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-05-PLAN.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-06-PLAN.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-07-PLAN.md
@README.md
@RELEASE-NOTES.md

<interfaces>
<!-- The four sub-harnesses produced by Plans 02, 03, 05. -->

```
scripts/verify-skinServer.mjs              (Plan 02) — node-only; tests skin HTTP server contract
scripts/verify-mojangSkinLookup.mjs        (Plan 03) — tsx; tests Mojang lookup pipeline against stubbed endpoints (incl. legacy 64×32 normalization)
scripts/verify-phase9-installs.mjs         (Plan 05) — tsx; tests cross-platform install detection with temp dirs
scripts/verify-phase9-csl-config.mjs       (Plan 05) — tsx; tests CSL config writer (asserts CustomSkinAPI loader type per WARNING 6)
```

Run commands:
- `node scripts/verify-skinServer.mjs`
- `npx tsx scripts/verify-mojangSkinLookup.mjs`
- `npx tsx scripts/verify-phase9-installs.mjs`
- `npx tsx scripts/verify-phase9-csl-config.mjs`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: scripts/verify-phase9.mjs master harness + package.json verify:phase9 wiring</name>
  <read_first>
    - scripts/verify-skinServer.mjs (Plan 02 output — shape of PASS line)
    - scripts/verify-mojangSkinLookup.mjs (Plan 03)
    - scripts/verify-phase9-installs.mjs (Plan 05)
    - scripts/verify-phase9-csl-config.mjs (Plan 05)
    - scripts/verify-260514-gam.mjs (existing harness pattern — uses console.log assertions and exit code)
    - package.json `scripts` map (current entries for verify:phase2, verify:phase3)
  </read_first>
  <action>
    Create `scripts/verify-phase9.mjs`:

    ```js
    #!/usr/bin/env node
    /**
     * Phase 9 master verification harness.
     *
     * Runs all four sub-harnesses in series. Each must exit 0; otherwise this
     * script exits with code 1 and prints which harness failed.
     *
     * Run via:  node scripts/verify-phase9.mjs    (npm run verify:phase9)
     */
    import { spawnSync } from 'node:child_process';
    import path from 'node:path';
    import { fileURLToPath } from 'node:url';

    const __dirname = path.dirname(fileURLToPath(import.meta.url));
    const repoRoot = path.join(__dirname, '..');

    const HARNESSES = [
      { name: 'Plan 02 — skin HTTP server',          cmd: 'node',     args: ['scripts/verify-skinServer.mjs'] },
      { name: 'Plan 03 — Mojang lookup (incl. legacy 64×32 → 64×64 normalization)', cmd: 'npx', args: ['tsx', 'scripts/verify-mojangSkinLookup.mjs'] },
      { name: 'Plan 05 — MC install scanner',        cmd: 'npx',      args: ['tsx', 'scripts/verify-phase9-installs.mjs'] },
      { name: 'Plan 05 — CSL config writer (CustomSkinAPI schema)', cmd: 'npx', args: ['tsx', 'scripts/verify-phase9-csl-config.mjs'] },
    ];

    let totalPass = 0;
    let totalFail = 0;
    const failures = [];

    for (const h of HARNESSES) {
      process.stdout.write(`\n=== ${h.name} ===\n`);
      const r = spawnSync(h.cmd, h.args, {
        cwd: repoRoot,
        stdio: 'inherit',
        encoding: 'utf8',
      });
      if (r.status === 0) {
        totalPass++;
      } else {
        totalFail++;
        failures.push(`${h.name} (exit ${r.status})`);
      }
    }

    console.log(`\n=== Phase 9 verification summary ===`);
    console.log(`  PASS: ${totalPass}/${HARNESSES.length}`);
    if (totalFail > 0) {
      console.log(`  FAIL: ${totalFail}`);
      for (const f of failures) console.log(`    - ${f}`);
      process.exit(1);
    }
    console.log(`  Phase 9 verification: PASS`);
    process.exit(0);
    ```

    Note: tsx is a devDependency now (Plan 03 Task 3A), so `npx tsx <script>` resolves to the local copy without needing `--yes` or `--no-save` flags (INFO 9 fix already shipped).

    Add to `package.json` `scripts`:
    ```json
    "verify:phase9": "node scripts/verify-phase9.mjs"
    ```

    Insert alphabetically next to existing `verify:phase2` / `verify:phase3` / `verify:phase9-skin-server` / `verify:phase9-mojang` / `verify:phase9-installs` / `verify:phase9-csl-config` entries.

    Also create a top-level convenience: `"verify": "node scripts/verify-phase9.mjs"` — this lets future phases extend the umbrella `verify` script to chain prior-phase harnesses. (Skip if `"verify"` already exists in package.json — only add if missing.)
  </action>
  <verify>
    <automated>node scripts/verify-phase9.mjs && grep -F "verify:phase9" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-phase9.mjs` prints `Phase 9 verification: PASS` and exits 0
    - `grep -F "verify:phase9" package.json` matches at least once (the master script entry)
    - `grep -c "verify:phase9" package.json` returns at least 5 (master + 4 sub-harness entries from prior plans: skin-server, mojang, installs, csl-config)
    - The four sub-harnesses still pass independently (regression check; the master harness invokes them, so this is implicit)
  </acceptance_criteria>
  <done>
    A developer can run `npm run verify:phase9` and see a single PASS/FAIL summary covering all Phase 9 modules.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: README.md + RELEASE-NOTES.md — user-facing docs for the new skin + wizard feature</name>
  <read_first>
    - README.md (current — append a "Custom skins (Phase 9)" section near the bottom, alongside any existing feature sections)
    - RELEASE-NOTES.md (current — add a v0.2 entry as the new top section)
    - .planning/phases/09-.../CONTEXT.md §domain (what's in scope + what's out)
    - .planning/phases/09-.../RESEARCH.md §5 (the honest caveat about vanilla-LAN visibility — peers need the mod too)
  </read_first>
  <action>
    **2A — Append a "## Custom skins" section to `README.md`** (after the existing feature sections; before the Development / Building section):

    ```markdown
    ## Custom skins

    Sei can give each character a custom Minecraft skin and a custom in-game
    username, visible inside your own Minecraft world. The flow is:

    1. Open the Sei app. On first launch the setup wizard runs automatically.
       (You can also re-run it later from Settings → "Re-run setup".)
    2. The wizard scans your Minecraft installs (vanilla launcher + CurseForge
       instances) and installs [CustomSkinLoader](https://github.com/xfl03/MCCustomSkinLoader)
       into each one you select. For vanilla installs the wizard also installs
       [Fabric Loader](https://fabricmc.net/) for you using Minecraft's own
       bundled Java runtime (no need to install Java yourself).
    3. On the character page, pick a skin: drop a 64×64 PNG, or search a real
       Minecraft username. Sei previews the skin in 3D before you apply it.
    4. The next time you launch Minecraft, pick the new "fabric-loader" profile
       from the launcher dropdown (or just launch your CurseForge instance
       normally). When you summon a Sei character into your LAN world, the
       bot will appear with the chosen skin and username.

    **About visibility on plain vanilla LAN:** custom skins are rendered
    client-side by CustomSkinLoader, so the host (you) sees them correctly.
    Friends connecting to your LAN world will see the bot with a default
    Minecraft skin **unless** they also install CustomSkinLoader. This is a
    vanilla Minecraft constraint that no purely-server-side approach can
    work around — see `.planning/phases/09-.../RESEARCH.md` for the full
    analysis.

    The skin file itself never leaves your computer: Sei runs a tiny loopback
    HTTP server on `127.0.0.1` that CustomSkinLoader queries for skins. No
    third-party servers, no signed-texture pipeline, no Mojang dependency
    once a skin is applied.
    ```

    **2B — Add a top-section entry to `RELEASE-NOTES.md`:**

    Prepend (or insert as the new top section if RELEASE-NOTES already has v0.1):

    ```markdown
    ## v0.2.0 — Custom bot skins + setup wizard

    **New: per-persona custom skins and usernames.** Each character can now wear
    a custom Minecraft skin and join your world under any in-game username. Two
    skin sources:
    - **Upload PNG:** drop a 64×64 PNG on the character page
    - **Search Minecraft username:** look up any real Mojang account and use
      their current skin (legacy 64×32 skins are auto-upscaled to 64×64 — no
      Notch-skin edge case)

    A 3D preview shows the skin before you apply it. Skin bytes stay on your
    computer.

    **New: first-launch setup wizard.** Sei detects your Minecraft installs
    (vanilla launcher + CurseForge instances) and installs Fabric Loader +
    CustomSkinLoader for you. Re-run from Settings → Minecraft skins setup.
    Idempotent: re-running detects existing mods and only updates when needed.
    The wizard uses Minecraft's own bundled Java runtime — you don't need to
    install Java separately.

    **Three default personas (Sui, Mochineko, Clawd)** ship with bundled
    placeholder skins.

    **Visibility caveat.** Custom skins are rendered client-side. Peers on
    your LAN see the bot with a default Minecraft skin unless they also
    install CustomSkinLoader. This is a vanilla Minecraft architecture
    constraint, not a Sei limitation — full analysis in
    `.planning/phases/09-.../RESEARCH.md` §5.

    **Windows notes.** Mod installation uses Minecraft's bundled Java runtime
    (located automatically inside `%APPDATA%\.minecraft\runtime\` after you've
    launched the vanilla profile at least once). The wizard writes to your
    real Minecraft directory under `%APPDATA%\.minecraft` (vanilla) or
    `%USERPROFILE%\curseforge\minecraft\Instances\` (CurseForge). Sei does
    not modify `launcher_profiles.json` — you pick the Fabric profile from
    the launcher dropdown each time you want to play with Sei.

    ### Implementation summary

    - **Local HTTP skin server** at `http://127.0.0.1:<random-port>` (loopback only)
    - **Port-drift detection** on bootstrap: if the OS picks a new port across launches, CSL configs are rewritten to stay in sync
    - **Cross-platform install detection** for macOS, Windows, Linux
    - **Fabric installer** headless via `<bundled-java> -jar <installer> client -noprofile`
    - **CustomSkinLoader** JAR sourced from Modrinth (with GitHub releases fallback); config uses the canonical `CustomSkinAPI` loader type
    - **All external calls timeout-wrapped** per CLAUDE.md
    - **Wizard cancel** crosses the IPC boundary so the in-flight Fabric installer child process is SIGTERM'd cleanly
    - 4 verification harnesses + 1 master harness (`npm run verify:phase9`)
    ```
  </action>
  <verify>
    <automated>grep -F "Custom skins" README.md && grep -F "CustomSkinLoader" README.md && grep -F "v0.2.0" RELEASE-NOTES.md && grep -iF "custom bot skins" RELEASE-NOTES.md && grep -F "RESEARCH.md" RELEASE-NOTES.md README.md | wc -l | awk '{ if ($1 < 2) { exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Custom skins" README.md` matches (section heading present)
    - `grep -F "CustomSkinLoader" README.md` matches at least once
    - `grep -F "v0.2.0" RELEASE-NOTES.md` matches at least once
    - `grep -F "Custom bot skins" RELEASE-NOTES.md` matches the top section heading (case-insensitive search via grep -i: `grep -iF "custom bot skins" RELEASE-NOTES.md` matches)
    - Both files reference RESEARCH.md so users can read the full peer-visibility analysis if they want: `grep -F "RESEARCH.md" RELEASE-NOTES.md README.md | wc -l` returns 2 or more
    - Honest caveat present in README: `grep -F "default Minecraft skin unless they also install" README.md` matches OR equivalent phrasing
    - Bundled-Java note in RELEASE-NOTES (BLOCKER 3 user-facing): `grep -iF "bundled Java" RELEASE-NOTES.md` matches
    - CSL `CustomSkinAPI` reference in RELEASE-NOTES (WARNING 6 user-facing): `grep -F "CustomSkinAPI" RELEASE-NOTES.md` matches
  </acceptance_criteria>
  <done>
    README has a user-readable Custom Skins section. RELEASE-NOTES has a v0.2 entry covering features, caveats, Windows specifics, and the bundled-Java + CustomSkinAPI implementation details surfaced from BLOCKER 3 / WARNING 6.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 09-VERIFICATION.md goal-backward audit — every must_have truth from Plans 01-07 PASS or DEFER-TO-LIVE</name>
  <read_first>
    - Each of Plans 01-07's `must_haves.truths` section
    - .planning/phases/09-.../CONTEXT.md §"In scope" 6 bullets
    - ROADMAP Phase 9 success criteria (user workflow)
    - Each Plan's `<verify>` automated commands
  </read_first>
  <action>
    Create `.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-VERIFICATION.md`.

    Goal-backward structure — every claim in the must_have truths from Plans 01-07 + every "In scope" bullet from CONTEXT + every Phase 9 ROADMAP success bullet becomes a row in a single audit table. Status column: PASS (with grep / harness evidence) or DEFER-TO-LIVE (with the specific live test required).

    Table columns:
    | # | Source | Truth (verbatim) | Evidence | Status |

    Source values:
    - `P01.T1` ... `P07.T3` for plan-level must_haves
    - `CTX-S1` ... `CTX-S6` for CONTEXT §"In scope" bullets
    - `RM-W1` ... `RM-W6` for ROADMAP Phase 9 success workflow steps
    - `CSP-1` ... `CSP-N` for UI-SPEC §"Component Inventory" components

    Evidence values examples:
    - `grep -F "X" path/to/file matches` (verifiable inline)
    - `node scripts/verify-Y.mjs prints PASS` (harness output)
    - `manual smoke: action → expected observation` (when no automation possible)

    Status values:
    - **PASS** — evidence is automated and the executor confirms it ran green
    - **DEFER-TO-LIVE** — requires running on real Minecraft (e.g. "peers see custom skin" needs a real LAN session with the mod installed; "Fabric installer succeeds" needs a real `.minecraft` dir with a bundled Java runtime; "Cancel SIGTERM aborts in-flight installer" needs a real Java exec mid-download). Each DEFER row MUST specify the live test required (matches Phase 8 08-HOTSPOTS.md convention).

    Minimum row count: every truth in Plans 01-07 produces ≥ 1 row. Aim for 35+ rows total (plan count went from 7 → 8 per WARNING 4 split, and several new must_have truths landed for BLOCKERS 1-3 + WARNINGS 4-8 + INFO 9-10). The executor builds this table by walking each plan's must_haves.truths array and pasting each truth verbatim, then naming the verification command used.

    Required rows from the revision (call these out explicitly):
    - **BLOCKER 1 (IdSchema for characterId):** evidence is `grep -F "characterId: IdSchema" src/main/ipc.ts` matches + `grep -c "IdSchema" src/main/ipc.ts ≥ 2`. Status PASS.
    - **BLOCKER 2 (wizard cancel crosses IPC):** evidence is `grep -F "sei.wizardCancel" src/renderer/src/lib/stores/useWizardStore.ts ≥ 2 hits` AND `grep -F "abortWizardSession" src/main/ipc.ts matches` AND `grep -F "new AbortController" src/renderer/src/lib/stores/useWizardStore.ts == 0 hits`. Status PASS for IPC wiring; DEFER-TO-LIVE for "real Fabric installer mid-download gets SIGTERM" (needs real Java + real download in progress).
    - **BLOCKER 3 (bundled Java probe):** evidence is `grep -F "findBundledJava" src/main/fabricInstaller.ts matches` AND `grep -F "Launch Minecraft once" src/main/fabricInstaller.ts matches`. Status PASS for code path; DEFER-TO-LIVE for "real bundled JRE found at runtime/java-runtime-gamma/..." (needs a real MC install).
    - **WARNING 4 (plan split):** 7 plans became 8 plans; verify ROADMAP plan list shows 09-01 through 09-08 — `grep -c "09-0.*PLAN.md" .planning/ROADMAP.md ≥ 8`. Status PASS.
    - **WARNING 5 (atomic skin+username):** evidence is `grep -F "username: usernameDraft.trim() || null" src/renderer/src/components/SkinEditor.tsx matches` AND `grep -F "sei.saveCharacter" src/renderer/src/components/SkinEditor.tsx == 0` (no separate IPC call) AND `grep -F "username: nextUsername" src/main/skinStore.ts matches`. Status PASS.
    - **WARNING 6 (CustomSkinAPI loader type):** evidence is `grep -F "CustomSkinAPI" src/main/customSkinLoader.ts matches` AND `grep -F "Legacy" src/main/customSkinLoader.ts == 0` AND `verify-phase9-csl-config.mjs asserts type === 'CustomSkinAPI'`. Status PASS.
    - **WARNING 7 (port-drift detection):** evidence is `grep -F "skin server port drift detected, rewriting" src/main/index.ts matches`. Status PASS for code path; DEFER-TO-LIVE for "actual port-drift rewrite happens when restart-and-port-changes" (needs two-launch sequence with a real MC install).
    - **WARNING 8 (legacy 64×32 normalization):** evidence is `verify-mojangSkinLookup.mjs PASS 5/5` (T2 specifically exercises the legacy → 64×64 conversion). Status PASS.
    - **INFO 9 (tsx as devDep):** evidence is `grep -F '"tsx"' package.json matches`. Status PASS.
    - **INFO 10 (SkinEditor useEffect deps):** evidence is `grep -E "useEffect.*character\\.username" src/renderer/src/components/SkinEditor.tsx matches`. Status PASS.

    Then a "DEFER-TO-LIVE summary" sub-section listing the live tests required for Phase 9 to be Pass-on-real-Minecraft:
    1. Run `npm run dev` on macOS with the Vanilla Launcher installed; verify the wizard detects it.
    2. Click through wizard; verify Fabric installer downloads + runs successfully using the bundled Java runtime (BLOCKER 3); verify CSL JAR appears in `~/Library/Application Support/minecraft/mods/`; verify `~/Library/Application Support/minecraft/config/CustomSkinLoader/CustomSkinLoader.json` contains `"root": "http://127.0.0.1:<port>"` AND `"type": "CustomSkinAPI"` (WARNING 6).
    3. Open MC launcher → select the new fabric-loader profile → launch.
    4. In MC, Open to LAN.
    5. In Sei, summon a character with a custom skin applied via Mojang search ("Notch" — exercises WARNING 8 legacy normalization on a real legacy account).
    6. Verify the bot joins the world wearing Notch's skin under the persona's chosen username.
    7. Cancel test (BLOCKER 2): re-run the wizard, while the Fabric installer download is in flight, click Cancel; verify the install row goes "Cancelled" and main's log shows the java child process was SIGTERM'd.
    8. Port-drift test (WARNING 7): restart Sei (the OS picks a fresh port); confirm main log emits `skin server port drift detected, rewriting N CSL configs` and the CSL config on disk now reflects the new port.
    9. Repeat 1-8 on Windows (deferred per Phase 8 Wave 3 — same Windows VM constraint).

    Goal-backward summary:
    - **Phase 9 goal:** "Bot's custom skin and username are visible to the host in their own LAN world, with zero manual config beyond clicking through Sei's first-launch wizard."
    - **What's TRUE after this phase:**
      1. (P02) Skin server serves persona skins on 127.0.0.1
      2. (P02) Bot init payload carries skinServerBaseUrl + per-persona username; characterId validated via IdSchema (BLOCKER 1)
      3. (P03) User can upload OR search a skin via the GUI; legacy 64×32 skins are normalized (WARNING 8)
      4. (P04) Wizard modules detect installs, find bundled Java (BLOCKER 3), install Fabric, download CSL, write config with verified CustomSkinAPI loader type (WARNING 6)
      5. (P05) Wizard orchestrator composes the modules; cancel crosses IPC (BLOCKER 2); port-drift rewrites configs on launch (WARNING 7)
      6. (P06) Character page has SKIN & USERNAME section with 3D preview; single applySkin call writes skin + username atomically (WARNING 5); preview refreshes on username keystrokes (INFO 10)
      7. (P07) First-launch wizard auto-triggers + Settings re-run row; cancel button fires sei.wizardCancel(sessionId) (BLOCKER 2)
    - **What CANNOT be verified without live MC:** end-to-end skin propagation (DEFER-TO-LIVE summary above).

    Wrap up with a "## Phase 9 status" footer:
    - All 4 sub-harnesses + 1 master harness PASS (cite output)
    - All TypeScript projects typecheck clean
    - Cross-platform path conventions inherited from Phase 8 (cite 08-HOTSPOTS.md row 1)
    - 0 truths are unverifiable; every DEFER row has a specific live test
    - All 3 BLOCKER + 5 WARNING + 2 INFO checker issues from the revision are accounted for
  </action>
  <verify>
    <automated>test -f .planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-VERIFICATION.md && grep -c "PASS\|DEFER-TO-LIVE" .planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-VERIFICATION.md | awk '{ if ($1 < 35) { exit 1 } }' && grep -F "verify-phase9.mjs\|verify-skinServer.mjs\|verify-mojangSkinLookup.mjs\|verify-phase9-installs.mjs\|verify-phase9-csl-config.mjs" .planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-VERIFICATION.md | wc -l | awk '{ if ($1 < 4) { exit 1 } }' && grep -F "BLOCKER 1\|BLOCKER 2\|BLOCKER 3" .planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-VERIFICATION.md | wc -l | awk '{ if ($1 < 3) { exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/09-.../09-VERIFICATION.md` exists
    - Table has at least 35 rows (verified by `grep -c "PASS\|DEFER-TO-LIVE"` returning 35+ — bumped from 30 because the revision added several BLOCKER/WARNING/INFO truths)
    - All four sub-harnesses referenced by name (count returns 4+)
    - DEFER-TO-LIVE summary lists 9 concrete live tests (was 7; revision added BLOCKER 2 cancel test + WARNING 7 port-drift test)
    - Goal-backward summary lists the 7 plan-level "what's TRUE" statements (was 6; revision added one for Plan 05 split)
    - All three BLOCKERS explicitly referenced in row evidence: `grep -F "BLOCKER 1\|BLOCKER 2\|BLOCKER 3" .planning/phases/09-.../09-VERIFICATION.md | wc -l` returns 3 or more
  </acceptance_criteria>
  <done>
    Every Phase 9 must_have truth has a row with PASS evidence or a specific live test. The DEFER set is bounded and actionable. All 3 BLOCKERs + 5 WARNINGs + 2 INFOs from the revision are accounted for.
  </done>
</task>

</tasks>

<verification>
After all tasks:
- `node scripts/verify-phase9.mjs` prints `Phase 9 verification: PASS`
- README + RELEASE-NOTES both reference the Phase 9 feature
- 09-VERIFICATION.md has ≥35 rows with PASS/DEFER-TO-LIVE status, covering all 3 BLOCKERs + 5 WARNINGs + 2 INFOs
- `npx tsc --noEmit -p tsconfig.node.json && npx tsc --noEmit -p tsconfig.web.json` exits 0 (regression guard — neither doc edit broke anything)
</verification>

<threat_model>
## Trust Boundaries

(Documentation plan — no new runtime boundaries.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-X3 | DocOps | RELEASE-NOTES underplays the peer-visibility caveat | mitigate | Explicit "default Minecraft skin unless they also install" sentence in README AND a "Visibility caveat" section in RELEASE-NOTES point users at RESEARCH.md §5. |
| T-09-X4 | UserExpectation | User runs wizard without Java → confusing failure | mitigate | RELEASE-NOTES "Windows notes" describes the bundled-Java mechanism (BLOCKER 3); the wizard itself throws `FABRIC_INSTALL_FAILED: Java not found. Launch Minecraft once (vanilla profile) to install its bundled Java runtime, then re-run the wizard.` with action text. |
</threat_model>

<success_criteria>
- `npm run verify:phase9` is the single command a developer needs to gate Phase 9 changes
- README + RELEASE-NOTES set honest user expectations (incl. bundled-Java + CustomSkinAPI implementation details from the revision)
- 09-VERIFICATION.md is the goal-backward audit that turns "did we ship Phase 9?" into a checklist — every BLOCKER and WARNING from the checker has a verifiable row
- STATE.md update (Plan-phase committer responsibility, not this plan's): mark Phase 9 progress
</success_criteria>

<output>
After completion, create `.planning/phases/09-.../09-08-SUMMARY.md`. Include: `node scripts/verify-phase9.mjs` output, row count in 09-VERIFICATION.md, confirmation that README + RELEASE-NOTES both reference Phase 9's caveats, and confirmation that all 3 BLOCKER + 5 WARNING + 2 INFO checker issues are addressed in the verification audit.

This is the closing plan of Phase 9. The orchestrator should after-this-plan run `gsd-execute-phase 9 --verify` (or equivalent next-step command) to walk the 09-VERIFICATION.md DEFER-TO-LIVE list with a real Minecraft session.
</output>
