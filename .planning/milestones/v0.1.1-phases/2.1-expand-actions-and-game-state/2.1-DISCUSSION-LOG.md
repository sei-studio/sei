# Phase 2.1 Discussion Log

**Date:** 2026-04-25
**Mode:** discuss (manual orchestration; gsd-sdk query subcommand unavailable)

## Areas Discussed

### Area 1 — Action set scope
- **Presented:** ship 8 base actions (dig, placeBlock, equip, attackEntity, consumeItem, lookAt, dropItem, activateItem); defer crafting; optional adds: sleep, chest interaction
- **User decision:** crafting is its own phase; include sleep and chest (openContainer + withdraw/deposit)
- **Locked:** D-22, D-23

### Area 2 — Targeting model
- **Presented:** (a) by coordinate, (b) by name + auto-resolve nearest, (c) by snapshot index — recommended hybrid (b)+(c)
- **User input:** mineflayer has `findBlock` for radius search — use it. Ambiguity (e.g. "get some wood" depends on biome) should be handled by Sei asking the owner. Personality LLM needs basic Minecraft knowledge in its prompt and a "be specific in hand-offs" rule.
- **Locked:** D-25 (mineflayer resolvers, layered name/index/coord targeting), plus D-31 Minecraft primer and the "ask owner when ambiguous" rule (folded into D-32)

### Area 3 — Observation snapshot contents & cadence
- **Presented:** ~500-token line-oriented snapshot with pos/biome/time/vitals/holding/inventory/nearby blocks(r=16)/nearby entities(r=24)/goals; numbered `#N` handles with ~10s TTL; injected every personality turn after the cache breakpoint; plus on-demand `look()` action; no delta mode
- **User decision:** confirmed as recommended
- **Locked:** D-26, D-27, D-29, plus `look()` in D-22

### Area 4 — Capability overview shape & guardrails
- **Presented:** ~150-token NL paragraph in cached prefix with explicit inline "can't" list; adversarial prompt set for verify harness
- **User decision:** paragraph works for now
- **Locked:** D-30, D-37

### Area 5 — Failure feedback to personality (with mid-area refinement)
- **Initially presented:** both NL result string from movement LLM AND snapshot delta
- **User pushback:** assume Qwen is dumb — what is it being asked to do beyond function calls?
- **Refinement:** movement LLM contract narrowed to NL-intent → tool_call only (no prose); snapshot moved to personality-only; result strings made handler-generated (deterministic), *what*-only (no *why*); personality asks owner when *why* is unclear
- **Persona trait added:** "still learning, asks the human" — Sei knows it's still learning and asks for help rather than guessing
- **Locked:** D-28, D-32, D-33, D-34, D-35, D-36

## Out-of-Phase Questions Raised

- **"Is OS-level screenshot planned for a later phase?"** — Answered: deferred to v2 (VIS-01–04). Not in current 4-phase milestone. Phase 2.1's text snapshot is the baseline that v2 vision will eventually augment.
- **"What else is in v2?"** — Answered: VIS-01–04, GUI-V2-01/02/03 (Ollama indicator, model source selector, auto-updater), MEM-V2-01/02 (per-non-owner memory, vector retrieval). Out of scope entirely: voice/TTS, multi-bot.

## Notes

- This phase has no new requirement IDs; it's an inserted enabler for existing LLM/PERS surface (per ROADMAP.md §Phase 2.1).
- Movement LLM contract was actively narrowed during discussion — important not to backslide in planning.
