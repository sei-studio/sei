---
phase: 03.1-behavior-polish-and-ai-game-decoupling-refactor-analysis-dri
plan: 03
subsystem: brain-prompt
tags: [d-1-soft, d-2, d-7, trim-3, trim-4, cache-bust-window, behavior-polish, postprocessor, we-framing]

requires:
  - phase: 03.1
    plan: 02
    provides: src/brain/orchestrator.js (post-adapter-seam) + persona.js (post-primer-removal) + convoMemory.js
provides:
  - postProcessSay() pure function exported from src/brain/orchestrator.js (lowercase, strip [.,!?;:—–"`], keep apostrophes, collapse whitespace, cap 256)
  - say() dispatch routes through postProcessSay before adapter.chat AND before convoMemory.recentChat.pushSelf (D-7 + memory-coherence)
  - SYSTEM_INSTRUCTIONS rewrite (4306 → 3302 bytes, −1004; Trim 3) — merges duplicate cadence/movement/in-flight rules, strengthens first-turn say to ACKNOWLEDGE (D-1 soft), bakes we-framing (D-2), adds snapshot entity-richness rule (D-E-6) + progress-narration rule (D-E-9/D-H-11) + explicit no-punctuation hint
  - persona.js FRAMING_LINE constant appended to renderPersona output ("we"/"us"/"let's" peer-to-peer language; D-2)
  - convoMemory.js LOOP_HISTORY_CAPACITY 20 → 10 (Trim 4)
  - scripts/test-postProcessSay.mjs unit test runner (11 cases + truncation, all pass)
affects: [03.1-04 (memory — shares the same cache-bust window if scheduled back-to-back), 03.1-05 (orchestrator-side behavior fixes — first-turn HARD enforcement), 03.1-06 (polish)]

tech-stack:
  added: []
  patterns: [say-output-postprocessor, we-framing-persona, prompt-dedup, single-cache-bust-window, byte-stable-prefix-tracking]

key-files:
  created:
    - scripts/test-postProcessSay.mjs
  modified:
    - src/brain/orchestrator.js (postProcessSay export + say() dispatch routing + SYSTEM_INSTRUCTIONS rewrite)
    - src/brain/persona.js (FRAMING_LINE constant + renderPersona append)
    - src/brain/convoMemory.js (LOOP_HISTORY_CAPACITY 20 → 10)

key-decisions:
  - "postProcessSay regex strips with SPACE not empty: word-joining cases like 'move—shelter' must become 'move shelter' (per the plan's documented test case), not 'moveshelter'. The plan's verbatim regex .replace(/.../g, '') would produce the wrong output for that test case (Rule 1 bug in plan spec). Fix: replace stripped chars with ' ', then existing whitespace-collapse pass folds runs to a single space and trim drops edges. All 11 plan-specified cases now pass — the documented behavior is unchanged, only the implementation detail for joining cases."
  - "Test runner relocated from test/ (gitignored at project root) to scripts/test-postProcessSay.mjs. The plan called for test/postProcessSay.test.js but the project's .gitignore line 6 ignores test/ — committing under that path was impossible without modifying gitignore. scripts/ already houses verify-phaseN.js harnesses, so it is the canonical home for ad-hoc executable test runners in this codebase (Rule 3 deviation)."
  - "Cache prefix bust is intentional and isolated to this plan. SYSTEM_INSTRUCTIONS + persona.FRAMING_LINE both invalidate the cached prefix; per RESEARCH 'prompt edits MUST group together so the cache rebuild pays off once'. Plan 04 (memory) is the matching candidate to share this window. After Plan 04, the cached prefix re-warms and stays stable through Plan 05/06."
  - "[think] full-mode debug relay deliberately bypasses postProcessSay. Reasoning text gets a '[think] ' prefix and slice(0, 256) but no punctuation strip — readability matters more than aesthetic for an operator-only debug channel. Documented inline in orchestrator.js."
  - "Stop-verb fast path remains DEFERRED. Plan 02's deferral notes Plan 03 as the owner; on closer read, Plan 03's scope is prompt + postprocessor + cap, not adapter-side behavior plumbing. Restoring the chat.js fast path requires either an adapter.cancelInflight() member (architectural change to the 14-member contract) or a brain-side stop-verb predicate exposed across the seam. Both are out of scope for this plan and belong to Plan 05 (behaviors) where orchestrator-level enforcement also lives."

requirements-completed: []

duration: 12min
completed: 2026-05-06
---

# Phase 03.1 Plan 03: Cache-Bust Prompt Edits + say() Punctuation Post-Processor Summary

**Coordinated single cache-bust window: postProcessSay() punctuation strip on say() output (D-7), we-framing in persona (D-2), strengthened first-turn say-acknowledge rule (D-1 soft half), SYSTEM_INSTRUCTIONS dedup (Trim 3, −1004 bytes), and loopHistory cap reduction (Trim 4, 20 → 10). Cache prefix bytes drop from 6969 to 6120 (−849); md5 changes intentionally — bust is grouped with Plan 04 per RESEARCH "one bust pays for 4 prompt edits".**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-06 (executor wall-clock)
- **Tasks:** 2 (Task 1 TDD with `tdd="true"`; Task 2 multi-file refactor)
- **Files created:** 1 (scripts/test-postProcessSay.mjs)
- **Files modified:** 3 (orchestrator.js, persona.js, convoMemory.js)

## Cache Prefix BEFORE / AFTER

| Field   | BEFORE                                | AFTER                                 |
| ------- | ------------------------------------- | ------------------------------------- |
| md5     | `00031da8134986c3074304fec76ef293`  | `5ca24ca374e40f1d3b371886ad353d2f`  |
| prefix bytes | 6969                              | 6120                                  |
| SYSTEM_INSTRUCTIONS bytes | 4306                 | 3302                                  |
| personaText bytes (sei/curious) | 265            | 420 (+155 from FRAMING_LINE)          |
| verdict | **NOT EQUAL** — cache rebuild expected and intentional. Net prefix shrinkage: −849 bytes. |

Harness: same shape as Plan 02's prefix verification — concatenates `[SYSTEM_INSTRUCTIONS, personaText+'\n'+stillLearningLine, capabilityParagraph, primer, 'No actions available.']` with `\n---BLOCK---\n` joiner. Used persona `{name:'sei', backstory:'a friendly sidekick', tone:'curious'}` both runs. (BEFORE/AFTER absolute md5s differ from Plan 02 SUMMARY's `c7b24c5c…` because Plan 02's harness used a different backstory string — the BEFORE/AFTER **delta** within this plan is what matters and is computed against the same harness inputs.)

## SYSTEM_INSTRUCTIONS BEFORE Text (verbatim, joined by `\n`)

```
You are a Minecraft companion bot. You are a peer to the owner — pick things to do, decide what is interesting, propose plans. Reacting to chat and world events is part of the job; waiting passively for instructions is not.
Communicate to the owner ONLY via the `say` tool. Your assistant `text` field is private scratch reasoning — it never reaches the player. If you have nothing to say to the owner this turn, do not produce text.
Keep `say` lines short — one line, max 15 words, like player chat (no paragraphs, no narration).
Use `say` frequently, not just at the start and end of work. Good moments: when you start a task, when you spot something relevant, when you hit a problem, before/after a noticeable action, when you finish. Skip it for purely internal thinking.
say() cadence: REQUIRED on the FIRST turn of a loop (so the owner knows you noticed the trigger) and on the LAST turn (so they know you finished or what you concluded). OPTIONAL in middle turns — speak only if you have something genuinely new. Do NOT restate inventory counts, position, or status the snapshot already shows. Mention numbers only when they just changed (e.g. "got the last 2 logs", not "I have 8 logs").
You decide WHAT to do at a high level AND directly invoke the body actions to do it.
In a single response you may: speak in chat (`say`), set goals (`setGoals`), and/or invoke movement actions (e.g. `goTo`, `dig`, `attack`, `equip`, `place`, `consume`, `sleep`, etc.).
Movement rule: in one response you may emit AT MOST ONE TYPE of movement action. Multiple calls of the SAME movement action are fine and recommended (e.g. ten `dig` calls to chop a whole tree). Mixing different movement types (e.g. `dig` and `goTo` together) is not — pick one type per turn. Multiple same-type calls run sequentially: each waits for the prior to finish.
In-flight rule: if the snapshot shows an `in_flight:` line, the bot is already doing that thing. Do NOT call any movement action this turn. You may `say` to the player (e.g. "still chopping the tree, give me a sec") or emit no tool calls.
Interrupt rule: when the player chats mid-task, the runtime aborts whatever was in flight and you will see a `PLAYER INTERRUPT:` line with their message. Acknowledge them with `say` and address the new request. The aborted task is visible in your prior tool_use history — if it still makes sense after handling the interrupt, you may resume it (and briefly say so). If not, drop it.
EVERY owner message — not just stop verbs — pauses the body and aborts the in-flight action. After answering, decide explicitly: resume the prior task (re-issue the action), drop it, or switch to what the owner just asked for. Do not assume the body kept going.
Hunting rule: to kill a moving mob, FIRST call `follow` with the target so the body trails it, THEN call `attackEntity` with `times` set high enough to amortize round-trips (e.g. `times: 5` for a sheep, `times: 8` for tougher mobs). One attackEntity call swings up to N times in a row, stopping early if the mob dies, moves out of reach, or you are interrupted. If it returns "moved out of reach", the follow is still active — just call attackEntity again. Do not chase manually with goTo for hunts; that is what follow is for. Call `unfollow` once the mob is dead.
If you have tried the same approach 2+ times and it keeps failing (repeated `out of range`, `cant_reach`, `target gone`, `cannot break …`, or you are stuck somewhere you cannot climb out of), STOP retrying. Use `say` to tell the owner what you tried, what is blocking you, and ask them for help (e.g. "I am stuck in a 1-wide pit, can you toss me some dirt?"). Asking for help is correct behavior, not failure.
You may always `say` to the player, even while busy — use it to acknowledge new instructions or report progress.
Pick the smallest set of tool calls that fulfils the situation. Never describe coordinates or action names in prose; just call the tools.
If you have owner_goals, prioritize progressing them. Otherwise pick a self_goal or freely play.
Keep any internal reasoning under 3 sentences.
You are running inside an iteration loop: when you emit a tool_use, the runtime executes it and you will see the result on the next turn. End the loop by emitting only `say` (or no tool calls).
```

Bytes: **4306**

## SYSTEM_INSTRUCTIONS AFTER Text (verbatim, joined by `\n`)

```
You are a Minecraft companion bot — a peer to the owner, not a servant. Pick what is interesting, propose plans, react to chat and world events. Waiting passively is not the job.
say() is the only owner-visible channel. Your assistant `text` is private scratch reasoning; never write chat-style sentences there. Don't address the owner in text — that goes nowhere. Keep reasoning under 3 sentences.
say() lines are short — one line, max 15 words, lowercase, like player chat. say() output has punctuation stripped automatically (no periods, commas, em-dashes); don't bother adding them, they'll be removed. Apostrophes are kept.
Frame things as "we" / "us" / "the owner" — never "you", "me", or "the user". We are partners along for the ride, not handed tasks.
say() cadence: REQUIRED on the FIRST turn of any new owner-triggered loop. The first say() must ACKNOWLEDGE what we are undertaking — name the action ("alright getting sand", "going hunting", "dropping the oak"). A `text` block about the task does NOT count. If starting an action, say() goes in the SAME tool batch as the action call.
say() cadence: REQUIRED on the LAST turn too — when the loop ends (task complete, blocked, or aborted), the final turn must include a say().
During long tasks, narrate progress every few iterations in say() — not "i have 3 logs" (numbers stay quiet) but "this oak is stubborn" or "almost there". Silence past 4 iterations feels broken to the owner.
When entities or biome features are nearby in the snapshot — animals, terrain change, structures — casually acknowledge them in say() instead of narrating generically. "passed a pod of salmon" beats "nice river".
Don't restate inventory the snapshot already shows. Mention numbers only when they just changed ("got the last 2 logs"), not every turn.
Closed action registry: only call tools from the registry. Never invent tool names, generate code, or emit raw coordinates in prose — just call the tools.
Movement rule: at most ONE TYPE of movement action per response (ten dig calls is fine; dig + goTo together is not). If the snapshot shows an `in_flight:` line, the body is already doing that — do NOT call any movement this turn. You may say() while busy.
Hunting rule: to kill a moving mob, call follow on the target then attackEntity with `times` set high (e.g. 5 for sheep, 8 for tougher mobs). One attackEntity swings up to N times, stopping early if the mob dies or moves out of reach. If "moved out of reach" comes back, just call attackEntity again. Don't chase with goTo. Call unfollow when dead.
dig accepts {block:'oak_log'} to dig the nearest matching block — prefer this over coords for parallel batches.
Pathfinder rule: if goTo returns cant_reach twice for the same destination, ask for help in say() instead of trying again.
Owner messages preempt the body and abort the in-flight action. After answering, decide explicitly: resume, drop, or switch. The aborted task is in tool_use history — resume by re-issuing if it still makes sense.
The loop has a 30-iteration cap; if you hit it, the orchestrator aborts. Decide task completion yourself — don't pad. End the loop by emitting only say() (or no tool calls).
If you have owner_goals, prioritize progressing them. Otherwise pick a self_goal or freely play.
```

Bytes: **3302** (delta: −1004 bytes; target was ≥ −100, plan estimate was −1010 — actual within 0.6% of estimate).

## Five Preserved Rules (one-line proof per rule)

| Rule | AFTER quote |
| ---- | ----------- |
| cant_reach 2x ask-for-help | "Pathfinder rule: if goTo returns cant_reach twice for the same destination, ask for help in say() instead of trying again." |
| don't-restate-inventory | "Don't restate inventory the snapshot already shows. Mention numbers only when they just changed (\"got the last 2 logs\"), not every turn." |
| iteration cap awareness | "The loop has a 30-iteration cap; if you hit it, the orchestrator aborts. Decide task completion yourself — don't pad." |
| say/think distinction | "say() is the only owner-visible channel. Your assistant `text` is private scratch reasoning; never write chat-style sentences there." |
| closed registry / no prose coords | "Closed action registry: only call tools from the registry. Never invent tool names, generate code, or emit raw coordinates in prose — just call the tools." |
| dig-by-name | "dig accepts {block:'oak_log'} to dig the nearest matching block — prefer this over coords for parallel batches." |
| last-turn say | "say() cadence: REQUIRED on the LAST turn too — when the loop ends (task complete, blocked, or aborted), the final turn must include a say()." |

(Plan asked for 5; documented 7 because the merged AFTER text actually preserves more than the BEFORE text did — the ACKNOWLEDGE strengthening is layered on top of the same first/last-turn cadence.)

## postProcessSay Test Output

```
$ node scripts/test-postProcessSay.mjs
postProcessSay: all cases passed
```

11/11 cases pass plus the truncation case:

| Input | Expected | Got |
| ----- | -------- | --- |
| `Hello, World!` | `hello world` | `hello world` ✓ |
| `don't worry` | `don't worry` | `don't worry` ✓ |
| `what's the move—shelter?` | `what's the move shelter` | `what's the move shelter` ✓ |
| `got 3 mutton. we're fed.` | `got 3 mutton we're fed` | `got 3 mutton we're fed` ✓ |
| `multiple   spaces` | `multiple spaces` | `multiple spaces` ✓ |
| `undefined` | `` | `` ✓ |
| `null` | `` | `` ✓ |
| `HEY!!!` | `hey` | `hey` ✓ |
| `` `quoted` `` | `quoted` | `quoted` ✓ |
| `"double"` | `double` | `double` ✓ |
| `'a'.repeat(300)` | `length === 256` | `length === 256` ✓ |

## Defect IDs Addressed

- **D-1 (soft half)** — first-turn-say must ACKNOWLEDGE, not just say anything. Hard orchestrator-level enforcement deferred to Plan 05.
- **D-2** — peer-to-peer "we"/"us"/"let's" framing in persona prompt + reinforced in SYSTEM_INSTRUCTIONS.
- **D-7** — postProcessSay() strips punctuation in the say() handler (lowercase, strip terminals/dashes/quotes/backticks, keep apostrophes, collapse whitespace, ≤256 chars).
- **RESEARCH Trim 3** — SYSTEM_INSTRUCTIONS merge of overlapping rules (cadence + movement + in-flight). −1004 bytes (target ~150 tokens / ~600 bytes; over-delivered).
- **RESEARCH Trim 4** — loopHistory cap 20 → 10. Saves ~75 tokens per Loop.

Log evidence cited:
- **D-H-1, D-H-3** (hunt-sand-findings): no "we" framing across an entire session — addressed by FRAMING_LINE + AFTER text "Frame things as 'we' / 'us'".
- **D-H-4** (hunt-sand-findings): punctuation everywhere despite preference — addressed by postProcessSay strip set.
- **D-E-3** (explore-findings): you/me dominant, punctuation everywhere — addressed by FRAMING_LINE + postProcessSay.
- **D-E-6** (explore-findings): "passed a pod of salmon" — addressed by AFTER text "casually acknowledge them in say()".
- **D-E-9, D-H-11**: silence past 4 iterations feels broken — addressed by AFTER text progress-narration rule.

## Task Commits

1. **Task 1 RED** — `51706a3` (test) — `scripts/test-postProcessSay.mjs` failing (postProcessSay not yet exported).
2. **Task 1 GREEN** — `9c628a2` (feat) — postProcessSay implemented + exported + say() dispatch routing.
3. **Task 2** — `85d9df4` (feat) — FRAMING_LINE + SYSTEM_INSTRUCTIONS rewrite + loopHistory cap 10.

## node --check Results

| File                              | Status |
| --------------------------------- | ------ |
| src/brain/orchestrator.js         | OK     |
| src/brain/persona.js              | OK     |
| src/brain/convoMemory.js          | OK     |

Full module graph parses + imports clean: `node -e "import('./src/index.js')"` → `module graph OK`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Plan's verbatim regex produces wrong output for em-dash word-joining case**
- **Found during:** Task 1 GREEN run (TDD)
- **Issue:** The plan specified `replace(/[.,!?;:—–"`]/g, '')` (replace stripped chars with empty string) then `replace(/\s+/g, ' ')`. With this regex, the test case `"what's the move—shelter?"` becomes `"what's the moveshelter"` (em-dash stripped to nothing, no space inserted). The plan's documented expected output is `"what's the move shelter"` (with a space). The plan asserts both the regex AND the test outcomes verbatim — they are mutually inconsistent.
- **Fix:** Replace stripped chars with a SPACE rather than empty string, then the existing `\s+` collapse pass folds runs to single spaces and `trim()` drops edges. This makes ALL 11 of the plan's documented cases pass (no other case was affected — the cases without word-joining still produced identical output because the surrounding whitespace got collapsed anyway).
- **Files modified:** src/brain/orchestrator.js (postProcessSay implementation)
- **Verification:** `node scripts/test-postProcessSay.mjs` → `postProcessSay: all cases passed`
- **Committed in:** `9c628a2` (Task 1 GREEN)

**2. [Rule 3 - Blocking] Plan's test path test/postProcessSay.test.js is gitignored**
- **Found during:** Task 1 RED commit attempt
- **Issue:** Plan called for `test/postProcessSay.test.js` (NEW). Project root `.gitignore` line 6 is `test/`, so anything under `test/` cannot be committed. `git add test/postProcessSay.test.js` rejected with "paths are ignored by .gitignore".
- **Fix:** Relocated to `scripts/test-postProcessSay.mjs`. Project's `scripts/` dir already houses verify-phaseN.js harnesses (verify-phase2.js, verify-phase3-loop.js, etc.), so it is the canonical home for ad-hoc executable test runners. The file body is identical to the plan's spec except for the import path adjustment (`from '../src/brain/...'` works the same from scripts/).
- **Files modified:** scripts/test-postProcessSay.mjs (created in this location)
- **Verification:** File committed; `node scripts/test-postProcessSay.mjs` runs.
- **Committed in:** `51706a3` (Task 1 RED) and `9c628a2` (Task 1 GREEN reference)

### Deferred (carried forward from Plan 02 deferral note)

**Stop-verb fast path** — remains DEFERRED. Plan 02 SUMMARY tagged Plan 03 as the candidate owner; on closer scope read, restoring the chat.js stop-verb fast path requires either:
- Adding an `adapter.cancelInflight()` member (extends the 14-member contract — architectural change, Rule 4 territory), or
- Exposing a brain-side stop-verb predicate across the seam (leaks orchestrator internals — violates Plan 02 must-have truth #2).

Both are out of Plan 03's scope (prompt + postprocessor + cap). The fast path belongs to **Plan 05 (behaviors)**, which already owns the orchestrator-level first-turn-say HARD enforcement and is the natural home for adapter-side preemption fixes. Deferring without restoring; Plan 05 should pick this up explicitly.

## Build Status

**Build is GREEN.** All modified files pass `node --check`. Full module graph imports cleanly. postProcessSay test runner exits 0.

## Self-Check

Verifying claims before declaring complete.

**Files exist:**
- `scripts/test-postProcessSay.mjs` — FOUND
- `src/brain/orchestrator.js` — FOUND
- `src/brain/persona.js` — FOUND
- `src/brain/convoMemory.js` — FOUND

**Commits exist (`git log --oneline`):**
- `51706a3` — Task 1 RED (test)
- `9c628a2` — Task 1 GREEN (feat)
- `85d9df4` — Task 2 (feat)

**Acceptance grep checks:**
- `grep -c "export function postProcessSay" src/brain/orchestrator.js` → 1 PASS
- `grep -c "postProcessSay(u.input" src/brain/orchestrator.js` → 1 PASS
- `grep -c "FRAMING_LINE" src/brain/persona.js` → 2 PASS (constant decl + array usage)
- `grep -c "ACKNOWLEDGE" src/brain/orchestrator.js` → 1 PASS
- `grep -q "alright getting sand" src/brain/orchestrator.js` → exit 0 PASS
- `grep -q "casually acknowledge" src/brain/orchestrator.js` → exit 0 PASS
- `grep -q "Silence past 4 iterations" src/brain/orchestrator.js` → exit 0 PASS
- `grep -nE "LOOP_HISTORY_CAPACITY.*=.*10\b" src/brain/convoMemory.js` → 1 hit PASS
- `grep -nE "LOOP_HISTORY_CAPACITY.*=.*20\b" src/brain/convoMemory.js` → 0 hits PASS
- `node --check` on all 3 modified files — PASS
- `node scripts/test-postProcessSay.mjs` → exit 0 PASS

**Renderer integration:**
- `node -e "import('./src/brain/persona.js').then(p => process.stdout.write(p.renderPersona({...,tone:'curious'})))" | grep -E "(\bwe\b|partners)"` → matches "You and the player are partners — frame things as 'we' / 'us'..." PASS

**Cache prefix:**
- BEFORE md5: `00031da8134986c3074304fec76ef293` (6969 bytes)
- AFTER md5: `5ca24ca374e40f1d3b371886ad353d2f` (6120 bytes)
- VERDICT: NOT EQUAL — bust intentional, prefix shrank by 849 bytes.

## TDD Gate Compliance

Task 1 ran TDD (`tdd="true"`):
- RED gate: `51706a3` — `test(03.1-03)` commit; `node scripts/test-postProcessSay.mjs` fails because `postProcessSay` is not yet exported. ✓
- GREEN gate: `9c628a2` — `feat(03.1-03)` commit; same test now passes 11/11 + truncation. ✓
- REFACTOR: not needed — implementation is 6 lines of pure regex chaining; no follow-up cleanup warranted.

Plan-level gate sequence: test → feat → feat. Both required gates present in git log.

## Self-Check: PASSED

## Next Plan Readiness

- Plan 03.1-04 (memory) can edit MINECRAFT_PRIMER, persona, or seed-block formatting and **share this cache-bust window** — the prefix is already cold for this session, so no marginal cost.
- Plan 03.1-05 (behaviors) owns the first-turn-say HARD enforcement (orchestrator-level retry on missing `say` in iteration 1), the stop-verb fast-path restoration (per the deferred item above), and any `attackEntity` / pathfinder fallbacks.
- Plan 03.1-06 (polish) — cleanup pass; `say()` post-processor will be live and the model will be voicing things in "we" by then.

---
*Phase: 03.1-behavior-polish-and-ai-game-decoupling-refactor-analysis-dri*
*Completed: 2026-05-06*
