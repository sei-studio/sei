---
status: testing
phase: 03.1-behavior-polish-and-ai-game-decoupling-refactor-analysis-dri
source:
  - 03.1-01-SUMMARY.md (brain/adapter directory split)
  - 03.1-02-SUMMARY.md (border-file split + adapter wiring + BREAKING config)
  - 03.1-03-SUMMARY.md (postProcessSay + we-framing + SYSTEM_INSTRUCTIONS rewrite)
  - 03.1-04-SUMMARY.md (noteToSelf + AFFECT.md + diary OR-gate + compaction prompt)
  - 03.1-05-SUMMARY.md (first-turn-say HARD + parallel-dig cap=1 + idle/loop_end + cadence)
  - 03.1-06-SUMMARY.md (VALIDATION.md catalog of 37 defects + refactor invariants)
  - 03.1-07-SUMMARY.md (Bucket A tone polish — D-NEW-TONE-1, D-NEW-DM-1/2/3, D-W-8)
  - 03.1-08-SUMMARY.md (Bucket A memory polish — D-NEW-MEM-2/3, D-W-9, WR-05/06)
  - 03.1-09-SUMMARY.md (Bucket A — NEW-W-A, D-H-15/16, D-W-7, WR-07)
  - 03.1-10-SUMMARY.md (Bucket A — WR-01 FSM, WR-02 abortController, WR-04 attack/interrupt, WR-08)
started: 2026-05-07T00:00:00Z
updated: 2026-05-07T00:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 1
name: Cold Start Smoke (BREAKING config migration + boot composer)
expected: |
  After Plan 02's BREAKING config schema change (top-level mineflayer keys → adapter.minecraft.*), running `sei start` against an existing config.json must auto-migrate at parse time and boot to LAN discovery + connect. Verify: (a) `sei start` boots without throwing on an existing pre-Plan-02 config.json, (b) loadConfig() migrates legacy flat keys, (c) `sei config` re-saves canonical adapter.minecraft.* shape, (d) bot connects to LAN/configured server and spawns. Run from a clean process (kill any existing Sei).
awaiting: user response

## Tests

### 1. Cold Start Smoke (BREAKING config migration + boot composer)
expected: Kill any running sei process. Run `sei start` against an existing config.json. Bot loads config (auto-migrating legacy top-level host/auth/username if present), boots brain.start({adapter}) wired through src/index.js boot composer, connects to the Minecraft server, and spawns without errors. Re-running `sei config` writes the canonical `adapter.minecraft.*` shape. (Plans 01–02 — directory split + breaking config + boot composer.)
result: [pending]

### 2. Unit Test Runners All Pass
expected: From the repo root, `node scripts/test-postProcessSay.mjs`, `node scripts/test-affectLog.mjs`, `node scripts/test-firstTurnSay.mjs`, and `node --test scripts/test-progressCadence.mjs` all exit 0 with their expected pass lines (postProcessSay 11+1 cases, affectLog 7 cases, firstTurnSay 7 cases, progressCadence 5/5). These guard the four pure predicates that production code calls.
result: [pending]

### 3. Tone & Voice Polish (live)
expected: In-game, Sei speaks in lowercase with no terminal punctuation (no `.`, `!`, `?`, `—`, quotes, backticks); apostrophes preserved (`don't`, `we're`). Sei refers to "we" / "us" / "the owner" — never "you", "me", or "the user". On the FIRST turn of any owner-triggered loop, Sei calls `say` to acknowledge what's being undertaken (e.g. "alright getting sand", "going hunting"). On the LAST turn of a loop, Sei says something to close out. (Plans 03 + 05 + 07.)
result: [pending]

### 4. Memory Feels Durable (pure-chat session → diary)
expected: Have a pure-chat session with Sei (no digging/movement). Praise Sei ("good job", "nice"), state a preference ("I like quiet"), and reveal your name ("my name is X"). Sei should fire `noteToSelf` (visible in `[think]` mode or via memory/AFFECT.md). End the session. Restart Sei. On reconnect, Sei references the name/preference/praise — `memory/DIARY.md` contains a declarative entry ≤80 words quoting praise verbatim, no purple prose ("we shared a quiet moment…" gone), no second-person "you" inside the diary. (Plan 04 + Plan 07/08 polish + Plan 03 compaction prompt.)
result: [pending]

### 5. Behavior Cleanup — Hunting, Digging, Idle, Loop-End
expected: In-game, observe four behaviors:
  (a) Hunt: `follow + attackEntity` issued same turn → follow becomes a no-op ("already pursuing"); on `moved out of reach` Sei re-issues attackEntity without manually goTo-chasing.
  (b) Dig: parallel `dig` calls cap at 1 per turn (no 5-identical-dig storms); abort message hints `{block: "<name>"}` for repeats. `cant_reach` returns a closest-distance hint; on second cant_reach to the same destination Sei asks for help instead of retrying.
  (c) Idle (sei:idle, ~60s quiet): Sei may observe / comment, but does NOT auto-start dig/place/drop/openContainer/attack without an explicit owner_goal.
  (d) Loop end (task complete): Sei settles, says a brief acknowledgement, does NOT auto-start a new mutating task.
After ~4 silent iterations within a long task, Sei narrates progress once via say() (single soft nudge, not repeated). (Plan 05.)
result: [pending]

### 6. WR-04 Known Partial — Username Preservation Across Attack
expected: While Sei is in a loop and the OWNER says something mid-loop, an attack mob hits Sei. The attack preempts (P0). After the attack handles, the preserved owner chat re-fires at P1 and Sei addresses the owner BY THEIR ACTUAL USERNAME — not the literal string "owner". This is the gap currently flagged in 03.1-VERIFICATION.md (status: gaps_found, partial). If the chat is mis-attributed to "owner", report it — that's the known WR-04 sub-defect needing closure.
result: [pending]

### 7. Disconnect-on-Attacked Regression Check
expected: When attacked by a mob in-game, Sei does NOT disconnect from the server. (User flagged this as a previously-fixed issue that may have reappeared — tracked in backlog Phase 999.5. Confirming whether the regression is still present helps decide if 999.5 needs to be promoted now or after Phase 4.)
result: [pending]

### 8. Refactor Invariants Hold (brain/adapter seam)
expected: Brain layer remains game-agnostic. Quick checks: `grep -rln 'mineflayer' src/brain/` returns nothing (exit=1). `grep -rln 'adapter/minecraft' src/brain/` returns nothing. `node -e "import('./src/index.js')"` resolves the module graph cleanly. The 14-member Adapter contract in `src/adapter/minecraft/index.js` matches `src/brain/types.js`. No regression on Phase 1 connection / reconnect / chat / attack / auto-eat behaviors. (Plans 01–02.)
result: [pending]

## Summary

total: 8
passed: 0
issues: 0
pending: 8
skipped: 0
blocked: 0

## Gaps

[none yet]
