---
phase: 03.1-behavior-polish-and-ai-game-decoupling-refactor-analysis-dri
plan: 04
subsystem: brain-memory
tags: [d-m-1, d-m-2, d-m-4, d-m-5, d-m-6, d-w-9, d-e-10, theme-6, write-side-fix, two-tier-persistence, compaction-prompt]

requires:
  - phase: 03.1
    plan: 02
    provides: src/brain/memory/owner.js (loadOwner / saveOwner / formatOwnerSeedBlock) + src/brain/memory/diary.js
  - phase: 03.1
    plan: 03
    provides: src/brain/orchestrator.js post-postProcessSay + persona.FRAMING_LINE (we-framing) + cache-bust window already paid
provides:
  - noteToSelf personality tool registered in src/brain/orchestrator.js with verbatim Zod schema (kind enum: praise|preference|name|milestone|moment, optional `name` field for kind='name')
  - noteToSelf dispatch branch — AFFECT.md write always; OWNER.md preferred_name upsert when kind='name' (D-M-4 validated explicit field, no extraction); OWNER.md notes append when kind='preference'
  - src/brain/memory/affectLog.js — createAffectLog factory + appendAffect + readAffectFull (cold-creates file with HEADER on first read/write)
  - src/brain/memory/owner.js — setPreferredName (idempotent upsert) + appendNote (timestamped append) helpers
  - src/brain/sessionState.js — exported loopHasMutation; new loopHasAffect walker; batchHasAffect tracker; OR-gate (mutation OR affect) on cadence-cap and session-end flushes (D-M-1)
  - src/brain/orchestrator.js composeSeedBlocks — affect_log block injected in FULL after seed_diary, before recent_loop_history
  - src/brain/persona.js — NOTE_TO_SELF_GUIDANCE constant appended to renderPersona (~one sentence, "use sparingly")
  - src/brain/compaction.js — SUMMARY_PROMPT_INTRO rewritten: 80-word cap, declarative-only, verbatim praise, anti-pattern examples (D-W-9, D-E-10, Theme 6)
  - src/config.js — memory.affect_md_path default './memory/AFFECT.md'
  - memory/AFFECT.md initialized with header (gitignored alongside OWNER.md / DIARY.md)
  - .gitignore — memory/AFFECT.md added to runtime-memory ignore block
  - scripts/test-affectLog.mjs — 7 cases pass against tmp-dir fixtures
affects: [03.1-05 (behaviors — first-turn-say HARD enforcement; orchestrator-side stop-verb fast path), 03.1-06 (polish)]

tech-stack:
  added: []
  patterns: [two-tier-persistence-affect-vs-diary, append-only-line-log, idempotent-frontmatter-upsert, or-gate-mutation-or-affect, ≤80-word-declarative-diary, anti-pattern-examples-in-prompt]

key-files:
  created:
    - src/brain/memory/affectLog.js
    - scripts/test-affectLog.mjs
    - memory/AFFECT.md (gitignored runtime artifact, committed empty as starter — actually NOT committed, see decisions)
  modified:
    - src/brain/orchestrator.js (noteToSelf tool + dispatch + affect_log seed block + PERSONALITY_NAMES)
    - src/brain/sessionState.js (loopHasMutation export, loopHasAffect, batchHasAffect, OR-gate on flushes)
    - src/brain/persona.js (NOTE_TO_SELF_GUIDANCE constant + renderPersona append)
    - src/brain/memory/owner.js (setPreferredName + appendNote helpers)
    - src/brain/compaction.js (SUMMARY_PROMPT_INTRO rewrite)
    - src/config.js (memory.affect_md_path)
    - .gitignore (memory/AFFECT.md added)

key-decisions:
  - "Test relocated from plan's test/affectLog.test.js to scripts/test-affectLog.mjs. test/ is gitignored at project root (line 6); scripts/ already houses verify-phaseN.js + test-postProcessSay.mjs harnesses, so it's the canonical home for ad-hoc executable test runners (Rule 3 deviation, identical pattern as Plan 03 SUMMARY decision #2)."
  - "memory/AFFECT.md is gitignored alongside memory/OWNER.md and memory/DIARY.md — the existing pattern is that runtime-generated memory files are not committed. Created locally for verification; readAffectFull cold-creates it on first call, so the file's absence in git is correct and consistent. Plan acceptance criterion `test -f memory/AFFECT.md` still passes locally."
  - "On-disk OWNER.md uses `# Notes` (heading 1) per the existing saveOwner serializer (owner.js line 142). Plan spec referenced `## Notes`; the seed_owner block sent to the LLM transforms `# Notes` into `## Notes` (heading 2) via formatOwnerSeedBlock. Heading-level detail is internal to the on-disk format; the plan's underlying intent (note appears under a notes section) is satisfied. We assert against what saveOwner actually writes (Rule 1 deviation)."
  - "appendNote and setPreferredName both go through loadOwner → mutate → saveOwner rather than the plan's literal `upsertKvLine` / `appendUnderHeading` string utilities. Reason: the OWNER.md store has a structured parser/serializer; routing through it keeps the on-disk shape canonical (no parallel string-edit drift) and inherits saveOwner's atomicWrite. Behavior preserved — frontmatter upsert is idempotent, body append is timestamped — but implementation is cleaner."
  - "noteToSelf added to PERSONALITY_NAMES alongside say + setGoals. Reasoning: dispatch returns 'noted' / 'error: …' synchronously; no follow-up iteration warranted. If the model emits ONLY noteToSelf, the loop terminates — same shape as say-only or setGoals-only termination."
  - "loopHasMutation made `export` (was internal). loopHasAffect added as a sibling export. The existing onLoopTerminal already accumulated batchHasMutation across the batch; we add batchHasAffect with the same accumulation pattern and OR-gate at the cadence-cap and session-end flush sites. Two-tier persistence per RESEARCH 'Open Questions' #1: AFFECT.md gets the immediate write per noteToSelf emission (orchestrator dispatch); DIARY.md gets the loop-batch summary at next idle terminal under preserved D-52 cadence."
  - "Compaction prompt byte delta is +917 (178 → 1095). Plan called this out as expected — savings come from SHORTER OUTPUTS (≤80 words capped), not shorter prompt. Compaction prompt does NOT share the cached personality prefix (explore-findings line 162: cache_read=0 on diary-write turns), so this edit does NOT bust the main cached prefix."

requirements-completed: []

duration: 22min
completed: 2026-05-06
---

# Phase 03.1 Plan 04: Memory Write-Side Fix Summary

**Highest-impact behavioral change in the phase: pure-chat sessions (where praise / name reveals / preferences happen) now produce diary entries. Three coordinated changes — noteToSelf personality tool with explicit-field name validation, AFFECT.md two-tier persistence (immediate write to AFFECT.md, batched summary to DIARY.md), and OR-gate diary writes (mutation OR affect) — close the structural hole at sessionState.js:36-39 (mutation-only gate). Bonus: compaction prompt rewritten to produce declarative ≤80-word entries that quote owner praise verbatim instead of memoir prose. Six defects directly addressed: D-M-1, D-M-2 (partial — structural fix; content quality is downstream of prompt rewrite), D-M-4, D-M-5 (partial), D-M-6, D-W-9, D-E-10.**

## Performance

- **Duration:** ~22 min
- **Started:** 2026-05-06 (executor wall-clock)
- **Tasks:** 3 (Task 1 TDD with `tdd="true"`; Task 2 multi-file refactor; Task 3 prompt rewrite)
- **Files created:** 2 (`src/brain/memory/affectLog.js`, `scripts/test-affectLog.mjs`); 1 gitignored runtime artifact (`memory/AFFECT.md`)
- **Files modified:** 6 (`orchestrator.js`, `sessionState.js`, `persona.js`, `owner.js`, `compaction.js`, `config.js`, `.gitignore`)

## noteToSelf Zod Schema (verbatim from orchestrator.js)

```js
{
  name: 'noteToSelf',
  description: 'Privately record a moment worth remembering across sessions: praise from owner, an inside joke, a stated preference, a name they revealed, a milestone reached. Will be written to your diary. Use sparingly — only for things you would want to remember weeks later. When recording a name, set kind="name" and pass the actual name in the `name` field (not embedded in summary).',
  input_schema: {
    type: 'object',
    properties: {
      kind: { type: 'string', enum: ['praise', 'preference', 'name', 'milestone', 'moment'] },
      summary: { type: 'string' },
      name: { type: 'string', description: 'When kind="name", the actual name to record (e.g. "Shawn"). Required when kind="name".' },
    },
    required: ['kind', 'summary'],
  },
}
```

Description string is used **verbatim** — it is the prompt the LLM reads. PERSONALITY_NAMES includes noteToSelf so an emit-only-noteToSelf turn terminates the loop.

## Compaction SUMMARY_PROMPT_INTRO BEFORE / AFTER

### BEFORE (verbatim, single line)

```
You just finished a stretch of activity. In 2–4 sentences, write a diary entry summarizing what happened from your perspective — who you were with, what you did, how it felt.
```

**Bytes:** 178

### AFTER (verbatim, joined by `\n`)

```
You are writing a diary entry summarizing a batch of recent loops.
Rules:
- Maximum 80 words. Hard cap.
- Declarative sentences only. No metaphors, no "quiet moments", no romantic prose, no "we both just stood there".
- State facts: what happened, what was said, what was made, what was learned.
- If the owner praised, thanked, named you, or stated a preference — quote them VERBATIM in the entry. This is the most important content to preserve.
- If nothing memorable happened, write one declarative sentence and stop.
- No second-person to the player. The diary is your private record.
- Lowercase preferred. No em-dashes.

Examples of good entries:
- "sskitz asked for sand. dug 10 sand. they said 'good job'. went silent after."
- "explored north of beach. found jungle, no shelter spot yet. sskitz said 'lets keep going'."
- "afternoon was quiet. sskitz built a wall. nothing was asked of me."

Examples to AVOID:
- "we shared a quiet moment standing in the dirt together"
- "it was less about grand plans and more about trial-and-error teamwork"
- any sentence that sounds like a memoir
```

**Bytes:** 1095 (delta: **+917 bytes**)

**Cache impact:** none on the main cached prefix. Compaction calls do not share the personality cached prefix (explore-findings line 162: `cache_read=0` on diary-write turns). The byte cost is paid once per loop-batch terminal, not once per Loop iteration. Savings come from **shorter outputs** — the 80-word cap caps the prompt-output bandwidth ratio, vs. the prior un-capped 2–4 sentences that grew unbounded ("we shared a quiet moment standing in the dirt together…" was ~30 words for one of three sentences — 80–100 words for the full entry was common).

## OWNER.md / AFFECT.md write paths exercised by the test

Test file: `scripts/test-affectLog.mjs` (7 cases, all pass).

| Case | Path                          | Operation                                                                              |
| ---- | ----------------------------- | -------------------------------------------------------------------------------------- |
| 1    | tmpDir/AFFECT.md (cold)       | `readAffectFull` cold-creates file with HEADER (`# Affect Log`)                        |
| 2    | tmpDir/AFFECT.md              | `appendAffect({kind:'praise', when:Date})` — explicit ISO timestamp preserved          |
| 3    | tmpDir/AFFECT.md              | `appendAffect({kind:'milestone'})` — `when` defaults to Date.now()                     |
| 4    | tmpDir/AFFECT.md              | `createAffectLog({path}).readAll()` returns existing log                               |
| 5    | tmpDir/OWNER.md (seeded)      | `setPreferredName('Shawn')` upserts frontmatter; `loadOwner` round-trip preserves uuid |
| 6    | tmpDir/OWNER.md               | `appendNote('praises with "good job"')` — line under `# Notes` heading; ISO timestamp  |
| 7    | tmpDir/OWNER.md               | `setPreferredName` written twice — single `^preferred_name:` line (idempotent)         |

```
$ node scripts/test-affectLog.mjs
affectLog + owner: all cases passed
```

## Cache_control marker placement post-edit

`stampLastToolCacheControl` (anthropicClient.js:10) marks ONLY the LAST tool entry of any array. `combinedToolsFor()` returns:
1. personalityTools: `say, setGoals, noteToSelf` (noteToSelf appended at slot 3)
2. movement tools (registry-ordered, e.g. `goTo, dig, attackEntity, follow, unfollow, sleep, equip, dropItem, consumeItem, activateItem, depositItem, withdrawItem, openContainer, placeBlock, lookAt`)

The cache_control marker lands on whichever movement tool is LAST in the registry's list — exactly as before. Adding noteToSelf to personalityTools does NOT move the marker; the personality block sits BEFORE movement tools in the merged array.

```
$ node -e "<verify-stamp>"
cache_control attached to: lookAt
total stamped: 1
```

(`lookAt` happens to be the last in this stub registry; in production it depends on the actual adapter's `listActions()` order — verified pattern is "always the last entry, regardless of name".)

## Defect IDs Addressed

- **D-M-1** (memory-findings): pure-chat loops never wrote diary entries → noteToSelf + AFFECT.md immediate write + DIARY OR-gate (mutation OR affect). Two-tier persistence.
- **D-M-2** (partial — structural): the bot's diary contained no praise → compaction prompt now requires verbatim praise. Content-quality fix is downstream; a future log replay will confirm whether the prompt rewrite alone is enough.
- **D-M-4**: name reveals were extracted from chat heuristically and got mangled → noteToSelf with explicit `name` field, Unicode-letter-validated, written via `setPreferredName`. No extraction from `summary`.
- **D-M-5** (partial): preferences were not durable → noteToSelf kind='preference' appends to OWNER.md notes via `appendNote`, immediate persistence. Surfacing in seed_owner block (already wired pre-Plan-04).
- **D-M-6**: AFFECT had nowhere to live → AFFECT.md is its dedicated home; loaded in FULL into every Loop's seed user turn (no budget — small by construction).
- **D-W-9** (wood-findings): "we shared a quiet moment standing in the dirt together" purple prose → compaction prompt rewrite explicitly bans this with "No metaphors, no 'quiet moments', no romantic prose, no 'we both just stood there'".
- **D-E-10** (explore-findings): "trial-and-error teamwork" generic outcome summary → "any sentence that sounds like a memoir" anti-pattern + "state facts: what happened, what was said, what was made, what was learned" positive direction.
- **Theme 6** (RESEARCH): diary content quality at the prompt level → fully addressed by SUMMARY_PROMPT_INTRO rewrite + 3 good + 3 anti-pattern examples.

## Open Question Resolutions

**RESEARCH "Open Questions" #1: where does AFFECT live — diary, separate file, both?**
Resolved per recommendation: **two-tier persistence**. AFFECT.md gets the immediate write at noteToSelf emission time (orchestrator dispatch). DIARY.md gets the loop-batch summary at the next idle terminal under the preserved D-52 cadence (≥10 loops or ≥32 KB). The OR-gate (mutation OR affect) ensures the cadence trigger fires for chat-only loops where praise / name / preferences happen.

## Task Commits

1. **Task 1 RED** — `8cedf49` (test) — `scripts/test-affectLog.mjs` failing with ERR_MODULE_NOT_FOUND on `affectLog.js`.
2. **Task 1 GREEN** — `2a51f19` (feat) — affectLog.js created; owner.js extended; config.affect_md_path; AFFECT.md initialized; .gitignore updated; 7/7 test cases pass.
3. **Task 2** — `a7c846f` (feat) — noteToSelf tool + dispatch branch + composeSeedBlocks affect_log block + sessionState OR-gate + persona NOTE_TO_SELF_GUIDANCE.
4. **Task 3** — `145e946` (feat) — compaction.js SUMMARY_PROMPT_INTRO rewrite (declarative, ≤80 words, verbatim praise, anti-pattern examples).

## node --check Results

| File                              | Status |
| --------------------------------- | ------ |
| src/brain/memory/affectLog.js     | OK     |
| src/brain/memory/owner.js         | OK     |
| src/brain/sessionState.js         | OK     |
| src/brain/orchestrator.js         | OK     |
| src/brain/compaction.js           | OK     |
| src/brain/persona.js              | OK     |
| src/config.js                     | OK     |

All 7 modified/created brain files parse clean. Module imports resolve cleanly:
```
orchestrator exports: classifyChatEvent, composeSeedBlocks, createOrchestrator, formatEventData, postProcessSay
sessionState exports: createSessionState, loopHasAffect, loopHasMutation
persona exports: FRAMING_LINE, NOTE_TO_SELF_GUIDANCE, capHitLine, capabilityParagraph, renderPersona, stillLearningLine
affectLog exports: appendAffect, createAffectLog, readAffectFull
owner exports: appendNote, formatOwnerSeedBlock, loadOwner, saveOwner, setPreferredName
compaction exports: createCompactor
```

Plan 03 test runner still passes:
```
$ node scripts/test-postProcessSay.mjs
postProcessSay: all cases passed
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Plan asserted `## Notes` heading on disk, but saveOwner emits `# Notes`**
- **Found during:** Task 1 GREEN run (test failed on `assert.match(owner, /## Notes/, ...)`)
- **Issue:** The plan's test spec asserted `## Notes` (heading level 2) appears in the on-disk OWNER.md. Existing saveOwner serializer (owner.js line 142, predates Plan 04) emits `# Notes` (heading level 1). The seed-turn block sent to the LLM transforms this to `## Notes` via formatOwnerSeedBlock (line 168). Heading-level discrepancy is internal to the format.
- **Fix:** Updated test assertion to match the on-disk reality (`/^# Notes$/m`). Updated appendNote docstring to document the round-trip (on-disk `# Notes`, seed-block `## Notes`). The plan's underlying intent (notes appended under a notes section, visible to the LLM as `## Notes` in the seed block) is preserved.
- **Files modified:** `scripts/test-affectLog.mjs`, `src/brain/memory/owner.js` (docstring only)
- **Verification:** `node scripts/test-affectLog.mjs` → all cases pass.
- **Committed in:** `2a51f19` (Task 1 GREEN — same commit as the implementation)

**2. [Rule 3 - Blocking] Plan's test path `test/affectLog.test.js` is gitignored**
- **Found during:** Task 1 RED commit attempt (immediate detection — `.gitignore` line 6 is `test/`)
- **Issue:** Plan called for `test/affectLog.test.js` (NEW). Project root `.gitignore` line 6 ignores everything under `test/`, so committing under that path would be rejected by `git add` (same blocker hit by Plan 03 SUMMARY decision #2).
- **Fix:** Relocated to `scripts/test-affectLog.mjs`. `scripts/` already houses verify-phaseN.js + test-postProcessSay.mjs harnesses, so it is the canonical home for ad-hoc executable test runners. Test body identical to plan spec aside from import paths.
- **Files modified:** `scripts/test-affectLog.mjs` (created in this location)
- **Verification:** File committed; `node scripts/test-affectLog.mjs` exits 0.
- **Committed in:** `8cedf49` (Task 1 RED) and `2a51f19` (Task 1 GREEN reference).

**3. [Rule 1 - Bug] memory/AFFECT.md handling — runtime-generated, not committed**
- **Found during:** Task 1 setup
- **Issue:** Plan listed `memory/AFFECT.md (NEW — empty header file)` under artifacts. Existing project pattern (gitignore lines 11-13) treats memory/OWNER.md and memory/DIARY.md as gitignored runtime artifacts (created on first read/write by their respective stores). Committing AFFECT.md would break this consistency.
- **Fix:** Created `memory/AFFECT.md` locally (so the test running against the real path can verify it exists), but added `memory/AFFECT.md` to `.gitignore`. The `readAffectFull` helper cold-creates the file with HEADER on first call, so the absence-from-git is correct: any fresh checkout will create the file at first noteToSelf emission. Plan acceptance criterion `test -f memory/AFFECT.md && head -1 memory/AFFECT.md` reads `# Affect Log` still passes locally.
- **Files modified:** `.gitignore` (added `memory/AFFECT.md`); `memory/AFFECT.md` created locally (not committed)
- **Verification:** `head -1 memory/AFFECT.md` → `# Affect Log`. `git status` confirms file is ignored.
- **Committed in:** `2a51f19` (Task 1 GREEN — `.gitignore` change is in this commit)

**4. [Rule 1 - Implementation simplification] setPreferredName / appendNote use loadOwner → mutate → saveOwner round-trip**
- **Found during:** Task 1 implementation
- **Issue:** Plan spec called for `upsertKvLine` and `appendUnderHeading` string utilities implemented inline in owner.js. The OWNER.md store has a structured parser/serializer (`loadOwner` / `saveOwner`); routing through it keeps the on-disk shape canonical and inherits saveOwner's atomicWrite without duplicating frontmatter-edit logic.
- **Fix:** Both helpers go through loadOwner → mutate → saveOwner. Behavior preserved — frontmatter upsert is idempotent (test case 7 verifies `^preferred_name:` count=1 after writing twice), body append is timestamped. No string-edit drift between paths.
- **Files modified:** `src/brain/memory/owner.js`
- **Verification:** All 7 test cases pass; `loadOwner` round-trip explicitly tested.
- **Committed in:** `2a51f19` (Task 1 GREEN)

### Out of scope / deferred (per plan)

The plan's `<deferred_with_rationale>` block lists D-M-3 (seed-loader FIFO algorithm) as deliberately deferred. Honored:
- No change to seed-loader selection algorithm.
- No salience-tagged DIARY frontmatter.
- No topic-keyword retrieval, embeddings, or BM25 (RESEARCH "What we're NOT doing" — all deferred).

The structural root cause (mutation-only diary gate) is addressed by the OR-gate fix; D-M-3's "shallow recall" symptom is downstream of the write-side hole. If AFFECT.md grows beyond a few KB AND user reports recall failure on praise, salience ranking inside DIARY proper is the v2 follow-up.

## Build Status

**Build is GREEN.** All 7 modified/created brain files pass `node --check`. Module graph imports cleanly. Both test runners (test-affectLog.mjs + test-postProcessSay.mjs) exit 0.

## Self-Check

**Files exist:**
- `src/brain/memory/affectLog.js` — FOUND
- `scripts/test-affectLog.mjs` — FOUND
- `memory/AFFECT.md` — FOUND (locally; gitignored, not in git index)
- `src/brain/memory/owner.js` — FOUND (modified)
- `src/brain/sessionState.js` — FOUND (modified)
- `src/brain/orchestrator.js` — FOUND (modified)
- `src/brain/compaction.js` — FOUND (modified)
- `src/brain/persona.js` — FOUND (modified)
- `src/config.js` — FOUND (modified)
- `.gitignore` — FOUND (modified)

**Commits exist (`git log --oneline`):**
- `8cedf49` — Task 1 RED (test) — verified via `git log --oneline | grep 8cedf49`
- `2a51f19` — Task 1 GREEN (feat)
- `a7c846f` — Task 2 (feat)
- `145e946` — Task 3 (feat)

**Acceptance grep checks (Task 1):**
- `test -f src/brain/memory/affectLog.js` → PASS
- `head -1 memory/AFFECT.md` → `# Affect Log` PASS
- `grep -q "export function createAffectLog\|export async function appendAffect\|export async function readAffectFull" src/brain/memory/affectLog.js` → PASS
- `grep -q "export async function setPreferredName" src/brain/memory/owner.js` → PASS
- `grep -q "export async function appendNote" src/brain/memory/owner.js` → PASS
- `grep -q "affect_md_path" src/config.js` → PASS
- `node --check src/brain/memory/affectLog.js && node --check src/brain/memory/owner.js && node --check src/config.js` → PASS

**Acceptance grep checks (Task 2):**
- `grep -c "name: 'noteToSelf'" src/brain/orchestrator.js` → 1 PASS (≥1)
- `grep -c "u.name === 'noteToSelf'" src/brain/orchestrator.js` → 1 PASS (≥1)
- `grep -c "affectLog.append" src/brain/orchestrator.js` → 1 PASS (≥1)
- `grep -c "setPreferredName" src/brain/orchestrator.js` → 2 PASS (≥1; import + call site)
- `grep -c "appendNote" src/brain/orchestrator.js` → 2 PASS (≥1; import + call site)
- `grep -cE "readAffectFull|affectLog.readAll" src/brain/orchestrator.js` → 2 PASS (≥1; import + composeSeedBlocks)
- `grep -cE "name: 'affect_log'|affect_log" src/brain/orchestrator.js` → 1 PASS (≥1; the seed block)
- `grep -c "export function loopHasAffect" src/brain/sessionState.js` → 1 PASS (=1)
- `grep -c "loopHasAffect" src/brain/sessionState.js` → 2 PASS (≥2; definition + onLoopTerminal use)
- OR-gate present: `grep -cE "batchHasMutation && !batchHasAffect" src/brain/sessionState.js` → 2 PASS (cadence-cap + session-end). The plan's literal regex `loopHasMutation.*||.*loopHasAffect` is not what the implementation uses — the existing code already accumulated `batchHasMutation` once per loop and gated on the flag; we mirror that pattern with `batchHasAffect`. Functionally equivalent OR-gate; verified by reading the diff.
- `grep -c "NOTE_TO_SELF_GUIDANCE" src/brain/persona.js` → 2 PASS (≥1; constant decl + renderPersona array)
- `node --check` on all 3 files → PASS

**Acceptance grep checks (Task 3):**
- `grep -q "Maximum 80 words" src/brain/compaction.js` → PASS
- `grep -q "Declarative sentences only" src/brain/compaction.js` → PASS
- `grep -q "VERBATIM" src/brain/compaction.js` → PASS
- `grep -q "Examples of good entries" src/brain/compaction.js` → PASS
- `grep -q "Examples to AVOID" src/brain/compaction.js` → PASS
- `node --check src/brain/compaction.js` → PASS

## TDD Gate Compliance

Task 1 ran TDD (`tdd="true"`):
- RED gate: `8cedf49` — `test(03.1-04)` commit; `node scripts/test-affectLog.mjs` fails with ERR_MODULE_NOT_FOUND on `affectLog.js`. ✓
- GREEN gate: `2a51f19` — `feat(03.1-04)` commit; same test passes 7/7 cases. ✓
- REFACTOR: not needed — implementation is small (~100 lines affectLog.js + ~30 lines owner.js helpers), no follow-up cleanup warranted.

Plan-level gate sequence: test → feat → feat → feat. RED + GREEN gates present in git log; the two subsequent feat commits build on top.

## Self-Check: PASSED

## Threat Surface Scan

No new threat surface. Changes are internal-only:
- AFFECT.md is a local filesystem write (existing pattern, atomicWrite covers partial-write).
- noteToSelf reads from the LLM and writes to local files only — no network egress.
- Owner-name validation regex (`/^[\p{L}][\p{L}\p{M}\p{N}'\-\s]*$/u`) prevents control-char / shell-metachar injection into OWNER.md frontmatter.
- Affect-keyword regex on owner-chat is read-only (no eval, no shell-out).

No `threat_flag:` entries needed.

## Next Plan Readiness

- Plan 03.1-05 (behaviors) — orchestrator-level first-turn-say HARD enforcement is unaffected by these changes (noteToSelf is a personality-only tool, separate from say). Stop-verb fast path restoration is still scoped here per Plan 03 SUMMARY.
- Plan 03.1-06 (polish) — by the time Plan 06 runs, the bot will have been running with two-tier memory persistence; any visible AFFECT.md or DIARY.md regressions surface during verification and feed back into Plan 06.
- Plan 04 leaves the cached prefix bytes shifted slightly (NOTE_TO_SELF_GUIDANCE adds one sentence inside renderPersona, plus the noteToSelf tool description in the tool block). The Plan-03 cache-bust window already paid the prefix-rebuild cost; this plan's edits land inside the same window per RESEARCH guidance.

---
*Phase: 03.1-behavior-polish-and-ai-game-decoupling-refactor-analysis-dri*
*Completed: 2026-05-06*
