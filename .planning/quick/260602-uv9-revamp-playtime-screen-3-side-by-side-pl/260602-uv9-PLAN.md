---
phase: quick-260602-uv9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/lib/playtimeEstimate.ts
  - src/renderer/src/lib/playtimeEstimate.test.ts
  - src/renderer/src/components/UsageBar.tsx
  - src/renderer/src/components/UsageBar.module.css
  - src/renderer/src/components/UsageBar.test.tsx
  - src/renderer/src/components/PercentBar.module.css
  - src/renderer/src/components/PreCtaDisclosure.tsx
  - src/renderer/src/screens/CreditsScreen.tsx
  - src/renderer/src/screens/CreditsScreen.module.css
  - src/main/cloud/proxyClient.ts
  - src/main/cloud/proxyClient.test.ts
  - src/main/ipc.ts
  - src/shared/legalVersions.ts
  - /Users/ouen/slop/sei-website/terms.html
autonomous: true
requirements: [PROXY-05-OVERRIDE, PROXY-12]
user_setup: []

must_haves:
  truths:
    - "Playtime screen shows three side-by-side plan cards: Trial, Quest, Party"
    - "Each card shows plan name, a money number, a tilde-X-hours playtime line, then a buy/affordance button"
    - "The word Depleted never appears in the user-visible renderer UI"
    - "The usage-bar unused track is visibly distinct from the page background in both themes"
    - "The refund line is gone from PreCtaDisclosure; the three CA-required lines remain (price, auto-renew, cancel-in-settings)"
    - "The Party buy button still passes through AutoRenewalConsentModal before checkout"
    - "Quest/Party checkout opens inside an in-app Electron popup window, not the system browser"
    - "Manage billing is a single button at the bottom after the plan cards, with an estimate disclaimer below it"
    - "terms.html section 8 is a no-refund clause and TOS_VERSION is bumped so users re-accept"
    - "The live estimate lands on tilde-1h Trial / tilde-5h Quest / tilde-20h Party on full grants"
  artifacts:
    - path: "src/renderer/src/screens/CreditsScreen.tsx"
      provides: "Three-card PLANS section + bottom manage-billing button + disclaimer"
      contains: "PLANS"
    - path: "src/renderer/src/lib/playtimeEstimate.ts"
      provides: "Recalibrated estimator (tilde-6800 tok/min, round-to-nearest hour)"
      contains: "DEFAULT_TOKENS_PER_MIN"
    - path: "src/shared/legalVersions.ts"
      provides: "Bumped TOS_VERSION"
      contains: "TOS_VERSION"
    - path: "src/main/ipc.ts"
      provides: "In-app checkout popup BrowserWindow handler"
      contains: "credits.openCheckout"
  key_links:
    - from: "src/renderer/src/screens/CreditsScreen.tsx"
      to: "useCreditsStore.openCheckout"
      via: "Quest/Party buy buttons (Party through consent modal)"
      pattern: "openCheckout"
    - from: "src/main/ipc.ts"
      to: "BrowserWindow"
      via: "popup loads proxy-minted + allowlist-validated Polar URL"
      pattern: "new BrowserWindow"
    - from: "src/main/cloud/proxyClient.ts"
      to: "externalUrlValidator.assertSafeExternalUrl"
      via: "validate proxy URL before returning it to the popup opener"
      pattern: "assertSafeExternalUrl"
---

<objective>
Revamp the in-app Playtime/Credits screen and the policies behind it: three side-by-side
plan cards (Trial / Quest / Party) with real prices + marketing playtime hours, in-app
Electron popup checkout (replacing the system-browser handoff), removal of the "Depleted"
label and the refund line, a distinct usage-bar track color, a consolidated bottom
Manage-billing button with an estimate disclaimer, a no-refund rewrite of the website terms
+ a TOS version bump, and an investigate-then-decide audit of the Polar refund-clawback path.

Purpose: Reposition the screen as a clear plan picker with honest, marketing-aligned playtime,
keep the legally-required surfaces intact (AutoRenewalConsentModal, CA disclosure lines),
and make the refund posture consistent across GUI + website + (audited) backend.

Output: Recalibrated estimator + tests, new three-card CreditsScreen, distinct usage-bar
track, in-app popup checkout, no-refund GUI/website copy + TOS bump, and a documented
DB/edge audit finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260602-uv9-revamp-playtime-screen-3-side-by-side-pl/260602-uv9-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. No exploration required. -->

useCreditsStore selectors/actions (src/renderer/src/lib/stores/useCreditsStore.ts):
  - state: plan ('trial'|'pack'|'unlimited'|'depleted'), renews_at (string|null),
    usage_pct (number), remaining_tokens (number|undefined), trial_claimed (boolean)
  - actions:
      openCheckout(kind: 'pack' | 'subscription') => Promise<void>
      cancelSubscription() => Promise<{ok:true; portalUrl:string} | {ok:false; code:string}>
  - INVARIANT (PROXY-05, do NOT change): state shape carries NO token/dollar/micro fields.
    usage_pct / remaining_pct / remaining_tokens stay as-is. Only RENDERED card copy gains
    dollar amounts.

playtimeEstimate (src/renderer/src/lib/playtimeEstimate.ts):
  export const DEFAULT_TOKENS_PER_MIN: number
  export function tokensRemainingToPlaytime(remainingTokens?: number, tokensPerMin?: number)
    => { display: string; minutes: number }

proxyClient.openCheckout (src/main/cloud/proxyClient.ts), CURRENT shape:
  export async function openCheckout(kind: 'pack'|'subscription'):
    Promise<{ok:true} | {ok:false; code: ProxyErrorCode}>
  // Currently: mints URL via POST /billing/checkout, validates via assertSafeExternalUrl,
  // then calls shell.openExternal(url). Task 4 changes the tail so the URL is returned
  // (popup opens it in main) instead of shell.openExternal.

externalUrlValidator (src/main/lib/externalUrlValidator.ts):
  export function assertSafeExternalUrl(url: string): void  // throws if not allowlisted
  // Trusts polar.sh + dot-anchored .polar.sh suffix. DO NOT relax the allowlist.

IPC handler (src/main/ipc.ts):
  ipcMain.handle(IpcChannel.credits.openCheckout, ...) // currently delegates to proxyClient.openCheckout
  // BrowserWindow is already imported at top of ipc.ts (line 10).

createMainWindow (src/main/windowChrome.ts) — reference pattern for popup webPreferences:
  { contextIsolation: true, nodeIntegration: false, sandbox: false, preload: <main preload> }

PLAN_LABEL (src/renderer/src/screens/CreditsScreen.tsx):
  { trial:'Trial', pack:'Quest', unlimited:'Party', depleted:'Depleted' }
  // Remove the user-visible 'Depleted' string; internal 'depleted' enum value stays in the store.

Known grant constants (CONTEXT specifics; MICRO_PER_TOKEN_BLENDED = 2, so tokens = micro/2):
  Trial  $1     -> 1,000,000 microUSD  ->   500,000 tokens
  Quest  $3.825 -> 3,825,000 microUSD  -> 1,912,500 tokens
  Party  $16.65 -> 16,650,000 microUSD -> 8,325,000 tokens
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recalibrate playtime estimator to marketing hours</name>
  <files>src/renderer/src/lib/playtimeEstimate.ts, src/renderer/src/lib/playtimeEstimate.test.ts</files>
  <behavior>
    On full grants (tokens = grant_micro / 2), tokensRemainingToPlaytime().display must read:
      - Trial 500,000 tokens -> "~1h left"
      - Quest 1,912,500 tokens -> "~5h left"
      - Party 8,325,000 tokens -> "~20h left"
    Sub-hour behavior stays floor/down (never overpromise small remainders):
      - the existing under-60min tests (15min granularity, "0min left" for sub-1-min) still pass
        when given an explicit tokensPerMin of 850.
    The 60min-and-over branch rounds to NEAREST hour (was floor): e.g. 1224 min -> 20.4h -> "~20h left",
    281 min -> 4.69h -> "~5h left", 73.5 min -> 1.22h -> "~1h left".
  </behavior>
  <action>
    Per CONTEXT "Playtime numbers" decision: set DEFAULT_TOKENS_PER_MIN to 6800 (anchor; the overlap
    window is roughly 6768 to 7083 tok/min, pick a value where Quest rounds to 5 AND Party rounds to
    20). In tokensRemainingToPlaytime(), change ONLY the rawMin >= 60 branch from
    Math.floor(rawMin / 60) to Math.round(rawMin / 60) so the 60min-and-over estimate rounds to the
    nearest hour. Leave the under-60min branch (15-min floor, sub-1-min produces "0min left", the
    "Calculating..." guards) untouched. Update the file-header docstring + the inline rounding-rule
    comment to describe round-to-nearest-hour and the marketing calibration (remove the contradictory
    "aggressive DOWN-rounding never overpromise" claim for the hour branch; keep it for the sub-hour
    branch). In playtimeEstimate.test.ts: update the DEFAULT_TOKENS_PER_MIN assertion from 7700 to
    the new constant; replace the "260602-hbr flat playtime multiplier" describe block's Party/Quest
    cases with assertions that full Trial/Quest/Party token counts (500000 / 1912500 / 8325000)
    yield "~1h left" / "~5h left" / "~20h left"; the explicit-tokensPerMin(850) rounding tests stay as-is.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/src/lib/playtimeEstimate.test.ts</automated>
  </verify>
  <done>vitest passes; DEFAULT_TOKENS_PER_MIN is approximately 6800; full-grant tokens map to ~1h/~5h/~20h; the 60min-and-over branch uses Math.round.</done>
</task>

<task type="auto">
  <name>Task 2: Distinct usage-bar track color + UsageBar test update</name>
  <files>src/renderer/src/components/PercentBar.module.css, src/renderer/src/components/UsageBar.tsx, src/renderer/src/components/UsageBar.module.css, src/renderer/src/components/UsageBar.test.tsx</files>
  <action>
    Per CONTEXT "Usage-bar unused color" discretion: give the PercentBar track (.root in
    PercentBar.module.css, currently background: var(--bg-2)) a distinct, theme-aware treatment so
    the UNUSED portion reads against the page background in both themes. Add a 1px var(--border)
    inset outline (e.g. box-shadow: inset 0 0 0 1px var(--border)) and/or a slightly stronger track
    tone using existing theme tokens. No raw hex; keep accessible contrast; keep the existing fill
    (var(--accent)) and border-radius. The UsageBar continues to render PercentBar + the "~Xh left"
    estimate (do not change its store wiring or PROXY-05 percent-only behavior). Only add styling.
    Add/adjust a UsageBar.test.tsx grep-style assertion that PercentBar.module.css .root carries the
    distinct-track marker (var(--border) inset/outline). Do NOT introduce any token/dollar count into
    UsageBar (Test 5 / PROXY-05 invariant must still hold).
  </action>
  <verify>
    <automated>grep -v '^ \*' src/renderer/src/components/PercentBar.module.css | grep -c "var(--border)" && npx vitest run src/renderer/src/components/UsageBar.test.tsx</automated>
  </verify>
  <done>PercentBar .root has a distinct theme-aware track (var(--border) inset/outline + token-based tone); UsageBar tests pass; no raw hex; no token/dollar count added.</done>
</task>

<task type="auto">
  <name>Task 3: Three-card PLANS CreditsScreen — prices, playtime, bottom manage-billing, no Depleted</name>
  <files>src/renderer/src/screens/CreditsScreen.tsx, src/renderer/src/screens/CreditsScreen.module.css</files>
  <action>
    Rewrite the body of CreditsScreen.tsx per CONTEXT domain + decisions. Keep the BackRow + h1
    "Playtime" + the USAGE section (UsageBar size="lg" + neutral plan label). Then:

    (a) PLAN_LABEL: drop the user-visible 'Depleted' string. Render PLAN_LABEL[plan] ?? 'Trial' and
    change the 'depleted' entry so it never shows the word "Depleted" (e.g. map 'depleted' to 'Trial'
    or "No active plan", or omit the label for that case). The internal 'depleted' enum value stays
    in the store (do not touch the store).

    (b) Replace the stacked QUEST + PARTY sections with ONE section titled "PLANS" containing a
    flex/grid row of three cards (Trial, Quest, Party). Card body order per the explicit layout spec:
    plan NAME, then big money number, then small "~X hours of playtime" line, then button.
      - Trial card: name "Trial", money "Free" (or "$0"), playtime "~1 hour of playtime", CTA is a
        NON-checkout affordance reflecting current-plan state where known (e.g. disabled "Included" /
        "Current plan"). Trial is auto-claimed, NO Polar checkout.
      - Quest card: name "Quest", money "$5.00" with a small "one time" qualifier, playtime
        "~5 hours of playtime", button "Get a Quest" calling void openCheckout('pack') (one-time
        pack, NO consent gate).
      - Party card: name "Party", money "$20.00" with a small "/month" qualifier, playtime
        "~20 hours of playtime", button "Join a Party" calling setShowConsentModal(true). The
        AutoRenewalConsentModal consent gate MUST remain (CA ARL section 17602(b)) — Party checkout
        flows through it, never directly. Keep PreCtaDisclosure (renewsAt={renewsAt}) rendered for the
        non-subscriber Party purchase decision (its refund line is removed in Task 5).

    (c) Remove the per-card "Manage billing or unsubscribe" link, the per-card subscriber
    "Unsubscribe" button, and the showPortalFallback inline link. Add a SINGLE "Manage billing"
    button at the BOTTOM, after the PLANS section, routed through handleManage() (existing Polar
    customer-portal flow — keep handleManage). Below it add a small muted disclaimer line:
    "Playtime shown is an estimate — actual playtime varies by usage." (reuse ESTIMATE_TOOLTIP
    spirit). Keep the AutoRenewalConsentModal mount + showConsentModal state.

    (d) Update the CreditsScreen.tsx AND CreditsScreen.module.css file-header docstrings: remove the
    "NO monetary amounts" / "PROXY-05 forbids monetary amounts" claims (this screen now intentionally
    shows dollar amounts per the CONTEXT PROXY-05 override) and describe the new three-card layout.
    Note that the IPC/store state shape stays percent-only.

    (e) Add CreditsScreen.module.css styles for the three-card row (e.g. .plansRow flex/grid wrap,
    .planCard, .planName, .planPrice big number, .planPriceQualifier small, .planPlaytime small,
    .planCardActions, .manageBilling bottom button row, .estimateDisclaimer muted). Use theme tokens,
    no raw hex. At max-width 720px three approximately 226px cards fit; choose flex or grid +
    responsive wrap.
  </action>
  <verify>
    <automated>grep -c "Depleted" src/renderer/src/screens/CreditsScreen.tsx | grep -qx 0 && grep -c "PLANS" src/renderer/src/screens/CreditsScreen.tsx && grep -c "AutoRenewalConsentModal" src/renderer/src/screens/CreditsScreen.tsx && npx tsc --noEmit -p tsconfig.web.json</automated>
  </verify>
  <done>Three plan cards render (Trial/Quest/Party) with name then price then playtime then button order; no "Depleted" string in the file; AutoRenewalConsentModal gate retained for Party; single bottom Manage-billing button + estimate disclaimer present; renderer tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: In-app Electron popup checkout (replace system-browser handoff)</name>
  <files>src/main/cloud/proxyClient.ts, src/main/cloud/proxyClient.test.ts, src/main/ipc.ts</files>
  <action>
    Per CONTEXT "Checkout UX — in-app popup window": move the open step from proxyClient into the
    main-process IPC handler so the Polar checkout loads inside a hardened popup BrowserWindow.

    (a) proxyClient.ts openCheckout: keep the proxy mint (POST /billing/checkout), the
    assertSafeExternalUrl(url) allowlist gate (DO NOT relax it), and all error mapping. Change the
    SUCCESS tail: instead of shell.openExternal(url), RETURN the validated URL to the caller. New
    return shape: { ok: true; url: string } | { ok: false; code: ProxyErrorCode }. Update the
    function docstring (it no longer opens a window itself; the main IPC handler owns BrowserWindow).
    Remove the now-unused shell.openExternal call inside openCheckout; keep the shell import (the
    portal/cancelSubscription flow still uses it).

    (b) src/main/ipc.ts credits.openCheckout handler: after proxyClient.openCheckout(parsed.kind)
    returns { ok:true, url }, open the URL in a NEW popup BrowserWindow (BrowserWindow is already
    imported at top of ipc.ts). Harden the popup webContents per CONTEXT: contextIsolation:true,
    nodeIntegration:false, NO preload, parent set to the focused/main window, a reasonable size
    (e.g. 480x720). After creation: block in-popup window.open via
    popup.webContents.setWindowOpenHandler(() => ({ action: 'deny' })); constrain navigation to the
    validated Polar origin via a 'will-navigate' listener that calls assertSafeExternalUrl on the
    target (or compares origin) and event.preventDefault() on any off-allowlist navigation. Then
    popup.loadURL(url). Return the same {ok}/{ok:false,code} contract to the renderer (the renderer's
    useCreditsStore.openCheckout(kind) signature is Promise<void> and ignores the body, so the handler
    may return ok/code without a renderer-side change). The MAIN window stays untouched. Closing the
    popup is manual; balance refresh still flows through the existing webhook -> onCreditsStatusUpdate
    push (no success-detection wiring required).

    (c) proxyClient.test.ts: update the openCheckout tests — they currently assert
    shell.openExternal toHaveBeenCalledWith(checkoutUrl). Since openCheckout no longer opens a window,
    change those assertions to expect the returned shape { ok: true, url: checkoutUrl } and that
    shell.openExternal is NOT called from openCheckout. Keep the PROXY_NO_SESSION, kind-passthrough
    (body JSON.stringify({kind})), and non-allowlisted-URL-rejection (PROXY_NETWORK, no open) tests,
    adjusting them to the new return shape.
  </action>
  <verify>
    <automated>npx vitest run src/main/cloud/proxyClient.test.ts && grep -c "new BrowserWindow" src/main/ipc.ts && grep -c "setWindowOpenHandler" src/main/ipc.ts</automated>
  </verify>
  <done>openCheckout returns { ok:true, url } and no longer calls shell.openExternal; ipc.ts opens a hardened popup BrowserWindow (contextIsolation true, nodeIntegration false, no preload, setWindowOpenHandler deny, will-navigate origin guard) loading the allowlist-validated URL; allowlist NOT relaxed; proxyClient tests pass.</done>
</task>

<task type="auto">
  <name>Task 5: Remove refund line from PreCtaDisclosure (keep CA-required lines)</name>
  <files>src/renderer/src/components/PreCtaDisclosure.tsx</files>
  <action>
    Per CONTEXT "Policy scope — GUI": delete the "14-day refund on unused credits" line (the third
    disclosure paragraph) from PreCtaDisclosure.tsx. KEEP the three CA-required lines:
    (1) "$20.00 USD per month", (2) the renewal/auto-renew line (renewalLine(renewsAt)),
    (3) "Cancel anytime in Settings -> Cloud AI -> Cancel subscription". Update the component header
    docstring: remove the refund line from the "Lines rendered, in EXACT order" list and renumber so
    the auto-renew line and cancel line are the remaining required disclosures (no contradictory
    comment referencing a refund line). Do NOT touch AutoRenewalConsentModal (the "Polar handles
    checkout, payment, and refunds" sentence there is a payment-partner description, not a refund
    offer, and is out of scope for this line removal).
  </action>
  <verify>
    <automated>grep -ic "refund" src/renderer/src/components/PreCtaDisclosure.tsx | grep -qx 0 && grep -c "Cancel anytime" src/renderer/src/components/PreCtaDisclosure.tsx</automated>
  </verify>
  <done>No "refund" string remains in PreCtaDisclosure.tsx; the three CA-required lines (price, auto-renew, cancel-in-settings) still render.</done>
</task>

<task type="auto">
  <name>Task 6: No-refund website terms.html rewrite + TOS version bump</name>
  <files>/Users/ouen/slop/sei-website/terms.html, src/shared/legalVersions.ts</files>
  <action>
    WORKFLOW DEVIATION (inherited from quick 13-22 / 12-15): /Users/ouen/slop/sei-website is NOT a
    git repo on this machine. terms.html edits are persistent filesystem mods the operator deploys
    separately. The sei-repo commit for this task covers legalVersions.ts ONLY.

    (a) Rewrite terms.html section 8 "Refunds and Cancellations" (keep id="refunds" anchor + the
    "8. Refunds and Cancellations" heading) to a NO-REFUND clause: all sales are final and
    non-refundable. Use the framing "proxied AI inference credits powered by Sei", never "Anthropic
    API access" (keeps payment-partner product copy audit-safe per the D-49 convention noted in STATE
    13-22). Cancellation stops auto-renewal at the end of the current paid billing period,
    non-prorated. Remove the "14-day refund" paragraph, the "How to request a refund" paragraph, and
    the "Refunds: Unused credits refundable within 14 days..." bullet in the Subscription Terms list
    (replace that bullet with a no-refund line, e.g. "Refunds: All sales final; no refunds."). Keep
    the "Cancel anytime" online-cancel paragraph (FTC Click-to-Cancel) and the Failed-transactions
    paragraph (reconciliation is not a refund). Do not renumber other sections (section 8 stays
    section 8; only its body changes).

    (b) Bump src/shared/legalVersions.ts TOS_VERSION from '2026-05-25' to '2026-06-03' (matches the
    CONTEXT-gathered date). Leave PRIVACY_VERSION unchanged (privacy.html is NOT touched, per
    CONTEXT). Update the terms.html "Effective Date" line(s) — the header strong line AND the footer
    "Effective YYYY-MM-DD" — to the SAME new date so legalVersions.ts and the website stay in sync.
    The version mismatch makes AcceptToSModal re-prompt users on next sign-in.
  </action>
  <verify>
    <automated>grep -c 'id="refunds"' /Users/ouen/slop/sei-website/terms.html && grep -c "2026-06-03" src/shared/legalVersions.ts && grep -c "2026-06-03" /Users/ouen/slop/sei-website/terms.html && (grep -ic "refundable within 14\|14-day refund\|14 days of purchase" /Users/ouen/slop/sei-website/terms.html | grep -qx 0 && echo "no-stale-refund-OK")</automated>
  </verify>
  <done>terms.html section 8 is a no-refund clause (id="refunds" intact, no stale 14-day refund language); both terms.html Effective Date lines and legalVersions.ts TOS_VERSION read 2026-06-03; PRIVACY_VERSION untouched. NOTE: terms.html is a filesystem-only edit (operator deploys separately).</done>
</task>

<task type="auto">
  <name>Task 7: Audit Polar refund/clawback path (investigate-then-decide, record finding)</name>
  <files>.planning/quick/260602-uv9-revamp-playtime-screen-3-side-by-side-pl/260602-uv9-AUDIT.md</files>
  <action>
    INVESTIGATE-then-decide per CONTEXT "DB / Edge audit". Read supabase/functions/polar-webhook/index.ts
    and the apply_polar_event function in supabase/migrations/20260602000000_polar_billing_migration.sql.
    Confirm two things and write a short AUDIT.md finding (do NOT write a speculative migration).

    Finding 1 — cancellation is end-of-period, non-prorated: apply_polar_event maps an active sub with
    cancel_at_period_end=true to subscription_status='cancelled' (usable until ends_at), and
    subscription.canceled to 'cancelled'. This already matches "cancel = stop autorenewal at period
    end". Record CONFIRMED with the migration line references.

    Finding 2 — a refund-clawback path DOES exist (this contradicts the audit's likely "no DB change"
    expectation, so surface it deliberately): the 'order.refunded' event handler issues a compensating
    NEGATIVE ledger grant (kind='refund', credits_micro = -original) and sets subscription_status='refunded'
    (migration ~lines 286-307; polar-webhook ALLOWED_EVENTS includes 'order.refunded'). Decide and
    document the disposition: this is a POLAR-INITIATED handler — Polar is the Merchant of Record and can
    issue a refund from its own dashboard (chargeback, fraud, legal/consumer-protection obligation) even
    though Sei's stated policy is "no refunds". REMOVING the handler would leave the ledger inconsistent
    if Polar ever refunds out-of-band, which is worse than keeping it. RECOMMENDED disposition: KEEP the
    clawback handler as a safety/consistency mechanism (it reconciles the ledger when a refund happens
    upstream); the "no refunds" policy is a customer-facing posture (we do not OFFER refunds), not a
    promise that the ledger ignores an MoR-issued refund. NO DB migration required. Record this clearly,
    including: if the operator instead wants to hard-block all refunds (so a Polar dashboard refund leaves
    the ledger untouched), that is a deliberate future change to apply_polar_event's order.refunded arm —
    out of scope for this quick task unless the operator explicitly requests it.

    AUDIT.md must state: cancellation = end-of-period/non-prorated CONFIRMED; refund-clawback path EXISTS
    (cite files+lines); chosen disposition = KEEP, NO migration; rationale; and the explicit operator
    option if they later want a hard refund block.
  </action>
  <verify>
    <automated>test -f .planning/quick/260602-uv9-revamp-playtime-screen-3-side-by-side-pl/260602-uv9-AUDIT.md && grep -c "order.refunded" .planning/quick/260602-uv9-revamp-playtime-screen-3-side-by-side-pl/260602-uv9-AUDIT.md && grep -ic "no migration\|no db change\|no db migration" .planning/quick/260602-uv9-revamp-playtime-screen-3-side-by-side-pl/260602-uv9-AUDIT.md</automated>
  </verify>
  <done>AUDIT.md records: cancel-at-period-end CONFIRMED; refund-clawback path EXISTS with file+line citations; disposition KEEP + rationale; NO DB migration; operator option to hard-block refunds noted as out-of-scope-unless-requested. No migration file created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main IPC (credits:openCheckout) | renderer-controlled `kind` enum crosses into main; Zod-validated at the handler |
| proxy -> main (checkout URL) | proxy-supplied URL is loaded into a BrowserWindow; untrusted until allowlist-validated |
| popup webContents -> network | the loaded Polar page can attempt navigation / window.open to arbitrary origins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-uv9-01 | Tampering | proxy-minted checkout URL loaded in popup | mitigate | assertSafeExternalUrl(url) gate in proxyClient.openCheckout BEFORE the URL is returned; allowlist NOT relaxed (polar.sh + dot-anchored .polar.sh suffix only) |
| T-uv9-02 | Elevation of Privilege | popup BrowserWindow webContents | mitigate | contextIsolation:true, nodeIntegration:false, NO preload — the hosted Polar page gets zero Node/IPC surface (Task 4) |
| T-uv9-03 | Tampering | in-popup navigation / window.open to off-allowlist origin | mitigate | setWindowOpenHandler => deny; 'will-navigate' guard runs assertSafeExternalUrl/origin-compare + preventDefault on off-allowlist targets (Task 4) |
| T-uv9-04 | Information Disclosure | main window state | accept | popup is a separate BrowserWindow with no parent IPC bridge; main window untouched. Low risk: contextIsolation prevents cross-window access |
| T-uv9-05 | Repudiation | auto-renewal consent for Party subscription | mitigate | AutoRenewalConsentModal gate retained before Party checkout (CA ARL 17602(b)); recordSubscriptionConsent path unchanged (Task 3) |
| T-uv9-06 | Tampering | ledger consistency on upstream (Polar MoR) refund | accept | KEEP order.refunded clawback handler (Task 7 finding) so an MoR-issued refund reconciles the ledger; "no refunds" is a customer-facing posture, not a ledger guarantee |
</threat_model>

<verification>
Renderer typecheck: `npx tsc --noEmit -p tsconfig.web.json` clean.
Renderer tests: `npx vitest run src/renderer` green (UsageBar, playtimeEstimate updated; useCreditsStore unchanged and still green).
Main tests: `npx vitest run src/main/cloud/proxyClient.test.ts` green.
Greps:
  - `grep -rn "[Dd]epleted" src/renderer/src/screens/CreditsScreen.tsx` returns no user-visible string (internal enum elsewhere is fine).
  - `grep -ic refund src/renderer/src/components/PreCtaDisclosure.tsx` == 0.
  - `grep -c PLANS src/renderer/src/screens/CreditsScreen.tsx` >= 1; three plan cards present; manage-billing button after the cards.
  - `grep -c "new BrowserWindow" src/main/ipc.ts` >= 1; `grep -c setWindowOpenHandler src/main/ipc.ts` >= 1.
  - terms.html: `id="refunds"` present; no "14 days of purchase"/"14-day refund" language; Effective Date == 2026-06-03; legalVersions.ts TOS_VERSION == 2026-06-03.
Playtime: full-grant token counts (500000 / 1912500 / 8325000) assert ~1h / ~5h / ~20h with DEFAULT_TOKENS_PER_MIN ~= 6800 and round-to-nearest-hour on the >=60min branch.
</verification>

<success_criteria>
- Three side-by-side plan cards (Trial/Quest/Party) with name -> price -> "~X hours of playtime" -> button.
- No user-visible "Depleted"; distinct usage-bar track in both themes.
- Quest/Party checkout opens in a hardened in-app popup BrowserWindow (allowlist not relaxed); Party still gated by AutoRenewalConsentModal.
- Refund line removed from PreCtaDisclosure (3 CA-required lines remain); terms.html section 8 rewritten no-refund; TOS_VERSION bumped to 2026-06-03 (privacy untouched).
- useCreditsStore state shape unchanged (PROXY-05: no token/dollar/micro fields); only rendered card copy gains dollar amounts.
- Estimator recalibrated to marketing hours; AUDIT.md records cancel-at-period-end CONFIRMED + refund-clawback KEEP + NO migration.
- Renderer + main tests green; renderer tsc clean.
</success_criteria>

<output>
Create `.planning/quick/260602-uv9-revamp-playtime-screen-3-side-by-side-pl/260602-uv9-SUMMARY.md` when done.
Note in the SUMMARY: terms.html is a filesystem-only edit (sei-website is not a git repo on this machine) — the operator deploys it separately; the sei-repo commit covers legalVersions.ts only.
</output>
