---
verdict: PASS
plans_checked: 4
bucket_a_coverage: 17/17
bucket_b_leaks: 0
checked: 2026-05-06
checker: gsd-plan-checker (Bucket A gap-closure cycle, plans 07-10)
---

# Plan Check — Phase 03.1 Gap-Closure Cycle (Plans 07-10)

## Executive Summary

All four gap-closure plans (03.1-07 through 03.1-10) cover the 17 Bucket A items from VERIFICATION.md exactly once each, with no Bucket B leaks. The wave structure is a strict sequential chain (wave 2→3→4→5, each depending on the previous), which trivially satisfies the same-wave file-overlap rule because no two plans share a wave. Each plan has populated frontmatter (`gap_closure: true`, `phase: 03.1`, accurate `files_modified`), citations of the defect IDs in task names, grep-verifiable acceptance criteria, and tests routed to `scripts/test-*.mjs`. The four risk-flag items called out in the verification checklist (WR-06 R-M-W serialization, D-W-7 explicit counter, D-W-9 trigger definition, D-NEW-MEM-3 root-cause clarity) are all addressed concretely, not nudged with prose. Verdict: PASS — plans are ready for execution.

## Bucket A Coverage Matrix (17/17)

User-priority quick fixes:

| # | ID | Plan | Task | Status |
|---|---|---|---|---|
| 1 | D-NEW-TONE-1 | 07 | T1 | covered |
| 2 | D-NEW-DM-1/2/3 | 07 | T2 | covered |
| 3 | D-NEW-MEM-2 | 08 | T2 (Change 1) | covered |
| 4 | D-NEW-MEM-3 | 08 | T2 (Change 2) | covered |
| 5 | D-W-8 / D-NEW-TONE-2 | 07 | T3 | covered |
| 6 | D-W-9 | 08 | T3 | covered |
| 7 | NEW-W-A | 09 | T1 (Change 1) | covered |
| 8 | D-H-15 | 09 | T1 (Change 2) | covered |
| 9 | D-H-16 | 09 | T2 | covered |
| 10 | D-W-7 | 09 | T3 (Change 1) | covered |

Code-review WARNINGs:

| # | ID | Plan | Task | Status |
|---|---|---|---|---|
| 11 | WR-01 | 10 | T1 | covered |
| 12 | WR-02 | 10 | T2 (Change 1+2) | covered |
| 13 | WR-04 | 10 | T2 (Change 3) | covered |
| 14 | WR-05 | 08 | T2 (Change 3) | covered |
| 15 | WR-06 | 08 | T1 | covered |
| 16 | WR-07 | 09 | T3 (Change 2) | covered |
| 17 | WR-08 | 10 | T3 | covered |

**Cross-plan ID counting:** grep showed several IDs appearing in 2 plans, but inspection confirmed the second mention is always Plan 10's coverage rollup section (lines 56-60) summarizing prior plans for accounting. No double-implementation.

## Bucket B Leak Audit (0 leaks)

Items expected NOT to appear (out of scope for `--gaps` cycle):

| ID / keyword | Hits in plans | Status |
|---|---|---|
| D-NEW-SCAV-* / smart_find / veined / find() | 0 | clean |
| D-NEW-LOG-* / "debug log" / "log readability" | 0 | clean |
| D-NEW-MEM-1 / "noteToSelf activation" | 0 | clean |
| D-H-12 / "disconnect" | 1 false positive | clean |
| D-H-14 / "first-turn-say architecture" | 0 | clean |
| NEW-W-C / "pillar-up" / "scaffold" | 0 | clean |

The single "disconnect" hit is in `03.1-09-PLAN.md:522` inside a code comment about reconnect ("e.g. on disconnect/reconnect, or a future explicit-clear pathway") in the `stopFollow` change for D-H-16. Not a D-H-12 implementation reference. Clean.

## Wave / File-Overlap Audit

| Plan | Wave | depends_on | Files modified |
|---|---|---|---|
| 07 | 2 | [] | orchestrator.js, persona.js, test-postProcessSay.mjs |
| 08 | 3 | [07] | orchestrator.js, sessionState.js, affectLog.js, owner.js, fileLock.js, +3 tests |
| 09 | 4 | [07, 08] | drop.js, attack.js, follow.js, registry.js, orchestrator.js, types.js, mc/index.js, src/index.js, +3 tests |
| 10 | 5 | [07, 08, 09] | fsm.js, orchestrator.js, loop.js, +2 tests |

All four plans land in different waves (sequential chain). The "no two plans in the same wave share a `files_modified` entry" rule is **trivially satisfied** since each wave has exactly one plan. The orchestrator.js touchpoints in plans 07/08/09/10 are correctly serialized via `depends_on` so each plan applies its diff on top of the previous plan's file state. Plan 09's coverage rationale section (lines 73-74) explicitly notes this design choice. Plan 10's coverage rationale (lines 62-63) echoes the same.

`depends_on` integrity: linear forward chain, no cycles, no missing references, no forward references.

## Per-Plan Verdicts

### Plan 03.1-07 — Tone & say polish — PASS

- Bucket A items: D-NEW-TONE-1, D-NEW-DM-1/2/3, D-W-8/D-NEW-TONE-2 (3 items)
- Tasks: 3 — all `auto`, all have files+action+verify+done
- T2 includes a `tdd: true` flag with 6 named behavior cases
- Acceptance criteria are grep-verifiable; T1 includes a defensible "either form" alternative for the unicode-escape grep gate
- D-NEW-DM-1/2/3 fix is byte-equality-after-normalize (not fuzzy), gated by `loop._triggerEvent === 'sei:loop_end'` and a 2000ms window — the design choice is justified inline against the L21/L27 13ms gap and the L183/189/197 3s gap
- T3 cap-close fix correctly distinguishes "model returned empty" vs "API threw", logs both branches, and runs the model output through `postProcessSay`
- Issues: none blocking

### Plan 03.1-08 — Memory & diary fixes — PASS

- Bucket A items: D-NEW-MEM-2, D-NEW-MEM-3, D-W-9, WR-05, WR-06 (5 items)
- Tasks: 3 — all complete, T2 marked `tdd: true` with 8 named behavior cases
- **WR-06 R-M-W serialization (risk flag):** Plan introduces `withFileLock(filePath, async () => { load → mutate → atomicWrite })` so the entire R-M-W sequence is held under a single lock acquisition. The `_writeOwnerSerialized` helper avoids same-path lock nesting (would self-deadlock). T1's test runner exercises 20 concurrent appendAffect + 20 concurrent appendNote and asserts ALL 40 lines persist — proves serialization, not just hides the race.
- **D-NEW-MEM-3 (risk flag — investigative):** Plan does the investigation in the interfaces section (lines 113-135 quote the exact `if (compactor && isIdleEvent) { ... }` guard) and concludes the byte-cap gate must be promoted out from under `isIdleEvent`. The fix is concrete (lines 538-575 in the plan) and explicitly preserves the loop-count cap and consolidation-pressure paths under idle. T6/T7 in the test runner cover both the byte-cap firing and the no-mutation/no-affect skip path. Math sanity (8 × 4096 = 32768 → exactly 1 flush across 11 ticks) is called out and verifiable.
- **D-W-9 (risk flag — trigger):** Trigger is explicit: `oversize > 0 && all.length > 5` where oversize = body word count > 80. Consolidated `## Earlier (...)` blocks are excluded from the count, preventing rewrite loops. Test T9 exercises the counter directly.
- **WR-05:** Task 2 Change 3 wraps the consolidation kick in try/catch around the `.then().catch().finally()` chain, releasing the lock on synchronous throw. Same pattern applied at both kick sites. Test T8 forces a sync throw and proves a second consolidation goes through.
- Issues: none blocking. (Minor: the `withFileLock` impl in T1 has a verbose first draft followed by a "cleaner final form" — executor will need to use the clean form. Acceptable.)

### Plan 03.1-09 — Minecraft adapter behaviors — PASS

- Bucket A items: NEW-W-A, D-H-15, D-H-16, D-W-7, WR-07 (5 items)
- Tasks: 3 — all complete, T1 and T2 marked `tdd: true`
- **D-W-7 (risk flag — explicit counter):** `loop._cantReachMap = new Map()` keyed on `${x}|${y}|${z}|${range}` and `loop._cantReachNudgedKeys = new Set()` for one-shot nudging. Counter-based, not prompt-only. The 2nd-hit nudge text is concrete and references the existing SYSTEM_INSTRUCTIONS Pathfinder rule. The cant_reach nudge correctly takes precedence over the silence nudge via `cantReachNudge ?? silenceNudgeText`.
- **NEW-W-A:** Multi-slot accumulation logic is precisely specified (filter all matching slots → sum counts → loop bot.toss until target hit). Test T3 covers the under-count case ("dropped 7 oak_log (only 7 available)").
- **D-H-15:** Refusal covers the three known mineflayer entity classes (object, orb, global) plus the `entity.name === 'item'` defensive fallback for version skew. Player refusal preserved.
- **D-H-16:** Defense-in-depth (clear `_target` in stopFollow) AND assertion (unfollow handler reads back `getFollowTargetLabel()` and reports failure if non-null).
- **WR-07:** `_adapter` captured at module scope; `detach()` exposed on adapter; called in BOTH `onEnd` (before `_bot = null`) and `stop()`. detach itself idempotent.
- T3 has no end-to-end test runner for the cant_reach nudge — the rationale ("requires building an end-to-end orchestrator harness") is reasonable for a gap-closure cycle, and the grep gates enforce wiring presence (≥3 hits for `_cantReachMap`, ≥2 for `_cantReachNudgedKeys`, ≥3 for `cantReachNudge`).
- Issues: none blocking

### Plan 03.1-10 — FSM concurrency & error handling — PASS

- Bucket A items: WR-01, WR-02, WR-04, WR-08 (4 items)
- Tasks: 3 — T1 and T2 marked `tdd: true`
- **WR-01:** Fix is precisely the right one — drop `processing = false` from the re-queue branch and let the in-flight action's trailing `setImmediate(processNext)` drain the queue. T1 in the test runner explicitly exercises the held-P0/queued-P3 scenario the bug describes.
- **WR-02:** Replaces `Object.defineProperty` with a clean `loop._setAbortController` setter and re-bridges the external signal on every swap via a dedicated `bridgeExternalAbort(loop)` helper. Test T1 proves the swap; the WR-02 grep gates (`_externalSignal` ≥3 hits, `bridgeExternalAbort` ≥3 hits, no remaining `Object.defineProperty(loop, 'abortController'`) cover the wiring.
- **WR-04:** Plan correctly identifies that `pendingAttack` early-return drops `pendingInterrupt`. Fix forwards the chat text via `pa.preservedInterrupt` and re-enqueues at P1 after the P0 attack re-enqueue. Plan transparently flags that no end-to-end test runner is included for WR-04 (orchestrator harness too costly for this cycle); grep gates (`preservedInterrupt` ≥3 hits, `WR-04 preserved interrupt re-enqueued` =1) verify wiring.
- **WR-08:** Narrows the catch to ENOENT/EACCES, plumbs `logger` (default `console`) through composeSeedBlocks signature, surfaces non-fs errors as warns. Existing `affectLog` test still must pass.
- Issues: none blocking. (Minor: WR-04 verification depth is grep-only — acceptable per the plan's explicit rationale, and the live replay following execution will catch any wiring miss.)

## Risk-Flag Resolution Summary

| Risk flag | Resolution | Severity |
|---|---|---|
| D-NEW-MEM-3 investigative — clear decision tree? | Plan does the investigation in interfaces and presents a single concrete fix (broaden byte-cap gate from idle-only to any loop_terminal). Not a tree, but justification is sound. | none |
| WR-06 R-M-W race — actually serialize? | Yes: `withFileLock(path, async () => { load → mutate → atomicWrite })` holds the lock across the entire R-M-W. T1 proves 40 concurrent writes preserve every entry. | none |
| D-W-9 trigger clearly defined? | Yes: oversize body > 80 words, gated on >5 entries total, excludes consolidated blocks. T9 exercises the counter. | none |
| D-W-7 explicit counter not prompt-only? | Yes: `loop._cantReachMap` Map + `loop._cantReachNudgedKeys` Set, keyed on full destination tuple. | none |
| File-overlap analysis on orchestrator.js | Wave structure is sequential (07→08→09→10), so file overlap is N/A. Each plan applies its diff on top of the previous. Plan 09 + Plan 10 explicitly cite this in their coverage rationale. | none |

## Gates / Issues

No blockers. No warnings. All seven verification-checklist categories pass:

- Coverage: 17/17 with no double-implementation
- Bucket B: 0 leaks
- Wave/parallelism: sequential chain, depends_on integrity OK
- Task quality: all tasks have read_first, action with concrete values, verify with `<automated>`, done with measurable acceptance
- Frontmatter: `gap_closure: true`, `phase: 03.1`, accurate files_modified in all four plans
- Goal-backward: must_haves.truths in each plan trace to the assigned defect IDs
- Test home: all new tests routed to `scripts/test-*.mjs` (test/ is gitignored, scripts/ is the established home)

## Recommendation

**PASS.** Execute plans 07 → 08 → 09 → 10 in order. After execution, run a live replay against the same scenarios (wood, hunt+sand, memory) and re-verify the 17 items have moved from gap to fixed. The two non-end-to-end-tested wirings (D-W-7 cant_reach nudge, WR-04 preserved interrupt) should be the focal points of that live replay.
