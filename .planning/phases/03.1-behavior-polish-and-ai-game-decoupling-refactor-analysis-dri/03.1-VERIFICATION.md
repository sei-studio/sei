---
phase: 03.1-behavior-polish-and-ai-game-decoupling-refactor
verified: 2026-05-06
status: gaps_found
goal: "Behavior polish + AI/game decoupling refactor — close cited defects from 4-log analysis (37 total) AND establish brain/adapter directory split per architectural decisions D-5/D-6."
plans_complete: 6
plans_total: 6
verifier: orchestrator (verifier agent stream-timed-out, evidence aggregated from VALIDATION.md, 03.1-REVIEW.md, and all 6 SUMMARY.md files)

structural_goal:
  refactor_brain_adapter_split: PASS
  cited_defects_total: 37
  cited_defects_fixed: 23
  cited_defects_partial: 9
  cited_defects_deferred: 4
  cited_defects_not_exercised: 11

live_validation:
  source_logs_replayed: 3
  source_logs_missing: 1   # explore-postfix.txt was 0 bytes / not exercised
  new_defects_surfaced: 17

code_review:
  depth: quick
  files_reviewed: 17
  blocker: 0
  warning: 8
  info: 6
---

# Phase 03.1 Verification

## Goal Achievement

**Structural goal: PASS.** The brain/adapter directory split is in place and verifiable:
- `grep -rl 'mineflayer' src/brain/` returns nothing — brain is mineflayer-clean
- `grep -rl 'adapter/minecraft' src/brain/` returns nothing — brain doesn't know its adapter
- All 14 Adapter contract members from `src/brain/types.js` are implemented in `src/adapter/minecraft/index.js`
- Cache prefix change is intentional (Plan 03's coordinated cache-bust window)
- `node -e "import('./src/index.js')"` exits 0; build is clean

**Cited-defect goal: MOSTLY ACHIEVED.** Of the 37 cited defects from the 4-log analysis:
- 23 VERIFIED-FIXED (code-path proven by static checks AND, where exercised, behavior confirmed in postfix logs)
- 9 PARTIAL (structural fix shipped but live behavior surfaces a sub-issue)
- 4 DEFERRED with rationale per CONTEXT D-3 / Plan 04 deferred_with_rationale (D-W-5, D-E-2, D-H-2-text, D-M-3)
- 11 NOT-EXERCISED — 10 are D-E-* (explore-postfix.txt empty) + D-H-5 / D-H-9 (sand task wasn't run)

## Plan-by-Plan Roll-up

| Plan | Focus | Verdict |
|------|-------|---------|
| 03.1-01 | Brain/Adapter directory split (mechanical relocation) | ✓ PASS — 18 brain files + 24 adapter files moved with history; Adapter contract authored |
| 03.1-02 | Border-file split + orchestrator mineflayer leak removal | ✓ PASS — bot.js/fsm.js split, all 3 leaks closed, build restored, cache prefix byte-equality preserved through this commit |
| 03.1-03 | Coordinated cache-bust prompt window | ✓ PASS — postProcessSay (RED→GREEN), -1004 bytes SYSTEM_INSTRUCTIONS, FRAMING_LINE, loopHistory cap 20→10. **NOTE:** postProcessSay over-strips per live finding D-NEW-TONE-1 |
| 03.1-04 | Memory write-side fix | ✓ PASS — affectLog/AFFECT.md works end-to-end (memory-postfix proves it); OWNER.md.appendNote works; OR-gate diary writes structurally in place. **NOTE:** model activates noteToSelf only in explicit memory scenarios (D-NEW-MEM-1) |
| 03.1-05 | Behavior cleanup wave (14 cited defects) | ✓ PASS — 14/14 defects have proven code paths; static tests pass. **NOTE:** model behavior in some cases doesn't fully consume the new affordances (e.g. D-W-2 model still emits 7-way digs even though cap aborts them) |
| 03.1-06 | Validation against 4 source logs | ✓ PASS — 7/7 invariants pass, 4/4 test runners green, all 37 defects have a verdict. Live replay surfaced 17+ new defects (catalogued in VALIDATION.md). |

## Gaps Surfaced (Live Replay + Code Review)

Per the user's direction "Please fix these", the gaps fall into two buckets:

### Bucket A — `/gsd-plan-phase 03.1 --gaps` (single follow-up phase)

**User-priority quick-fixes:**

1. **THEME 2 — Punctuation policy refinement (user spec given verbatim).** postProcessSay over-strips. User's exact spec: keep `,` `!` `?` and apostrophes; strip only `.` (terminal) and `–` (em-dash) and `"` and backtick. Single character-class change in `src/brain/orchestrator.js::postProcessSay`. Update `scripts/test-postProcessSay.mjs` cases. — *D-NEW-TONE-1*

2. **THEME 1 — Double messaging suppression.** `sei:loop_end` re-fires LLM that re-emits say() bytes-identical to previous. Root cause overlaps with code-review **WR-03** (loop_terminal re-enqueue chain). Add: orchestrator-level dedupe predicate that suppresses say() in loop_end loops if last say() was <2s ago and content is byte-equal or near-equal. — *D-NEW-DM-1/2/3*

3. **THEME 3 — noteToSelf exempt from first-turn-say.** One-line predicate change in `shouldRepromptForFirstTurnSay` — saves 1 LLM iteration per memory-write turn. — *D-NEW-MEM-2*

4. **THEME 3 — Diary flush threshold investigation.** Cumulative_bytes hit 70349 with no diary write; either threshold is loop-counted not byte-counted at runtime, or SIGINT path bypasses session-end flush. Read code, fix root cause. — *D-NEW-MEM-3*

5. **THEME 2 — Iteration-cap fallback string.** Static "okay, brain melting — taking five." string user explicitly called out. Let model author its own one-liner. — *D-W-8 / D-NEW-TONE-2*

6. **D-W-9 — Force-recompact legacy purple diary.** Legacy entries persist (model reads them every turn). Need a one-shot rewrite-on-startup or a "compact immediately if any entry exceeds N words" pass.

7. **NEW-W-A — dropItem(count=10) drops 9.** Off-by-one between hand-slot and inventory accounting in `src/adapter/minecraft/behaviors/drop.js`.

8. **D-H-15 — attackEntity refuse on item-class entity.** Add target-class filter in `src/adapter/minecraft/behaviors/attack.js`.

9. **D-H-16 — Stale follow_target snapshot field.** Clear field when unfollow resolves in `src/adapter/minecraft/observers/snapshot.js`.

10. **D-W-7 — Force say() on 2× cant_reach same destination.** Path-help rule already in SYSTEM_INSTRUCTIONS but unobserved firing — verify execution path.

**Code-review WARNINGS that should land in the same `--gaps` cycle:**

11. **WR-01 — fsm.js priority queue stall.** Lower-priority item re-queued during higher-priority hold; `processing=false` with no scheduled wake-up.
12. **WR-02 — replaceAbortController bridge.** Second-turn interrupts not routed to the new controller's signal.
13. **WR-04 — pendingInterrupt vs pendingAttack race.** Attack arriving after chat interrupt forfeits chat text silently.
14. **WR-05 — consolidationLock leak on synchronous throw.** Lock never reset, blocking future compactions.
15. **WR-06 — OWNER.md / AFFECT.md R-M-W race.** appendNote/setPreferredName/saveOwner can lose `total_sessions` / `last_seen` updates under concurrent calls.
16. **WR-07 — bot.on('end') reconnect dispose leak.** Old bot listeners only GC-eligible by reference release.
17. **WR-08 — composeSeedBlocks swallows non-ENOENT errors.** Hides coding bugs.

### Bucket B — Needs new discuss/plan cycles (out of `--gaps` scope)

1. **THEME 5 — Major design decision: Veined tallying + smart_find + find()** (user's exact words: *"i think we finally can make scavenging resources work"*). Snapshot composer rewrite from "16 nearest blocks by distance" to "veins by type, nearest representative + connected count". Cross-chunk navigation needs a new tool. Natural-language→item-id mapping is a closed-world resolver. **Milestone-scope.** — *D-NEW-SCAV-1/2/3*

2. **THEME 4 — Debug log human readability.** Switch from line-tee to event-per-line emission with explicit `\n` between `[haiku?]/[haiku!]/[chat->]` sections. Elide repeated cache-prefix JSON in `[haiku?]` events via hash reference. Touches the logger format used everywhere — needs design decision on canonical event-record shape. — *D-NEW-LOG-1/2*

3. **THEME 3 — Strengthen noteToSelf activation.** Plumbing works, model just doesn't fire it consistently in non-memory scenarios. Push (LLM-decides) vs pull (rule-based extractor) — design choice. — *D-NEW-MEM-1*

4. **D-H-12 — Disconnect-on-attacked regression.** User: *"keeps disconnecting when attacked, this is an old issue we've fixed"*. Confirmed at hunt+sand-postfix lines 104, 179. Bisect needed against last-known-good before deciding whether to fix in `--gaps` or as a dedicated phase with regression test scaffolding.

5. **D-H-14 — first-turn-say architecture.** Prefill-in-prompt vs post-hoc abort+retry. Either approach is structural.

6. **NEW-W-C — Pillar-up / scaffolding behavior.** New behavior; touches DIG_DESCRIPTION, places, equip. New phase.

## Recommendation

Phase 03.1's planned scope is delivered; the structural goal (refactor + 37-defect catalog) is achieved per the data above. The gaps are real but were surfaced BY the validation step working correctly — the phase did its job, and live testing exposed everything it was supposed to expose.

**Recommended next step:** `/gsd-plan-phase 03.1 --gaps` to plan Bucket A as a single gap-closure cycle (~17 items, mostly small/targeted). Bucket B items should be queued as new phases or backlog entries; the scavenging redesign is at minimum a dedicated phase, possibly its own milestone.

Do NOT mark Phase 03.1 complete in ROADMAP until Bucket A is closed; do mark all 6 plans complete (they are).

## Files of Interest

- `.planning/phases/03.1-.../VALIDATION.md` — full per-defect Live verdicts + 17 new defects organized by Theme + classification matrix
- `.planning/phases/03.1-.../03.1-REVIEW.md` — 8 WARNING + 6 INFO from code review
- `logs/wood-postfix.txt`, `logs/hunt+sand-postfix.txt`, `logs/memory-postfix.txt` — user-captured live logs with embedded feedback
- `logs/explore-postfix.txt` — empty (0 bytes); rerun the explore scenario to validate D-E-*
