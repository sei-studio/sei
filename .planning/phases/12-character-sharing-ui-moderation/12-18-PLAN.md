---
phase: 12
plan: 18
type: execute
wave: 4
depends_on: [12-02, 12-03, 12-04, 12-05, 12-06, 12-07, 12-14, 12-15, 12-16]
files_modified:
  - src/renderer/src/components/DmcaContactModal.tsx
  - ../sei-website/terms.html
  - userData/config.json
autonomous: false
requirements: [SHARE-09]
requirements_addressed: [SHARE-09]
tags: [checkpoint, dmca, rollout, browse-enabled-flip]
user_setup:
  - service: dmca-registration
    why: "US Copyright Office registration is statutorily required before Browse goes live"
    env_vars: []
    dashboard_config:
      - task: "Submit DMCA Designated Agent eForm + pay $6 fee"
        location: "https://dmca.copyright.gov/"
      - task: "Capture registration receipt URL (public directory listing)"
        location: "dmca.copyright.gov user account"
must_haves:
  truths:
    - "DMCA agent registered with US Copyright Office; receipt URL captured"
    - "DmcaContactModal placeholder constants swapped for real registered values"
    - "terms.html §7 placeholder <span> ids swapped for real values"
    - "backfill-moderate-existing run loop returns processed=0 (D-36 pre-flight (b))"
    - "Edge Functions deployed: moderate-character-images, moderate-character-prompt, notify-report, submit-report, backfill-moderate-existing (D-36 pre-flight (c))"
    - "migration applied + Database Webhook configured in Supabase dashboard (D-36 pre-flight (d))"
    - "userData/config.json browse_enabled set to true ONLY AFTER all above gates pass"
---

<objective>
Final rollout checkpoint. Cannot complete autonomously — DMCA agent registration is a 24-48h manual process at dmca.copyright.gov requiring human submission of a $6 form. Once registration is confirmed AND all pre-flight gates pass (D-36 a/b/c/d), human flips BROWSE_ENABLED in production config.

Output: live registration receipt URL captured + placeholder swaps + BROWSE_ENABLED flipped to true.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-character-sharing-ui-moderation/12-CONTEXT.md
@.planning/phases/12-character-sharing-ui-moderation/12-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Register DMCA Designated Agent at dmca.copyright.gov</name>
  <action>
**Manual: human-only — no CLI/API available.**

Steps for the operator (Sei founder, sole proprietor per D-35):

1. Go to https://dmca.copyright.gov/.
2. Create an account (if not already done).
3. Submit the Designated Agent eForm:
   - Legal name (real — public per Copyright Office mandate, no PO Box workaround)
   - Mailing address (real residential or business address)
   - Email: dmca@sei.app (user-confirmed already set up)
   - Service: Sei (the desktop application)
4. Pay the $6 / 3-year fee via the form.
5. Wait for confirmation (24-48h typical processing).
6. Once approved, **capture the public Directory listing URL** — this is the link 12-14 modal + 12-15 terms.html §7 cite.
  </action>
  <resume-signal>Type "registered" + paste the receipt/Directory URL once Copyright Office confirmation lands.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Swap placeholder agent details in DmcaContactModal + terms.html</name>
  <files>src/renderer/src/components/DmcaContactModal.tsx, ../sei-website/terms.html</files>
  <action>
After Task 1 completes (operator provides registration data):

1. In `src/renderer/src/components/DmcaContactModal.tsx`, replace:
   - `AGENT_NAME = '[Designated Agent — pending registration]'` → `AGENT_NAME = '<real legal name>'`
   - `AGENT_ADDRESS = '[Mailing address pending registration]'` → multiline string with the real mailing address
   - `DIRECTORY_LISTING_URL = 'https://dmca.copyright.gov/list'` → the specific public listing URL captured in Task 1

2. In `../sei-website/terms.html` §7, replace the placeholder `<span>` contents:
   - `<span id="dmca-name">[Designated Agent — pending registration]</span>` → `<span id="dmca-name">Real Legal Name</span>`
   - `<span id="dmca-address">[Mailing address pending registration]</span>` → real address (with `<br>` line breaks)
   - `<a href="https://dmca.copyright.gov/list" id="dmca-receipt">` → href set to the captured public listing URL

3. Commit: `docs(12-18): swap DMCA agent placeholders for live registration values`
  </action>
  <verify>
    <automated>grep -c "pending registration" src/renderer/src/components/DmcaContactModal.tsx ../sei-website/terms.html | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <done>All "pending registration" placeholder strings removed; real agent values in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pre-flight checklist for BROWSE_ENABLED flip</name>
  <what-built>
    All Wave 1-3 plans landed + 12-14, 12-15, 12-16, 12-17 + Task 2 above completed.
  </what-built>
  <how-to-verify>
    Verify ALL D-36 pre-flight conditions before flipping BROWSE_ENABLED:

    (a) DMCA registration confirmed — Task 1 above is complete; receipt URL captured.

    (b) Backfill complete:
        - Run `curl -X POST -H "Authorization: Bearer $SERVICE_ROLE" https://<ref>.supabase.co/functions/v1/backfill-moderate-existing` in a loop until response is `{processed: 0, ..., nextCursor: 'done'}`.
        - Verify via SQL: `select count(*) from public.characters where shared = true and moderation_status is null` returns `0`.
        - Verify via SQL: `select count(*) from public.characters where shared = false and moderation_status = 'flagged'` shows the auto-rejected rows (review whether any need manual restoration).

    (c) Edge Functions deployed:
        - `supabase functions list` shows all five: moderate-character-images, moderate-character-prompt, notify-report, submit-report, backfill-moderate-existing.
        - Each function has its required secrets set (`supabase secrets list` shows SIGHTENGINE_*, OPENAI_API_KEY, DISCORD_REPORT_WEBHOOK_URL, RESEND_API_KEY, SUPABASE_SERVICE_ROLE_KEY).

    (d) Migration applied + Database Webhook configured:
        - `supabase migration list` shows `20260523000000_moderation_and_reports.sql` applied.
        - In Supabase Dashboard → Database → Webhooks, verify `report_new_webhook` exists with target = `https://<ref>.supabase.co/functions/v1/notify-report` + Authorization Bearer service_role header.
        - `ALTER DATABASE postgres SET app.settings.edge_url = '...'` and `app.settings.service_role_key = '...'` executed (per 12-01 Task 4 SUMMARY).

    (e) End-to-end smoke (manual):
        - Sign in to Sei (production build).
        - Publish a test character with a clean portrait + persona → succeeds, appears under My Library.
        - On a SECOND account, Browse tab is visible after BROWSE_ENABLED flip; the test character appears.
        - Click "Add to Mine" → character appears under second account's library.
        - Click Report → ReportModal submits → Discord webhook fires + dmca@sei.app receives email.
        - Submit 6 reports rapidly → 6th returns friendly rate-limit copy.
        - Attempt to publish a character with a deliberately-flaggable persona (e.g., a hate-speech sentence) → block error renders with friendly D-33c copy.
  </how-to-verify>
  <resume-signal>Type "all-gates-clear" once every checkbox above passes. If any fail, describe what's broken and DO NOT flip BROWSE_ENABLED.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Flip BROWSE_ENABLED in production config.json</name>
  <files>userData/config.json</files>
  <action>
After Task 3 confirms all gates pass:

1. Locate the production `<userData>/config.json` (on the operator's machine this is platform-specific — macOS `~/Library/Application Support/sei/config.json`, Windows `%APPDATA%/sei/config.json`, Linux `~/.config/sei/config.json`).
2. Edit (or create) the file and set `"browse_enabled": true`.
3. Restart Sei.
4. Verify: CharactersScreen shows the Home + Browse tab bar.

Note for executors: this is a manual config edit, not a code change. The file is in user-specific application support directory; it is NOT in the git repo. Document the exact path used in the SUMMARY.

If the project ships an installer to other users later, document a `BROWSE_ENABLED=true` env var path for them (same module, dev override path from D-36a).
  </action>
  <verify>
    <automated>echo "Manual flip — verified via Task 3 smoke test step (e) requiring Browse tab visible after restart" && true</automated>
  </verify>
  <done>config.json browse_enabled=true; CharactersScreen renders tab bar on next launch.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-18-01 | Information Disclosure | Public listing of legal name + home address | accept | Statutorily required by 17 U.S.C. § 512 — no anonymous DMCA agent option exists. D-35 documented |
| T-12-18-02 | Tampering | Flipping browse_enabled before checklist passes | mitigate | Task 3 human-verify gate; operator MUST type "all-gates-clear" before Task 4 |
| T-12-18-03 | DoS | Going live with un-backfilled characters | mitigate | search_public_characters filters moderation_status — pre-backfill rows show via `null OR clean` clause; checklist verifies count=0 before flip |
| T-12-18-04 | Repudiation | Operator skips the smoke test | accept | Trust model: solo operator; checklist is self-policed. Future hardening: add a programmatic preflight script. |
</threat_model>

<verification>
1. Task 1 returned receipt URL.
2. Task 2 grep for "pending registration" returns 0.
3. Task 3 checklist confirmed.
4. Task 4 config flip done; Browse tab visible.
5. End-to-end smoke (publish → browse → add-to-mine → report → moderation block) all green.
</verification>

<success_criteria>
- DMCA agent registered + receipt URL captured.
- All placeholders swapped for real values (no grep hit for "pending registration").
- D-36 pre-flight (a/b/c/d) confirmed via Task 3 checklist.
- config.json flip executed; Browse tab live in production.
</success_criteria>

<output>
After completion, create `.planning/phases/12-character-sharing-ui-moderation/12-18-SUMMARY.md` documenting:
- DMCA registration receipt URL (committed to docs)
- D-36 pre-flight checklist results
- Exact userData path used for the config flip
- Smoke test results (publish / browse / add / report / moderation block)
- Phase 12 closure status — SHARE-01..SHARE-10 all live
</output>
