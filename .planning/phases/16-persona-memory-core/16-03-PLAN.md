---
phase: 16-persona-memory-core
plan: 03
type: execute
wave: 3
depends_on: ["16-01"]
files_modified:
  - src/bot/brain/memory/memoryLog.js
  - src/bot/brain/orchestrator.js
  - src/bot/brain/prompts.js
autonomous: false
requirements: [MEM-01, MEM-02, MEM-03, MEM-06]
must_haves:
  truths:
    - "MEMORY.md is reliably injected into the assembled prompt each session"
    - "The remember() contract steers subjective, well-paced entries and bans transaction logs"
    - "The companion is instructed to bring up what it knows about the player during ordinary play, not only when asked"
    - "Runtime memory stays local-only and is never synced to the cloud"
  artifacts:
    - path: "src/bot/brain/orchestrator.js"
      provides: "verified memory injection wiring in composeSeedBlocks"
      contains: "readMemoryForSeed"
    - path: "src/bot/brain/prompts.js"
      provides: "tuned remember() cadence + active-callback framing"
      contains: "remember"
  key_links:
    - from: "composeSeedBlocks"
      to: "readMemoryForSeed(config.memory.memory_md_path)"
      via: "memory block pushed with memoryPreamble"
      pattern: "readMemoryForSeed"
    - from: "PERSONALITY_TOOL_DESCRIPTIONS.remember"
      to: "subjective entries, proper cadence, banned transaction logs"
      via: "GOOD/BAD example steering"
      pattern: "remember"
---

<objective>
Make the memory plumbing correct and the remember()/active-recall contract sharp,
without adding scoring, importance ratings, or a facts store (descoped per D-07).
Diagnose and fix any defect in MEMORY.md injection (MEM-01), confirm mid-session
appends are usable within the same session (MEM-04 is owned by Plan 01's runtime
but verified here for the memory path), tune the remember() cadence/content
contract (MEM-02), reinforce active fact-recall in conversation (MEM-03), and
confirm runtime memory stays local-only (MEM-06).

Purpose: Memory is the substrate that makes the companion not-a-stranger. This
phase keeps it dead simple (append-only MEMORY.md, current seeding) and only fixes
injection correctness and usage quality.

Output: A verified/fixed memory injection path, a tuned remember() description, an
active-recall reinforcement, and tests/assertions proving MEMORY.md reaches the
prompt and never leaves the machine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-persona-memory-core/16-CONTEXT.md
@.planning/phases/16-persona-memory-core/16-PATTERNS.md
@.planning/phases/16-persona-memory-core/16-01-SUMMARY.md

<interfaces>
<!-- The memory injection path the executor must verify, extracted from the codebase. -->

composeSeedBlocks (orchestrator.js lines 293-317): memory injection is GATED on
config.memory.memory_md_path. If unset on a config path, memory is silently NOT
injected. The memory block (line 310) is pushed with memoryPreamble (lines 305-309)
BEFORE the heartbeat cache breakpoint (line 348).

readMemoryForSeed (memoryLog.js lines 150-192): reads full file, returns as-is under
budgetBytes, else truncates oldest entries newest-first while preserving the
`# Memory` header and ALL `## World N` markers. Called with
config.memory.seed_memory_budget_bytes ?? 8192.

appendMemory (memoryLog.js lines 78-99): one-arg `remember(text)`, append-only,
atomic write under a file lock. Stays one-arg per D-07 (no importance score).

PERSONALITY_TOOL_DESCRIPTIONS.remember (prompts.js lines 58-77): the cadence/content
contract with GOOD/BAD example shapes (lines 61-72) and a quality-bar paragraph
(lines 74-75). This is the tuning target.

memoryPreamble (orchestrator.js lines 305-309): already cues active callbacks
("BRING IT UP naturally"). The MEM-03 reinforcement seam.

Invariants to preserve: `## World N` markers are NOT entry lines (`- [`); both
readMemoryForSeed and the compactor preserve them; forget() only deletes `- [` lines.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diagnose and fix MEMORY.md injection correctness</name>
  <read_first>
    - src/bot/brain/orchestrator.js composeSeedBlocks (lines 293-351; the memory_md_path gate at line 293; memoryPreamble lines 305-309; the heartbeat cache breakpoint comment lines 341-348)
    - src/bot/brain/memory/memoryLog.js readMemoryForSeed (lines 150-192) and readMemoryFull (lines 134-144)
    - .planning/phases/16-persona-memory-core/16-PATTERNS.md (§memoryLog.js — "Injection wiring to verify", "Invariants to preserve")
    - .planning/phases/16-persona-memory-core/16-CONTEXT.md (D-08, D-09, Claude's Discretion on the injection mechanism)
    - src/bot/brain/index.js (where config.memory.memory_md_path is set for production — confirm it is always set)
  </read_first>
  <behavior>
    - With config.memory.memory_md_path set and a MEMORY.md present, composeSeedBlocks produces a block named 'memory' whose text includes the memoryPreamble and the readMemoryForSeed output.
    - With memory_md_path set but the file absent (ENOENT), composeSeedBlocks does NOT throw and produces no spurious memory block (or an empty one) — matching current graceful handling.
    - readMemoryForSeed preserves the `# Memory` header and every `## World N` marker even when truncating to budget (existing invariant — assert it still holds).
    - The diagnosis confirms production (src/bot/brain/index.js) always sets memory_md_path so memory is never silently dropped.
  </behavior>
  <action>
    Diagnose the injection path end to end: confirm config.memory.memory_md_path is
    always set on the production bot entry (src/bot/brain/index.js) so the line-293
    gate never silently skips memory; if any production path leaves it unset, fix it
    so memory is reliably injected (MEM-01). Verify the cache-breakpoint assumption
    at lines 341-348 is consistent with mid-session remember() appends (the comment
    claims memory+heartbeat are "stable across consecutive loops" but remember()
    appends mid-session) — since composeSeedBlocks re-reads MEMORY.md fresh per loop
    seed via readMemoryForSeed, confirm a mid-session append is reflected on the next
    fresh-loop seed (this is the memory-path side of MEM-04, whose runtime ownership
    is Plan 01). If the cache assumption is actually wrong (a stale cached memory
    block hides a fresh append), document and fix it; if it is correct (fresh re-read
    each seed), assert it with a test and leave the cache wiring untouched. Make NO
    scoring/importance/facts-store changes (D-07/D-09); appendMemory stays one-arg.
    Add/extend memory injection tests (in the established memoryLog/orchestrator test
    location) covering the behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/bot/brain/memory/memoryLog.test.js src/bot/brain/orchestrator.test.js 2>/dev/null || npm test -- memoryLog orchestrator</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts composeSeedBlocks emits a 'memory' block containing both the memoryPreamble and the readMemoryForSeed body when memory_md_path is set and a file exists.
    - A test asserts the ENOENT path does not throw and yields no broken memory block.
    - A test asserts readMemoryForSeed preserves the `# Memory` header and all `## World N` markers under truncation.
    - A test (or documented diagnosis with an assertion) confirms a mid-session appendMemory is visible on the next composeSeedBlocks call (fresh re-read), establishing the memory side of MEM-04.
    - Production memory_md_path is confirmed always-set (grep/diagnosis noted in the SUMMARY); any defect found is fixed.
    - appendMemory remains one-arg (no importance parameter added).
    - All memory/orchestrator tests pass.
  </acceptance_criteria>
  <done>MEMORY.md injection is verified-correct (or fixed), preserves world-segmentation invariants, and reflects mid-session appends on the next loop seed — with no scoring added.</done>
</task>

<task type="auto">
  <name>Task 2: Tune remember() cadence/content contract and reinforce active recall</name>
  <read_first>
    - src/bot/brain/prompts.js (PERSONALITY_TOOL_DESCRIPTIONS.remember lines 58-77; the memoryPreamble lives in orchestrator.js lines 305-309)
    - src/bot/brain/orchestrator.js (memoryPreamble lines 305-309 — the MEM-03 active-callback seam)
    - .planning/phases/16-persona-memory-core/16-PATTERNS.md (§"GOOD/BAD example steering for Haiku", the remember analog)
    - .planning/phases/16-persona-memory-core/16-CONTEXT.md (D-06 honesty-to-memory link, D-08 cadence/content, D-00)
    - .planning/research/v0.4-memory-and-relationship.md (fact-recall callbacks, subjective entries)
  </read_first>
  <action>
    Tune the existing PERSONALITY_TOOL_DESCRIPTIONS.remember contract (do not
    rebuild it): sharpen the cadence guidance (when to write vs skip) and keep the
    subjective-only / no-transaction-log rule, editing the existing GOOD/BAD example
    block which is the established Haiku-steering pattern. Add the honesty-to-memory
    cadence cue connecting to Plan 01's PERSONA-06 instruction: when the character
    was wrong or something failed, it records that failure with remember() so it does
    not repeat it (the cadence side of D-06; the in-character acknowledgement side
    lives in Plan 01's BASELINE_INSTRUCTIONS). Reinforce active recall (MEM-03) so
    the companion brings up what it knows about the player during ordinary play, not
    only when asked: strengthen the memoryPreamble framing in orchestrator.js
    (lines 305-309) and/or the remember description to make natural callbacks
    explicit, keeping it from reciting the list. All edited instruction text obeys
    D-00 (objective, factual, no em-dashes); the in-voice GOOD examples keep their
    character voice. Do NOT add importance args, scoring, or a facts store (D-07/D-09).
  </action>
  <verify>
    <automated>npx vitest run src/bot/brain/prompts.test.js src/bot/brain/orchestrator.test.js 2>/dev/null || npm test -- prompts orchestrator</automated>
  </verify>
  <acceptance_criteria>
    - PERSONALITY_TOOL_DESCRIPTIONS.remember still bans transaction/coordinate/event logs and keeps the GOOD/BAD example block (grep for the BAD-example markers succeeds).
    - The remember contract now references recording a failure/mistake so it is not repeated (grep for a distinctive token tying remember to mistakes succeeds), connecting to PERSONA-06.
    - The memoryPreamble (or remember description) instructs the character to bring up known facts naturally during ordinary play, not only when asked (active-recall framing present).
    - Any new/edited instruction text contains no em-dash or en-dash.
    - No importance argument or scoring is introduced; appendMemory call sites are unchanged in arity.
    - Affected tests pass.
  </acceptance_criteria>
  <done>The remember() contract steers subjective, well-paced entries including failure-recording, and active fact-recall is reinforced for ordinary play (MEM-02, MEM-03).</done>
</task>

<task type="auto">
  <name>Task 3: Assert runtime memory is local-only (MEM-06)</name>
  <read_first>
    - src/bot/brain/memory/memoryLog.js (all I/O is local file read/write/atomicWrite under userData)
    - CLAUDE.md (§"Local vs Cloud mode" — "Runtime memory stays local-only"; cloud plumbing in src/main/cloud/)
    - .planning/phases/16-persona-memory-core/16-CONTEXT.md (D-08 local-only, MEM-06 carried from v0.3)
    - src/main/cloud/ (confirm no path uploads MEMORY.md / heartbeat / PLAYER.md)
  </read_first>
  <action>
    Confirm and lock the local-only guarantee: verify no code path in src/main/cloud
    (proxyClient, cloudCharacterClient, syncQueue, cacheOnDemand) or the bot reads
    MEMORY.md / heartbeat / PLAYER.md and sends it to the proxy or Supabase. memoryLog
    is local file I/O only; assert that boundary. Add a guard test (in the memory or
    a cloud-sync test location) that asserts the memory file path and contents are not
    referenced by the cloud sync payload builders — e.g. a test that the character
    sync payload does not include MEMORY.md content, or a documented grep-based
    assertion in the SUMMARY if a unit test is not feasible. This is a verification
    task: if a leak is found, fix it; otherwise add the regression guard so a future
    change cannot silently sync runtime memory.
  </action>
  <verify>
    <automated>grep -rn "MEMORY.md\|memory_md_path\|readMemoryFull\|readMemoryForSeed" src/main/cloud/ | grep -v -c "^#" ; test $(grep -rln "MEMORY.md\|memory_md_path\|readMemoryFull" src/main/cloud/ 2>/dev/null | wc -l) -eq 0 && echo "LOCAL-ONLY-OK"</automated>
  </verify>
  <acceptance_criteria>
    - No file under src/main/cloud/ references MEMORY.md, memory_md_path, or the memory read functions (grep returns zero matches) — confirming runtime memory is not part of any cloud payload.
    - A regression guard exists (a unit test asserting the cloud sync payload excludes memory content, OR a documented grep assertion recorded in the SUMMARY) so a future change re-introducing a sync is caught.
    - If any leak path were found it is fixed; the SUMMARY records the verification result.
  </acceptance_criteria>
  <done>Runtime memory (MEMORY.md / heartbeat / PLAYER.md) is confirmed never synced to the cloud, with a regression guard against future leaks (MEM-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot process → local MEMORY.md file (userData) | Memory read/written only on the local disk; must never cross to the network |
| LLM remember() call → MEMORY.md append | Model-controlled append; content is subjective text only, length-bounded by the contract |
| MEMORY.md → cloud sync payloads (src/main/cloud) | Runtime memory must NOT enter any proxy/Supabase payload (MEM-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure | runtime MEMORY.md leaking into a cloud sync payload | mitigate | Task 3 grep guard + regression test asserts src/main/cloud never references memory paths/content |
| T-16-06 | Denial of Service | readMemoryForSeed on a corrupt/oversized MEMORY.md | mitigate | Existing budget-truncation + ENOENT handling; Task 1 tests assert no-throw on absent/over-budget files |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (prompt + memory-path edits only) |
</threat_model>

<verification>
- Memory injection tests pass (memory block present, invariants preserved, no-throw on ENOENT).
- The remember() contract steers subjective entries and failure-recording.
- src/main/cloud references no memory path/content (local-only guard).
</verification>

<success_criteria>
- MEMORY.md is reliably injected each session (MEM-01).
- remember()/forget() usage is properly steered with no scored retrieval (MEM-02).
- Active fact-recall is reinforced for ordinary play (MEM-03).
- Runtime memory stays local-only with a regression guard (MEM-06).
</success_criteria>

<output>
Create `.planning/phases/16-persona-memory-core/16-03-SUMMARY.md` when done.
</output>
