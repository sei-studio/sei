---
phase: 16-persona-memory-core
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/main/personaExpansion.ts
  - src/bot/brain/prompts.js
  - src/shared/characterSchema.ts
autonomous: false
requirements: [PERSONA-01, PERSONA-02, PERSONA-04]
must_haves:
  truths:
    - "The expander emits a # CORE section (name + ~3 traits + register, surface-agnostic) authored at character-save time"
    - "The expander separates surface-agnostic persona content from a distinct Minecraft addendum block so later phases can attach other surfaces"
    - "The # VOICE section's sample utterances demonstrate register and are free of any scenario reference"
    - "Proactive questions to the player are persona-modulated (a cold character asks fewer, a warm one more)"
    - "Saving a character preserves every fact the user wrote and adds no hardcoded scenario scripts"
  artifacts:
    - path: "src/main/personaExpansion.ts"
      provides: "# CORE instruction + universal/MC split + scenario-agnostic # VOICE + CORE validator"
      contains: "CORE"
    - path: "src/bot/brain/prompts.js"
      provides: "renderPersona handles the universal/MC-addendum structure"
      contains: "renderPersona"
  key_links:
    - from: "personaExpansion.ts EXPANSION_SYSTEM"
      to: "REQUIRED_SECTION_HEADERS + SECTION_LABELS"
      via: "parallel structures updated in lockstep when adding # CORE"
      pattern: "CORE"
    - from: "personaExpansion.ts # VOICE instruction"
      to: "scenario-agnostic sample utterances"
      via: "register-not-situations guidance (D-03)"
      pattern: "VOICE"
---

<objective>
Generalize the runtime-proven target format (Plan 01) back into the persona
expander so any character reproduces it. Add a `# CORE` section authored at
character-save time, split the expander output into a surface-agnostic universal
core plus a distinct Minecraft addendum (so Phase 18 can attach other surfaces
without rewriting persona logic), tighten `# VOICE` to scenario-agnostic samples,
and make proactive questions persona-modulated. Per the user's workflow guidance,
this runs AFTER Plan 01 because the expander's job is only to reproduce a format
already shown to work at runtime.

Purpose: PERSONA-01 (structured persona preserving every user fact, no scenario
scripts), PERSONA-02 (surface-generalizable persona via the universal/MC split),
and PERSONA-04 (few-shot say() voice examples present in the assembled prompt).

Output: A restructured EXPANSION_SYSTEM with a # CORE section and universal/MC
split, a CORE validator + label, a scenario-agnostic # VOICE instruction,
persona-modulated proactivity, and a renderPersona that handles the new structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-persona-memory-core/16-CONTEXT.md
@.planning/phases/16-persona-memory-core/16-PATTERNS.md
@.planning/phases/16-persona-memory-core/16-01-SUMMARY.md

<interfaces>
<!-- The three parallel structures in personaExpansion.ts that MUST stay in lockstep. -->

EXPANSION_SYSTEM numbered list — lines 62-67 (instruction text the model follows).
REQUIRED_SECTION_HEADERS — lines 158-167 (tolerant validators; missing → throws).
SECTION_LABELS — lines 175-182 (progress-bar labels indexed by section position).
The literal word "six" appears at line 60 and in the docblock (lines 27-30) and must
become "seven" when # CORE is added. computeExpansionProgress (line 207) divides the
bar by REQUIRED_SECTION_HEADERS.length, so the bar auto-resizes; SECTION_LABELS MUST
gain a parallel CORE entry or SECTION_LABELS[seen-1] returns undefined.

Tolerant-validator pattern to add (after the IDENTITY entry / at the right ordinal):
```typescript
{ name: 'CORE', re: /^\s*#+\s*CORE\b/im },
```

MC verbs currently embedded in section bodies (the split targets):
- # DEFAULT DYNAMIC (line 64): `follow`
- # PROACTIVENESS (line 65): `setGoal`, "biome", project goal-types
- PROACTIVENESS_TIER_HINTS (lines 254-258, USER message, already uncached): `setGoal`, "gathering wood", "placing walls"

renderPersona consumes persona.expanded (prompts.js lines 315-317). The Plan 01
runtime renderCore already slices # CORE from this same expanded string.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add # CORE section to the expander, keeping all three parallel structures in lockstep</name>
  <read_first>
    - src/main/personaExpansion.ts (EXPANSION_SYSTEM lines 57-76; REQUIRED_SECTION_HEADERS lines 158-167; SECTION_LABELS lines 175-182; docblock lines 27-30; computeExpansionProgress lines 207-235)
    - .planning/phases/16-persona-memory-core/16-PATTERNS.md (§"Three parallel structures that MUST stay in lockstep", §"# CORE authoring", §"Header-detection note")
    - .planning/phases/16-persona-memory-core/16-CONTEXT.md (D-01, D-02, D-00)
    - .planning/phases/16-persona-memory-core/16-01-SUMMARY.md (the runtime renderCore contract the expander must feed)
    - src/main/personaExpansion.test.ts (existing expansion tests, if present, for the test convention)
  </read_first>
  <behavior>
    - A valid 7-section expansion output (with # CORE) passes REQUIRED_SECTION_HEADERS validation.
    - An output missing # CORE throws `persona expansion failed: missing sections (CORE)` (CORE appears in the missing list).
    - computeExpansionProgress over a 7-section stream reports a CORE label when the # CORE header is the current section, and the bar denominator is 7.
    - SECTION_LABELS has a parallel entry for CORE so SECTION_LABELS[seen-1] is never undefined for the CORE slot.
  </behavior>
  <action>
    Add `# CORE` as a new numbered instruction near the TOP of EXPANSION_SYSTEM
    (logically first, so the model writes the distillation before the long
    sections), per the §"# CORE authoring" guidance: ~3-5 lines = name + about
    three defining traits + register; surface-agnostic with NO Minecraft verbs
    (D-02). Update all three lockstep structures: add the `{ name: 'CORE', re:
    /^\s*#+\s*CORE\b/im }` validator to REQUIRED_SECTION_HEADERS at the matching
    ordinal, add a parallel "Core" entry to SECTION_LABELS at the same position,
    and renumber the EXPANSION_SYSTEM list. Change the literal "six" to "seven" at
    line 60 and in the docblock (lines 27-30). Keep EXPANSION_SYSTEM free of
    per-call data (stable-system invariant). Use objective factual instruction
    language, no em-dashes (D-00). Add/extend tests asserting the four behaviors
    above (use the existing expansion test fakes / fixtures).
  </action>
  <verify>
    <automated>npx vitest run src/main/personaExpansion.test.ts 2>/dev/null || npm test -- personaExpansion</automated>
  </verify>
  <acceptance_criteria>
    - REQUIRED_SECTION_HEADERS contains a CORE validator (grep `name: 'CORE'` in personaExpansion.ts succeeds).
    - SECTION_LABELS has one more entry than before and includes a Core label; its length equals REQUIRED_SECTION_HEADERS.length.
    - The literal "six" no longer appears as the section count at line 60 or the docblock; "seven" appears instead (grep confirms `seven` present and the old "six markdown sections" phrasing gone).
    - A test asserts a missing-# CORE output throws with CORE in the missing list, and a complete 7-section output validates.
    - The # CORE instruction text contains no em-dash/en-dash and names no Minecraft verb (no follow/dig/setGoal/biome).
    - All expansion tests pass.
  </acceptance_criteria>
  <done>The expander emits and validates a surface-agnostic # CORE section, with REQUIRED_SECTION_HEADERS, SECTION_LABELS, the count word, and the numbered list all updated in lockstep.</done>
</task>

<task type="auto">
  <name>Task 2: Split universal persona core from a Minecraft addendum + persona-modulated proactivity</name>
  <read_first>
    - src/main/personaExpansion.ts (# DEFAULT DYNAMIC line 64 with `follow`; # PROACTIVENESS line 65 with setGoal/biome/goal-types; PROACTIVENESS_TIER_HINTS lines 254-258)
    - src/bot/brain/prompts.js (renderPersona lines 315-317; PROACTIVENESS_DIRECTIVES lines 103-112; BASELINE_INSTRUCTIONS for any reinforcement seam)
    - .planning/phases/16-persona-memory-core/16-PATTERNS.md (§"Universal/MC split (D-04)", the renderPersona seam note)
    - .planning/phases/16-persona-memory-core/16-CONTEXT.md (D-04, D-05, D-00)
  </read_first>
  <action>
    Restructure the expander output per D-04: keep identity, voice, values, stance,
    and general proactivity FLAVOR surface-agnostic, and move the Minecraft-specific
    behaviors into a separate addendum block. Concretely: emit a distinct
    Minecraft-addendum section (e.g. `# MINECRAFT`) that carries the MC verbs and
    goal-type fit currently inline in # DEFAULT DYNAMIC (the `follow` stance) and
    # PROACTIVENESS (setGoal, biome, project goal-types), and rewrite those two
    universal sections to describe the surface-agnostic stance and initiative flavor
    without MC verbs. Add the new addendum section to all three lockstep structures
    (numbered list, REQUIRED_SECTION_HEADERS validator, SECTION_LABELS), updating the
    section count word accordingly. Fold persona-modulated proactivity (D-05) into
    the universal core/proactiveness instruction: a cold character asks the player
    fewer questions, a warm one more, derived from the described personality and
    framed in-character (not a uniform "ask about the user" behavior). Decide with
    reference to PATTERNS.md whether PROACTIVENESS_TIER_HINTS MC phrasing moves into
    the addendum framing (it is already per-call, uncached). Update renderPersona in
    prompts.js if the new structure requires it to render or order the universal core
    and the MC addendum (default to keeping persona.expanded as ONE string with both
    sections embedded, the lowest-ripple choice). All new/edited instruction text
    obeys D-00 (objective, factual, no em-dashes); untouched legacy prose is left
    alone unless it is being rewritten as part of the split.
  </action>
  <verify>
    <automated>npx vitest run src/main/personaExpansion.test.ts src/bot/brain/prompts.test.js 2>/dev/null || npm test -- personaExpansion prompts</automated>
  </verify>
  <acceptance_criteria>
    - personaExpansion.ts emits a distinct Minecraft-addendum section header (grep for the new MC section name succeeds) and the universal # DEFAULT DYNAMIC / # PROACTIVENESS instruction bodies no longer name `follow`/`setGoal`/`biome` (those moved to the addendum).
    - REQUIRED_SECTION_HEADERS and SECTION_LABELS both gained the addendum entry and remain equal length; the section count word matches the new total.
    - The universal proactivity instruction states that proactive questions scale with persona warmth (cold = fewer, warm = more) in objective language.
    - renderPersona still returns a usable string for the new structure (existing renderPersona test, if any, still passes; if renderPersona was changed, its test asserts the universal core and MC addendum both reach the output).
    - No new instruction text added in this task contains an em-dash or en-dash.
    - All affected tests pass.
  </acceptance_criteria>
  <done>The expander produces a surface-agnostic universal core plus a separate Minecraft addendum, with persona-modulated proactivity, ready for Phase 18 to attach other surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten # VOICE to scenario-agnostic register-demonstrating samples</name>
  <read_first>
    - src/main/personaExpansion.ts (# VOICE instruction at line 63; the GOOD/BAD example convention in # MEMORY line 67)
    - .planning/phases/16-persona-memory-core/16-PATTERNS.md (§"GOOD/BAD example steering for Haiku", §"# VOICE" notes; D-03 mapping)
    - .planning/phases/16-persona-memory-core/16-CONTEXT.md (D-03, D-00)
    - .planning/research/v0.4-persona-prompting-and-steering.md (Ali:Chat: demonstrate register, not situations)
  </read_first>
  <action>
    Edit the # VOICE instruction so the 5-7 sample utterances demonstrate the
    character's register and voice and are explicitly scenario-agnostic: instruct
    the model that samples must NOT be tied to any situation (no "gathering wood",
    "being attacked", "building a base") and instead show casing, tics, and the kind
    of lines this character says in general. Add a short GOOD/BAD example pair in the
    instruction (following the established # MEMORY GOOD/BAD steering convention) so
    Haiku reliably produces register-not-situation samples: GOOD = in-voice
    scenario-free lines, BAD = the same voice but pinned to a Minecraft scenario,
    with an inline note of why the BAD ones are wrong. Keep the universal chat rules
    (address player as "you", no stage directions, no em-dashes) already enforced
    elsewhere; do not restate them, just ensure the new examples obey them. The
    instruction/scaffolding text is objective and em-dash-free (D-00); the in-voice
    GOOD samples carry the example character's voice.
  </action>
  <verify>
    <automated>npx vitest run src/main/personaExpansion.test.ts 2>/dev/null || npm test -- personaExpansion</automated>
  </verify>
  <acceptance_criteria>
    - The # VOICE instruction text explicitly forbids scenario-tied samples and names the register-not-situations rule (grep for a distinctive token such as "scenario" or "situation" in the # VOICE instruction succeeds).
    - The # VOICE instruction includes a GOOD/BAD example pair following the # MEMORY convention.
    - The added instruction/scaffolding text contains no em-dash or en-dash.
    - Existing expansion tests still pass (the section still validates against the VOICE header regex).
  </acceptance_criteria>
  <done>The # VOICE expander instruction produces character-specific, scenario-agnostic few-shot samples that satisfy PERSONA-04, present in the assembled prompt via the cached renderPersona prefix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live character-save format verification</name>
  <action>Human verification that an end-to-end character save reproduces the Plan 01 target format (# CORE, scenario-free # VOICE, universal core + MC addendum) and preserves every user fact. See what-built / how-to-verify below.</action>
  <what-built>
    A restructured expander that emits a # CORE section, splits a universal core
    from a Minecraft addendum, modulates proactivity by persona warmth, and produces
    scenario-agnostic # VOICE samples. Validated automatically by expansion unit
    tests; this checkpoint confirms an end-to-end character save reproduces the
    Plan 01 target format and preserves every user-written fact.
  </what-built>
  <how-to-verify>
    1. In the app, create a new character from a short description that contains 2-3
       concrete facts (e.g. "a grumpy dwarf who hates the rain and loves gold").
    2. Confirm the save completes and the generated expanded persona contains a
       # CORE block, a # VOICE block with scenario-free samples, the universal
       sections, and a distinct Minecraft addendum.
    3. Confirm every fact from the source blurb is preserved in the expanded output
       and that no hardcoded scenario scripts were added (PERSONA-01).
    4. Confirm the # CORE matches the shape the Plan 01 runtime renderCore expects
       (summon the character and check it gets a re-injected core, drift-resistant
       over a >12-turn session).
    5. Confirm a cold-described character asks the player fewer questions than a
       warm-described one (D-05).
  </how-to-verify>
  <resume-signal>Type "approved" or describe format/preservation issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user source blurb → expansion LLM (main process) | Untrusted user text expanded by the LLM; real-person refusal already guarded by REFUSED:REAL_PERSON sentinel |
| expansion output → REQUIRED_SECTION_HEADERS validation | Malformed model output rejected before it is saved as persona.expanded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | expansion output missing the new # CORE/MC sections | mitigate | New REQUIRED_SECTION_HEADERS validators throw on missing sections before save (Task 1/2) |
| T-16-04 | Information Disclosure | per-call data leaking into the cacheable EXPANSION_SYSTEM | mitigate | Keep # CORE instruction stable and data-free (stable-system invariant, PATTERNS.md) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (prompt + schema edits only) |
</threat_model>

<verification>
- Expansion unit tests pass (CORE + MC addendum validation, scenario-agnostic VOICE).
- A live character save reproduces the Plan 01 target format and preserves all user facts (human checkpoint).
- All three parallel structures (numbered list, validators, labels) stay in lockstep.
</verification>

<success_criteria>
- The expander emits and validates a # CORE section and a Minecraft addendum separate from the universal core.
- # VOICE samples are scenario-agnostic and present in the assembled prompt.
- Proactivity is persona-modulated.
- All new instruction text obeys D-00.
</success_criteria>

<output>
Create `.planning/phases/16-persona-memory-core/16-02-SUMMARY.md` when done.
</output>
