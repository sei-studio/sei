---
phase: 11
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ../sei-website/terms.html
  - ../sei-website/privacy.html
  - ../sei-website/index.html
  - ../sei-website/pitch.html
autonomous: false
requirements: [LIB-06]
requirements_addressed: [LIB-06]
tags: [legal, gdpr, website, sibling-repo]
user_setup:
  - service: sei-website
    why: "ToS + Privacy Policy must be live at a stable URL before the first cloud write per Pitfall 11 (GDPR obligations apply from first EU signup)"
    env_vars: []
    dashboard_config:
      - task: "After editing files in ../sei-website/, commit + push to that repo; the existing Vercel deploy publishes the new pages automatically"
        location: "Vercel auto-deploy on push to main of ../sei-website"
must_haves:
  truths:
    - "../sei-website/terms.html exists at the sibling repo root and contains a Terms of Service with sections covering: account terms, acceptable use, user-generated content (D-22 + Pitfall 9 PII clause), shared character public visibility warning, ownership/license grant, termination + 30-day deletion, governing law placeholder, contact email (D-25)"
    - "../sei-website/privacy.html exists at the sibling repo root and discloses processors: Supabase (DB + Auth + Storage), Anthropic (inference). Lemon Squeezy and additional providers are forward-noted but Phase 11 only names current processors (Assumption A8)"
    - "Both files are accessible from app via shell.openExternal at https://sei.gg/terms.html and https://sei.gg/privacy.html (or whatever the deployed sei-website domain is — confirm with the existing index.html href patterns)"
    - "../sei-website/index.html footer (and ../sei-website/pitch.html footer if it has one) includes anchor links to /terms.html and /privacy.html so the pages are discoverable without knowing the URL"
    - "TOS_VERSION constant value matches the version string baked into the rendered terms.html footer (date-stamped — e.g., '2026-05-21')"
  artifacts:
    - path: "../sei-website/terms.html"
      provides: "Terms of Service body (LIB-06)"
      contains: "Terms of Service"
    - path: "../sei-website/privacy.html"
      provides: "Privacy Policy body (LIB-06 + Pitfall 11)"
      contains: "Privacy Policy"
    - path: "../sei-website/index.html"
      provides: "Legal-link footer update"
      contains: "terms.html"
  key_links:
    - from: "src/renderer/src/components/AcceptToSModal.tsx (Plan 11-13)"
      to: "../sei-website/terms.html + ../sei-website/privacy.html"
      via: "shell.openExternal('https://sei.gg/terms.html')"
      pattern: "shell\\.openExternal.*terms\\.html"
---

<objective>
Add live `terms.html` and `privacy.html` to the sibling `../sei-website/` repo so the app's ToS-acceptance modal (Plan 11-13) can link to real URLs. Update the site's homepage footer to surface the new pages.

Purpose: Pitfall 11 — GDPR obligations apply from first EU signup. Phase 11 cannot ship a cloud write until users can review + accept the actual published documents. Hosting them in the sibling repo (rather than in-app) lets the user update legal text without an app release.

Output: 2 new HTML pages + 1-2 footer updates in the sei-website repo, pushed and auto-deployed via existing Vercel pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cloud-character-library/11-CONTEXT.md
@.planning/phases/11-cloud-character-library/11-RESEARCH.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- The sibling repo is OUT-OF-REPO; the executor must `ls ../sei-website/` first to confirm existence + read existing index.html / pitch.html for HTML/CSS conventions. -->

From .planning/phases/11-cloud-character-library/11-CONTEXT.md D-25:
"Privacy Policy and Terms of Service pages live in the sibling repo at `../sei-website/` — Phase 11 plan adds `terms.html` and `privacy.html` to that repo (linked from the app via `shell.openExternal`)."

From RESEARCH §Pattern 7 + §Common Pitfalls 9:
- ToS clause covers: don't put PII in shared character content
- Privacy: list current subprocessors (Supabase, Anthropic) — Phase 13/14 will revisit
- Versioning: `TOS_VERSION` and `PRIVACY_VERSION` constants in `src/shared/legalVersions.ts` (Plan 11-12) — match the date-stamp footer in the HTML
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read sibling repo structure + write terms.html</name>
  <files>../sei-website/terms.html</files>
  <read_first>
    - ../sei-website/index.html (READ FIRST — match HTML structure, head/meta tags, CSS link references, footer pattern)
    - ../sei-website/pitch.html if it exists (additional style precedent)
    - .planning/phases/11-cloud-character-library/11-RESEARCH.md §Common Pitfalls 9 (PII clause)
    - .planning/phases/11-cloud-character-library/11-RESEARCH.md §Don't Hand-Roll row "ToS/Privacy boilerplate" — Iubenda/Termly is permitted as a starting point but the subprocessor list MUST be accurate
  </read_first>
  <action>
First: `ls ../sei-website/` to confirm the sibling repo exists. If it does not, surface the path so the user can clone it. If it does, read `../sei-website/index.html` to extract its HTML structure (DOCTYPE, head, body wrapper, CSS file reference, footer pattern).

Write `../sei-website/terms.html` mirroring index.html's outer structure (same `<head>`, same CSS link, same overall body wrapper) with a centered article-style content block. Include these sections AS HEADINGS (`<h2>`) with paragraph body text below each:

1. **Effective Date** — the literal string date-stamp `2026-05-21` (this is the TOS_VERSION value baked into Plan 11-12's `legalVersions.ts`)
2. **Acceptance of Terms** — by signing up or signing into Sei the user agrees
3. **The Service** — Sei is a Minecraft AI companion desktop app; cloud features include character library and (later) hosted AI
4. **Accounts** — sign-up, password hygiene, one account per person, 13+ minimum
5. **User-Generated Content** — characters (persona, prompts, skin, portrait) the user creates; user retains ownership; by setting `shared = true` the user grants a worldwide royalty-free license to display the content publicly within Sei's Browse tab
6. **Do Not Include Personal Information** — Pitfall 9 — characters set to `shared = true` are publicly visible; do not include real names, addresses, phone numbers, or identifying details in any character content
7. **Acceptable Use** — no harassment, no illegal content, no impersonation, no CSAM (Phase 12 will add automated scanning)
8. **Termination + Account Deletion** — user may delete their account from in-app Settings; deletion purges cloud rows and Storage objects within 30 days per the deletion_queue cron (Phase 10 mechanism)
9. **Disclaimers** — provided as-is; no warranty
10. **Limitation of Liability** — to the maximum permitted by law
11. **Governing Law** — placeholder (e.g., "Washington State, USA" — confirm with user) — if user has not specified, leave as `<!-- TBD: jurisdiction -->`
12. **Changes to These Terms** — Sei will prompt re-acceptance via the blocking modal when these terms materially change (the TOS_VERSION mechanism)
13. **Contact** — `support@sei.gg` (or whatever email the user wants — match existing index.html if it lists one)

Footer: include the date-stamp `Effective 2026-05-21` at the bottom of the article.

Style guidance: match the existing pages — no new CSS file, no new design system. If index.html uses a `<main class="...">` wrapper, mirror it.
  </action>
  <acceptance_criteria>
    - File `../sei-website/terms.html` exists
    - `grep -c "Terms of Service" ../sei-website/terms.html` outputs at least `1`
    - `grep -c "2026-05-21" ../sei-website/terms.html` outputs at least `1` (Effective Date matches TOS_VERSION constant in Plan 11-12)
    - `grep -ic "shared" ../sei-website/terms.html` outputs at least `1` (public-content warning present)
    - `grep -ic "personal information" ../sei-website/terms.html` outputs at least `1` (Pitfall 9 PII clause)
    - `grep -ic "account deletion\|delete.*account" ../sei-website/terms.html` outputs at least `1` (30-day deletion clause)
    - File starts with `<!DOCTYPE html>` or equivalent matching index.html's preamble
  </acceptance_criteria>
  <verify>
    <automated>test -f ../sei-website/terms.html && grep -ic "personal information" ../sei-website/terms.html | awk '{ exit ($1 >= 1 ? 0 : 1) }'</automated>
  </verify>
  <done>terms.html exists in sibling repo with all required sections, date-stamped to 2026-05-21, structured to match index.html's HTML conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Write privacy.html</name>
  <files>../sei-website/privacy.html</files>
  <read_first>
    - ../sei-website/terms.html (Task 1 output — same structure)
    - ../sei-website/index.html (style reference)
    - .planning/phases/11-cloud-character-library/11-RESEARCH.md §Assumptions Log A8 (current subprocessors only — Anthropic + Supabase)
    - .planning/research/PITFALLS.md §Pitfall 11 (GDPR obligations)
  </read_first>
  <action>
Write `../sei-website/privacy.html` mirroring terms.html's outer structure. Sections (each as `<h2>` with body):

1. **Effective Date** — `2026-05-21` (matches PRIVACY_VERSION constant in Plan 11-12)
2. **What We Collect** —
   - Account data: email address, hashed password (or Google OAuth profile if Google sign-in)
   - Session data: encrypted session tokens stored locally on the user's machine (Electron safeStorage)
   - Character data (cloud): name, persona text, system prompt, skin PNG, portrait image — stored when user is signed-in and `shared` is true OR they explicitly upload via the migration prompt
   - Local-only data (NEVER uploaded): runtime memory (`OWNER.md`, `DIARY.md`), conversation history, local-mode characters created in "Continue Locally" mode
3. **Why We Collect It** — provide the Sei service, enable cross-device sync of character definitions, enable the public character library
4. **Subprocessors** —
   - **Supabase** (Postgres DB + Auth + Storage) — hosts cloud character library, account credentials, deletion queue. Located in [region — Supabase project region — confirm].
   - **Anthropic** — when the user supplies their own API key or (Phase 13+) uses Sei's hosted proxy, character prompts and conversations are sent to Anthropic's API per their Privacy Policy.
   - (Forward note: Phase 13 will add **Lemon Squeezy** as a payment processor; Phase 14 will add additional model providers — this policy will be updated and users prompted to re-accept at that time per the PRIVACY_VERSION mechanism.)
5. **What We Don't Do** — no sale of personal data; no advertising; no tracking pixels; no third-party analytics in v1.0
6. **Your Rights (GDPR / CCPA)** —
   - Access + export: in-app "Export My Data" produces a JSON download with all your cloud character definitions (Phase 10 AUTH-07)
   - Deletion: in-app "Delete Account" purges Supabase rows + Storage objects within 30 days (Phase 10 AUTH-06)
   - Correction: edit any character via the GUI; the change syncs to cloud
7. **Data Retention** — cloud data persists until you delete it or your account; after account deletion, complete purge within 30 days
8. **Children** — Sei is not directed at children under 13
9. **International Transfers** — Supabase region disclosure
10. **Changes to This Policy** — version bumps trigger re-acceptance via blocking modal
11. **Contact** — `support@sei.gg` (or matching contact from terms.html)

Footer: `Effective 2026-05-21`.
  </action>
  <acceptance_criteria>
    - File `../sei-website/privacy.html` exists
    - `grep -c "Privacy Policy" ../sei-website/privacy.html` outputs at least `1`
    - `grep -c "2026-05-21" ../sei-website/privacy.html` outputs at least `1`
    - `grep -c "Supabase" ../sei-website/privacy.html` outputs at least `1` (subprocessor disclosure)
    - `grep -c "Anthropic" ../sei-website/privacy.html` outputs at least `1` (subprocessor disclosure)
    - `grep -ic "OWNER.md\|DIARY.md\|local-only\|local only" ../sei-website/privacy.html` outputs at least `1` (LIB-02 invariant disclosed)
    - `grep -ic "delete.*account\|account.*deletion" ../sei-website/privacy.html` outputs at least `1`
  </acceptance_criteria>
  <verify>
    <automated>test -f ../sei-website/privacy.html && grep -c "Supabase" ../sei-website/privacy.html | awk '{ exit ($1 >= 1 ? 0 : 1) }' && grep -c "Anthropic" ../sei-website/privacy.html | awk '{ exit ($1 >= 1 ? 0 : 1) }'</automated>
  </verify>
  <done>privacy.html lists current subprocessors (Supabase + Anthropic), discloses local-only invariant for runtime memory, covers GDPR rights with pointers to Phase 10's export + delete features, and date-stamps to 2026-05-21.</done>
</task>

<task type="auto">
  <name>Task 3: Add legal-link footer to homepage</name>
  <files>../sei-website/index.html, ../sei-website/pitch.html</files>
  <read_first>
    - ../sei-website/index.html (find existing footer; if none, add a minimal one)
    - ../sei-website/pitch.html (if it exists and has a footer, mirror the same edit; if not, skip)
  </read_first>
  <action>
In `../sei-website/index.html`, locate the footer (or the closing `</body>` if no footer exists yet). If a footer exists, append `<a href="/terms.html">Terms</a>` and `<a href="/privacy.html">Privacy</a>` links to it (separator: ` · ` between, matching adjacent links' style). If no footer exists, add a minimal one immediately before `</body>`:

```html
<footer class="site-footer">
  <a href="/terms.html">Terms</a> ·
  <a href="/privacy.html">Privacy</a>
</footer>
```

If `../sei-website/pitch.html` exists AND has its own footer, apply the same edit. If pitch.html has no footer or doesn't exist, do not create one — just edit index.html.

Do not introduce any new CSS classes or styles; reuse whatever the existing footer (if any) uses.
  </action>
  <acceptance_criteria>
    - `grep -c "/terms.html" ../sei-website/index.html` outputs at least `1`
    - `grep -c "/privacy.html" ../sei-website/index.html` outputs at least `1`
    - If `../sei-website/pitch.html` exists and previously had a footer block, `grep -c "/terms.html" ../sei-website/pitch.html` outputs at least `1`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "/terms.html" ../sei-website/index.html | awk '{ exit ($1 >= 1 ? 0 : 1) }' && grep -c "/privacy.html" ../sei-website/index.html | awk '{ exit ($1 >= 1 ? 0 : 1) }'</automated>
  </verify>
  <done>index.html has footer links to /terms.html and /privacy.html; pitch.html updated if it has a footer.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Push sei-website changes + verify deploy</name>
  <what-built>terms.html, privacy.html, footer updates — staged in the sibling repo</what-built>
  <how-to-verify>
This is a human-required action because:
1. The sei-website repo is a sibling tree with its own git remote
2. The deploy is auto-triggered by Vercel on push (user memory: don't introduce new Vercel infra, but the EXISTING deploy is fine)

Steps:
1. `cd ../sei-website && git status` → show the 3 changed files (terms.html, privacy.html, index.html)
2. `cd ../sei-website && git add terms.html privacy.html index.html pitch.html 2>/dev/null; git commit -m "docs(legal): add Terms of Service + Privacy Policy"`
3. Push the commit (target whatever branch the existing deploy watches — typically main)
4. Wait for Vercel auto-deploy (1-2 minutes)
5. Verify via `curl -sI https://sei.gg/terms.html | head -1` returns `HTTP/2 200` (or whatever the actual domain is — check `vercel.json` in `../sei-website/` for the production domain)
6. `curl -sI https://sei.gg/privacy.html | head -1` likewise returns 200

If the deployed domain differs from `sei.gg`, capture the actual URL — Plan 11-13 (AcceptToSModal) needs the correct hostname for shell.openExternal.
  </how-to-verify>
  <resume-signal>Reply "legal-deployed: https://<domain>/terms.html" with the verified live URL (or "legal-deploy-failed: <error>")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-app shell.openExternal → public URL | App opens system browser at a Sei-controlled domain — domain control is the trust anchor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | I | Privacy policy fails to disclose Anthropic as data processor | mitigate | Task 2 acceptance criterion forces "Anthropic" string in privacy.html; required by GDPR Art. 28 |
| T-11-02-02 | I | Privacy policy fails to disclose Supabase as data processor | mitigate | Task 2 acceptance criterion forces "Supabase" string in privacy.html |
| T-11-02-03 | R | User claims they never agreed to ToS | mitigate | Phase 11-12 records `tos_acceptance` row with versioned doc IDs; this plan establishes the version-stamped HTML so the row corresponds to a deterministic doc revision |
| T-11-02-04 | T | Attacker substitutes a malicious page at /terms.html | accept | Domain control + DNS + Vercel deploy chain are the trust boundary; same threat model as the rest of the marketing site |
</threat_model>

<verification>
- ../sei-website/terms.html exists with all required sections + Effective Date stamp
- ../sei-website/privacy.html exists with Supabase + Anthropic subprocessor disclosures + GDPR rights
- ../sei-website/index.html footer links to both pages
- Live pages return HTTP 200 at the deployed domain
</verification>

<success_criteria>
- Both legal pages live and accessible from a stable URL
- Live URL captured for Plan 11-13 (AcceptToSModal will use this URL via shell.openExternal)
- Date-stamps in HTML match the TOS_VERSION / PRIVACY_VERSION constants that Plan 11-12 will create
</success_criteria>

<output>
After completion, create `.planning/phases/11-cloud-character-library/11-02-SUMMARY.md` capturing the deployed URL (downstream plans need it).
</output>
