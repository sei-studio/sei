---
phase: 09
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src/main/mcInstallScan.ts
  - src/main/fabricInstaller.ts
  - src/main/customSkinLoader.ts
  - src/main/wizardStateStore.ts
  - src/main/wizard.ts
  - src/main/ipc.ts
  - src/main/index.ts
  - src/main/paths.ts
  - scripts/verify-mcInstallScan.mjs
  - scripts/verify-customSkinLoaderConfig.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Main process can scan known Minecraft launcher + CurseForge paths on macOS and Windows, returning a list of McInstall records with detected version, loader, CSL presence"
    - "Main process can install Fabric Loader into a vanilla .minecraft directory by downloading the Fabric installer JAR (meta.fabricmc.net) and shelling out to java -jar <installer> client -dir <path> -mcversion <ver> -noprofile with a 90s timeout"
    - "Main process can fetch the latest CustomSkinLoader Fabric/Forge JAR from Modrinth (or fall back to GitHub releases) and drop it into the install's mods/ folder atomically"
    - "Main process writes a CustomSkinLoader config JSON pointing at the loopback skin server URL into <install>/config/CustomSkinLoader/CustomSkinLoader.json"
    - "Wizard state persists at <userData>/skin-setup-state.json with hasRunOnce, enabledInstallIds, lastSkinServerPort"
    - "wizard:install emits per-install WizardProgressEvent push events via webContents.send('wizard:progress')"
    - "All external HTTP calls (Fabric installer download, Modrinth JAR lookup, JAR download) carry wall-clock timeouts (60s for downloads, 30s for metadata, 90s for the java -jar exec)"
  artifacts:
    - path: src/main/mcInstallScan.ts
      provides: "scanMcInstalls() — cross-platform scanner for vanilla launcher + CurseForge instances"
      exports: ["scanMcInstalls"]
    - path: src/main/fabricInstaller.ts
      provides: "installFabricLoader(opts) — fetches installer JAR, runs java -jar headlessly with timeout, returns installed loader version"
      exports: ["installFabricLoader", "isJavaAvailable", "selectLatestFabricLoader"]
    - path: src/main/customSkinLoader.ts
      provides: "downloadCustomSkinLoader(loaderKind, mcVersion, modsDir) + writeCustomSkinLoaderConfig(installDir, loaderKind, skinServerBaseUrl)"
      exports: ["downloadCustomSkinLoader", "writeCustomSkinLoaderConfig", "isCustomSkinLoaderInstalled"]
    - path: src/main/wizardStateStore.ts
      provides: "loadWizardState() / saveWizardState() — JSON at <userData>/skin-setup-state.json with atomic writes"
      exports: ["loadWizardState", "saveWizardState"]
    - path: src/main/wizard.ts
      provides: "runWizardInstall(args, onProgress) — orchestrates detect/install/place/config per install id"
      exports: ["runWizardInstall"]
  key_links:
    - from: src/main/wizard.ts
      to: src/main/fabricInstaller.ts
      via: "installFabricLoader call for vanilla installs without an existing loader"
      pattern: "installFabricLoader"
    - from: src/main/wizard.ts
      to: src/main/customSkinLoader.ts
      via: "downloadCustomSkinLoader + writeCustomSkinLoaderConfig"
      pattern: "downloadCustomSkinLoader|writeCustomSkinLoaderConfig"
    - from: src/main/ipc.ts
      to: src/main/wizard.ts
      via: "wizard:install handler"
      pattern: "runWizardInstall"
    - from: src/main/index.ts
      to: src/main/wizardStateStore.ts
      via: "wizard:get-state IPC handler reads persisted state"
      pattern: "loadWizardState"
---

<objective>
Build the wizard backend that automates the user-side setup CONTEXT promised: detect MC installs, install Fabric Loader where needed, drop CustomSkinLoader, write its config pointing at our loopback skin server, and persist state so the wizard is idempotent on re-runs. Every external call is timeout-wrapped per CLAUDE.md. The wizard is a chain of small modules so each piece can be re-run on partial failure (CONTEXT §"First-launch wizard scope" idempotency requirement).

Purpose: directly satisfies CONTEXT §decisions "First-launch wizard scope", "Cross-platform paths", and the idempotent re-runnable wizard. After this plan lands, Plan 06's wizard UI fires `wizard:detectInstalls` then `wizard:install` IPC and sees real progress events from a real Fabric installer running against the user's real Minecraft directory.

Output: 5 new TS modules, 2 new verification scripts, IPC handler additions, main bootstrap wiring (push channel + state-store load).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/CONTEXT.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/RESEARCH.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-UI-SPEC.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-01-PLAN.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-02-PLAN.md
@.planning/phases/09-implement-custom-bot-skins-via-customskinloader-mod-first-la/09-03-PLAN.md
@.planning/phases/08-cross-platform-compatibility-verify-and-fix-sei-to-run-on-wi/08-HOTSPOTS.md
@src/shared/ipc.ts
@src/main/ipc.ts
@src/main/index.ts
@src/main/paths.ts
@src/main/personaExpansion.ts

<interfaces>
<!-- After Plans 01-03. -->

From src/shared/ipc.ts (Plan 01):
- `McInstall` interface (id, kind, label, path, mc_version, loader, loader_version, csl_installed, csl_version, sei_enabled)
- `WizardState { version: 1; hasRunOnce; enabledInstallIds; lastRunAt; lastSkinServerPort }`
- `WizardInstallResult`
- `WizardProgressEvent` discriminated union (queued / fabric-downloading / fabric-installing / mod-downloading / mod-placing / config-writing / done / failed)
- `IpcChannel.wizard` (detectInstalls, install, getState, progress)

From src/main/paths.ts (Plan 02 — Plan 04 extends further):
- skinsDir, skinPngPath
- Plan 04 adds: wizardStatePath

External APIs:
- Fabric installer list: `https://meta.fabricmc.net/v2/versions/installer` returns JSON array; first stable element is the recommended installer. Direct download URL pattern: `https://maven.fabricmc.net/net/fabricmc/fabric-installer/<version>/fabric-installer-<version>.jar`.
- Fabric loader list per MC version: `https://meta.fabricmc.net/v2/versions/loader/<mc-version>` for compatible loader list.
- CustomSkinLoader Modrinth: `https://api.modrinth.com/v2/project/customskinloader/version` returns version array filtered by `loaders` and `game_versions`.
- CustomSkinLoader GitHub fallback: `https://api.github.com/repos/xfl03/MCCustomSkinLoader/releases/latest` returns assets array.
- CustomSkinLoader Fabric+Forge config path (modern builds): `<install>/config/CustomSkinLoader/CustomSkinLoader.json`. Legacy Forge fallback: `<install>/CustomSkinLoader/CustomSkinLoader.json`.

CustomSkinLoader config shape (minimal — verified via CustomSkinLoader project README §Configuration → loadlist → Legacy):
```json
{
  "version": 14,
  "loadlist": [
    { "name": "SeiLocal", "type": "Legacy", "userAgent": "Sei", "root": "http://127.0.0.1:<port>", "skin": "/skins/{USERNAME}.png" }
  ],
  "enableTransparentSkin": true,
  "enableUpdateSkull": true,
  "enableLocalProfileCache": false,
  "enableCacheAutoClean": true
}
```

The `Legacy` loader type lets you specify literal URL templates with `{USERNAME}`/`{UUID}` substitution. The CSL client substitutes the in-game username at fetch time, which matches the skin server's regex `^/skins/([A-Za-z0-9_]{1,16})\.png$`.

OS-specific MC paths (per CONTEXT §"Cross-platform paths" + Phase 8 conventions):
- macOS vanilla: `~/Library/Application Support/minecraft`
- macOS CurseForge: `~/Library/Application Support/curseforge/minecraft/Instances/<instance-name>`
- Windows vanilla: `%APPDATA%/.minecraft` (= `<USERPROFILE>/AppData/Roaming/.minecraft`)
- Windows CurseForge: `<USERPROFILE>/curseforge/minecraft/Instances/<name>` AND `<USERPROFILE>/Documents/curseforge/minecraft/Instances/<name>`
- Linux vanilla: `~/.minecraft` (best-effort, non-blocking)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: mcInstallScan.ts + wizardStateStore.ts — cross-platform install detection + persisted wizard state</name>
  <read_first>
    - src/main/paths.ts (extend with wizardStatePath)
    - src/main/characterStore.ts (atomic JSON + withFileLock pattern)
    - .planning/phases/09-.../CONTEXT.md §"Cross-platform paths"
    - .planning/phases/08-.../08-HOTSPOTS.md (Phase 8 cross-platform conventions; specifically rows 1, 3, 11 — all path joining via node:path, never string concat)
    - src/bot/cli/index.js L309-321 (existing electronUserDataDir shows the platform-branched home-dir/env-var pattern this plan mirrors)
  </read_first>
  <action>
    **1A — Extend `src/main/paths.ts`:**

    Add (alongside skinsDir / skinPngPath from Plan 02):
    ```ts
    wizardStatePath: () => path.join(userDataRoot(), 'skin-setup-state.json'),
    ```

    **1B — Create `src/main/wizardStateStore.ts`:**

    Public exports:
    - `loadWizardState(): Promise<WizardState>` — reads `<userData>/skin-setup-state.json`. Returns defaults `{ version: 1, hasRunOnce: false, enabledInstallIds: [], lastRunAt: null, lastSkinServerPort: null }` on ENOENT or parse error.
    - `saveWizardState(next: WizardState): Promise<void>` — atomic-writes via `withFileLock` + `atomicWrite`. mkdir-recursive on `paths.userData()` first.

    Use the same atomic-write pattern as `characterStore.ts` (`withFileLock` + `atomicWrite`). Defensive parsing: ignore any unknown fields, coerce arrays via `Array.isArray + .filter(typeof === 'string')`. Corrupted file → reset to defaults rather than crash boot.

    **1C — Create `src/main/mcInstallScan.ts`:**

    Public export: `scanMcInstalls(): Promise<McInstall[]>`.

    Implementation outline:

    1. Helper `homeDir() = os.homedir()` — Node resolves this to `%USERPROFILE%` on Windows.

    2. Helper `appData()` on Windows: `process.env.APPDATA ?? path.join(homeDir(), 'AppData', 'Roaming')`. Used only on win32.

    3. Helper `vanillaPaths()` returns an array of candidate vanilla `.minecraft` directories per platform:
       - darwin: `[path.join(homeDir(), 'Library', 'Application Support', 'minecraft')]`
       - win32: `[path.join(appData(), '.minecraft')]`
       - linux: `[path.join(homeDir(), '.minecraft')]`
       - default: `[]`

    4. Helper `curseforgePaths()` returns candidate Instances directories per platform:
       - darwin: `[path.join(homeDir(), 'Library', 'Application Support', 'curseforge', 'minecraft', 'Instances')]`
       - win32: `[path.join(homeDir(), 'curseforge', 'minecraft', 'Instances'), path.join(homeDir(), 'Documents', 'curseforge', 'minecraft', 'Instances')]`
       - linux: `[]`

    5. Helper `idFor(kind, absPath)` = `crypto.createHash('sha1').update(kind + ':' + absPath).digest('hex').slice(0, 12)` — stable across re-scans so the UI's selected-row state survives.

    6. For each vanilla path: `fs.stat`; if it exists, build an McInstall record:
       - `kind: 'vanilla'`, `label: 'Vanilla Launcher'`, `path: vanillaPath`
       - `mc_version`: read `<vanillaPath>/launcher_profiles.json`; pick the most-recently-played `selectedProfile` (or the first profile with `lastVersionId` set). Pure-JSON parse — null on failure.
       - Fabric loader detection: does any `versions/fabric-loader-*` directory exist under `<vanillaPath>/versions/`? Read dir; if a name matches `^fabric-loader-(\d+\.\d+\.\d+)-(\d+\.\d+(\.\d+)?)$`, set `loader: 'fabric'` and parse loader version from group 1.
       - `csl_installed`: `fs.readdir(<vanillaPath>/mods/)` (ENOENT → false), look for any filename matching `/^CustomSkinLoader[_-].*\.jar$/i`.
       - `csl_version`: regex parse from the matching filename `CustomSkinLoader(?:_Fabric|_Forge)?-(\d+\.\d+(\.\d+)?)\.jar`.

    7. For each curseforge instances root path: `fs.readdir` to list subdirectories. For each subdirectory:
       - `kind: 'curseforge'`, `label: <subdir basename>`, `path: <subdir absolute path>`
       - `mc_version`: read `<subdir>/minecraftinstance.json` if present; pick `gameVersion`.
       - `loader`: from `minecraftinstance.json.baseModLoader.name` — `forge-*` → `'forge'`, `fabric-loader-*` → `'fabric'`, otherwise null.
       - `loader_version`: parsed from same field.
       - `csl_installed` + `csl_version`: same `<subdir>/mods/CustomSkinLoader*.jar` check.

    8. Cross-reference with persisted `WizardState.enabledInstallIds` (call `loadWizardState`) to set `sei_enabled: enabledInstallIds.includes(record.id)`.

    9. Return concatenated list. Skip with `logger.warn` on per-install permission errors — return the list of installs we could scan successfully.

    Every filesystem call uses `path.join` — never string concat. `fs.readdir` + `fs.stat` errors with `ENOENT` are not propagated. Other errors logged + skipped per install. Hard limit: 50 CurseForge instances per Instances directory.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json && grep -q "scanMcInstalls" src/main/mcInstallScan.ts && grep -E "process\.platform === ['\"]darwin['\"]|process\.platform === ['\"]win32['\"]" src/main/mcInstallScan.ts | wc -l > /tmp/p4-t1-plat.txt && [ "$(cat /tmp/p4-t1-plat.txt)" -ge 2 ] && grep -c "path.join" src/main/mcInstallScan.ts > /tmp/p4-t1-join.txt && [ "$(cat /tmp/p4-t1-join.txt)" -ge 5 ] && grep -q "wizardStatePath" src/main/paths.ts && grep -F "loadWizardState\|saveWizardState" src/main/wizardStateStore.ts | wc -l > /tmp/p4-t1-state.txt && [ "$(cat /tmp/p4-t1-state.txt)" -ge 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/mcInstallScan.ts` exists and exports `scanMcInstalls` (grep returns 1+)
    - `src/main/wizardStateStore.ts` exists and exports both `loadWizardState` and `saveWizardState` (grep returns 2+)
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
    - `grep -E "process\.platform === ['\"]darwin['\"]|process\.platform === ['\"]win32['\"]" src/main/mcInstallScan.ts | wc -l` returns 2 or more (both platform branches present)
    - `grep -c "path.join" src/main/mcInstallScan.ts` returns 5 or more (every path is path.join-built; defense against Phase 8 row 1 invariant — no string concat)
    - `grep -E "Library/Application Support/minecraft" src/main/mcInstallScan.ts` matches AND `grep -E "\\.minecraft" src/main/mcInstallScan.ts` matches (mac vanilla + windows vanilla paths both present)
    - `grep -E "curseforge/minecraft/Instances" src/main/mcInstallScan.ts` matches
    - `grep -F "wizardStatePath" src/main/paths.ts` matches once (paths.ts extension)
  </acceptance_criteria>
  <done>
    Cross-platform scanner returns deduped, ID-stable McInstall records. WizardState persists at `<userData>/skin-setup-state.json` with atomic writes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: fabricInstaller.ts + customSkinLoader.ts — Fabric Loader install + CSL JAR download + config writer</name>
  <read_first>
    - .planning/phases/09-.../CONTEXT.md §decisions ("First-launch wizard scope" — vanilla MC = Fabric Loader install + CSL Fabric build; CurseForge already-Forge = just CSL Forge build)
    - src/main/personaExpansion.ts (existing 30s-timeout pattern to mirror for HTTP calls — adapted to 60s for downloads since JARs are larger)
    - Fabric meta API: `https://meta.fabricmc.net/v2/versions/installer` + `https://meta.fabricmc.net/v2/versions/loader/<mc-version>`
    - Modrinth API: `https://api.modrinth.com/v2/project/customskinloader/version`
    - GitHub releases fallback: `https://api.github.com/repos/xfl03/MCCustomSkinLoader/releases/latest`
    - CustomSkinLoader project README §Configuration (Legacy loader type)
  </read_first>
  <action>
    **2A — Create `src/main/fabricInstaller.ts`. Public exports:**
    - `installFabricLoader(opts: { mcInstallDir: string; mcVersion: string; loaderVersion?: string; onProgress?: (pct: number) => void; signal?: AbortSignal }): Promise<{ loaderVersion: string }>`
    - `isJavaAvailable(): Promise<boolean>` — `child_process.execFile('java', ['-version'])` with a 5s timeout; returns false on ENOENT or non-zero exit
    - `selectLatestFabricLoader(mcVersion: string): Promise<{ installerVersion: string; loaderVersion: string }>`

    Implementation outline:

    1. `selectLatestFabricLoader`: GET `https://meta.fabricmc.net/v2/versions/installer` with 30s timeout. Parse JSON array; pick the first element where `stable === true` (else the first element). `installerVersion = elem.version`. Then GET `https://meta.fabricmc.net/v2/versions/loader/${encodeURIComponent(mcVersion)}` with 30s timeout; pick the first stable loader. `loaderVersion = elem.loader.version`.

    2. `installFabricLoader`:
       - Confirm Java is available via `isJavaAvailable()`. If false → throw `FABRIC_INSTALL_FAILED: Java is required but not on PATH. Install Minecraft normally (which bundles Java) and re-run the wizard.`
       - Call `selectLatestFabricLoader(opts.mcVersion)` unless `opts.loaderVersion` is provided.
       - Download `https://maven.fabricmc.net/net/fabricmc/fabric-installer/${installerVersion}/fabric-installer-${installerVersion}.jar` (60s timeout, AbortController). Save to `<paths.userData()>/tmp/fabric-installer-${installerVersion}.jar`; mkdir recursive first.
       - Verify the JAR by reading first 4 bytes: PK ZIP magic `0x50 0x4B 0x03 0x04`. Throw `FABRIC_INSTALL_FAILED: downloaded installer is not a valid JAR` on mismatch.
       - Shell out: `child_process.execFile('java', ['-jar', installerJarPath, 'client', '-dir', opts.mcInstallDir, '-mcversion', opts.mcVersion, '-loader', loaderVersion, '-noprofile'], { timeout: 90_000, signal: opts.signal })`. Capture stdout+stderr; on non-zero exit throw `FABRIC_INSTALL_FAILED: fabric installer exited ${code}: <stderr tail 512 chars>`.
       - On success: confirm `<opts.mcInstallDir>/versions/fabric-loader-${loaderVersion}-${opts.mcVersion}/` directory exists; if not, throw `FABRIC_INSTALL_FAILED: post-install version directory missing`.
       - Cleanup: `fs.unlink` the installer JAR (best-effort).
       - Return `{ loaderVersion }`.

    Use `node:child_process.execFile` (NOT `exec`, which would shell-interpolate). Pass `timeout` and `signal` via the options object. AbortSignal handling: if user cancels mid-install, the child is SIGTERM'd by the runtime.

    The `-noprofile` flag skips `launcher_profiles.json` editing — we intentionally leave the user to pick the Fabric profile from the launcher dropdown (CONTEXT §"Whether to set Fabric as default in launcher_profiles.json automatically or leave the user to select it manually each time" is Claude's Discretion; this plan picks LEAVE TO USER per principle of least surprise + UI-SPEC §"4 — Done" copy "pick the {profile_name} profile from the launcher dropdown").

    All file writes use `path.join`. The installer temp dir `<userData>/tmp/` is mkdir-recursive'd before write.

    **2B — Create `src/main/customSkinLoader.ts`. Public exports:**
    - `downloadCustomSkinLoader(opts: { loaderKind: 'fabric' | 'forge'; mcVersion: string; modsDir: string; signal?: AbortSignal; onProgress?: (pct: number) => void }): Promise<{ jarPath: string; version: string }>`
    - `writeCustomSkinLoaderConfig(opts: { mcInstallDir: string; loaderKind: 'fabric' | 'forge'; skinServerBaseUrl: string }): Promise<{ configPath: string }>`
    - `isCustomSkinLoaderInstalled(modsDir: string): Promise<{ installed: boolean; version: string | null; jarPath: string | null }>`

    Implementation outline:

    1. `isCustomSkinLoaderInstalled(modsDir)`: `fs.readdir(modsDir)`. Return `installed: true` if any filename matches `/^CustomSkinLoader[_-].*\.jar$/i`. Parse version via regex `CustomSkinLoader(?:_Fabric|_Forge)?-(\d+\.\d+(\.\d+)?)\.jar`. ENOENT → `{ installed: false, version: null, jarPath: null }`.

    2. `downloadCustomSkinLoader`:
       - GET `https://api.modrinth.com/v2/project/customskinloader/version` (30s timeout). Filter results where `result.loaders.includes(opts.loaderKind)` AND `result.game_versions.includes(opts.mcVersion)`. Pick the most-recent (Modrinth sorts newest-first).
       - If no Modrinth match, fall back to GitHub: GET `https://api.github.com/repos/xfl03/MCCustomSkinLoader/releases/latest` (30s); pick the asset named matching `CustomSkinLoader_${capitalize(loaderKind)}-*.jar`.
       - On both failures: throw `MOD_DOWNLOAD_FAILED: no compatible CustomSkinLoader release found for ${opts.mcVersion} ${opts.loaderKind}`.
       - Download the chosen JAR URL (60s timeout, AbortController, `opts.signal` propagation). Save to a temp path inside `<paths.userData()>/tmp/`. Verify ZIP magic `0x50 0x4B 0x03 0x04`. Throw `MOD_DOWNLOAD_FAILED: downloaded JAR is corrupt` on magic mismatch.
       - mkdir-recursive `opts.modsDir`. Atomic move via `fs.rename` into `${opts.modsDir}/CustomSkinLoader_${capitalize(loaderKind)}-${version}.jar`. If `fs.rename` throws `EXDEV` (cross-filesystem), fall back to `copyFile + unlink`.
       - Return `{ jarPath, version }`.

    3. `writeCustomSkinLoaderConfig`: build the config object below; write to `path.join(opts.mcInstallDir, 'config', 'CustomSkinLoader', 'CustomSkinLoader.json')`. Use atomic write (via `atomicWrite` from `src/bot/brain/storage/atomicWrite.js`). mkdir-recursive the parent dir.

       ```ts
       const cfg = {
         version: 14,
         loadlist: [{
           name: 'SeiLocal',
           type: 'Legacy',
           userAgent: 'Sei',
           root: opts.skinServerBaseUrl,
           skin: '/skins/{USERNAME}.png',
         }],
         enableTransparentSkin: true,
         enableUpdateSkull: true,
         enableLocalProfileCache: false,
         enableCacheAutoClean: true,
       };
       ```

       Notes:
       - The Legacy loader type does literal URL-template substitution — `{USERNAME}` becomes the in-game username the CSL client reads from the profile. Matches the skin server's regex `^/skins/([A-Za-z0-9_]{1,16})\.png$`.
       - `enableLocalProfileCache: false` so the user's MC client doesn't cache a stale skin between sessions when Sei updates the persona's skin via the editor.
       - Return `{ configPath }` so the wizard's logger.info can prove the write.

    All HTTP calls timeout-wrapped at 30s metadata + 60s downloads. Use the same `fetchWithTimeout` helper pattern from Plan 03 — copy/paste rather than share, because the wizard backend keeps a separate timeout discipline (30/60/90 vs Mojang's 15s).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json && grep -q "execFile" src/main/fabricInstaller.ts && grep -q "meta.fabricmc.net" src/main/fabricInstaller.ts && grep -F "api.modrinth.com" src/main/customSkinLoader.ts && grep -F "api.github.com/repos/xfl03" src/main/customSkinLoader.ts && grep -F "0x50, 0x4B, 0x03, 0x04" src/main/fabricInstaller.ts src/main/customSkinLoader.ts | wc -l > /tmp/p4-t2-zip.txt && [ "$(cat /tmp/p4-t2-zip.txt)" -ge 2 ] && grep -F "SeiLocal" src/main/customSkinLoader.ts && grep -F "/skins/{USERNAME}.png" src/main/customSkinLoader.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
    - `src/main/fabricInstaller.ts` exports `installFabricLoader`, `isJavaAvailable`, `selectLatestFabricLoader` (grep returns 3+)
    - `src/main/customSkinLoader.ts` exports `downloadCustomSkinLoader`, `writeCustomSkinLoaderConfig`, `isCustomSkinLoaderInstalled` (grep returns 3+)
    - `grep -F "execFile" src/main/fabricInstaller.ts` matches (child_process API used, not `exec`)
    - `grep -F "exec(" src/main/fabricInstaller.ts` returns ZERO hits (no shell interpolation; defense-in-depth)
    - `grep -F "meta.fabricmc.net" src/main/fabricInstaller.ts` matches
    - `grep -F "api.modrinth.com" src/main/customSkinLoader.ts` matches AND `grep -F "api.github.com/repos/xfl03" src/main/customSkinLoader.ts` matches
    - `grep -c "0x50, 0x4B, 0x03, 0x04" src/main/fabricInstaller.ts src/main/customSkinLoader.ts` returns 2 or more (ZIP magic verified at both JAR-download sites)
    - `grep -F "SeiLocal" src/main/customSkinLoader.ts` matches (CSL config has the Sei loadlist entry)
    - `grep -F "/skins/{USERNAME}.png" src/main/customSkinLoader.ts` matches (URL template uses literal CSL substitution syntax)
    - `grep -E "timeout:\s*90_?000|timeout:\s*60_?000|timeout:\s*30_?000" src/main/fabricInstaller.ts src/main/customSkinLoader.ts | wc -l` returns 3 or more (all three timeouts are present at the call sites)
  </acceptance_criteria>
  <done>
    Fabric installer and CustomSkinLoader downloader modules compile, time-bound every external call, validate every download by magic-byte, and write a CSL config that points at the loopback skin server with the literal `{USERNAME}` substitution.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: wizard.ts orchestrator + IPC handlers + main bootstrap wiring + 2 verification scripts</name>
  <read_first>
    - src/main/ipc.ts (Plans 02+03 handler patterns — wizard handlers follow the same dynamic-import + zod-validation rule)
    - src/main/index.ts (registerIpcHandlers call — Task 3 widens IpcHandlerDeps further)
    - src/shared/ipc.ts (WizardProgressEvent + IpcChannel.wizard.progress = 'wizard:progress')
    - .planning/phases/09-.../CONTEXT.md §"First-launch wizard scope" (5 steps the orchestrator implements)
    - scripts/verify-skinServer.mjs (Plan 02 harness pattern)
  </read_first>
  <action>
    **3A — Create `src/main/wizard.ts`. Public export:**

    ```ts
    export async function runWizardInstall(args: {
      installIds: string[];
      skinServerBaseUrl: string;
      onProgress: (ev: WizardProgressEvent) => void;
      signal?: AbortSignal;
    }): Promise<{ results: WizardInstallResult[] }>;
    ```

    Implementation outline:

    1. Call `scanMcInstalls()` to get the current install list. Filter to those whose `id` is in `args.installIds`. Anything not found → emit `WizardProgressEvent { installId, stage: 'failed', error: 'MC_INSTALL_NOT_FOUND', message: 'Install no longer detected. Re-run setup.' }` and skip.

    2. For each matched install, in series (NOT parallel — multiple `java -jar` runs concurrently could thrash and the user-facing UI shows one progress row at a time per UI-SPEC §InstallProgressList):
       - Emit `{ installId, stage: 'queued' }`.
       - Determine mc version: `install.mc_version ?? '1.21.4'` (fallback to a known-good version when launcher_profiles is unreadable). If still null, fail with `MC_INSTALL_NOT_FOUND` ("Couldn't determine MC version").
       - Determine loader kind:
         * vanilla install with `loader === null`: install Fabric first. Emit `{ stage: 'fabric-downloading', pct: 0 }`. Call `installFabricLoader({ mcInstallDir: install.path, mcVersion, signal, onProgress: (pct) => onProgress({ installId, stage: 'fabric-downloading', pct }) })`. Catch and re-throw with stage `'failed'` + error `FABRIC_INSTALL_FAILED`. After success emit `{ stage: 'fabric-installing' }` then continue.
         * vanilla install with `loader === 'fabric'`: skip Fabric step. Use 'fabric' as the loader kind.
         * curseforge install: use `install.loader` (could be 'forge' or 'fabric') as the loader kind. If `install.loader === null`, treat as 'forge' (CurseForge defaults to Forge for the vast majority of modpacks).
       - Emit `{ stage: 'mod-downloading', pct: 0 }`. Call `downloadCustomSkinLoader({ loaderKind, mcVersion, modsDir: path.join(install.path, 'mods'), signal, onProgress: (pct) => onProgress({ installId, stage: 'mod-downloading', pct }) })`. Catch → fail with `MOD_DOWNLOAD_FAILED`.
       - Emit `{ stage: 'mod-placing' }` (the actual placement happened inside `downloadCustomSkinLoader` via fs.rename — this stage is purely a UI signal).
       - Emit `{ stage: 'config-writing' }`. Call `writeCustomSkinLoaderConfig({ mcInstallDir: install.path, loaderKind, skinServerBaseUrl: args.skinServerBaseUrl })`. Catch → fail with `WIZARD_PERMISSION_DENIED` (EACCES is the typical cause).
       - Emit `{ stage: 'done' }`. Push a `WizardInstallResult { installId, ok: true, installedFabricVersion?: ..., installedCslVersion: ... }` to the results array.

    3. After all installs processed: load current `WizardState`, update `hasRunOnce: true`, merge successful `installIds` into `enabledInstallIds` (dedupe), set `lastRunAt: new Date().toISOString()`, set `lastSkinServerPort: parseInt(URL().pathname...)` — actually parse port from `args.skinServerBaseUrl` via the URL constructor. Save.

    4. Return `{ results }`.

    AbortSignal handling: each step checks `signal.aborted` between operations; throws `WIZARD_CANCELLED: user cancelled` if aborted. Caller-side IPC translates that to a clean unsubscribe (no progress event emitted after cancel).

    **3B — Extend `src/main/ipc.ts` with three wizard handlers + the progress push channel.**

    Update `IpcHandlerDeps`:
    ```ts
    export interface IpcHandlerDeps {
      supervisor: BotSupervisor;
      getSkinServerBaseUrl: () => string | null;
      sendWizardProgress: (ev: WizardProgressEvent) => void;
    }
    ```

    Add handlers immediately after the Plan 03 `skin:search-mojang` handler:

    ```ts
    ipcMain.handle(IpcChannel.wizard.detectInstalls, async () => {
      const { scanMcInstalls } = await import('./mcInstallScan');
      const installs = await scanMcInstalls();
      return { installs };
    });

    ipcMain.handle(IpcChannel.wizard.install, async (_event, argsRaw: unknown) => {
      const args = z.object({
        installIds: z.array(z.string().min(1)).min(1),
        skinServerBaseUrl: z.string().url(),
      }).parse(argsRaw);
      const { runWizardInstall } = await import('./wizard');
      return await runWizardInstall({
        ...args,
        onProgress: (ev) => deps.sendWizardProgress(ev),
      });
    });

    ipcMain.handle(IpcChannel.wizard.getState, async () => {
      const { loadWizardState } = await import('./wizardStateStore');
      return await loadWizardState();
    });
    ```

    **3C — Extend `src/main/index.ts`:**

    Add `broadcastWizardProgress`:
    ```ts
    function broadcastWizardProgress(ev: WizardProgressEvent): void {
      if (mainWindow && !mainWindow.isDestroyed()) {
        mainWindow.webContents.send(IpcChannel.wizard.progress, ev);
      }
    }
    ```

    Pass to `registerIpcHandlers`:
    ```ts
    registerIpcHandlers({ supervisor, getSkinServerBaseUrl: () => skinServer?.baseUrl ?? null, sendWizardProgress: broadcastWizardProgress });
    ```

    Import `WizardProgressEvent` from `../shared/ipc`.

    **3D — Create `scripts/verify-mcInstallScan.mjs`:**

    Pure Node-only verification that:
    1. Creates a temp directory (`mkdtempSync` under `os.tmpdir()`).
    2. Writes a fake vanilla `.minecraft` tree inside it: `<tmp>/minecraft/launcher_profiles.json` (valid JSON with `selectedProfile`), `<tmp>/minecraft/versions/fabric-loader-0.16.9-1.21.4/`, `<tmp>/minecraft/mods/CustomSkinLoader_Fabric-14.16.jar` (empty file is fine for filename-detection).
    3. Writes a fake CurseForge instance tree: `<tmp>/curseforge/minecraft/Instances/Pixelmon/minecraftinstance.json` with `{ "gameVersion": "1.20.1", "baseModLoader": { "name": "forge-47.3.0" } }`.
    4. Reimplements the `vanillaPaths()` / `curseforgePaths()` resolution but pointed at the temp dir (use environment override `SEI_TEST_HOME=<tmp>` or copy the scan logic inline and parameterize it). Cleanest: write a thin wrapper module `src/main/mcInstallScan.ts` that accepts an optional `homedirOverride` arg (default `os.homedir()`), and the harness calls `scanMcInstalls({ homedirOverride: tmpDir })`.
    5. Asserts the scan returns the expected records: 1 vanilla install with `loader: 'fabric'` + `csl_installed: true`; 1 curseforge install with `loader: 'forge'` + `csl_installed: false`.
    6. Print `PASS 5/5` on success.

    To support this, add a 1-arg optional override to `scanMcInstalls` in Task 1 (already noted; if missed, this task extends Task 1's signature with a backwards-compatible default):
    ```ts
    export async function scanMcInstalls(opts?: { homedirOverride?: string; platformOverride?: NodeJS.Platform }): Promise<McInstall[]>
    ```

    The harness runs via `npx tsx scripts/verify-mcInstallScan.mjs`. Add `"verify:phase9-installs": "tsx scripts/verify-mcInstallScan.mjs"` to package.json.

    **3E — Create `scripts/verify-customSkinLoaderConfig.mjs`:**

    Pure Node-only verification that:
    1. Creates a temp directory.
    2. Calls `writeCustomSkinLoaderConfig({ mcInstallDir: tmpDir, loaderKind: 'fabric', skinServerBaseUrl: 'http://127.0.0.1:54321' })`.
    3. Reads back `<tmpDir>/config/CustomSkinLoader/CustomSkinLoader.json`. Parses JSON.
    4. Asserts:
       - `cfg.version === 14`
       - `cfg.loadlist[0].name === 'SeiLocal'`
       - `cfg.loadlist[0].type === 'Legacy'`
       - `cfg.loadlist[0].root === 'http://127.0.0.1:54321'`
       - `cfg.loadlist[0].skin === '/skins/{USERNAME}.png'`
       - `cfg.enableLocalProfileCache === false`
    5. Print `PASS 6/6` on success.

    Add `"verify:phase9-csl-config": "tsx scripts/verify-customSkinLoaderConfig.mjs"` to package.json.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json && grep -q "runWizardInstall" src/main/wizard.ts && grep -F "IpcChannel.wizard.detectInstalls\|IpcChannel.wizard.install\|IpcChannel.wizard.getState" src/main/ipc.ts | wc -l > /tmp/p4-t3-ipc.txt && [ "$(cat /tmp/p4-t3-ipc.txt)" -ge 3 ] && grep -F "sendWizardProgress\|broadcastWizardProgress" src/main/index.ts && npx tsx scripts/verify-customSkinLoaderConfig.mjs && npx tsx scripts/verify-mcInstallScan.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
    - `src/main/wizard.ts` exports `runWizardInstall`
    - `grep -c "IpcChannel.wizard" src/main/ipc.ts` returns 3 or more (detectInstalls + install + getState handlers)
    - `grep -F "sendWizardProgress" src/main/index.ts` matches AND `grep -F "broadcastWizardProgress" src/main/index.ts` matches (push wiring present)
    - `npx tsx scripts/verify-mcInstallScan.mjs` prints `PASS 5/5` and exits 0
    - `npx tsx scripts/verify-customSkinLoaderConfig.mjs` prints `PASS 6/6` and exits 0
    - `grep -F "verify:phase9-installs\|verify:phase9-csl-config" package.json` returns 2 lines
  </acceptance_criteria>
  <done>
    Wizard backend end-to-end wired. The two verification scripts cover the cross-platform scanner and the CSL config writer using temp directories, with no real-network dependency. Renderer can fire `wizard:detectInstalls`/`wizard:install`/`wizard:getState` and receive `wizard:progress` push events.
  </done>
</task>

</tasks>

<verification>
After all tasks:
- `npx tsc --noEmit -p tsconfig.node.json` exits 0
- `npx tsx scripts/verify-mcInstallScan.mjs` prints `PASS 5/5`
- `npx tsx scripts/verify-customSkinLoaderConfig.mjs` prints `PASS 6/6`
- Plan 02 + Plan 03 harnesses still pass (`node scripts/verify-skinServer.mjs && npx tsx scripts/verify-mojangSkinLookup.mjs`) — regression guards
- A developer can run `npm run dev` on macOS with a real Minecraft launcher installed, then `await window.sei.detectMcInstalls()` in the renderer DevTools console — should return the actual vanilla install path under `~/Library/Application Support/minecraft`. (Manual smoke; documented in summary.)
- Same developer can run `await window.sei.runWizardInstall({ installIds: [<id>], skinServerBaseUrl: 'http://127.0.0.1:<port>' })` and watch the Fabric installer + CSL JAR download progress via the `wizard:progress` push channel.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main process → user filesystem (write) | Wizard writes JAR files and config JSON into directories outside `<userData>`. Every write target is constructed from `install.path` which the user explicitly selected in the UI |
| Main process → external HTTP (Fabric meta, Modrinth, GitHub releases, JAR downloads) | Untrusted bytes return from each call. All HTTP responses are timeout-bound. JAR/installer downloads verify ZIP magic before placement |
| Main process → java child process | `execFile('java', ...)` runs the user's local Java with arguments derived from app-controlled strings. No shell interpolation possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-T4 | T (Tampering) | Modrinth/GitHub JAR download | mitigate | ZIP magic-byte verification before fs.rename into mods/. Future hardening (PR-after-v1): sha256 the downloaded JAR against the value Modrinth ships in the version metadata. |
| T-09-T5 | T (Tampering) | Fabric installer JAR | mitigate | Same ZIP magic check. Installer is published by FabricMC over HTTPS; we trust their HTTPS chain. |
| T-09-E2 | E (Elevation of Privilege) | execFile('java', ...) | mitigate | Arguments are all derived from app-controlled or strictly-validated values (`opts.mcVersion` is matched against Mojang's version metadata before passing). No `shell: true` option. No string interpolation. |
| T-09-D3 | D (Denial of Service) | runaway Fabric installer | mitigate | 90s wall-clock timeout via `child_process.execFile(..., { timeout: 90_000, signal })`. SIGTERM on AbortController cancellation. |
| T-09-I3 | I (Information Disclosure) | wizard scan exposes user MC paths to renderer | accept | Renderer is same-trust-domain as main (user owns both); paths surface in the UI so the user can pick the right install. Path strings are NOT shipped off-machine. |
| T-09-T6 | T (Tampering) | CustomSkinLoader config write | mitigate | atomicWrite (tmp + rename). withFileLock guards against concurrent wizard runs. Write target `<install>/config/CustomSkinLoader/CustomSkinLoader.json` is path.join-built; no user input flows into the path string. |
</threat_model>

<success_criteria>
- `await window.sei.detectMcInstalls()` returns a list of real installs on the dev machine
- `await window.sei.runWizardInstall({...})` produces a CSL JAR under the install's `mods/` directory AND a valid `CustomSkinLoader.json` under `config/CustomSkinLoader/` AND emits a stream of `wizard:progress` events
- `<userData>/skin-setup-state.json` persists `hasRunOnce: true` + the enabled install ids
- Re-running `runWizardInstall` for the same install is idempotent: detects existing CSL JAR, replaces with newer version (or leaves alone if same version)
- All three verification scripts (Plan 02 + Plan 03 + Plan 04 x2) pass with no real-network dependency
</success_criteria>

<output>
After completion, create `.planning/phases/09-.../09-04-SUMMARY.md`. Include: output of both verification scripts, the bound config path (e.g. `<userData>/skin-setup-state.json` resolved on the test machine), and confirmation that `grep -F "execFile" src/main/fabricInstaller.ts` matches (no shell-based exec).
</output>
