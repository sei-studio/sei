---
phase: 17-minecraft-competence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bot/adapter/minecraft/behaviors/reflex.js
  - src/bot/adapter/minecraft/behaviors/reflex.test.js
  - src/bot/adapter/minecraft/behaviors/hostiles.js
  - src/bot/adapter/minecraft/behaviors/combat.js
  - src/bot/config.js
autonomous: true
requirements: [MCRAFT-02, MCRAFT-03]
must_haves:
  truths:
    - "A ~20 Hz physicsTick loop scans the entity list every tick and can evade before damage lands"
    - "An incoming arrow whose ray passes within the miss threshold of the bot triggers a lateral sidestep pulse"
    - "A creeper inside the flee-enter radius triggers a GoalInvert(GoalFollow) retreat that owns the pathfinder goal and restores the prior goal on exit"
    - "A melee mob inside the kite band triggers a circle-strafe control-state alternation"
    - "The loop never enqueues into fsm.js and tears down its tick handler on entityGone/death"
  artifacts:
    - path: "src/bot/adapter/minecraft/behaviors/reflex.js"
      provides: "startReflex(bot, config) survival controller + exported pure closestApproach() ray test"
      exports: ["startReflex", "closestApproach"]
      min_lines: 120
    - path: "src/bot/adapter/minecraft/behaviors/hostiles.js"
      provides: "shared HOSTILE_MOBS set hoisted from combat.js"
      exports: ["HOSTILE_MOBS"]
    - path: "src/bot/config.js"
      provides: "seven reflex config keys under adapter.minecraft"
      contains: "reflex_tick_ms"
  key_links:
    - from: "src/bot/adapter/minecraft/behaviors/reflex.js"
      to: "mineflayer-pathfinder goals.GoalInvert / goals.GoalFollow"
      via: "creeper flee goal takeover"
      pattern: "GoalInvert"
    - from: "src/bot/adapter/minecraft/behaviors/reflex.js"
      to: "bot._seiReflexActive"
      via: "mutex set during goal-owning flee"
      pattern: "_seiReflexActive"
---

<objective>
Build the reflex evasion micro-controller (`startReflex`) as a sibling of `startCombat` — a ~20 Hz `physicsTick` survival loop that evades incoming damage BEFORE it lands (the named failures: reacts only after `entityHurt`, dies to creepers, freezes in fights). This plan delivers the controller in isolation: the threat scan, the three evasion mechanisms (arrow sidestep pulse, creeper goal-owning flee, melee circle-strafe), the aggro telegraphs, the `bot._seiReflexActive` mutex flag, the announcement event emission, and the config keys. Live wiring + consumer yield is Plan 02.

Purpose: highest-leverage item in the phase (D-05) — directly satisfies MCRAFT-02 (proactive detection / evade before damage) and MCRAFT-03 (positioning-based combat).
Output: `behaviors/reflex.js`, `behaviors/hostiles.js`, seven `adapter.minecraft` config keys, a unit test for the ray math.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-minecraft-competence/17-CONTEXT.md
@.planning/phases/17-minecraft-competence/17-PATTERNS.md
@.planning/research/v0.4-mc-reflex-dodge.md

<interfaces>
<!-- combat.js — the factory shape + NaN-guard + HOSTILE_MOBS to mirror. -->
From src/bot/adapter/minecraft/behaviors/combat.js:
- `const HOSTILE_MOBS = new Set([...])`  (lines 4-10) — hoist verbatim into hostiles.js, re-import in combat.js
- `export function startCombat(bot, config)` (line 35) — config slice at line 49: `const mc = config?.adapter?.minecraft ?? config ?? {}`
- NaN pre-check (lines 67-71): skip the tick when `bot.entity.velocity` / `position` has any non-finite component
- combat.js emits `bot.emit('sei:attacked', payload)` (line 111) — the announcement-emit precedent to mirror with `sei:reflex`

From src/bot/adapter/minecraft/behaviors/follow.js (lines 1-2) — the pathfinder import path:
- `import pkg from 'mineflayer-pathfinder'` ; `const { pathfinder, Movements, goals } = pkg`
- flee goal: `new goals.GoalInvert(new goals.GoalFollow(creeper, FLEE_RANGE))` then `bot.pathfinder.setGoal(goal, true)`

From src/bot/config.js (MinecraftAdapterSchema, lines 17-34) — declaration style:
- `attack_react_throttle_ms: z.number().int().min(0).default(3500)` is the closest sibling for the new int keys
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hoist HOSTILE_MOBS + add reflex config keys</name>
  <files>src/bot/adapter/minecraft/behaviors/hostiles.js, src/bot/adapter/minecraft/behaviors/combat.js, src/bot/config.js</files>
  <read_first>
    - src/bot/adapter/minecraft/behaviors/combat.js (lines 1-10, 49-51 — the set + config-slice access path)
    - src/bot/config.js (lines 17-34 — MinecraftAdapterSchema, the z.number declaration style)
    - .planning/phases/17-minecraft-competence/17-PATTERNS.md (sections "reflex config keys" and combat.js HOSTILE_MOBS hoist)
    - .planning/research/v0.4-mc-reflex-dodge.md (§5 thresholds table, §6 config keys)
  </read_first>
  <action>
    Create `src/bot/adapter/minecraft/behaviors/hostiles.js` exporting `HOSTILE_MOBS` — move the Set verbatim from combat.js:4-10 (all 26 mob names). In combat.js, delete the local Set literal and `import { HOSTILE_MOBS } from './hostiles.js'` so combat.js behavior is byte-identical aside from the import.
    In config.js `MinecraftAdapterSchema` add seven keys following the line-33 style: `reflex_enabled: z.boolean().default(true)`, `reflex_tick_ms: z.number().int().min(0).default(50)`, `arrow_watch_blocks: z.number().int().min(0).default(16)`, `arrow_miss_threshold: z.number().min(0).default(1.2)` (non-int → no `.int()`), `creeper_flee_enter_blocks: z.number().int().min(0).default(8)`, `creeper_flee_exit_blocks: z.number().int().min(0).default(12)`, `melee_kite_blocks: z.number().min(0).default(4.5)`. These are read in reflex.js via the `mc = config?.adapter?.minecraft ?? config ?? {}` slice.
  </action>
  <verify>
    <automated>npx vitest run src/bot/adapter/minecraft/behaviors/combat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - hostiles.js exports HOSTILE_MOBS containing exactly the 26 names from combat.js:4-10 (grep: hostiles.js contains 'creeper', 'skeleton', 'zombie')
    - combat.js imports HOSTILE_MOBS from './hostiles.js' and no longer declares the Set literal (grep -v '^//' combat.js | grep -c "new Set(\[" == 0)
    - config.js MinecraftAdapterSchema declares reflex_tick_ms with default 50 and arrow_miss_threshold without .int() (source assertion)
    - existing combat.test.js still passes (no regression from the hoist)
  </acceptance_criteria>
  <done>HOSTILE_MOBS lives in one shared module; the seven reflex keys parse with their documented defaults; combat tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Threat scan + arrow dodge + melee strafe + telegraphs</name>
  <files>src/bot/adapter/minecraft/behaviors/reflex.js, src/bot/adapter/minecraft/behaviors/reflex.test.js</files>
  <read_first>
    - .planning/research/v0.4-mc-reflex-dodge.md (§1 arrow ray math, §3 strafe, §5 aggro telegraphs + activation ranges, §6 architecture)
    - src/bot/adapter/minecraft/behaviors/combat.js (lines 35-90 — factory shape, NaN guard, setInterval/clear pattern)
    - src/bot/adapter/minecraft/behaviors/hostiles.js (HOSTILE_MOBS — created in Task 1)
    - .planning/phases/17-minecraft-competence/17-PATTERNS.md (behaviors/reflex.js section, items 1-3 and 5)
  </read_first>
  <behavior>
    - closestApproach(arrowPos, arrowVel, botPos) returns { missDist, ahead }: for a ray from arrowPos along arrowVel, missDist is the point-to-line distance to botPos and ahead = dot(botPos - arrowPos, arrowVel) > 0
    - Test: an arrow aimed directly at the bot (velocity points at botPos) → missDist ≈ 0, ahead = true
    - Test: an arrow already past the bot (velocity points away) → ahead = false (no dodge)
    - Test: an arrow offset by 5 blocks laterally → missDist ≈ 5, ahead = true (above arrow_miss_threshold → no dodge)
    - scanThreats(bot, mc) classifies each in-range entity into {kind:'arrow'|'creeper'|'melee', entity, sideHint?} and picks the highest-priority (creeper-panic > arrow > creeper > melee)
  </behavior>
  <action>
    Create `startReflex(bot, config)` mirroring the `startCombat` factory: resolve `const mc = config?.adapter?.minecraft ?? config ?? {}`; if `mc.reflex_enabled === false` return a no-op disposer. Subscribe `bot.on('physicsTick', tick)`; each tick reuse the NaN/finite pre-check from combat.js:67-71 and return early on bot death. Export a PURE `closestApproach(arrowPos, arrowVel, botPos)` (closest-approach / point-to-line distance, returns { missDist, ahead }) for the arrow test.
    scanThreats: iterate `bot.entities`, distance-cap first; collect arrows (`e.name === 'arrow'` with non-zero `e.velocity`), creepers (name 'creeper'), and HOSTILE_MOBS melee within `melee_kite_blocks + 2`. Arrow threat = any arrow with `closestApproach(...).ahead && missDist < mc.arrow_miss_threshold`, OR a skeleton within `arrow_watch_blocks` with bow-draw telegraph `(skeleton.metadata?.[8] & 0x01) === 1`. Creeper-panic telegraph = `creeper.metadata?.[16] === 1` (fuse) or `creeper.metadata?.[18]` (ignited).
    ARROW response (pulse, non-interruptive — do NOT touch the pathfinder goal): pick the lateral axis perpendicular to the arrow's horizontal velocity, `bot.setControlState('left'|'right', true)`, schedule release after ~4 ticks (clearTimeout-guarded). MELEE response (pulse): `bot.lookAt(mob.position)` then alternate `setControlState('left'/'right')` every ~8-12 ticks holding a 2.5-4 band — suppress when `bot._seiOffensiveTarget === mob.id`. Creeper flee is Task 3.
    Teardown: return a `dispose()` that removes the physicsTick listener and clears any pending pulse timers; also self-tear-down on `bot.once('death')` / `entityGone` of a tracked threat. Never call `fsm.enqueue`/`dispatch` (D-05: runs outside fsm.js).
  </action>
  <verify>
    <automated>npx vitest run src/bot/adapter/minecraft/behaviors/reflex.test.js</automated>
  </verify>
  <acceptance_criteria>
    - reflex.js exports startReflex and closestApproach (grep: 'export function startReflex', 'export function closestApproach')
    - reflex.test.js asserts the three closestApproach cases above AND that scanThreats prioritizes a fusing creeper over an arrow
    - reflex.js subscribes bot.on('physicsTick') and contains NO fsm enqueue/dispatch (grep -v '^//' reflex.js | grep -c "fsm" == 0)
    - arrow/melee responses call bot.setControlState and never call bot.pathfinder.setGoal (source assertion — only the creeper flee in Task 3 owns the goal)
    - melee strafe is suppressed when bot._seiOffensiveTarget matches the mob id (source assertion)
  </acceptance_criteria>
  <done>The tick loop classifies threats and issues arrow-sidestep + melee-strafe pulses with no pathfinder-goal contention; ray math unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Creeper flee (goal takeover) + mutex + announcement event</name>
  <files>src/bot/adapter/minecraft/behaviors/reflex.js, src/bot/adapter/minecraft/behaviors/reflex.test.js</files>
  <read_first>
    - .planning/research/v0.4-mc-reflex-dodge.md (§2 creeper flee, §6 save/restore + mutex, "In-character announcement")
    - src/bot/adapter/minecraft/behaviors/follow.js (lines 1-2 pathfinder import; lines 104-106 setGoal idle-yield precedent)
    - src/bot/adapter/minecraft/behaviors/combat.js (lines 108-112 — bot.emit('sei:attacked') announcement precedent)
    - .planning/phases/17-minecraft-competence/17-PATTERNS.md (reflex.js items 4 and 7)
  </read_first>
  <behavior>
    - Entering flee when a creeper is within creeper_flee_enter_blocks sets bot._seiReflexActive = true, saves bot._seiSavedGoal = current goal, and calls bot.pathfinder.setGoal(GoalInvert(GoalFollow(creeper, exit_blocks)), true)
    - Exiting flee when the creeper is gone or distance > creeper_flee_exit_blocks restores bot._seiSavedGoal and clears bot._seiReflexActive
    - Enter/exit hysteresis (enter 8, exit 12) prevents oscillation across the boundary
    - Activation emits exactly one sei:reflex event per engagement (not every tick)
  </behavior>
  <action>
    Add the creeper-flee branch to the Task-2 tick. On enter (creeper dist ≤ `creeper_flee_enter_blocks`, or panic telegraph): if not already fleeing, set `bot._seiReflexActive = true`, snapshot `bot._seiSavedGoal = bot.pathfinder.goal` (+ its dynamic flag), then `bot.pathfinder.setGoal(new goals.GoalInvert(new goals.GoalFollow(creeper, mc.creeper_flee_exit_blocks)), true)`. On exit (creeper absent or dist > `creeper_flee_exit_blocks`): `bot.pathfinder.setGoal(bot._seiSavedGoal, savedDynamic)`, set `bot._seiReflexActive = false`, clear `bot._seiSavedGoal`. Use the enter/exit bands as hysteresis. Respect the 12s pathfinder timeout invariant — do not block; setGoal is fire-and-forget.
    Announcement: on a NEW engagement (rising edge, throttled to once per engagement — reuse the leading-edge idea, not per-tick), `bot.emit('sei:reflex', { threat, threatLabel, noticed, count })` where `noticed` derives from the telegraph (skeleton bow-draw / creeper fuse / facing heuristic) and `threat` names the mob. The payload is shaped so Plan 02's fsmWires translation can frame an in-character say() offering attack()/explore(). The emit MUST NOT block evasion and MUST NOT enqueue into fsm.js.
  </action>
  <verify>
    <automated>npx vitest run src/bot/adapter/minecraft/behaviors/reflex.test.js</automated>
  </verify>
  <acceptance_criteria>
    - reflex.js sets bot._seiReflexActive true on flee-enter and false on flee-exit (grep + test assertion against a mock bot with a creeper entity)
    - flee saves and restores bot.pathfinder.goal via bot._seiSavedGoal (source assertion)
    - reflex.test.js drives a creeper from 7→13 blocks and asserts GoalInvert is set once on enter and the saved goal restored once on exit (no oscillation at the boundary)
    - exactly one sei:reflex event fires per engagement (test counts emissions across multiple in-range ticks == 1)
    - the creeper flee is the ONLY branch that calls bot.pathfinder.setGoal (source assertion)
  </acceptance_criteria>
  <done>Creeper flee transparently takes over and restores the goal under a mutex; one in-character announcement fires per engagement; reflex loop is fully outside the FSM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Minecraft server → bot entity list / metadata | Entity positions, velocities, and `metadata[*]` are server-controlled, untrusted input read every tick |
| reflex loop → pathfinder / control state | The loop drives motor primitives; a runaway loop or leaked handler is an availability risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Denial of Service | 20 Hz physicsTick loop | mitigate | Distance-cap the entity scan before any math; reuse combat.js NaN/finite guard (lines 67-71) so malformed knockback packets skip the tick instead of throwing; `dispose()` removes the listener on death/entityGone — Task 2/3 acceptance assert no leaked handler |
| T-17-02 | Denial of Service | creeper flee pathfinder takeover | mitigate | Flee is fire-and-forget setGoal honoring the existing 12s `pathfinder_timeout_ms` wall-clock invariant; never awaits; enter/exit hysteresis prevents goal-thrash oscillation |
| T-17-03 | Tampering | spoofed entity metadata (fake fuse/bow-draw) | accept | A false telegraph only causes a harmless sidestep/flee (research §5: "false sidesteps are harmless"); reflexes are never gated on provable aggro and dispatch no code/coordinates |
| T-17-SC | Tampering | dependency installs | n/a | No new npm packages — arrow dodge is hand-rolled, creeper flee reuses already-installed mineflayer-pathfinder, strafe is ported not added. No install tasks → package legitimacy gate not triggered |

No threat reaches `high` requiring a blocking human checkpoint: the loop reads structured state only, dispatches no LLM-injected code/coordinates (D-03 preserved), and the only availability surface (T-17-01/02) is mitigated by guards + teardown + the existing pathfinder timeout. ASVS L1: input bounds (distance cap), resource teardown (dispose), no injection path — all satisfied.
</threat_model>

<verification>
- `npx vitest run src/bot/adapter/minecraft/behaviors/reflex.test.js` green (ray math + threat priority + flee save/restore + single announcement)
- `npx vitest run src/bot/adapter/minecraft/behaviors/combat.test.js` green (no regression from the HOSTILE_MOBS hoist)
- `node -e "import('./src/bot/config.js')"` style parse: config.js still loads; the seven reflex keys carry their defaults
- grep gates in acceptance criteria pass (no fsm reference, single setGoal owner)
</verification>

<success_criteria>
startReflex is a self-contained, unit-tested survival controller: arrow sidestep + melee strafe are non-interruptive pulses, creeper flee is a transparent goal takeover under bot._seiReflexActive, one in-character announcement fires per engagement, and the loop tears down cleanly. Config keys parse. No live wiring yet (Plan 02).
</success_criteria>

<output>
Create `.planning/phases/17-minecraft-competence/17-01-SUMMARY.md` when done. Record: the reflex.js public surface (startReflex signature, closestApproach, sei:reflex payload shape, bot._seiReflexActive / bot._seiSavedGoal contract) so Plan 02 wires against it without re-reading.
</output>
