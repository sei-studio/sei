---
phase: 17-minecraft-competence
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bot/adapter/minecraft/observers/progression.json
  - src/bot/adapter/minecraft/observers/progression.js
  - src/bot/adapter/minecraft/observers/progression.test.js
  - src/bot/adapter/minecraft/observers/snapshot.js
  - src/bot/brain/orchestrator.js
  - src/bot/brain/orchestrator.test.js
autonomous: true
requirements: [MCRAFT-07]
must_haves:
  truths:
    - "The progression spine is externalized to progression.json and loaded as the DAG"
    - "nextMilestone(state, goal) returns the nearest ready milestone + its single advancing action"
    - "The per-turn snapshot carries one advisory `next:` line naming the current milestone and advancing action"
    - "When a milestone newly completes, a terse known-good procedure is written once to per-world MEMORY.md"
  artifacts:
    - path: "src/bot/adapter/minecraft/observers/progression.json"
      provides: "the externalized spine DAG (was the in-code SPINE literal) with optional procedure/next_action per node"
      contains: "iron_pickaxe"
    - path: "src/bot/adapter/minecraft/observers/progression.js"
      provides: "JSON-loaded SPINE + nextMilestone walker; existing compute/match preserved"
      exports: ["nextMilestone", "computeProgression", "matchGoalToNode", "getProgression"]
    - path: "src/bot/adapter/minecraft/observers/snapshot.js"
      provides: "advisory next: line"
      contains: "next:"
  key_links:
    - from: "src/bot/adapter/minecraft/observers/snapshot.js"
      to: "nextMilestone"
      via: "advisory next: line composition"
      pattern: "nextMilestone"
    - from: "src/bot/brain/orchestrator.js"
      to: "memoryLog.append"
      via: "procedural write-back on milestone completion"
      pattern: "memoryLog.append"
---

<objective>
Externalize the progression spine to static data (D-07) and surface long-horizon coherence: move the in-code `SPINE` literal into `progression.json`, add a pure `nextMilestone(state, goal)` walker, surface one advisory `next:` line in the per-turn snapshot, and write a terse known-good procedure to per-world memory when a milestone newly completes (D-08, procedural memory). No GOAP planner — the static graph supersedes it. This is the long-horizon backbone for unaided iron-tier progression (MCRAFT-07).

Purpose: gives the single-layer model a free curriculum + a "what's next" advisory + retrieval of past procedures instead of re-derivation, all with no planner module and no code generation.
Output: progression.json, the walker, the snapshot line, the procedural write-back.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-minecraft-competence/17-CONTEXT.md
@.planning/phases/17-minecraft-competence/17-PATTERNS.md
@.planning/research/v0.4-mc-llm-planning.md

<interfaces>
<!-- progression.js is already the walker; this plan re-homes the data + adds nextMilestone. -->
From src/bot/adapter/minecraft/observers/progression.js:
- `export const SPINE = [ {id,kind,label,key,needs,goal}, ... ]` (lines 52-69) — move this literal to progression.json verbatim
- derived maps `SPINE_BY_ID` (71), `SUCCESSORS` (73-74) — keep, rebuilt from the loaded JSON
- `computeProgression(state)` (110-136) returns `{ done, raw, frontier, currentMilestone, furthest, complete }`
- `matchGoalToNode(goalText, candidates)` (144-153) — longest-key match
- `readProgressionState(bot, flags)` (190-201) — defensive, degrades to empty; live reader STAYS in .js
- `getProgression(bot, flags)` (206-208) — read+compute convenience (exposed at index.js:98)

From src/bot/adapter/minecraft/observers/snapshot.js:
- `composeSnapshot(bot, opts)` (line 70); `lines.push(...)` pattern; `follow_target:` line (225-227) is the exact shape to mirror for `next:`

From src/bot/brain/orchestrator.js:
- `refreshProgressionForSeed()` (~line 1098) ALREADY detects milestone completion: `if (activeFrontierGoal && prog?.done?.has?.(activeFrontierGoal.id))` sets `justCompletedNote` — the hook point for procedural write-back
- `memoryLog.append(text, new Date())` + `memoryCompactor.maybeCompact()` (the remember handler at 1854-1857) — the exact append + compaction-trigger pattern to reuse
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Externalize SPINE to progression.json + nextMilestone walker</name>
  <files>src/bot/adapter/minecraft/observers/progression.json, src/bot/adapter/minecraft/observers/progression.js, src/bot/adapter/minecraft/observers/progression.test.js</files>
  <read_first>
    - src/bot/adapter/minecraft/observers/progression.js (entire file — SPINE 52-69, maps 71-74, computeProgression 110-136, matchGoalToNode 144-153, readProgressionState 190-208)
    - src/bot/adapter/minecraft/observers/progression.test.js (lines 1-30 — the achieve() walk helper + import shape)
    - src/bot/config.js (lines 1-2, 229-230 — readFileSync usage precedent for loading a JSON file)
    - .planning/phases/17-minecraft-competence/17-PATTERNS.md (progression.json + nextMilestone section, node shape to preserve)
    - .planning/research/v0.4-mc-llm-planning.md (progression-as-data, next: line)
  </read_first>
  <behavior>
    - The JSON-loaded SPINE is identical in content to the prior literal: progression.test.js connectivity test still reaches defeat(dragon) from empty
    - nextMilestone(state) with no goal returns computeProgression(state).currentMilestone
    - nextMilestone(state, 'iron pickaxe') returns the iron_pickaxe node (matchGoalToNode wins over currentMilestone when the goal matches a frontier node)
    - nextMilestone returns { node, action } where action is the node's next_action (or a fallback derived from kind+label)
    - A malformed/missing progression.json degrades to an empty spine (no throw) — mirrors readProgressionState's defensive contract
  </behavior>
  <action>
    Create `progression.json` containing the spine array (move the 16 nodes from progression.js:52-69 verbatim — same id/kind/label/key/needs/goal). Add to each node TWO optional fields: `next_action` (a terse advancing-action phrase for the advisory line, e.g. iron_pickaxe → "smelt iron then craft an iron pickaxe at a table") and `procedure` (a terse known-good procedure string for D-08 write-back, e.g. iron_pickaxe → "iron: stone pickaxe → mine iron_ore (y≤16) → smelt in furnace w/ coal → craft iron_pickaxe at table"). Iron-tier nodes (logs..iron_pickaxe) MUST have both fields populated; later nodes may leave them empty.
    In progression.js: replace the `export const SPINE = [...]` literal with a load from progression.json via `readFileSync(new URL('./progression.json', import.meta.url), 'utf-8')` + `JSON.parse`, wrapped defensively (try/catch → `[]` on error, matching readProgressionState's degrade-to-empty contract). Keep `export const SPINE` as the loaded array so existing imports (and the test) are unchanged. Rebuild SPINE_BY_ID / SUCCESSORS from the loaded array. Add `export function nextMilestone(state, goal)` = `matchGoalToNode(goal, computeProgression(state).frontier) ?? computeProgression(state).currentMilestone`, returning `{ node, action: node?.next_action ?? node?.label ?? null }` (null-safe when complete). Do NOT change computeProgression / matchGoalToNode / readProgressionState logic.
  </action>
  <verify>
    <automated>npx vitest run src/bot/adapter/minecraft/observers/progression.test.js</automated>
  </verify>
  <acceptance_criteria>
    - progression.json contains all 16 spine ids incl. iron_pickaxe, with next_action + procedure populated for logs..iron_pickaxe (grep + JSON.parse check)
    - progression.js loads SPINE from progression.json (grep: import.meta.url + progression.json) and still exports SPINE, computeProgression, matchGoalToNode, getProgression
    - progression.js exports nextMilestone (grep)
    - existing progression.test.js connectivity + frontier tests still pass UNCHANGED
    - new test cases assert nextMilestone(no-goal)=currentMilestone, nextMilestone('iron pickaxe')=iron_pickaxe node, and malformed-JSON degrades to empty (added to progression.test.js)
  </acceptance_criteria>
  <done>The spine is data-driven, the walker returns the nearest milestone + advancing action, and all prior progression guarantees hold.</done>
</task>

<task type="auto">
  <name>Task 2: Advisory next: snapshot line</name>
  <files>src/bot/adapter/minecraft/observers/snapshot.js</files>
  <read_first>
    - src/bot/adapter/minecraft/observers/snapshot.js (lines 70-104 composeSnapshot head, 213-262 follow_target/owner/last_action_result push block)
    - src/bot/adapter/minecraft/observers/progression.js (nextMilestone + readProgressionState — Task 1)
    - .planning/phases/17-minecraft-competence/17-PATTERNS.md (next: snapshot line section — additive, do NOT remove the heartbeat frontier path)
  </read_first>
  <action>
    In composeSnapshot, import `nextMilestone` and `readProgressionState` from `./progression.js`. After the follow_target push (around line 227) add ONE advisory line: compute `const nm = nextMilestone(readProgressionState(bot))` defensively (wrap in try/catch → skip the line on any error, never throw a snapshot tick), and if `nm?.node` push `next: ${nm.node.label} — ${nm.action}` (mirror the `follow_target:` push shape). When progression is complete (nm.node null) push nothing or `next: (game complete)`. This is the per-turn surfacing of currentMilestone; it is ADDITIVE and must not touch the existing heartbeat frontierText path in orchestrator.js (renderHeartbeat) — they are complementary.
  </action>
  <verify>
    <automated>npx vitest run src/bot/adapter/minecraft/observers/ 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - snapshot.js imports nextMilestone + readProgressionState from './progression.js' (grep)
    - composeSnapshot pushes a `next:` line built from nextMilestone, wrapped so an error skips the line rather than throwing (source assertion)
    - no existing snapshot/observer test regresses
    - the heartbeat frontier path in orchestrator.js is untouched (this plan's snapshot edit is additive)
  </acceptance_criteria>
  <done>Every per-turn snapshot carries a single advisory next: line so the model always sees its nearest milestone and the action that advances it.</done>
</task>

<task type="auto">
  <name>Task 3: Procedural memory write-back on milestone completion (D-08)</name>
  <files>src/bot/brain/orchestrator.js, src/bot/brain/orchestrator.test.js</files>
  <read_first>
    - src/bot/brain/orchestrator.js (refreshProgressionForSeed ~1090-1120 — the milestone-complete detection; the remember handler 1847-1864 — append + maybeCompact pattern; line ~463 where memoryLog is held)
    - src/bot/adapter/minecraft/observers/progression.json (the per-node `procedure` field — Task 1)
    - src/bot/brain/memory/memoryLog.js (lines 34-43 API, 78-99 appendMemory)
    - .planning/phases/17-minecraft-competence/17-PATTERNS.md (procedural memory write-back section — reuse memoryLog.append; do NOT add a parallel store)
  </read_first>
  <action>
    In `refreshProgressionForSeed`, at the point a milestone is detected as newly completed (the existing `if (activeFrontierGoal && prog?.done?.has?.(activeFrontierGoal.id))` branch that sets justCompletedNote), ALSO write the node's procedure to per-world memory: look up the completed node (from `prog` / SPINE by id) and if it carries a non-empty `procedure` string, call `await memoryLog.append(procedureText, new Date())` then `memoryCompactor.maybeCompact().catch(...)` — exactly the remember-handler pattern (1854-1857). Dedupe so the same procedure is not appended twice in a world: before appending, check `memoryLog.readAll()` (or a latched in-memory Set keyed by node id for this session) and skip if already present. Keep it best-effort and non-fatal (wrap in try/catch, never break the seed path). This reuses the existing per-world MEMORY.md (segmented by noteWorld) — no new store. Add a unit test in orchestrator.test.js that injects a stub `memoryLog` (spy on `append`) and drives a milestone transition from not-done to done, asserting append is called once with the node's procedure string and is not called again for the same node id on a subsequent seed in the same session.
  </action>
  <verify>
    <automated>npx vitest run src/bot/brain/orchestrator.test.js 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - orchestrator.js calls memoryLog.append with the completed node's `procedure` inside the milestone-complete branch (grep + source assertion)
    - the write is deduped (readAll check or latched Set) so a procedure is not appended twice per world (source assertion)
    - the write is wrapped best-effort (try/catch) and triggers memoryCompactor.maybeCompact (source assertion)
    - a NEW orchestrator.test.js case stubs memoryLog.append and asserts (a) it is called with the completed node's procedure string when a milestone newly completes, and (b) it is NOT called a second time for the same node id within the same session (dedupe) — this is the direct unit proof of D-08, not just a grep
    - orchestrator.test.js still passes (no regression to the seed path)
  </acceptance_criteria>
  <done>Completing a milestone (e.g. iron) records its known-good procedure once into per-world memory, so future turns retrieve it instead of re-deriving — using only the existing memory store + compactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| progression.json (repo asset) → walker | Static, repo-controlled data — not untrusted, but a parse failure must not crash a snapshot tick |
| live bot inventory/dimension → readProgressionState | Server-controlled item counts feed the walker; already read defensively |
| procedure text → MEMORY.md → future prompt | Procedure strings are repo-authored constants (not world input), so no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Denial of Service | progression.json parse failure | mitigate | JSON load wrapped in try/catch → empty spine (mirrors readProgressionState's degrade-to-empty); the next: line is itself try/caught so a snapshot tick never throws — Task 1/2 acceptance |
| T-17-12 | Tampering | procedure text into prompt | accept | Procedure strings are repo-authored constants in progression.json, not world-derived — no untrusted content reaches the model via this path |
| T-17-13 | Denial of Service | unbounded memory growth from repeated write-back | mitigate | Dedupe (readAll / latched Set) prevents repeated appends; the existing byte-threshold compactor bounds MEMORY.md size — Task 3 acceptance |
| T-17-SC | Tampering | dependency installs | n/a | No new npm packages — uses readFileSync (node core) + existing memoryLog/compactor. No install tasks |

No `high`-severity threat: all inputs are either repo-controlled (progression.json, procedure strings) or already-defensive (readProgressionState). ASVS L1: defensive parsing + resource bounds (dedupe + compaction) satisfied.
</threat_model>

<verification>
- `npx vitest run src/bot/adapter/minecraft/observers/progression.test.js` green (connectivity preserved + nextMilestone cases)
- `npx vitest run src/bot/adapter/minecraft/observers/` green (snapshot next: line, no regression)
- `npx vitest run src/bot/brain/orchestrator.test.js` green (procedural write-back, no seed regression)
- grep gates: progression.json loaded via import.meta.url; nextMilestone exported; snapshot pushes next:; orchestrator appends procedure deduped
</verification>

<success_criteria>
The spine is static data with a pure nextMilestone walker, the snapshot always shows the nearest milestone + advancing action, and completing a milestone records a terse procedure once to per-world memory — no GOAP planner, no new store, all prior progression guarantees intact.
</success_criteria>

<output>
Create `.planning/phases/17-minecraft-competence/17-04-SUMMARY.md` when done. Record the nextMilestone return shape ({ node, action }) and the next: line format so Plan 05's disposition framing can reference it.
</output>
