---
phase: 17-minecraft-competence
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/bot/adapter/minecraft/connect.js
  - src/bot/adapter/minecraft/behaviors/follow.js
  - src/bot/adapter/minecraft/behaviors/pathfind.js
  - src/bot/adapter/minecraft/behaviors/mineVein.js
  - src/bot/adapter/minecraft/fsmWires.js
autonomous: true
requirements: [MCRAFT-02, MCRAFT-03]
must_haves:
  truths:
    - "startReflex is started beside startCombat on first spawn and re-armed on respawn"
    - "While bot._seiReflexActive is true, follow/goTo/gather do NOT (re)install their own pathfinder goal — they yield to the flee"
    - "A reflex engagement surfaces an in-character say()-style line naming the threat and offering attack()/explore(), without aborting any in-flight action"
  artifacts:
    - path: "src/bot/adapter/minecraft/connect.js"
      provides: "startReflex wired alongside startCombat"
      contains: "startReflex"
    - path: "src/bot/adapter/minecraft/fsmWires.js"
      provides: "sei:reflex → handlers translation"
      contains: "sei:reflex"
  key_links:
    - from: "src/bot/adapter/minecraft/behaviors/follow.js"
      to: "bot._seiReflexActive"
      via: "yield gate before setGoal"
      pattern: "_seiReflexActive"
    - from: "src/bot/adapter/minecraft/connect.js"
      to: "startReflex"
      via: "first-spawn + respawn wiring"
      pattern: "startReflex\\(bot, config\\)"
---

<objective>
Integrate the Plan 01 reflex controller into the live bot: wire `startReflex` beside `startCombat` at connect, make the goal-setting consumers (`follow`, `goTo`, `gather`) yield to the `bot._seiReflexActive` mutex so a creeper flee is not fought tick-for-tick, and translate the `sei:reflex` announcement event into the brain handler path (fsmWires) so the bot speaks the in-character threat line. The reflex evasion stays entirely outside fsm.js.

Purpose: makes MCRAFT-02 / MCRAFT-03 real in a running session (Plan 01 built the mechanism in isolation; this turns it on and prevents the active-action goal from stomping the flee).
Output: live reflex on spawn, yield-respecting movement, surfaced announcement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-minecraft-competence/17-CONTEXT.md
@.planning/phases/17-minecraft-competence/17-PATTERNS.md
@.planning/phases/17-minecraft-competence/17-01-SUMMARY.md
@.planning/research/v0.4-mc-reflex-dodge.md

<interfaces>
<!-- The Plan 01 contract this plan integrates against (confirm against 17-01-SUMMARY.md). -->
From src/bot/adapter/minecraft/behaviors/reflex.js (Plan 01):
- `export function startReflex(bot, config): () => void`  (returns dispose)
- mutex: `bot._seiReflexActive` (boolean, true while a creeper flee owns the goal); `bot._seiSavedGoal` holds the pre-empted goal
- announcement: `bot.emit('sei:reflex', { threat, threatLabel, noticed, count })`

From src/bot/adapter/minecraft/connect.js:
- line 17: `import { startFollow, stopFollow } from './behaviors/follow.js'`
- line 21: `import { startCombat } from './behaviors/combat.js'`
- lines 250-251 (first spawn): `startCombat(bot, config)` then `startFollow(bot, config)`
- line 255 (respawn branch): `startFollow(bot, config)`

From src/bot/adapter/minecraft/behaviors/follow.js (line 104): re-installs its GoalFollow only `if (!bot.pathfinder.isMoving())` — add the `_seiReflexActive` check here.

From src/bot/adapter/minecraft/fsmWires.js:
- `onSeiAttacked` (lines 53-69) wires `bot.on('sei:attacked')` → `handlers.onAttacked(...)` — the exact precedent for a `sei:reflex` wire; the dispose fn (lines 103-109) must also detach it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire startReflex at connect (spawn + respawn)</name>
  <files>src/bot/adapter/minecraft/connect.js</files>
  <read_first>
    - src/bot/adapter/minecraft/connect.js (lines 17-21 imports, 242-261 spawn/respawn block)
    - src/bot/adapter/minecraft/behaviors/reflex.js (startReflex signature — Plan 01)
    - .planning/phases/17-minecraft-competence/17-PATTERNS.md (reflex.js item 6 — wiring site)
  </read_first>
  <action>
    Import `startReflex` at connect.js:21 next to the combat import: `import { startReflex } from './behaviors/reflex.js'`. In the first-spawn block (after line 250 `startCombat(bot, config)`) add `startReflex(bot, config)`. In the respawn branch (line 255, beside `startFollow(bot, config)`) add `startReflex(bot, config)` as well so the loop is re-armed after death (Plan 01's disposer tears down on death, so re-arming on respawn is required). Capture the returned disposer if connect.js already tracks teardown handles; otherwise rely on the controller's self-teardown on death.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('/Users/ouen/slop/sei-studio/sei/src/bot/adapter/minecraft/connect.js').then(()=>console.log('ok'))"</automated>
  </verify>
  <acceptance_criteria>
    - connect.js imports startReflex from './behaviors/reflex.js' (grep)
    - startReflex(bot, config) appears in BOTH the first-spawn block and the respawn branch (grep -c "startReflex(bot, config)" == 2)
    - connect.js module imports without throwing (the node import check above prints ok)
  </acceptance_criteria>
  <done>The reflex loop starts on first spawn and re-arms on respawn, mirroring startCombat/startFollow.</done>
</task>

<task type="auto">
  <name>Task 2: Make follow / goTo / gather yield to the reflex mutex</name>
  <files>src/bot/adapter/minecraft/behaviors/follow.js, src/bot/adapter/minecraft/behaviors/pathfind.js, src/bot/adapter/minecraft/behaviors/mineVein.js</files>
  <read_first>
    - src/bot/adapter/minecraft/behaviors/follow.js (lines 96-125 — the 1s tick that re-installs GoalFollow at line 104)
    - src/bot/adapter/minecraft/behaviors/pathfind.js (the goTo goal-setting site — read the whole file)
    - src/bot/adapter/minecraft/behaviors/mineVein.js (the gather goal-setting / movement site — find every bot.pathfinder.setGoal)
    - .planning/research/v0.4-mc-reflex-dodge.md (§6 "One mutex for goal ownership" + the yield contract)
    - .planning/phases/17-minecraft-competence/17-PATTERNS.md (Shared Patterns → "Pathfinder goal yield / mutex")
  </read_first>
  <action>
    In each of the three files, guard every site that (re)installs a pathfinder goal so it yields while a flee owns the goal: before calling `bot.pathfinder.setGoal(...)`, check `if (bot._seiReflexActive) return` (or skip the re-install for that tick). In follow.js this goes at the line-104 re-install (`if (!bot.pathfinder.isMoving() && !bot._seiReflexActive)`). In pathfind.js (`goTo`) gate the initial setGoal and any retry so an in-flight goTo pauses rather than races the flee; when `_seiReflexActive` clears, normal flow resumes (the flee restores `bot._seiSavedGoal` which was goTo's/gather's goal). In mineVein.js (`gather`) gate each setGoal/re-path so the gather detours during a flee and resumes its vein afterward. Do NOT abort the action — yielding is non-destructive; the saved goal restore (Plan 01) brings it back.
  </action>
  <verify>
    <automated>npx vitest run src/bot/adapter/minecraft/behaviors/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - follow.js line-104 re-install is gated on `!bot._seiReflexActive` (source assertion)
    - every bot.pathfinder.setGoal in pathfind.js and mineVein.js is preceded by a `_seiReflexActive` yield check (grep: each file references `_seiReflexActive`)
    - no behavior test in behaviors/ regresses
    - the yield is non-destructive: no file calls abort/stop on the action when yielding (source assertion — gather/goTo are paused, not cancelled)
  </acceptance_criteria>
  <done>While a creeper flee owns the goal, follow/goTo/gather stand down and then resume their restored goal — gather() is not cancelled.</done>
</task>

<task type="auto">
  <name>Task 3: Surface the reflex announcement via fsmWires</name>
  <files>src/bot/adapter/minecraft/fsmWires.js</files>
  <read_first>
    - src/bot/adapter/minecraft/fsmWires.js (lines 51-69 onSeiAttacked, 103-109 dispose)
    - src/bot/adapter/minecraft/behaviors/reflex.js (the sei:reflex payload shape — Plan 01 / 17-01-SUMMARY.md)
    - .planning/research/v0.4-mc-reflex-dodge.md ("In-character announcement" spec — name threat + noticed + offer attack()/explore())
    - src/bot/adapter/minecraft/prompts.js (lines 124-167 ATTACKED_ADDENDUM / EVENT_GUIDANCE — how an attack event is framed; the reflex line is framed similarly in Plan 05)
  </read_first>
  <action>
    Add an `onSeiReflex` wire mirroring `onSeiAttacked` (lines 53-69): `bot.on('sei:reflex', payload => handlers.onAttacked?.({...}))` translating the Plan 01 payload into the brain handler shape. Reuse the existing `handlers.onAttacked` path (no new brain handler needed) so the announcement rides the established P1 chat-reaction route and surfaces a say()-style line; pass a flag/label distinguishing it from a real hit (e.g. `attackerKind: 'reflex'` plus the threat label) so Plan 05's prompt framing can phrase it as a proactive warning offering attack()/explore() rather than "you were hit". Detach `sei:reflex` in the dispose function (lines 103-109). Keep this a thin translation layer (no business logic) per the module's contract; the announcement must not enqueue evasion work.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('/Users/ouen/slop/sei-studio/sei/src/bot/adapter/minecraft/fsmWires.js').then(m=>console.log(typeof m.wireBotEvents))"</automated>
  </verify>
  <acceptance_criteria>
    - fsmWires.js subscribes bot.on('sei:reflex', ...) and routes it through handlers.onAttacked with a reflex-distinguishing label/kind (grep: 'sei:reflex')
    - the dispose() function detaches sei:reflex (grep: bot.off?.('sei:reflex'))
    - wireBotEvents still exports as a function (import check prints 'function')
  </acceptance_criteria>
  <done>A reflex engagement reaches the brain as an in-character announcement event without touching the evasion loop or the FSM enqueue path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reflex loop ↔ active action (goTo/gather) | Shared pathfinder goal ownership — the integration risk is an action being cancelled or a goal-thrash |
| bot event bus → brain handlers | sei:reflex payload crosses into the brain; must not carry executable content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Denial of Service | goal-thrash between flee and active action | mitigate | The `_seiReflexActive` yield (Task 2) makes consumers stand down rather than re-race; flee restores the exact saved goal (Plan 01), so gather/goTo resume — Task 2 acceptance asserts non-destructive yield (no abort) |
| T-17-05 | Tampering | sei:reflex payload into brain prompt | mitigate | Payload is a fixed-shape object (threat name from HOSTILE_MOBS, booleans, count) translated in the thin fsmWires layer; no raw world strings or coordinates flow to dispatch (D-03 closed registry preserved) |
| T-17-06 | Denial of Service | listener leak across respawn | mitigate | Re-arm on respawn pairs with Plan 01's death-teardown disposer; fsmWires dispose detaches sei:reflex — Task 1/3 acceptance assert the pairing |

No `high`-severity threat: integration touches goal ownership (mitigated by the yield contract) and a fixed-shape event (no injection surface). ASVS L1 satisfied — no untrusted free-text crosses to the model here (the announcement is templated in Plan 05).
</threat_model>

<verification>
- `npx vitest run src/bot/adapter/minecraft/behaviors/` green (no movement regression)
- connect.js and fsmWires.js import without throwing
- grep gates: startReflex wired twice in connect.js; `_seiReflexActive` referenced in follow/pathfind/mineVein; sei:reflex wired + disposed in fsmWires
- Real-session evasion behavior is validated end-to-end in Plan 06 (checkpoint)
</verification>

<success_criteria>
The reflex controller is live on spawn/respawn, active actions yield to a creeper flee without being cancelled, and a reflex engagement surfaces an in-character announcement — all while the evasion loop stays outside fsm.js.
</success_criteria>

<output>
Create `.planning/phases/17-minecraft-competence/17-02-SUMMARY.md` when done. Record the `attackerKind: 'reflex'` (or chosen label) contract so Plan 05's prompt framing matches it.
</output>
