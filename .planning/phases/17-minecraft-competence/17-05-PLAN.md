---
phase: 17-minecraft-competence
plan: 05
type: execute
wave: 2
depends_on: ["17-03", "17-04"]
files_modified:
  - src/bot/adapter/minecraft/prompts.js
autonomous: true
requirements: [MCRAFT-01, MCRAFT-04, MCRAFT-05, MCRAFT-06, MCRAFT-08]
must_haves:
  truths:
    - "The capability contract tells the bot it CAN smelt, read signs, open doors, and build shelters — the 'you can't smelt, ask the player' deferral is gone (D-09, prompts side)"
    - "Each new action (furnace ops, activateBlock, readSign, shelter) has an ACTION_DESCRIPTIONS entry"
    - "The bot is framed as able to solo to iron tier but choosing to involve the player; idle escalates passive → chores → invite-the-player; a genuine block is framed as an in-character invitation"
    - "On navigation failure / ambiguous terrain / player ask, the bot reaches for the existing look tool (vision-assisted nav)"
  artifacts:
    - path: "src/bot/adapter/minecraft/prompts.js"
      provides: "rewritten capability paragraph, new action descriptions, disposition + vision-on-failure framing"
      contains: "smelt"
  key_links:
    - from: "src/bot/adapter/minecraft/prompts.js"
      to: "registered action names (openFurnace/smeltInput/addFuel/takeSmelted/activateBlock/readSign/shelter)"
      via: "ACTION_DESCRIPTIONS entries matching registry names"
      pattern: "activateBlock"
---

<objective>
Rewrite the Minecraft capability contract and per-action descriptions to match the new primitives (Plan 03) and progression awareness (Plan 04), and encode the companion disposition (D-11/12/13) and vision-on-failure navigation (D-10) as prompt framing. This is the single owner of `prompts.js` for the phase — all prompt-text edits land here so the cached system block churns once. The bot must stop telling itself "you can't smelt, ask the player," must know it CAN now solo to iron yet chooses to involve the player, and must reach for `look` when navigation fails.

Purpose: makes the new code-level capabilities legible to the model and aligns its disposition with the north star ("can beat the game alone but chooses to play SMP with me").
Output: a rewritten CAPABILITY_PARAGRAPH, new ACTION_DESCRIPTIONS entries, disposition + vision framing in the relevant blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-minecraft-competence/17-CONTEXT.md
@.planning/phases/17-minecraft-competence/17-PATTERNS.md
@.planning/phases/17-minecraft-competence/17-03-SUMMARY.md
@.planning/phases/17-minecraft-competence/17-04-SUMMARY.md

<interfaces>
<!-- prompts.js is all text; these are the blocks + line refs to edit, and the action names to match. -->
From src/bot/adapter/minecraft/prompts.js:
- `CAPABILITY_PARAGRAPH` (lines 9-19) — line 10 is the load-bearing "You can't smelt in a furnace … ask the player … a smelted ingot is something you request, not something you make" sentence to DELETE/REWRITE
- `ACTION_DESCRIPTIONS` (lines 76-116) — add entries mirroring placeBlock (101-102) / attackEntity (86-87) / openContainer style
- `EVENT_GUIDANCE['sei:idle']` (lines 150-164) — already says "state it as one short invitation to the player"; strengthen the passive→chores→invite tiers + the "can solo but chooses to involve" framing
- `PATHFINDER_RULE_VISION` (line 50) / `SEEING_RULE_VISION` (line 46) — the on-failure look() guidance to strengthen for D-10
- `ATTACKED_ADDENDUM` / EVENT_GUIDANCE (124-167) — where the reflex announcement (Plan 02 `attackerKind: 'reflex'`) is framed

From 17-03-SUMMARY.md: the exact registered action NAMES + arg schemas to advertise.
From 17-04-SUMMARY.md: the `next:` line format the disposition framing references.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite capability contract + add new action descriptions</name>
  <files>src/bot/adapter/minecraft/prompts.js</files>
  <read_first>
    - src/bot/adapter/minecraft/prompts.js (CAPABILITY_PARAGRAPH 9-19, ACTION_DESCRIPTIONS 76-116)
    - .planning/phases/17-minecraft-competence/17-03-SUMMARY.md (registered action names + arg schemas)
    - .planning/phases/17-minecraft-competence/17-PATTERNS.md (prompts.js capability rewrite section — the exact sentence to delete, line 10)
  </read_first>
  <action>
    In CAPABILITY_PARAGRAPH (line 9-19): DELETE the "You can't smelt in a furnace … those aren't available to you yet … a smelted ingot is something you request, not something you make" deferral (line 10). Replace with a sentence affirming the new abilities: the bot CAN now smelt and cook in a furnace (load input + fuel, take the output), read signs, open and pass through doors and gates, and build a simple shelter. Keep the paragraph concise (it is a CACHED system block — anthropicClient.js index 2; per-turn churn is expensive). Leave the build/cuboid honesty sentence (line 18) intact.
    Add ACTION_DESCRIPTIONS entries (one short paragraph each, mirroring placeBlock 101-102 style) for EACH new action using the EXACT names from 17-03-SUMMARY.md: openFurnace, smeltInput, addFuel, takeSmelted (note the 3-slot workflow: open → load input + fuel → wait → takeSmelted), activateBlock (doors/gates/levers — pass the block target), readSign (returns the sign text), shelter (one call builds a hollow box + roof + doorway; size 3-5). Names MUST match the registry exactly or the descriptions attach to nothing.
  </action>
  <verify>
    <automated>grep -v '^//' src/bot/adapter/minecraft/prompts.js | grep -c "can't smelt"</automated>
  </verify>
  <acceptance_criteria>
    - the "can't smelt … ask the player" deferral is GONE (the grep above returns 0)
    - CAPABILITY_PARAGRAPH affirms smelt + sign + door + shelter abilities (grep: 'smelt', 'sign', 'door', 'shelter')
    - ACTION_DESCRIPTIONS has entries for openFurnace, smeltInput, addFuel, takeSmelted, activateBlock, readSign, shelter (grep each name)
    - the action-description names match the registry names from 17-03-SUMMARY.md exactly (cross-check)
  </acceptance_criteria>
  <done>The bot's prompt advertises the real new capabilities with correctly-named action descriptions and no stale "ask the player to smelt" deferral.</done>
</task>

<task type="auto">
  <name>Task 2: Disposition (solo-but-involve) + vision-on-failure framing</name>
  <files>src/bot/adapter/minecraft/prompts.js</files>
  <read_first>
    - src/bot/adapter/minecraft/prompts.js (EVENT_GUIDANCE sei:idle 150-164, ATTACKED_ADDENDUM 124-167, PATHFINDER_RULE_VISION 50, SEEING_RULE_VISION 46)
    - .planning/phases/17-minecraft-competence/17-CONTEXT.md (D-10, D-11, D-12, D-13 — the disposition + vision decisions)
    - .planning/phases/17-minecraft-competence/17-02-SUMMARY.md (the reflex announcement label — attackerKind 'reflex')
    - .planning/phases/17-minecraft-competence/17-04-SUMMARY.md (the next: line — disposition references it)
  </read_first>
  <action>
    DISPOSITION (D-11/12/13): in CAPABILITY_PARAGRAPH (or a concise addition near it) add the north-star framing: the bot is now CAPABLE of progressing to iron tier on its own, but its default disposition is to INVOLVE the player, not to run off and solo the world. In EVENT_GUIDANCE['sei:idle'] (150-164) sharpen the tiered behavior: passive = observe/comment; reactive = light chores (gather wood, food, cobble); agentic = when it would otherwise push the game forward alone, frame the next milestone (the snapshot `next:` line from Plan 04) as an INVITATION to the player ("I could go mine iron — want to come cave-diving?") rather than soloing it. On a genuine block (no iron, missing tool, stuck) the bot asks the player IN CHARACTER, turning the block into an invite (D-13) — not silent retry, not solo pivot.
    VISION-ON-FAILURE (D-10, MCRAFT-08): strengthen PATHFINDER_RULE_VISION / SEEING_RULE_VISION so that when navigation FAILS (goTo timeout/unreachable), terrain is ambiguous, or the player asks, the bot reaches for the existing `look(around)` tool to orient before continuing — this is on-demand/on-failure, NOT always-on (respect vision-token cost). Do not add any new tool; the Phase 15 look tool already exists and is vision-gated.
    REFLEX FRAMING: ensure the reflex announcement event (Plan 02 `attackerKind: 'reflex'`) is framed as a proactive in-character warning that names the threat, says whether it noticed the bot, and offers the player attack() or explore()-to-flee — distinct from the "you were hit" ATTACKED_ADDENDUM. Keep all additions concise (cached block).
  </action>
  <verify>
    <automated>grep -v '^//' src/bot/adapter/minecraft/prompts.js | grep -ci "invit"</automated>
  </verify>
  <acceptance_criteria>
    - capability/idle framing states the bot can solo to iron but chooses to involve the player (grep: 'iron', 'involve' or equivalent in the disposition sentence)
    - sei:idle guidance encodes passive → chores → invite tiers and frames the next milestone as an invitation (grep above returns ≥1 for 'invit')
    - PATHFINDER_RULE_VISION / SEEING_RULE_VISION direct the bot to look() on navigation failure / ambiguous terrain / player ask, framed as on-failure not always-on (source assertion)
    - reflex announcement framing offers attack()/explore() and is distinct from ATTACKED_ADDENDUM (source assertion)
    - the off-vision variants (SEEING_RULE_NOVISION, PATHFINDER_RULE_NOVISION) are NOT told to call look() (no regression — those still drop look guidance)
  </acceptance_criteria>
  <done>The prompt encodes the capable-but-involves-the-player disposition, the invite-on-block behavior, and on-failure vision use, with the reflex warning framed for player choice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt text → model | This plan edits the cached system prompt only; no runtime untrusted input is introduced here |
| (cross-ref) sign text → prompt | The actual untrusted-content surface (sign text) is bounded/sanitized in Plan 03 (T-17-07), not here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-14 | Information disclosure / drift | stale capability prompt | mitigate | Removing the "can't smelt" deferral and adding correctly-named descriptions keeps the model from mis-advertising abilities; action names cross-checked against 17-03-SUMMARY.md (Task 1 acceptance) |
| T-17-15 | Denial of Service | vision-token cost from always-on look | mitigate | Framing is explicitly on-demand/on-failure (Task 2), preserving the Phase 15 cost posture; no new tool, no always-on render |
| T-17-SC | Tampering | dependency installs | n/a | Prompt-text only — no code, no npm packages, no install tasks |

No `high`-severity threat in this plan: it is prompt text only. The untrusted-world-input surface lives in Plan 03 and is mitigated there. ASVS L1: N/A surfaces beyond ensuring no secret/coordinate is hardcoded into prompts (none added).
</threat_model>

<verification>
- `grep -v '^//' prompts.js | grep -c "can't smelt"` returns 0 (deferral removed)
- grep: all seven new action names present in ACTION_DESCRIPTIONS; 'invit' present in idle framing
- prompts.js parses (it is imported by the running bot — `node --input-type=module -e "import('.../prompts.js')"` succeeds)
- Behavioral effect (bot actually smelts/reads/invites) validated in Plan 06
</verification>

<success_criteria>
prompts.js advertises the new primitives with matching names, drops the smelt deferral, and encodes the solo-capable-but-involves-the-player disposition + on-failure vision use — all in the single cached prompt owner for the phase.
</success_criteria>

<output>
Create `.planning/phases/17-minecraft-competence/17-05-SUMMARY.md` when done.
</output>
