---
phase: 17-minecraft-competence
plan: 06
type: execute
wave: 3
depends_on: ["17-02", "17-03", "17-04", "17-05"]
files_modified: []
autonomous: false
requirements: [MCRAFT-07]
must_haves:
  truths:
    - "From a fresh LAN world the companion progresses the early/mid spine toward iron tier unaided (wood → tools → food → shelter → mine → smelt iron)"
    - "The companion proactively evades a creeper (does not get blown up) and dodges/strafes vs skeletons and melee mobs"
    - "The companion smelts in a furnace, reads a sign, opens a door, and builds a shelter — all observed live"
    - "When vision is enabled and navigation is ambiguous/failing, the companion uses look() to orient"
    - "The companion frames blocks/next-steps as invitations to the player rather than silently soloing"
  artifacts: []
  key_links: []
---

<objective>
Integrative end-to-end validation of the phase goal (MCRAFT-07 and the requirements it composes): a real summoned companion in a fresh LAN world demonstrates unaided progression toward iron tier, proactive threat reflexes, the four new primitives, vision-on-failure navigation, and the involve-the-player disposition. This is a human-verify checkpoint — all automation in Plans 01-05 has shipped; this confirms the integrated behavior in a live world.

Purpose: the phase's success criteria are behavioral and only provable in a running game; unit tests cannot show "the bot reached iron tier unaided" or "did not get blown up by a creeper."
Output: a recorded pass/fail against each observable truth, with any gaps surfaced for `/gsd:plan-phase --gaps`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-minecraft-competence/17-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-minecraft-competence/17-01-SUMMARY.md
@.planning/phases/17-minecraft-competence/17-02-SUMMARY.md
@.planning/phases/17-minecraft-competence/17-03-SUMMARY.md
@.planning/phases/17-minecraft-competence/17-04-SUMMARY.md
@.planning/phases/17-minecraft-competence/17-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight automated gate (full suite green)</name>
  <files></files>
  <read_first>
    - .planning/phases/17-minecraft-competence/17-01-SUMMARY.md through 17-05-SUMMARY.md (what each plan shipped + how to exercise it)
  </read_first>
  <action>
    Before the live session, confirm the whole bot test surface is green so the human checkpoint is not chasing a regression that a test already catches. Run the adapter + brain test suites touched this phase: reflex, combat, furnace, interact, shelter, progression, snapshot/observers, and orchestrator. Resolve any failure before proceeding to the live world (a red here blocks the checkpoint).
  </action>
  <verify>
    <automated>npx vitest run src/bot/adapter/minecraft/ src/bot/brain/orchestrator.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - the adapter test directory and orchestrator.test.js pass with no failures
    - reflex.test.js, furnace.test.js, interact.test.js, shelter.test.js, progression.test.js are all present and green
  </acceptance_criteria>
  <done>The automated surface for the phase is green; the live checkpoint will only test what tests cannot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 17 competence stack: the reflex evasion controller (arrow dodge / creeper flee / melee strafe with in-character announcement), the four new primitives (furnace smelting, door/gate activation, sign reading, shelter building), progression-as-data with the advisory next: line + procedural memory, the rewritten capability prompt, and on-failure vision navigation.
  </what-built>
  <how-to-verify>
    Start a fresh LAN (offline-mode) Minecraft Java world and summon the companion (or run the standalone `sei` CLI against the world). Verify each observable truth:
    1. PROGRESSION (MCRAFT-07): leave the companion to play with minimal direction. Over a session it should progress the spine unaided — gather wood → craft tools → get food → build/seek shelter → mine → smelt iron in a furnace. Confirm it reaches at least iron tooling without you doing the work. Watch the snapshot's `next:` line track the milestone.
    2. REFLEX (MCRAFT-02/03): spawn/lure a creeper near it — it should flee before detonation and NOT get blown up, and emit an in-character warning offering attack()/explore(). Spawn a skeleton — it should sidestep arrows. Spawn a single zombie — it should strafe/reposition rather than stand still. Confirm an in-flight gather() is NOT cancelled by a passing creeper flee (it resumes).
    3. PRIMITIVES: confirm it smelts in a furnace unaided (MCRAFT-01), reads a sign you place and references the text (MCRAFT-04), opens and walks through a door/gate (MCRAFT-05), and builds a small enclosed shelter with a doorway at an intended spot (MCRAFT-06).
    4. VISION (MCRAFT-08): with vision enabled, give it a navigation task where coordinates are ambiguous or goTo fails — confirm it calls look() to orient rather than spinning.
    5. DISPOSITION (D-11/12/13): confirm that when it could solo the next step it instead INVITES you, and that a genuine block (no iron / missing tool / stuck) is voiced as an in-character invitation, not silent retry.
    Note any truth that fails or is flaky.
  </how-to-verify>
  <acceptance_criteria>
    - All five observable-truth groups pass in a live world, OR any failures are written up as concrete gaps (which truth, what happened) for gap closure
    - Specifically: the companion is NOT killed by a creeper (the headline MCRAFT-02 failure this phase fixes), and it reaches iron tier without the human performing the steps
  </acceptance_criteria>
  <resume-signal>Type "approved" if all truths hold, or list the failing truths (these become the gap-closure backlog for `/gsd:plan-phase --gaps`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live world → bot | This is the integration test of every prior boundary: untrusted entity metadata (reflex), sign text (readSign), furnace contents — all exercised live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-16 | (validation) | integrated behavior | mitigate | This checkpoint is itself the residual-risk control: it confirms the per-plan mitigations (reflex teardown T-17-01/02, sign sanitize T-17-07, session timeouts T-17-08) hold under real world state. A creeper-kill or a hung reflex loop observed here is a blocking failure, not an accepted risk |
| T-17-SC | Tampering | dependency installs | n/a | No installs — validation only |

No new threat surface introduced; this plan verifies the mitigations of Plans 01-05 under live, untrusted world state. ASVS L1: end-to-end confirmation of the input-bounding + resource-teardown controls.
</threat_model>

<verification>
- Task 1 automated gate green (full phase test surface)
- Human checkpoint records pass/fail per observable truth; the headline checks (no creeper death, unaided iron tier) are explicit
</verification>

<success_criteria>
A live companion demonstrates unaided progression toward iron tier, proactive threat survival (notably surviving creepers), the four new primitives, on-failure vision navigation, and the involve-the-player disposition — or the gaps are captured for closure.
</success_criteria>

<output>
Create `.planning/phases/17-minecraft-competence/17-06-SUMMARY.md` when done. Record the pass/fail of each observable truth and any gaps (feed `/gsd:plan-phase --gaps` if MCRAFT-07 reliability is shaky — the deferred self-verifying-action-returns item is the named fast-follow if so).
</output>
