# Plan 14-01: LlmProvider abstraction + adapters for 11 providers

**Goal:** Replace the orchestrator's hardcoded Anthropic call site with a provider-agnostic `LlmProvider` interface and ship 11 provider adapters via 4 implementations (Anthropic native, OpenAI-compatible shared, Gemini native, Ollama native). Existing Anthropic + cloud-proxy behavior preserved verbatim.

## Files

### New

- `src/bot/brain/llm/index.js` — `createLlmProvider(config, deps?)` factory.
- `src/bot/brain/llm/anthropicProvider.js` — wraps existing `anthropicClient.js`.
- `src/bot/brain/llm/openaiCompatProvider.js` — OpenAI, Grok, OpenRouter, DeepSeek, Mistral, Together, Groq, Fireworks (8 via shared baseURL routing).
- `src/bot/brain/llm/geminiProvider.js` — Gemini REST.
- `src/bot/brain/llm/ollamaProvider.js` — local `/api/chat`.
- `src/bot/brain/llm/messageMappers.js` — anthropic ↔ openai ↔ gemini block translation.
- `src/bot/brain/llm/index.test.js` — factory dispatch unit test.
- `src/bot/brain/llm/messageMappers.test.js` — round-trip unit tests for both mappers.

### Modified

- `src/bot/brain/orchestrator.js` — `createAnthropicClient` → `createLlmProvider`. Test seam preserved.
- `src/bot/brain/memory/compactor.js` — takes generic provider (same `call(...)` shape).
- `src/bot/config.js` — extend `llm` schema with `provider` + `providers.<name>` blocks. Default `anthropic`.

## Implementation Sketch

```js
// llm/index.js
import { createAnthropicProvider } from './anthropicProvider.js'
import { createOpenAICompatProvider } from './openaiCompatProvider.js'
import { createGeminiProvider } from './geminiProvider.js'
import { createOllamaProvider } from './ollamaProvider.js'

const OPENAI_COMPAT = {
  openai:     { baseURL: 'https://api.openai.com/v1' },
  grok:       { baseURL: 'https://api.x.ai/v1' },
  openrouter: { baseURL: 'https://openrouter.ai/api/v1' },
  deepseek:   { baseURL: 'https://api.deepseek.com/v1' },
  mistral:    { baseURL: 'https://api.mistral.ai/v1' },
  together:   { baseURL: 'https://api.together.xyz/v1' },
  groq:       { baseURL: 'https://api.groq.com/openai/v1' },
  fireworks:  { baseURL: 'https://api.fireworks.ai/inference/v1' },
}

export function createLlmProvider(config, deps = {}) {
  const kind = config.llm?.provider ?? 'anthropic'
  if (kind === 'anthropic') return createAnthropicProvider(config)
  if (kind === 'gemini')    return createGeminiProvider(config, deps)
  if (kind === 'ollama')    return createOllamaProvider(config, deps)
  if (OPENAI_COMPAT[kind]) {
    return createOpenAICompatProvider(config, { ...deps, kind, defaultBaseURL: OPENAI_COMPAT[kind].baseURL })
  }
  throw new Error(`Unknown llm.provider: ${kind}`)
}
```

## Capabilities Matrix (static)

| Provider    | vision | cached | local |
|-------------|--------|--------|-------|
| anthropic   | ✓      | ✓      |       |
| openai      | ✓      | ✓      |       |
| gemini      | ✓      | ✓      |       |
| grok        | ✓      |        |       |
| openrouter  | ✓      | ✓      |       |
| deepseek    |        |        |       |
| mistral     | ✓      |        |       |
| together    | ✓      |        |       |
| groq        |        |        |       |
| fireworks   | ✓      |        |       |
| ollama      |        |        | ✓     |

## Verification

1. `npx vitest run src/bot/brain/llm/` — new tests pass.
2. Existing orchestrator + brain tests still pass (interface preserved).
3. Default config (no `llm.provider`) → factory returns Anthropic adapter; existing behavior unchanged.
4. `grep -c "from '../anthropicClient.js'" src/bot/brain/orchestrator.js` == 0 (orchestrator no longer imports directly).
5. Adapter dispatch: `createLlmProvider({ llm: { provider: 'openai', providers: { openai: { api_key: 'x', model: 'gpt-4o-mini' } } } })` returns an object exposing `call`, `buildCachedSystem`, `model`, `setAuthToken`, `capabilities`.

## Done When

- All 11 providers selectable via `config.llm.provider`.
- Orchestrator + compactor use the factory.
- Existing tests pass.
- `npx tsc --noEmit` clean.
