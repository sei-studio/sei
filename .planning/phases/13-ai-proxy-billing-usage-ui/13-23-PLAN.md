---
phase: 13
plan: 23
type: execute
wave: 5
depends_on: [13-01, 13-07, 13-10, 13-11, 13-12, 13-13, 13-15, 13-22]
files_modified:
  - .planning/phases/13-ai-proxy-billing-usage-ui/13-23-SUMMARY.md
autonomous: false
requirements: [PROXY-01, PROXY-02, PROXY-03, PROXY-07, PROXY-09, PROXY-12, PROXY-13]
requirements_addressed: [PROXY-01, PROXY-02, PROXY-03, PROXY-07, PROXY-09, PROXY-12, PROXY-13]
tags: [operator-runbook, fly-deploy, supabase-migrate, lemon-squeezy, sentry, anthropic-tos, checkpoint
]
user_setup:
  - service: lemon-squeezy
    why: "Merchant of Record for $5 pack + $20/mo subscription"
    env_vars:
      - name: LEMON_VARIANT_PACK
        source: "Lemon Squeezy Dashboard → Products → '$5 Pack' → Variant ID"
      - name: LEMON_VARIANT_SUBSCRIPTION
        source: "Lemon Squeezy Dashboard → Products → '$20/mo' → Variant ID"
      - name: LEMON_SQUEEZY_WEBHOOK_SECRET
        source: "Lemon Squeezy Dashboard → Settings → Webhooks → Generate signing secret"
    account_setup:
      - "Create LS store (sei.lemonsqueezy.com or chosen subdomain)"
      - "Create '$5 Pack' product (one-time, single variant; description framing: 'proxied AI inference credits powered by Sei')"
      - "Create '$20/mo Unlimited' product (subscription, single variant)"
      - "Configure tax/VAT handling per LS MoR (LS handles automatically)"
    dashboard_config:
      - task: "Add webhook URL"
        location: "Lemon Squeezy Dashboard → Settings → Webhooks → Add: https://<supabase-project>.supabase.co/functions/v1/lemon-webhook"
        selects: "order_created, subscription_created, subscription_updated, subscription_payment_success, subscription_cancelled, subscription_expired"
  - service: fly.io
    why: "Hono proxy hosting"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic console — org-level API key (NOT a personal key)"
      - name: SUPABASE_URL
        source: "Supabase project settings → API → Project URL"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase project settings → API → service_role secret"
      - name: SUPABASE_JWKS_URL
        source: "https://<project>.supabase.co/auth/v1/.well-known/jwks.json"
    account_setup:
      - "Install flyctl: `brew install flyctl` (macOS) or per https://fly.io/docs/hands-on/install-flyctl"
      - "Run `fly auth login` and select/create the Fly organization"
      - "Provision Anthropic Tier 2 by depositing $40 + waiting 14 days (PER PATTERNS Pitfall 5 — must happen BEFORE first paying user to avoid 429 storms)"
  - service: supabase
    why: "Hosting Edge Functions + Postgres"
    env_vars:
      - name: DISCORD_BILLING_ALERT_WEBHOOK_URL
        source: "Discord server → channel → Edit Channel → Integrations → Webhooks → Create (alert channel for failed-to-attribute payments)"
    account_setup:
      - "Supabase CLI: `brew install supabase/tap/supabase` OR use Supabase MCP"
      - "supabase login + supabase link"
must_haves:
  truths:
    - "Operator runbook is a SUMMARY-only deliverable (no code) — the 11-step rollout sequence to take Phase 13 from code-complete to live"
    - "Anthropic ToS framing check is enforced: LS product description says 'proxied AI inference credits powered by Sei' — NEVER 'Anthropic API access' or 'Claude access' (D-49 + PROXY-13)"
    - "Anthropic Tier 2 is verified BEFORE first paying user (PATTERNS Pitfall 5 — pre-launch $40 deposit + 14d wait)"
    - "All four launch-gate proxy controls (JWT auth, pre-deduction, RPM, TPM, daily-$) smoke-tested in production with a test user"
    - "Sentry / equivalent alert configured for sustained Anthropic 429s — triggers Tier-3 conversation"
    - "Checkpoint:human-verify gates the final config flip"
  artifacts:
    - path: ".planning/phases/13-ai-proxy-billing-usage-ui/13-23-SUMMARY.md"
      provides: "11-step operator runbook"
---

<objective>
Ship the operator runbook for Phase 13 production rollout. This plan is SUMMARY-only (mirrors Phase 12-18) — DMCA agent eForm, Lemon Squeezy product creation, Fly.io secret provisioning, Anthropic Tier 2 verification, Edge Function deploy, Database migration apply, and the final config flip live on operator infrastructure, not in the git repo.

Purpose: PROXY-12 (refund policy live) + PROXY-13 (Anthropic ToS framing checked) + the end-to-end launch gate.
Output: 13-23-SUMMARY.md with the strictly-ordered checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ai-proxy-billing-usage-ui/13-CONTEXT.md
@.planning/phases/13-ai-proxy-billing-usage-ui/13-RESEARCH.md
@.planning/phases/12-character-sharing-ui-moderation/12-18-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Step 1 — Lemon Squeezy product setup [OPERATOR ACTION REQUIRED]</name>
  <what-built>Phase 13 code is complete; LS products do not yet exist.</what-built>
  <how-to-verify>
    1. Sign in to lemonsqueezy.com.
    2. Create a Store (e.g., sei).
    3. Create Product "Sei Credits — $5 Pack":
       - Type: one-time
       - Price: $5.00
       - Description: "Proxied AI inference credits powered by Sei. Use Sei to chat with your bot. Unused credits are refundable within 14 days. See https://sei.gg/terms.html#refunds for refund policy."
       - **DO NOT include phrases like "Anthropic API access", "Claude access", or "buy API credits"** (D-49 / PROXY-13)
    4. Create Product "Sei Unlimited — $20/month":
       - Type: subscription, recurring monthly
       - Price: $20.00
       - Description: "Monthly subscription for proxied AI inference, powered by Sei. Cancel anytime via the customer portal. See https://sei.gg/terms.html#refunds for cancellation policy."
    5. Note the Variant IDs for both products.
    6. Discord screenshot of both product pages to the operator's notes — pre-launch evidence for PROXY-13 audit.
  </how-to-verify>
  <resume-signal>Type "approved — variant IDs captured: pack=&lt;ID&gt;, sub=&lt;ID&gt;"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Step 2 — Anthropic Tier 2 verification [OPERATOR ACTION REQUIRED]</name>
  <what-built>Phase 13 caps (D-51) assume Tier 2. Without it, the very first subscribers exceed Anthropic's Tier 1 RPM (50) per PATTERNS Pitfall 5.</what-built>
  <how-to-verify>
    1. Sign in to platform.claude.com → Plans &amp; Billing.
    2. Confirm "Tier 2" is shown on the org account.
    3. If on Tier 1, deposit $40 cumulative + wait 14 days (per Anthropic's auto-advancement rule). Re-verify before continuing.
    4. Confirm rate limits: 1,000 RPM / 450K input TPM (cache-reads excluded) / 90K output TPM on claude-haiku-4-5.
  </how-to-verify>
  <resume-signal>Type "approved — Tier 2 verified" once the dashboard confirms.</resume-signal>
</task>

<task type="auto">
  <name>Step 3 — Apply Phase 13 database migrations [OPERATOR ACTION REQUIRED]</name>
  <files>supabase/migrations/20260524000000_phase_13_ledger.sql, supabase/migrations/20260524000100_rate_buckets_rpc.sql</files>
  <action>
Run:
```bash
supabase db push --linked
```
Verify in Supabase Dashboard → Database → Tables that the following exist:
- ledger_grants, ledger_consumption, subscription_status, rate_buckets, trial_claims, user_balance_lock
- VIEW ledger_balance
- FUNCTION reserve_credits, settle_consumption, check_and_increment_bucket
- CRON job rate_buckets_cleanup (Dashboard → Database → Cron Jobs)

Run a smoke INSERT into rate_buckets via SQL editor to confirm RLS doesn't block service_role.
  </action>
  <verify>
    <automated>true # manual operator verification — no automated probe</automated>
  </verify>
  <done>Migrations applied. All Phase 13 tables visible.</done>
</task>

<task type="auto">
  <name>Step 4 — Deploy Edge Functions [OPERATOR ACTION REQUIRED]</name>
  <files>supabase/functions/lemon-webhook/index.ts, supabase/functions/trial-claim/index.ts</files>
  <action>
```bash
supabase secrets set LEMON_SQUEEZY_WEBHOOK_SECRET=<from Step 1> --linked
supabase secrets set DISCORD_BILLING_ALERT_WEBHOOK_URL=<discord webhook URL> --linked
supabase functions deploy lemon-webhook --linked
supabase functions deploy trial-claim --linked
```

Smoke:
```bash
# trial-claim: should return 401 missing_jwt without Authorization header
curl -X POST https://<project>.supabase.co/functions/v1/trial-claim
# lemon-webhook: should return 401 missing_signature without X-Signature
curl -X POST https://<project>.supabase.co/functions/v1/lemon-webhook -d '{}'
```
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Both Edge Functions deployed; CORS preflight + 401 envelope verified via curl.</done>
</task>

<task type="auto">
  <name>Step 5 — Configure Lemon Squeezy webhook → Edge Function</name>
  <action>
1. LS Dashboard → Settings → Webhooks → Add Webhook.
2. URL: `https://<supabase-project>.supabase.co/functions/v1/lemon-webhook`.
3. Select events: order_created, subscription_created, subscription_updated, subscription_payment_success, subscription_cancelled, subscription_expired.
4. Use the LEMON_SQUEEZY_WEBHOOK_SECRET from Step 1 (LS lets you reveal/copy it).
5. Click "Send test event" → confirm 200 OK in LS webhook log.
6. In Supabase → Edge Functions → lemon-webhook → Logs, confirm the event was received and the missing_user_id Discord alert fired (test event has no custom_data).
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Test webhook delivered; Discord alert received; logs show signature verification passed.</done>
</task>

<task type="auto">
  <name>Step 6 — Deploy Fly.io proxy</name>
  <files>proxy/</files>
  <action>
```bash
cd proxy
fly auth login
fly launch --copy-config --name sei-proxy --region iad --no-deploy

# Set secrets (NEVER commit these to repo):
fly secrets set ANTHROPIC_API_KEY=sk-ant-... -a sei-proxy
fly secrets set SUPABASE_URL=https://<project>.supabase.co -a sei-proxy
fly secrets set SUPABASE_SERVICE_ROLE_KEY=... -a sei-proxy
fly secrets set SUPABASE_JWKS_URL=https://<project>.supabase.co/auth/v1/.well-known/jwks.json -a sei-proxy
# Optional: fly secrets set SENTRY_DSN=https://... -a sei-proxy

fly deploy -a sei-proxy

# Smoke:
curl https://api.sei.gg/health
# → 200 { "status": "ok", "version": "1.0.0" }
```

If using a custom domain (e.g., proxy.sei.gg):
```bash
fly certs create proxy.sei.gg -a sei-proxy
# Add the CNAME per Fly's instructions
```
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Proxy deployed; /health returns 200; secrets are NOT in any git-tracked file; cold-start verified (~3-5s first request after idle).</done>
</task>

<task type="auto">
  <name>Step 7 — Configure SEI_PROXY_URL build env</name>
  <action>
In Sei's main repo, update `electron-vite.config.ts` or the equivalent `define` block to inject:
```
SEI_PROXY_URL=https://api.sei.gg   (or https://proxy.sei.gg if custom domain set)
LEMON_VARIANT_PACK=<from Step 1>
LEMON_VARIANT_SUBSCRIPTION=<from Step 1>
```

Rebuild Sei: `npm run build`.
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Production Sei build has proxy URL + Lemon variants baked in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Step 8 — End-to-end smoke test [OPERATOR ACTION REQUIRED]</name>
  <what-built>All infrastructure live; need to verify a real signed-in user can purchase + use credits.</what-built>
  <how-to-verify>
    Using a test Sei account (test@sei.gg or similar):
    1. Sign in via Sei. Settings → Cloud AI → "Switch to managed billing" (aiBackendKind=cloud-proxy).
    2. Open the pricing icon. CreditsScreen shows USAGE bar at 0%, plan='Depleted'.
    3. Click "Top up" → LS checkout opens. Complete with test card (4242 4242 4242 4242).
    4. Within ~10s of completing checkout, return to Sei. CreditsScreen should update: %~95 (4.75M of 5M daily cap) and plan='Pack'.
    5. Pricing icon in left rail now shows wallet partially filled.
    6. Summon a bot in a LAN world. First message should:
       a. Trigger trial-claim if not previously claimed → grant $1 more.
       b. Forward through the proxy → return a normal Anthropic response.
       c. Decrement remaining_pct visibly.
    7. Continue chatting until balance ~0 → HardStopModal appears with persona-aware copy.
    8. Click "Switch to your own API key" in the modal. Modal closes; left rail loses the pricing icon; bot reverts to BYOK path.
    9. Switch back to managed billing. Make another purchase to verify subscription flow with $20/mo product.
    10. After subscribing, CreditsScreen shows "Manage subscription" button → opens LS customer portal in browser.
    11. Cancel subscription via LS portal. Within ~10s the subscription_status row should flip to 'cancelled'; CreditsScreen helper text updates.

    Anti-abuse smoke:
    12. Confirm trial gate: try to claim same MC username from a second Sei account → 409 already_claimed.
    13. Confirm rate limit: spam 25 short messages in 60s → 21st onwards return rate_limited with banner showing retry_after countdown.
  </how-to-verify>
  <resume-signal>Type "approved — all 13 smoke steps passed" once verified.</resume-signal>
</task>

<task type="auto">
  <name>Step 9 — Sentry / alert configuration [OPERATOR ACTION REQUIRED]</name>
  <action>
1. Set up alerting on the Fly proxy logs for:
   - Sustained `service_at_capacity` responses (Anthropic 429s) — indicates need for Tier 3.
   - Error code 401 invalid_jwt over 50/min — possible JWT signing issue.
   - Error code 500 internal_error over 10/min — proxy bug.
2. Set up alerting on Supabase logs for:
   - lemon-webhook missing_user_id events (manual reconciliation queue).
   - trial-claim grant_failed events (atomicity gap; needs manual cleanup).
3. Add a simple monthly cron-equivalent reminder to verify Anthropic tier status manually (no API for this; dashboard-only).
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Alerts configured. Operator has runbook for each alert type.</done>
</task>

<task type="auto">
  <name>Step 10 — PROXY-13 Anthropic ToS pre-launch sign-off [OPERATOR ACTION REQUIRED]</name>
  <action>
Per D-49: the operator confirms in writing (paper checklist or Notion page) that:
1. Lemon Squeezy product descriptions use "proxied AI inference credits powered by Sei" framing — verified manually against the LS dashboard.
2. Sei marketing copy on the LS product page mentions "powered by Anthropic Claude" as attribution.
3. No part of Sei's promotional or contractual surface advertises "Anthropic API access" or "buy Claude credits".
4. Sei's terms.html §8 Refunds (deployed in 13-22) is reachable from the LS product page (link in description).
5. Anthropic API key in Fly secrets is org-owned (not a personal account key) — protects against personal-account suspension cascading into a billing outage.
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Operator signs off paper checklist. Date recorded in Notion / operator log.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Step 11 — Final config flip + production-live announcement</name>
  <what-built>All infrastructure validated; one operator gate remains before public availability.</what-built>
  <how-to-verify>
    1. Verify Phase 12-18 BROWSE_ENABLED flip is already live (if not, decide whether to ship Phase 13 alongside or separately — Phase 13 doesn't gate on Browse).
    2. Tag Sei release: `v1.0.0-billing` (or whatever versioning convention is in use).
    3. Build production installers (macOS .dmg, Windows .exe, Linux .AppImage) via electron-builder.
    4. Smoke-test the installer on a clean macOS VM (rebuild stuff per CLAUDE.md native-ABI invariant).
    5. Publish the installer to the release channel (sei.gg/download or wherever).
    6. Announce to users via existing channels (Discord, mailing list).
  </how-to-verify>
  <resume-signal>Type "approved — Phase 13 LIVE on &lt;DATE&gt;" once the installer is downloadable + at least one external user has successfully purchased.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (human) ↔ secret management | Fly + Supabase + LS secrets never enter git |
| Operator → Anthropic ToS framing | Manual review; legal/compliance audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-23-01 | Information Disclosure | Secrets committed to repo | mitigate | .gitignore covers proxy/.env; checklist explicitly says secrets via `fly secrets set` / `supabase secrets set` |
| T-13-23-02 | Repudiation | LS product description argued to be "Anthropic resale" | mitigate | Step 10 sign-off + screenshot evidence; framing audited pre-launch |
| T-13-23-03 | Tampering | Wrong Lemon variant ID baked into build → checkout fails | mitigate | Step 8 smoke step 3 catches this end-to-end |
| T-13-23-04 | DoS | Anthropic Tier 1 launch causes 429 storm | mitigate | Step 2 verifies Tier 2 BEFORE first paying user |
| T-13-23-05 | Information Disclosure | Anthropic personal API key used in Fly | mitigate | Step 10 verifies org-owned key |
| T-13-23-06 | Repudiation | User claims they didn't see refund policy | mitigate | 13-22 already enforces AcceptToSModal re-cycle on TOS_VERSION bump |
| T-13-23-07 | Tampering | Forgotten Sentry alert delays Tier-3 push | mitigate | Step 9 explicitly enumerates alert categories |
</threat_model>

<verification>
- All checkpoints approved by operator.
- Phase 13 13-23-SUMMARY.md exists documenting the 11-step rollout.
- At least one external user has successfully purchased and used credits.
- D-49 Anthropic ToS framing audit is signed-off + dated.
</verification>

<success_criteria>
- All 11 steps completed in order.
- Anthropic Tier 2 verified BEFORE first paying user.
- LS framing audit complete (PROXY-13).
- Sentry / alerts configured.
- Phase 13 LIVE; users can purchase + consume credits + cancel subscriptions end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-proxy-billing-usage-ui/13-23-SUMMARY.md` with:
- Date Phase 13 went live
- Variant IDs (redacted in commit but noted operator-side)
- Any deviations from this runbook
- Initial production observations (cold-start latency, first-day error rate, etc.)
- Open follow-up items (Sentry tuning, Tier 3 timing, etc.)
</output>
