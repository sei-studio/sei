---
phase: 15-in-game-vision-via-prismarine-viewer
plan: 02
type: execute
wave: 1
depends_on: []
# REPO SPLIT (audited 2026-06-10): this plan's code lives in the SEPARATE proxy
# repo. Working directory / git repo for ALL edits, verify commands, and
# commits in this plan: /Users/ouen/slop/sei-studio/sei-proxy
# (the plan file itself lives in the client repo's .planning — only the
# 15-02-SUMMARY.md output goes there). All paths below are relative to the
# sei-proxy repo root.
working_repo: /Users/ouen/slop/sei-studio/sei-proxy
files_modified:
  - supabase/migrations/20260610120000_vision_hourly_bucket.sql
  - src/rateLimit/buckets.ts
  - src/rateLimit/visionHourlyGate.ts
  - src/rateLimit/visionHourlyGate.test.ts
  - src/app.ts
autonomous: true
requirements: [VIS-07]
must_haves:
  truths:
    - "Cloud-AI users hitting the explicit-vision proxy path are capped to ~10 renders/hour, server-authoritatively (the bot cannot bypass it)"
    - "Exceeding the cap returns a clear rate_limited error with Retry-After, not a silent failure"
    - "Idle renders do NOT route through this gate (only the explicit /vision path is capped) — D-09"
    - "The vision_hourly bucket kind is accepted by the rate_buckets table CHECK constraint after the migration is pushed"
  artifacts:
    - path: "supabase/migrations/20260610120000_vision_hourly_bucket.sql"
      provides: "Extends rate_buckets_bucket_kind_check to allow 'vision_hourly'"
      contains: "vision_hourly"
    - path: "src/rateLimit/visionHourlyGate.ts"
      provides: "Hono middleware enforcing the per-hour vision cap"
      exports: ["visionHourlyGate", "VISION_HOURLY_LIMIT"]
    - path: "src/rateLimit/buckets.ts"
      provides: "vision_hourly added to BucketKind union"
      contains: "vision_hourly"
    - path: "src/app.ts"
      provides: "POST /vision/v1/messages route with the vision gate in the chain"
      contains: "/vision/v1/messages"
  key_links:
    - from: "src/rateLimit/visionHourlyGate.ts"
      to: "checkAndIncrementBucket"
      via: "vision_hourly bucket, 3600s window, limit 10"
      pattern: "checkAndIncrementBucket"
    - from: "src/app.ts"
      to: "visionHourlyGate"
      via: "middleware chain originLockGate -> ipRateLimitGate -> verifyJwt -> visionHourlyGate -> forward"
      pattern: "visionHourlyGate"
---

<objective>
REPO NOTE (post-split, 2026-06-10): every code edit, test, tsc check, and `supabase db push` in this plan happens in /Users/ouen/slop/sei-studio/sei-proxy (its own git repo). The pre-split `proxy/` and monorepo `supabase/` prefixes are gone — `src/...` and `supabase/migrations/...` below are relative to the sei-proxy repo root. Only the SUMMARY output file is written to the client repo's .planning directory.

Add the server-authoritative per-hour vision cap (VIS-07 / D-09) to the Fly.io proxy. Explicit `visualize`-triggered LLM turns are LLM-driven and therefore UNBOUNDED (the model could loop `visualize`), so the cap is the hard ceiling independent of credit balance. Idle renders are already bounded by the idle-tick cadence and MUST NOT route through this gate.

Purpose: This plan is fully independent of the render path (the proxy is a separate subsystem on Fly.io) — it runs in Wave 1 in parallel with the de-risk spike. It establishes the `/vision/v1/messages` path the bot routes explicit-vision turns to (the bot-side routing is wired in 15-06).

Output: a `vision_hourly` bucket kind (TS union + a migration extending the `rate_buckets` CHECK constraint), a `visionHourlyGate` middleware mirroring `personaDailyGate`, and a `POST /vision/v1/messages` route chained `originLockGate -> ipRateLimitGate -> verifyJwt -> visionHourlyGate -> forwardToAnthropic`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-PATTERNS.md

<interfaces>
<!-- VERIFIED by reading the codebase during planning; RE-VERIFIED against
     /Users/ouen/slop/sei-studio/sei-proxy on 2026-06-10 (post-split audit). -->

src/rateLimit/buckets.ts:22-29 — the union to extend (NOTE: 'sightengine_daily'
  and 'openai_moderation_daily' were REMOVED from the TS union since planning
  — the union is now 7 kinds, while the DB CHECK still enumerates 9):
  export type BucketKind =
    | 'rpm' | 'itpm' | 'otpm' | 'daily_dollar'
    | 'persona_daily' | 'reports_ip_daily' | 'customer_portal_minute';

src/rateLimit/buckets.ts:74-90 — checkAndIncrementBucket(userId, kind, increment, limit, windowSeconds)
  passes p_bucket_kind: kind as FREE TEXT to the RPC (RPC does not enumerate).
  Returns { allowed:true } | { allowed:false, kind, retry_after_seconds }.

CRITICAL FINDING (overturns 15-RESEARCH.md Open Q3 "likely no migration"):
  The rate_buckets TABLE has a CHECK constraint `rate_buckets_bucket_kind_check`
  that ENUMERATES allowed kinds. The RPC accepts free text but the table CHECK
  blocks any new kind. Current allowed kinds in the LIVE DB (latest migration
  touching the CHECK is 20260528001100_rate_buckets_reports_ip_customer_portal.sql
  — re-verified 2026-06-10; no later migration alters it):
    'rpm','itpm','otpm','daily_dollar','persona_daily',
    'sightengine_daily','openai_moderation_daily',
    'reports_ip_daily','customer_portal_minute'
  (sightengine_daily/openai_moderation_daily remain in the DB CHECK even though
  they were dropped from the TS union — the migration's drop-then-add MUST
  re-list all 9 + 'vision_hourly' so existing rows/kinds stay valid.)
  => A migration IS required (drop-then-add the CHECK adding 'vision_hourly'),
     and a [BLOCKING] live apply MUST run before verification (via the
     Management API procedure in Task 1 — NOT plain `supabase db push`; see
     the divergence note there), mirroring what
     20260527000000_persona_free_bucket.sql and
     20260528001100_rate_buckets_reports_ip_customer_portal.sql achieved.

src/app.ts route chain pattern (RE-VERIFIED app.ts:63,79,93 — unchanged):
  app.post('/free/v1/messages', originLockGate, ipRateLimitGate, verifyJwt, personaDailyGate, handler)

Retry-After convention (NEW since planning — 260610 incident): personaDailyGate
  (src/rateLimit/personaDailyGate.ts:17-43) now CAPS the Retry-After HEADER at
  10s — `c.header('Retry-After', String(Math.min(result.retry_after_seconds, 10)))`
  — while the JSON body keeps the honest retry_after_seconds. The Anthropic SDK
  obeys the header with an un-abortable sleep, so an honest multi-minute header
  freezes the client. visionHourlyGate's window is 3600s — clone the capped-header
  form, NOT a raw header.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — allow 'vision_hourly' in rate_buckets CHECK + extend BucketKind union [BLOCKING db push]</name>
  <files>supabase/migrations/20260610120000_vision_hourly_bucket.sql, src/rateLimit/buckets.ts</files>
  <read_first>
    - supabase/migrations/20260527000000_persona_free_bucket.sql (the drop-then-add CHECK pattern for adding ONE bucket kind — closest analog)
    - supabase/migrations/20260528001100_rate_buckets_reports_ip_customer_portal.sql (the most recent CHECK state — copy its full kind list, then ADD 'vision_hourly')
    - src/rateLimit/buckets.ts (lines 22-29: the BucketKind union to extend — now 7 kinds; lines 74-90: checkAndIncrementBucket passes p_bucket_kind as free text)
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-PATTERNS.md §"proxy/src/rateLimit/buckets.ts (EDIT — add vision_hourly to BucketKind)" (NOTE: PATTERNS predates the repo split — read its `proxy/src/...` paths as `src/...` in sei-proxy)
  </read_first>
  <action>
    Create `supabase/migrations/20260610120000_vision_hourly_bucket.sql` (timestamp deliberately AFTER 20260605130000_seed_builtin_characters_as_public.sql — the latest applied migration — so `supabase db push` doesn't see an out-of-order file; the plan's original 20260604000000 name predates already-applied migrations and was renamed during the 2026-06-10 staleness audit) mirroring `20260527000000_persona_free_bucket.sql`: `alter table public.rate_buckets drop constraint rate_buckets_bucket_kind_check;` then `add constraint rate_buckets_bucket_kind_check check (bucket_kind in (...));` where the list is the CURRENT 9 kinds in the DB CHECK ('rpm','itpm','otpm','daily_dollar','persona_daily','sightengine_daily','openai_moderation_daily','reports_ip_daily','customer_portal_minute') PLUS 'vision_hourly' (sightengine_daily/openai_moderation_daily stay in the CHECK even though the TS union dropped them). Include a header comment noting the RPC accepts free text and the CHECK is the sole gate (matching the persona_free_bucket header convention). Then edit `src/rateLimit/buckets.ts` to add `| 'vision_hourly'` to the `BucketKind` union (line ~22-29). AFTER authoring the migration, apply it to the live project [BLOCKING] — without it, the proxy tsc/tests pass but a real `vision_hourly` insert is rejected at runtime (false-positive verification per schema_push_note).

    MIGRATION-APPLY PROCEDURE (verified 2026-06-10 by the orchestrator — do NOT use plain `supabase db push`): the remote migration history has diverged from local files (~12 local versions never recorded remotely, ~36 remote-only versions from dashboard applies); `supabase db push` refuses and its suggested `migration repair --status reverted` mass-surgery MUST NOT be run. The project is already `supabase link`ed (ref wfloawnjgkpammmnjncm). Apply the single additive ALTER via the Management API using the CLI's keychain token, then record ONLY the new version:
    ```bash
    cd /Users/ouen/slop/sei-studio/sei-proxy
    TOKEN=$(security find-generic-password -l "Supabase CLI" -w | sed 's/^go-keyring-base64://' | base64 -d)
    # JSON-encode the migration SQL and execute it (verified working endpoint):
    node -e 'const fs=require("fs");process.stdout.write(JSON.stringify({query:fs.readFileSync("supabase/migrations/20260610120000_vision_hourly_bucket.sql","utf8")}))' \
      | curl -s -X POST "https://api.supabase.com/v1/projects/wfloawnjgkpammmnjncm/database/query" \
        -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d @-
    # Expect [] or a result array; any {"message":...} or "error" output = failure, STOP and report.
    # Verify the constraint now includes vision_hourly:
    curl -s -X POST "https://api.supabase.com/v1/projects/wfloawnjgkpammmnjncm/database/query" \
      -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
      -d '{"query":"select pg_get_constraintdef(oid) as def from pg_constraint where conname = '"'"'rate_buckets_bucket_kind_check'"'"';"}'
    # Then record the migration as applied (this exact version ONLY — no other repair commands):
    supabase migration repair --status applied 20260610120000
    ```
    The live CHECK's current definition (queried 2026-06-10) is exactly the 9 kinds listed above — author the migration's drop-then-add from that list + 'vision_hourly'.
  </action>
  <verify>
    <automated>cd /Users/ouen/slop/sei-studio/sei-proxy && npx tsc --noEmit && grep -c "vision_hourly" src/rateLimit/buckets.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260610120000_vision_hourly_bucket.sql` exists in sei-proxy; `grep -c "vision_hourly" supabase/migrations/20260610120000_vision_hourly_bucket.sql` >= 1; the CHECK list includes all 9 prior kinds + 'vision_hourly'.
    - `grep -c "vision_hourly" src/rateLimit/buckets.ts` >= 1 (union extended).
    - `cd /Users/ouen/slop/sei-studio/sei-proxy && npx tsc --noEmit` clean.
    - [BLOCKING] migration applied to the live project via the Management API procedure above AND recorded with `supabase migration repair --status applied 20260610120000` — the live CHECK constraint includes 'vision_hourly' (the SUMMARY records the apply + the verified constraint def). The vision gate cannot be runtime-verified until this lands. Plain `supabase db push` is FORBIDDEN in this repo until the history divergence is reconciled.
  </acceptance_criteria>
  <done>vision_hourly is in the BucketKind union AND accepted by the live rate_buckets CHECK constraint (migration pushed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: visionHourlyGate middleware + co-located test</name>
  <files>src/rateLimit/visionHourlyGate.ts, src/rateLimit/visionHourlyGate.test.ts</files>
  <read_first>
    - src/rateLimit/personaDailyGate.ts (lines 17-43: the whole-file gate shape to clone — single bucket kind + window + sendError on deny; NOTE it now caps the Retry-After HEADER at 10s, `Math.min(result.retry_after_seconds, 10)`, while the JSON keeps the honest value — 260610 convention, clone it)
    - src/rateLimit/customerPortalMinuteGate.test.ts (the closest single-bucket-gate test to clone for the test file)
    - src/middleware/sentinel.ts (sendError signature, :127)
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md §"Proxy per-hour vision gate (VIS-07/D-09)" (the exact target file)
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-PATTERNS.md §"proxy/src/rateLimit/visionHourlyGate.ts (NEW)" (read `proxy/src/...` as `src/...` post-split)
  </read_first>
  <behavior>
    - Under the limit: gate calls next() and the request proceeds.
    - At the limit (the 11th call in the window with VISION_HOURLY_LIMIT=10n): gate returns a rate_limited error (code 'rate_limited', kind 'vision_hourly') with a Retry-After header, does NOT call next().
    - The bucket is incremented by 1n per call, kind 'vision_hourly', window 3600s.
    - userId comes from c.get('userId') (set by verifyJwt upstream).
  </behavior>
  <action>
    Create `src/rateLimit/visionHourlyGate.ts` cloning `personaDailyGate.ts`: export `const VISION_HOURLY_LIMIT = 10n` (D-09 ~10/hr, configurable) and `const VISION_WINDOW_SECONDS = 3600`; export `visionHourlyGate: MiddlewareHandler<{ Variables: { userId: string } }>` that reads `c.get('userId')`, calls `checkAndIncrementBucket(userId, 'vision_hourly', 1n, VISION_HOURLY_LIMIT, VISION_WINDOW_SECONDS)`, and on `!allowed` sets the `Retry-After` header CAPPED at 10s per the 260610 convention (`c.header('Retry-After', String(Math.min(result.retry_after_seconds, 10)))` — the honest value can be ~3600s and an obedient SDK would freeze the bot) + returns `sendError(c, { code:'rate_limited', kind:result.kind, retry_after_seconds:result.retry_after_seconds })` (JSON keeps the honest value). Create `src/rateLimit/visionHourlyGate.test.ts` cloning `customerPortalMinuteGate.test.ts` — mock `checkAndIncrementBucket` to assert: under-limit calls next, at-limit returns rate_limited with Retry-After (header value <= 10), and the bucket call uses kind 'vision_hourly' + window 3600 + limit 10n.
  </action>
  <verify>
    <automated>cd /Users/ouen/slop/sei-studio/sei-proxy && npx vitest run src/rateLimit/visionHourlyGate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/rateLimit/visionHourlyGate.ts` exports `visionHourlyGate` and `VISION_HOURLY_LIMIT` (value 10n) with window 3600 (`grep -c "3600" ...` >= 1).
    - The test file asserts all three behaviors above and passes (`npx vitest run src/rateLimit/visionHourlyGate.test.ts` green).
    - The gate reads `c.get('userId')` and calls `checkAndIncrementBucket` with kind `'vision_hourly'` (`grep -c "vision_hourly" src/rateLimit/visionHourlyGate.ts` >= 1).
  </acceptance_criteria>
  <done>visionHourlyGate caps at 10/3600s, returns rate_limited + Retry-After on deny, and its co-located test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Mount POST /vision/v1/messages with the vision gate in the chain</name>
  <files>src/app.ts</files>
  <read_first>
    - src/app.ts (lines 63, 79, 93: the load-bearing middleware chain order originLockGate -> ipRateLimitGate -> verifyJwt -> <bucketGate> -> forward — RE-VERIFIED 2026-06-10, line numbers unchanged; line 79 is the /free/v1/messages precedent that adds an extra bucket gate; line 63 shows /v1/messages mounts a CLOSURE `(c) => forwardToAnthropic(c, { preDeduct, settle, ..., estimateInputTokens, fetchImpl: fetch, anthropicApiKey: env.ANTHROPIC_API_KEY })` — duplicate that closure verbatim for the vision route)
    - src/rateLimit/gate.ts (rateLimitGate — the standard RPM/TPM/$ gate that the /v1/messages route uses; the vision route runs it AFTER visionHourlyGate so credit + token caps still apply. NOTE post-planning rework: gate.ts now excludes the cache_control prefix from ITPM and stashes `estInputTokens` in the Hono context via c.set, which forward.ts reads back — chaining rateLimitGate before the same forward handler preserves this contract unchanged)
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-PATTERNS.md §"proxy/src/app.ts (EDIT — mount the vision route)" (read `proxy/src/...` as `src/...` post-split)
  </read_first>
  <action>
    In `src/app.ts`, import `visionHourlyGate` and add `app.post('/vision/v1/messages', originLockGate, ipRateLimitGate, verifyJwt, visionHourlyGate, rateLimitGate, <forwardToAnthropic handler>)`. Reuse the SAME forward handler closure the existing `/v1/messages` route uses (the vision path differs only by inserting `visionHourlyGate` before `rateLimitGate` — it is still a normal Anthropic message turn that also consumes credits/RPM/TPM). The middleware order is load-bearing: `originLockGate -> ipRateLimitGate -> verifyJwt -> visionHourlyGate -> rateLimitGate -> forward`. Per D-09, ONLY explicit-`visualize`-triggered LLM turns hit this path (the bot-side routing of just that one turn is wired in 15-06); idle renders continue using the standard `/v1/messages` path and are NOT counted here.
  </action>
  <verify>
    <automated>cd /Users/ouen/slop/sei-studio/sei-proxy && npx tsc --noEmit && grep -c "/vision/v1/messages" src/app.ts && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/vision/v1/messages" src/app.ts` >= 1 (in sei-proxy).
    - The route chain (in file order) is `originLockGate, ipRateLimitGate, verifyJwt, visionHourlyGate, rateLimitGate` — visionHourlyGate sits AFTER verifyJwt (needs userId) and BEFORE rateLimitGate.
    - `cd /Users/ouen/slop/sei-studio/sei-proxy && npx tsc --noEmit` clean; the full proxy vitest suite passes (no regression).
  </acceptance_criteria>
  <done>POST /vision/v1/messages is mounted with the verified middleware chain; the existing proxy suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot -> proxy /vision/v1/messages | The bot is UNTRUSTED for cap accounting; the per-hour ceiling must be enforced server-side |
| LLM tool-use loop -> credit balance | An LLM looping `visualize` is an unbounded cost vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Denial of Service / cost | LLM loops `visualize` to exhaust credits | mitigate | Server-authoritative vision_hourly bucket (10/3600s), atomic Postgres check_and_increment_bucket; bot-side count is never trusted (ASVS V4). |
| T-15-02-02 | Elevation / bypass | Bot tries to skip the vision cap by hitting /v1/messages directly | accept | By design idle renders use /v1/messages (D-09 — idle is cadence-bounded). Explicit renders route to /vision; a malicious bot could route explicit turns to /v1/messages but then they consume normal credits + RPM/TPM/$ caps (rateLimitGate) — the credit ceiling still bounds total cost. The hourly cap specifically bounds the explicit-render burst vector. |
| T-15-02-03 | Spoofing | Unauthenticated request hits the vision route | mitigate | verifyJwt runs BEFORE visionHourlyGate (chain order); originLockGate + ipRateLimitGate run first (existing pattern). No bypass of the auth/origin-lock chain. |
| T-15-02-04 | Tampering | Migration not pushed -> CHECK rejects vision_hourly at runtime | mitigate | [BLOCKING] supabase db push in Task 1 before verification (schema_push_note). |
</threat_model>

<verification>
(All commands run in /Users/ouen/slop/sei-studio/sei-proxy.)
- `npx tsc --noEmit` clean.
- `npx vitest run` — full suite green incl. the new visionHourlyGate test.
- `grep -c "/vision/v1/messages" src/app.ts` >= 1; `grep -c "vision_hourly" src/rateLimit/buckets.ts` >= 1.
- Migration pushed to the live project (SUMMARY records it).
</verification>

<success_criteria>
1. A cloud-AI user's explicit-vision turns are capped at ~10/hour server-side; exceeding returns rate_limited + Retry-After.
2. Idle renders do NOT count against the cap (they use /v1/messages).
3. The vision_hourly kind is accepted by the live rate_buckets CHECK constraint (migration pushed).
</success_criteria>

<output>
Create `.planning/phases/15-in-game-vision-via-prismarine-viewer/15-02-SUMMARY.md` when done — this SUMMARY lives in the CLIENT repo (/Users/ouen/slop/sei-studio/sei), the only artifact of this plan that does; all code commits go to the sei-proxy repo. Record the supabase db push outcome and the chosen VISION_HOURLY_LIMIT default.
</output>
