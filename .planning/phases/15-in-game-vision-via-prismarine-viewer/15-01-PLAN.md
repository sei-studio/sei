---
phase: 15-in-game-vision-via-prismarine-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/bot/adapter/minecraft/render/povRenderer.js
autonomous: false
requirements: [VIS-01, VIS-08]
must_haves:
  truths:
    - "prismarine-viewer + node-canvas-webgl install and their native deps (gl, canvas) rebuild against Electron 42 via the existing postinstall"
    - "renderPov(bot) produces a single, aggressively-downscaled JPEG buffer (~256px default + low quality, well under the 512 ceiling — D-03) from the bot's eye position + yaw/pitch inside the bot utilityProcess (NOT main/renderer)"
    - "the same render path works in a PACKAGED build, not just electron-vite dev"
    - "when chunks aren't loaded, renderPov returns a degrade signal (empty/black-frame detection) instead of hanging or crashing"
  artifacts:
    - path: "src/bot/adapter/minecraft/render/povRenderer.js"
      provides: "Headless single-frame POV render: world chunks -> JPEG buffer"
      contains: "renderPov"
      min_lines: 60
    - path: "package.json"
      provides: "prismarine-viewer + node-canvas-webgl dependencies"
      contains: "prismarine-viewer"
  key_links:
    - from: "src/bot/adapter/minecraft/render/povRenderer.js"
      to: "prismarine-viewer/viewer"
      via: "require('prismarine-viewer/viewer') Viewer + WorldView"
      pattern: "prismarine-viewer/viewer"
    - from: "src/bot/adapter/minecraft/render/povRenderer.js"
      to: "node-canvas-webgl"
      via: "createCanvas headless WebGL context"
      pattern: "node-canvas-webgl"
---

<objective>
De-risk the native-ABI render path — the make-or-break risk of this entire phase (15-RESEARCH.md Pitfall 1, 15-PATTERNS.md "No Analog Found"). Install `prismarine-viewer` + `node-canvas-webgl` (which transitively pull native `gl` + `canvas`), rebuild them against Electron 42's ABI via the existing postinstall, and build `povRenderer.js` — the one file in this phase with NO in-repo analog. Prove ONE headless JPEG comes out of `prismarine-viewer/viewer` from the bot's eye/yaw/pitch inside the bot utilityProcess, AND that it survives a PACKAGED build on this machine.

Purpose: Every feature plan in Waves 2-4 (the `visualize` action, image-content wiring, idle hook, renderer UI) is waved BEHIND this spike. If headless-gl cannot be made to build/load under Electron 42, the whole render approach must be reconsidered (the documented fallback is BrowserWindow-WebGL, which breaks the three-process invariant — a last resort). Proving the render path FIRST prevents building features on an unproven foundation.

Output: `package.json` with the two new deps; `src/bot/adapter/minecraft/render/povRenderer.js` exporting a wall-clock-timeout-wrapped `renderPov(bot, opts)` that returns a downscaled JPEG buffer or a degrade signal; a packaged-build verification confirming the native modules load from `app.asar.unpacked` in `dist`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-CONTEXT.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-PATTERNS.md

<interfaces>
<!-- The render path has NO in-repo analog. The authoritative source for the API shape is
     15-RESEARCH.md §"Pattern 1: Direct single-frame headless render (VIS-01)" — the executor
     MUST read it before writing povRenderer.js. Key API shape (verify against the INSTALLED
     package after `npm view` / reading node_modules/prismarine-viewer/viewer): -->

prismarine-viewer/viewer exports: { Viewer, WorldView }
  new Viewer(threeWebGLRenderer)
  viewer.setVersion(bot.version)
  viewer.listen(worldView)
  viewer.setFirstPersonCamera(eyePosVec3, yaw, pitch)
  viewer.update()
  await viewer.waitForChunksToRender()

new WorldView(bot.world, viewDistance, centerVec3)
  await worldView.init(centerVec3)
  worldView.listenToBot(bot)
  worldView.removeListenersFromBot?.(bot)

node-canvas-webgl: createCanvas(width, height) -> headless WebGL <canvas>
  canvas.toBuffer('image/jpeg', { quality })   // node-canvas encode API (VERIFY exposed)

bot.entity: { position: Vec3, eyeHeight: number, yaw: number, pitch: number }
bot.version: string  // guard: must exist for viewer.setVersion (VIS-08)

ESM/CJS interop (CRITICAL — the repo is ESM: package.json "type":"module"):
  prismarine-viewer/viewer and node-canvas-webgl are CJS-only packages. Inside an ESM
  module `require` is undefined. Load them the ESM-correct way at the top of povRenderer.js:
    import { createRequire } from 'module';
    const require = createRequire(import.meta.url);
  then `const { Viewer, WorldView } = require('prismarine-viewer/viewer')` and
  `const { createCanvas } = require('node-canvas-webgl')`. (A static `import` only works if
  the installed package ships proper ESM/named-export interop — the executor confirms during
  the install spike; createRequire(import.meta.url) is the default that always works.)
</interfaces>

<wall_clock_timeout_analog>
<!-- CLAUDE.md invariant: every external call has a wall-clock timeout. renderPov's
     waitForChunksToRender CAN hang if chunks never arrive. Copy the Promise.race + post-race
     cleanup discipline from these analogs (read them): -->
src/bot/adapter/minecraft/behaviors/pathfind.js:71-112  (Promise.race + clearTimeout + removeEventListener after the race)
src/bot/adapter/minecraft/behaviors/lookAt.js:32-39     (timeout/abort race shape, degrade-string return)
</wall_clock_timeout_analog>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install prismarine-viewer + node-canvas-webgl ([ASSUMED] package legitimacy gate)</name>
  <read_first>
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md §"Package Legitimacy Audit" (every package is [ASSUMED] — slopcheck was unavailable; T-15-SC)
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md §"Standard Stack" + §"Installation"
    - package.json (current dependencies + the existing `postinstall` script that runs electron-builder install-app-deps)
    - electron-builder.yml (confirm `asarUnpack: node_modules/**/*` already covers gl/canvas .node binaries)
  </read_first>
  <what-built>
    Nothing is auto-installed before this checkpoint. Per the Package Legitimacy Gate (slopcheck unavailable -> all packages [ASSUMED]), the human must verify the four packages on their registry pages BEFORE any `npm install` runs. The packages to verify: `prismarine-viewer` (npmjs.com/package/prismarine-viewer — confirm PrismarineJS-official, ~v1.33.0), `node-canvas-webgl` (npmjs.com/package/node-canvas-webgl — confirm it exists; note it is STALE/2023 v0.3.0, the [SUS] watch item), `gl` / headless-gl (npmjs.com/package/gl — native), `canvas` / node-canvas (npmjs.com/package/canvas — native). gl + canvas arrive transitively via node-canvas-webgl.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/prismarine-viewer — confirm publisher is PrismarineJS, repo github.com/PrismarineJS/prismarine-viewer, latest ~1.33.0.
    2. Open https://www.npmjs.com/package/node-canvas-webgl — confirm it exists; note last publish 2023 (v0.3.0). This is the stale/low-maintenance [SUS] dependency — confirm you accept it as the exact module prismarine-viewer's headless example imports.
    3. Open https://www.npmjs.com/package/gl and https://www.npmjs.com/package/canvas — confirm both are the well-known native modules (stackgl/headless-gl, Automattic/node-canvas).
    4. Reply "approved" to authorize the install, or "rejected — use BrowserWindow-WebGL fallback" / "rejected — descope" if you do not accept the supply-chain risk.
  </how-to-verify>
  <action>
    BLOCKING human gate — never auto-approve (workflow.auto_advance is ignored for legitimacy checkpoints). Only AFTER the human replies "approved": run `npm install prismarine-viewer node-canvas-webgl` (gl + canvas come transitively), then `npm run postinstall` (electron-builder install-app-deps — rebuilds gl + canvas against Electron 42 ABI). Do NOT add `sharp` (avoid the libvips native dep — 15-RESEARCH.md Standard Stack). If the rebuild fails on a pinned native version, pin/override gl or canvas to a version that builds on Node 22 / Electron 42 (15-RESEARCH.md Audit notes node-canvas-webgl@0.3.0 pins old canvas@^2.6.0 / gl@^6.0.0).
  </action>
  <acceptance_criteria>
    - Human reply is "approved" (verbatim) before any install command runs. If "rejected", STOP the phase and surface the fallback decision to the orchestrator.
    - `package.json` dependencies include `prismarine-viewer` and `node-canvas-webgl` (`grep -E 'prismarine-viewer|node-canvas-webgl' package.json` returns both).
    - `node --input-type=module -e "await import('node-canvas-webgl')"` exits 0 against system Node (proves the package + its native deps installed; ESM-valid dynamic import works for the CJS package via Node interop; the Electron-ABI load is proven in Task 3). If headless-gl will not load under system Node at all (built for the Electron ABI), this is expected — fall back to confirming `test -d node_modules/node-canvas-webgl` and defer the real load proof to Task 3's packaged-build checkpoint.
    - `npm run postinstall` exits 0 (native rebuild against Electron 42 succeeded).
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize install, or "rejected — fallback" / "rejected — descope".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write povRenderer.js — single-frame headless POV render</name>
  <files>src/bot/adapter/minecraft/render/povRenderer.js</files>
  <read_first>
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md §"Pattern 1: Direct single-frame headless render (VIS-01)" (the authoritative render shape — this file has NO in-repo analog)
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md §"Pitfall 5: GL context / WorldView listener leak" (reuse a single long-lived Viewer for the idle hot path, OR tear down per render)
    - src/bot/adapter/minecraft/behaviors/pathfind.js (lines 71-112: Promise.race + post-race clearTimeout/removeEventListener cleanup — the wall-clock-timeout discipline to copy)
    - src/bot/adapter/minecraft/behaviors/lookAt.js (lines 7-39: degrade-string return convention, timeout/abort race)
    - node_modules/prismarine-viewer/viewer (confirm the exact exported names + method signatures against the INSTALLED package — the README shape may differ slightly from the installed API)
  </read_first>
  <action>
    Create `src/bot/adapter/minecraft/render/povRenderer.js` exporting `async function renderPov(bot, { width = 256, height = 256, viewDistance = 4, quality = 0.4, timeoutMs = 5000 } = {})`. ESM/CJS interop (CRITICAL — the repo is ESM): the CJS-only packages MUST be loaded via `createRequire`, not `require` (which is undefined in an ESM module). At the top of the file: `import { createRequire } from 'module'; const require = createRequire(import.meta.url);` then `const { Viewer, WorldView } = require('prismarine-viewer/viewer')` and `const { createCanvas } = require('node-canvas-webgl')` (a static `import` is acceptable ONLY if the install spike confirms the package ships proper ESM interop — default to createRequire(import.meta.url)). Build a `node-canvas-webgl` `createCanvas(width, height)`, a `three` `WebGLRenderer({ canvas })`, a `Viewer(renderer)`, call `viewer.setVersion(bot.version)` (guard: if `!bot.version` return the degrade signal), `new WorldView(bot.world, viewDistance, bot.entity.position)`, `viewer.listen(worldView)`, `await worldView.init(center)`, `worldView.listenToBot(bot)`, `await viewer.waitForChunksToRender()`, compute eye = `bot.entity.position.offset(0, bot.entity.eyeHeight, 0)`, `viewer.setFirstPersonCamera(eye, bot.entity.yaw, bot.entity.pitch)`, `viewer.update()`, render, then `canvas.toBuffer('image/jpeg', { quality })`. CRITICAL — wrap the chunk-wait + render in `Promise.race([renderPromise, timeoutPromise])` against `timeoutMs` and, after the race, `clearTimeout` the timer and tear down (dispose renderer + `worldView.removeListenersFromBot?.(bot)`) to avoid the GL-context/listener leak (Pitfall 5). VIS-08: if the rendered frame is empty/black (chunks not loaded) OR the timeout fires OR `bot.version` is missing, return the sentinel `{ ok: false, reason: 'cant_see' }` instead of throwing/hanging; on success return `{ ok: true, buffer, mediaType: 'image/jpeg' }`. Detect black/empty frames cheaply (e.g. sample the buffer or check WorldView reported zero loaded chunks). Provide a `disposeRenderer()` export so callers can reuse OR release the GL context. Document at the top that this runs in the bot utilityProcess ONLY (three-process invariant — it needs `bot.world`).
  </action>
  <verify>
    <automated>grep -E "export (async )?function renderPov" src/bot/adapter/minecraft/render/povRenderer.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/bot/adapter/minecraft/render/povRenderer.js` exists and exports `renderPov` (`grep -E "export (async )?function renderPov" src/bot/adapter/minecraft/render/povRenderer.js` matches). NOTE: do NOT load this file in an automated verify — it transitively imports `node-canvas-webgl` -> native `gl`/`canvas`, which after `@electron/rebuild` are built for the Electron ABI and will NOT load under system Node (ERR / segfault). The real native load is proven in Task 3's packaged-build checkpoint; here a source assertion is the correct gate.
    - The CJS packages are loaded ESM-correctly: `grep -c "createRequire" src/bot/adapter/minecraft/render/povRenderer.js` >= 1 (no bare `require(` at module top in an ESM file).
    - `grep -c "prismarine-viewer/viewer" src/bot/adapter/minecraft/render/povRenderer.js` >= 1 and `grep -c "node-canvas-webgl" src/bot/adapter/minecraft/render/povRenderer.js` >= 1.
    - A `Promise.race` with a timeout AND a post-race `clearTimeout` are both present (`grep -c "Promise.race" ...` >= 1; `grep -c "clearTimeout" ...` >= 1).
    - The degrade path returns a sentinel (the literal `cant_see` reason appears: `grep -c "cant_see" ...` >= 1), never `throw`.
    - `toBuffer('image/jpeg'` with a `quality` argument is present (VIS-06/D-03 aggressive compression, ~256px default well under the 512 ceiling).
  </acceptance_criteria>
  <done>renderPov is exported, wraps the render in a wall-clock timeout with cleanup, returns a JPEG buffer on success and a degrade sentinel (never throws/hangs) when chunks aren't loaded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Prove the render path in a PACKAGED build (native ABI under Electron 42)</name>
  <read_first>
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md §"Pitfall 1" (works in dev / crashes in dist is the canonical failure; `.node` must load from app.asar.unpacked)
    - electron-builder.yml (confirm `asarUnpack: node_modules/**/*` covers gl/canvas)
    - package.json (the `dist`/build + `dev` scripts)
    - src/main/botSupervisor.ts (how the bot utilityProcess is forked — where a temporary render smoke-call can be wired for the spike)
  </read_first>
  <what-built>
    A temporary render smoke-probe wired into the bot utilityProcess that calls `renderPov(bot)` once after the bot spawns (behind a `SEI_VISION_SPIKE=1` env flag so it ships nothing permanent), logging whether a non-empty JPEG buffer came back. This proves: (1) `gl` + `canvas` load under the Electron 42 ABI inside the utilityProcess (NOT system Node), and (2) they load from `app.asar.unpacked` in a PACKAGED `dist` build, not just `electron-vite dev`.
  </what-built>
  <how-to-verify>
    1. DEV check: run `SEI_VISION_SPIKE=1 npm run dev` (or the project's dev command), summon a bot into a loaded world, confirm the log shows "vision spike: rendered N bytes JPEG" with N > 0 (not "Module did not self-register" / "getUniformLocation of null" / segfault).
    2. PACKAGED check (the load-bearing one): run the project's package/`dist` build, launch the packaged app, summon a bot, confirm the SAME "rendered N bytes" log appears and the app does NOT crash. This is the failure surface Pitfall 1 warns about.
    3. Confirm the gl/canvas `.node` binaries exist under the packaged app's `app.asar.unpacked/node_modules/` path.
    4. Reply "approved — packaged render works" to unblock Waves 2-4, or describe the failure (then the orchestrator escalates to the BrowserWindow-WebGL fallback or descope).
  </how-to-verify>
  <action>
    Wire the temporary `SEI_VISION_SPIKE=1`-gated smoke-probe into the bot utilityProcess spawn path (botSupervisor / bot brain init), calling `renderPov(bot)` once and logging the byte count. Build the packaged app. Surface the dev + packaged verification steps verbatim to the human; do NOT mark complete until the human confirms the PACKAGED build renders. After approval, REMOVE the temporary spike probe (it is scaffolding, not a feature) and note its removal in the SUMMARY. If the packaged build fails, STOP and surface the failure to the orchestrator — do not let Waves 2-4 proceed on an unproven render path.
  </action>
  <acceptance_criteria>
    - Human confirms (verbatim "approved — packaged render works") that the packaged `dist` build renders a non-empty JPEG inside the bot utilityProcess without crashing.
    - The temporary spike probe is removed after approval (`grep -rc "SEI_VISION_SPIKE" src/` returns 0 in the final state).
    - The SUMMARY records the packaged-build outcome (success, or failure + which fallback the orchestrator chose).
  </acceptance_criteria>
  <resume-signal>Type "approved — packaged render works" to unblock the feature waves, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local build | New `[ASSUMED]` packages (prismarine-viewer, node-canvas-webgl) + native deps (gl, canvas) compiled via node-gyp at install — untrusted code crosses into the build |
| native module -> bot utilityProcess | `.node` binaries dlopen'd into the Electron process at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-SC | Tampering / Elevation | npm install of prismarine-viewer, node-canvas-webgl, gl, canvas | mitigate | All four [ASSUMED] (slopcheck unavailable). Task 1 is a BLOCKING human-verify gate against npmjs.com pages BEFORE install; never auto-approved. node-canvas-webgl flagged [SUS] (stale 2023) — human explicitly accepts. |
| T-15-01-02 | Denial of Service | renderPov waitForChunksToRender hang | mitigate | Promise.race wall-clock timeout (default 5s) + post-race cleanup (Task 2); returns degrade sentinel, never hangs the bot. CLAUDE.md invariant. |
| T-15-01-03 | Denial of Service | GL context / WorldView listener leak across repeated renders | mitigate | disposeRenderer / removeListenersFromBot teardown after each render OR single long-lived Viewer reuse (Pitfall 5). |
| T-15-01-04 | Tampering | native .node loads only from app.asar.unpacked in packaged build | mitigate | Task 3 packaged-build checkpoint confirms ABI rebuild + unpacked load; existing asarUnpack covers it. |
</threat_model>

<verification>
- `grep -E "prismarine-viewer|node-canvas-webgl" package.json` returns both deps.
- `grep -E "export (async )?function renderPov" src/bot/adapter/minecraft/render/povRenderer.js` matches (source-level export check — povRenderer is NOT load-tested under system Node because it pulls native gl/canvas built for the Electron ABI; the real native load is the Task 3 packaged-build checkpoint).
- Human-confirmed packaged-build render (Task 3) — the gate for Waves 2-4.
- `grep -rc "SEI_VISION_SPIKE" src/` returns 0 (temporary probe removed).
</verification>

<success_criteria>
1. prismarine-viewer + node-canvas-webgl installed behind the [ASSUMED] legitimacy gate; native gl/canvas rebuilt against Electron 42.
2. renderPov(bot) returns a downscaled JPEG buffer from the bot's eye/yaw/pitch, or a degrade sentinel when chunks aren't loaded — wrapped in a wall-clock timeout with cleanup.
3. The render path is PROVEN in a packaged build inside the bot utilityProcess (human-verified) — unblocking the feature waves.
</success_criteria>

<output>
Create `.planning/phases/15-in-game-vision-via-prismarine-viewer/15-01-SUMMARY.md` when done. Record: packaged-build render outcome (the make-or-break result), any gl/canvas version pins needed, and confirmation the temporary spike probe was removed.
</output>
</content>
</invoke>
