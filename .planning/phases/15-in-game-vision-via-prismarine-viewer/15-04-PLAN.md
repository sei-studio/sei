---
phase: 15-in-game-vision-via-prismarine-viewer
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-03]
files_modified:
  - src/bot/adapter/minecraft/behaviors/visualize.js
  - src/bot/adapter/minecraft/behaviors/visualize.test.js
  - src/bot/adapter/minecraft/registry.js
  - src/bot/adapter/minecraft/registry.vision.test.js
  - src/bot/adapter/minecraft/index.js
  - src/bot/adapter/minecraft/prompts.js
  - src/bot/brain/orchestrator.js
autonomous: true
requirements: [VIS-02, VIS-03, VIS-08]
must_haves:
  truths:
    - "A visualize Zod action is registered in the closed registry ONLY when the active provider's capabilities.vision is true (D-10); this action is the explicit render path of D-01 (path a — the LLM-callable visualize; the idle auto-render path b lives in 15-07)"
    - "When the provider is non-VLM, visualize is absent from the registry AND filtered out of the orchestrator tool list (belt-and-suspenders, VIS-03)"
    - "The visualize handler renders the bot's POV via renderPov, downscales/dedupes, and returns a result the orchestrator can attach as an image; on unloaded chunks it returns the degrade string 'I can't see clearly right now' (VIS-08), never throws. Explicit visualize is ungated by owner-proximity (D-08) — it renders whatever the bot sees now; the 16-block+LOS gate applies to idle auto-render only (15-07)"
    - "visualizeAction's success result has the EXACT shape { text:string, image:{ mediaType:string, dataBase64:string } } (the contract 15-06 reads to attach the image) — asserted in visualize.test.js"
  artifacts:
    - path: "src/bot/adapter/minecraft/behaviors/visualize.js"
      provides: "visualize action handler (renderPov + downscale + degrade) — VIS-02/VIS-08"
      contains: "visualizeAction"
      min_lines: 40
    - path: "src/bot/adapter/minecraft/registry.js"
      provides: "conditional visualize registration gated on visionEnabled (D-10)"
      contains: "visionEnabled"
    - path: "src/bot/brain/orchestrator.js"
      provides: "combinedToolsFor filters visualize when capabilities.vision is false"
      contains: "visualize"
  key_links:
    - from: "src/bot/adapter/minecraft/behaviors/visualize.js"
      to: "render/povRenderer.js"
      via: "renderPov(bot) wrapped in timeout/abort race"
      pattern: "renderPov"
    - from: "src/bot/adapter/minecraft/registry.js"
      to: "capabilities.vision"
      via: "createDefaultRegistry({ visionEnabled }) skips visualize when false"
      pattern: "visionEnabled"
    - from: "src/bot/brain/orchestrator.js"
      to: "anthropic.capabilities.vision"
      via: "combinedToolsFor filters t.name !== 'visualize'"
      pattern: "capabilities"
---

<objective>
Add the `visualize` Zod action to the closed registry and gate it on `capabilities.vision` (VIS-02 registration half + VIS-03). The handler drives `renderPov` (proven in 15-01) behind a wall-clock timeout, applies the aggressive downscale/dedupe (D-02/D-03), and returns a degrade string when chunks aren't loaded (VIS-08). VLM gating (D-10) is belt-and-suspenders: skip registration when the provider lacks vision AND filter `visualize` out of the orchestrator's tool list so a non-VLM model never even sees the tool.

Purpose: This is the LLM's hook into vision. Explicit `visualize` is ungated by owner-proximity (D-08 — it renders whatever the bot sees now), unlike the idle path (15-07). The actual image-content-block delivery to the model (the VIS-02 wiring half) is in 15-06 — this plan makes the action EXIST, render, and gate correctly; 15-06 wires its output into the LLM turn.

Output: `visualize.js` handler + test (with an explicit success-shape assertion — the 15-06 data contract); conditional registration threaded through `createDefaultRegistry`/`createMinecraftAdapter` + a `registry.vision.test.js` that proves the conditional gate WITHOUT loading native render modules; an `ACTION_DESCRIPTIONS` entry; and the orchestrator tool-list filter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-CONTEXT.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-PATTERNS.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-01-SUMMARY.md
@.planning/phases/15-in-game-vision-via-prismarine-viewer/15-03-SUMMARY.md

<interfaces>
<!-- VERIFIED during planning. -->

src/bot/adapter/minecraft/render/povRenderer.js (from 15-01):
  renderPov(bot, opts) -> Promise<{ ok:true, buffer, mediaType } | { ok:false, reason:'cant_see' }>
  Opts include { width,height,viewDistance,quality,timeoutMs }. Already timeout-wrapped internally.
  NOTE: povRenderer.js statically requires node-canvas-webgl/gl/canvas (native) — anything that
  imports visualize.js (-> povRenderer.js) pulls the native chain. Tests that exercise the registry
  MUST vi.mock the visualize/povRenderer module so native modules never load under system Node.

src/bot/config.js config.vision (from 15-03): { auto_render, render_interval_ms, image_quality, resolution_px, explicit_cap_per_hour }

Project module system: package.json `"type": "module"` -> the repo is ESM. `node -e "require('./x.js')"`
  THROWS on these ESM modules (no require in ESM scope) — verification MUST use vitest (ESM-aware),
  not a bare `node -e require(...)` one-liner. (This also avoids the native-dep load problem.)

src/bot/registry.js — createRegistry() exposes:
  register(name, zodSchema, handler, description?)  // throws on duplicate name
  list() -> string[]   // registered action names (registry.js:44-46) — the accessor the gate test reads
  schema(name), execute(name, args)

src/bot/adapter/minecraft/registry.js (lines re-checked 2026-06-10 after the persistent-follow rework):
  createDefaultRegistry()  // UNCONDITIONAL today — line 115. register(name, zodSchema, handler).
  Empty-schema form: registry.register('activateItem', z.object({}), activateItemAction)  // :363
  This file statically imports its behaviors at top (registry.js:11-27) — after adding visualize it
  will import ./behaviors/visualize.js -> povRenderer.js (native). The gate test mocks that module.

src/bot/adapter/minecraft/index.js:
  createMinecraftAdapter({ bot, config })  // :34 — calls createDefaultRegistry() at :38 (no provider today)

src/bot/brain/orchestrator.js:
  combinedToolsFor()  // :364 ; movementTools = buildAnthropicTools(subRegistry, descMap) at :374
  The provider handle `anthropic` exposes `.capabilities` (anthropicProvider.js:8 CAPABILITIES = { vision, cached, local }).
  callPersonality (:2255) sends tools: combinedToolsFor() at :2270.

Behavior analogs (timeout/abort/degrade-string):
  src/bot/adapter/minecraft/behaviors/lookAt.js:7-39 (handler signature (args,bot,config), abort-first, race, degrade-string)
  src/bot/adapter/minecraft/behaviors/pathfind.js:104-112 (post-race clearTimeout/removeEventListener)

vi.mock analog: src/bot/adapter/minecraft/index.test.js (imports createMinecraftAdapter, stubs bot with vi.fn()).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: visualize.js handler — renderPov + downscale/dedupe + degrade (VIS-02/VIS-08)</name>
  <files>src/bot/adapter/minecraft/behaviors/visualize.js, src/bot/adapter/minecraft/behaviors/visualize.test.js</files>
  <read_first>
    - src/bot/adapter/minecraft/render/povRenderer.js (the renderPov contract from 15-01 — return shape, timeout, degrade sentinel)
    - src/bot/adapter/minecraft/behaviors/lookAt.js (lines 7-39: handler signature (args, bot, config), signal-aborted early return, degrade-STRING return convention — handlers return a short string on failure, never throw)
    - src/bot/adapter/minecraft/behaviors/pathfind.js (lines 71-112: Promise.race + post-race cleanup discipline)
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md §"Pattern 1" (downscale ~256px + JPEG q~0.4) and §"D-02 dedupe"
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-PATTERNS.md §"src/bot/adapter/minecraft/behaviors/visualize.js"
  </read_first>
  <behavior>
    - On signal.aborted at entry: returns 'aborted' immediately (lookAt convention).
    - On successful render: returns a structured result with EXACTLY the keys { text: string, image: { mediaType: string, dataBase64: string } } — no extra keys, no nesting changes. This is the contract 15-06 reads to attach the image; the test asserts the exact shape (see acceptance).
    - On renderPov returning { ok:false } OR timeout: returns the degrade STRING "I can't see clearly right now" (VIS-08) — never throws.
    - The handler passes config.vision.resolution_px + image_quality into renderPov (D-03/D-04 — config is source of truth).
    - Idle-only dedupe (D-02): the handler exposes a way for the idle caller (15-06/15-07) to skip near-duplicate frames; for the EXPLICIT path no dedupe (the model asked for a fresh look). Implement the dedupe hash here but only apply it when an `idle` flag is passed; the idle near-duplicate result is EXACTLY { skip: true }.
  </behavior>
  <action>
    Create `src/bot/adapter/minecraft/behaviors/visualize.js` exporting `async function visualizeAction(args, bot, config)`. Mirror lookAt's signature + signal-aborted early return. Read render params from `config.vision` (resolution_px, image_quality). Call `renderPov(bot, { width: res, height: res, quality })` (renderPov is already timeout-wrapped, but ALSO honor `config.signal` abort here per the lookAt pattern). If `result.ok === false` or render times out, return the degrade string "I can't see clearly right now" (VIS-08). On success, base64-encode the buffer and return a structured result `{ text: 'rendered view attached', image: { mediaType: result.mediaType, dataBase64: <b64> } }` (EXACTLY these keys — 15-06 destructures `image.mediaType` / `image.dataBase64`). Implement a cheap frame-dedupe hash (D-02 — position+yaw/pitch quantization OR a buffer hash, Claude's discretion) and a small last-frame cache; when called with `args.idle === true` (or a config flag) and the new frame hashes equal to the last sent, return the sentinel `{ skip: true }` so the idle caller drops the send. For the explicit path (no idle flag), never dedupe. Cap the base64 size before returning (request-size safety — ASVS V5; ~256px q0.4 keeps it small). Create `src/bot/adapter/minecraft/behaviors/visualize.test.js` mocking `renderPov` (vi.mock the povRenderer module so native deps never load) to assert: success -> structured image result with the EXACT shape; { ok:false } -> degrade string; idle duplicate -> { skip:true }; aborted signal -> 'aborted'.
  </action>
  <verify>
    <automated>npx vitest run src/bot/adapter/minecraft/behaviors/visualize.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "visualizeAction" src/bot/adapter/minecraft/behaviors/visualize.js` >= 1; `grep -c "renderPov" src/bot/adapter/minecraft/behaviors/visualize.js` >= 1.
    - The degrade copy literal "I can't see clearly right now" is present (`grep -c "can't see clearly" ...` >= 1) and reached on { ok:false }/timeout — handler never `throw`s (`grep -c "throw " ...` == 0 except inside re-thrown abort if any; prefer zero).
    - SHAPE CONTRACT (Warning 6): the test asserts the SUCCESS result deep-equals/matches `{ text: <string>, image: { mediaType: <string>, dataBase64: <string> } }` with EXACTLY those keys (e.g. `expect(Object.keys(res)).toEqual(['text','image'])` and `expect(Object.keys(res.image)).toEqual(['mediaType','dataBase64'])`, types asserted). 15-06 reads this exact shape.
    - The test asserts success/structured-image, degrade-string, idle-dedupe-skip ({ skip:true }), and aborted ('aborted') — all pass.
  </acceptance_criteria>
  <done>visualizeAction renders via renderPov, returns a structured image result with the EXACT { text, image:{ mediaType, dataBase64 } } shape on success (asserted), the degrade string on unloaded chunks, and a skip sentinel for idle duplicates — never throws. (D-11: this handler is provider-agnostic — BYO-key/local-VLM users get the same capability gating + same compression as cloud users; only the proxy per-hour cap (15-02) is cloud-only and is enforced outside this handler.)</done>
</task>

<task type="auto">
  <name>Task 2: Conditional visualize registration (D-10) + ACTION_DESCRIPTIONS entry + native-free gate test</name>
  <files>src/bot/adapter/minecraft/registry.js, src/bot/adapter/minecraft/index.js, src/bot/adapter/minecraft/prompts.js, src/bot/adapter/minecraft/registry.vision.test.js</files>
  <read_first>
    - src/bot/adapter/minecraft/registry.js (line 115: createDefaultRegistry() unconditional; :363 the empty-schema z.object({}) register form; :342-352 the lookAt register call shape; :11-27 the top-of-file static behavior imports — visualize joins these)
    - src/bot/registry.js (lines 44-46: list() -> string[], the accessor the gate test reads)
    - src/bot/adapter/minecraft/index.js (line 34: createMinecraftAdapter({ bot, config }); :38 const registry = createDefaultRegistry())
    - src/bot/adapter/minecraft/index.test.js (the vi.fn() bot-stub + import pattern to mirror for the gate test)
    - src/bot/adapter/minecraft/prompts.js (the ACTION_DESCRIPTIONS map — add a visualize description)
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-PATTERNS.md §"src/bot/adapter/minecraft/registry.js (EDIT — conditional visualize registration)"
  </read_first>
  <action>
    (a) Edit `createDefaultRegistry()` in `registry.js` to accept `{ visionEnabled = false } = {}` and, only when `visionEnabled`, call `registry.register('visualize', z.object({}), visualizeAction)` (empty schema — clone the `activateItem` form at :363). Import `visualizeAction` from `./behaviors/visualize.js`.
    (b) Edit `createMinecraftAdapter({ bot, config })` in `index.js` to accept and forward `visionEnabled`: `createMinecraftAdapter({ bot, config, visionEnabled })` -> `createDefaultRegistry({ visionEnabled })`. The caller (the bot brain that constructs the adapter) derives `visionEnabled` from the active provider's `capabilities.vision`; thread that boolean from wherever the adapter is constructed (the brain holds the provider). If the construction site does not yet have the provider handle, default `visionEnabled` from `config`-derived capability and rely on the orchestrator-level filter (Task 3) as the authoritative gate.
    (c) Add a `visualize` entry to `ACTION_DESCRIPTIONS` in `prompts.js` describing it for the LLM (e.g. "Render and look at your current surroundings; returns an image of what you see."). Keep it short — it only appears when vision is enabled.
    (d) Create `src/bot/adapter/minecraft/registry.vision.test.js` (Warning 5 — the repo is ESM and registry imports the native render chain, so a `node -e require(...)` check is doubly invalid). At the TOP of the test, `vi.mock('./behaviors/visualize.js', () => ({ visualizeAction: vi.fn(async () => ({ text:'x', image:{ mediaType:'image/jpeg', dataBase64:'AAAA' } })) }))` so `povRenderer.js` + native `gl`/`canvas` are NEVER imported. Then import `createDefaultRegistry` and assert: `createDefaultRegistry({ visionEnabled:true }).list()` includes `'visualize'`, and `createDefaultRegistry({ visionEnabled:false }).list()` does NOT include `'visualize'`. (If the mock specifier needs to target `./render/povRenderer.js` instead because visualize imports it eagerly, mock that path instead — the goal is: no native module load.)
  </action>
  <verify>
    <automated>npx vitest run src/bot/adapter/minecraft/registry.vision.test.js && grep -c "visionEnabled" src/bot/adapter/minecraft/registry.js && grep -c "visionEnabled" src/bot/adapter/minecraft/index.js && grep -c "visualize" src/bot/adapter/minecraft/prompts.js</automated>
  </verify>
  <acceptance_criteria>
    - `registry.vision.test.js` mocks the visualize/povRenderer module (NO native load) and asserts: `visionEnabled:true` -> list() includes 'visualize'; `visionEnabled:false` -> list() excludes it. The test passes under vitest (no "Module did not self-register" / native error).
    - `grep -c "visionEnabled" src/bot/adapter/minecraft/registry.js` >= 1 AND `grep -c "visionEnabled" src/bot/adapter/minecraft/index.js` >= 1.
    - `grep -c "visualize" src/bot/adapter/minecraft/prompts.js` >= 1.
    - No `node -e "require(...)"` is used to verify registration (ESM + native incompatible) — the gate is the vitest test above.
  </acceptance_criteria>
  <done>visualize registers only when visionEnabled is true (proven by a native-free vitest gate test); the flag threads through createMinecraftAdapter; a prompt description exists.</done>
</task>

<task type="auto">
  <name>Task 3: Orchestrator tool-list filter — drop visualize when capabilities.vision is false (VIS-03)</name>
  <files>src/bot/brain/orchestrator.js</files>
  <read_first>
    - src/bot/brain/orchestrator.js (lines 364-381: combinedToolsFor(); :374 the movementTools = buildAnthropicTools(...) line to .filter; :2255-2285 callPersonality sends combinedToolsFor() at :2270)
    - src/bot/brain/llm/anthropicProvider.js (line 8: CAPABILITIES = { vision, cached, local } — how the provider exposes capabilities)
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-PATTERNS.md §"src/bot/brain/orchestrator.js (EDIT — tool-list filter)" (part (a))
    - .planning/phases/15-in-game-vision-via-prismarine-viewer/15-RESEARCH.md §"Pattern 3: Conditional visualize registration (VIS-03/D-10)"
  </read_first>
  <action>
    In `orchestrator.js` `combinedToolsFor()` (~:374), after building `movementTools = buildAnthropicTools(subRegistry, descMap)`, append `.filter(t => visionOk || t.name !== 'visualize')` where `const visionOk = !!anthropic.capabilities?.vision` (the provider handle the orchestrator already holds). This is the belt-and-suspenders gate (Pattern 3): even if registration leaks, a non-VLM provider never receives `visualize` in its tool list. Also derive the `visionEnabled` boolean passed to `createMinecraftAdapter` (Task 2b) from this same `anthropic.capabilities?.vision` so both gates agree. Do NOT change the callPersonality call site — the filtered tool list rides the existing `tools: combinedToolsFor()`.
  </action>
  <verify>
    <automated>grep -c "capabilities?.vision\|capabilities.vision" src/bot/brain/orchestrator.js && grep -c "t.name !== 'visualize'" src/bot/brain/orchestrator.js && npx vitest run src/bot/brain/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "t.name !== 'visualize'" src/bot/brain/orchestrator.js` >= 1 (the filter is in combinedToolsFor).
    - `grep -c "capabilities" src/bot/brain/orchestrator.js` shows the vision-capability read.
    - The existing bot brain test suite still passes (`npx vitest run src/bot/brain/` green — no syntax regression, no orchestrator regression). (Note: the repo is ESM — do NOT use `node -e "require('./orchestrator.js')"`; the vitest suite is the load + regression check.)
  </acceptance_criteria>
  <done>combinedToolsFor drops visualize from the tool list when the provider lacks vision; the same flag feeds the adapter's conditional registration; the brain suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool-use -> closed registry | The model invokes visualize; the registry validates args via Zod (closed-registry invariant) |
| render buffer -> base64 -> LLM payload | Bot-generated image data crosses into the outbound LLM request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Elevation | widening the registry to run generated code | mitigate | visualize is a Zod-typed z.object({}) action dispatched through registry.execute (closed-registry invariant); no code generation, no coordinate synthesis. |
| T-15-04-02 | Information disclosure | non-VLM provider still offered visualize -> model confusion / wasted call | mitigate | Belt-and-suspenders: skip registration (Task 2) AND filter the tool list (Task 3) on capabilities.vision (D-10/VIS-03). |
| T-15-04-03 | Denial of Service | visualize hangs the bot loop | mitigate | renderPov is timeout-wrapped (15-01) AND the handler honors config.signal abort (lookAt pattern); returns a degrade string, never hangs. |
| T-15-04-04 | DoS / cost | oversized base64 image inflates the request | mitigate | ~256px q0.4 (D-03) caps payload to low KB; handler caps base64 size before returning (ASVS V5). The per-hour cap (15-02) bounds explicit-render frequency. |
</threat_model>

<verification>
- `npx vitest run src/bot/adapter/minecraft/behaviors/visualize.test.js` green (incl. the exact success-shape assertion).
- `npx vitest run src/bot/adapter/minecraft/registry.vision.test.js` green — conditional registration proven WITHOUT loading native render modules (mocked).
- `grep -c "t.name !== 'visualize'" src/bot/brain/orchestrator.js` >= 1.
- `npx vitest run src/bot/brain/` and `src/bot/adapter/` show no regressions.
</verification>

<success_criteria>
1. visualize is a Zod closed-registry action that renders the bot POV and degrades gracefully — registered ONLY when the provider supports vision (proven by a native-free gate test).
2. A non-VLM provider sees no visualize tool (registration skipped + tool list filtered).
3. The handler returns a structured image result with the EXACT { text, image:{ mediaType, dataBase64 } } shape (asserted — the 15-06 contract) or the degrade string, never throws/hangs.
</success_criteria>

<output>
Create `.planning/phases/15-in-game-vision-via-prismarine-viewer/15-04-SUMMARY.md` when done. Record the EXACT structured result shape visualizeAction returns (15-06 consumes it — { text:string, image:{ mediaType:string, dataBase64:string } }), the dedupe-hash algorithm chosen, and the mock specifier the gate test used to avoid native loads.
</output>
